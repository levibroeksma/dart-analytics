<!--
status: canonical
scope: decisions/game-engine
read-when: why an engine/ruleset/scoring/checkout choice was made
load-when: engine, GameEngine, ruleset, scoring, checkout, fact log, 501, Score Training, undo, leg, visit, bust, turns, darts
depends-on: decisions/architecture.md, decisions/database.md
related: decisions/api.md, decisions/testing.md, decisions/frontend/architecture.md
updated: 2026-08-26
-->

| # | Source | Decision | Rationale |
| - | ------ | -------- | --------- |
| D40 | 2026-07-09 | Game engine runs client-side; frontend owns temporary state, uploads completed gameplay batches; never touches DB | Offline-tolerant play, DB owns truth |
| D111 | 2026-07-16 | Score Training (Task 18) is the first fully-implemented game engine end-to-end: `scoreTrainingSetup`/`scoreTrainingPlay` `.data.ts` factories, three routed pages (`setup`/`play`/`results`), wired into `register-route-data.ts`; also the first real (non-doc-only) instance of the ruleset-validator-registry pattern (D15/D87) driving a live client flow | Closes the 18-task Score Training engine plan; establishes the concrete shape future game engines (D40) will replicate |
| D112 | 2026-07-16 | Whole-branch review fixes for Score Training: `SegmentTimer` wired into `scoreTrainingPlay` for MINUTES mode (`init`/`destroy`); `ScoreTrainingEngine` takes `startingSequence` so a resumed session's visit numbering continues from `store.turns.length` instead of resetting to 1; D88 reconciliation now matches the local session by `sessionId` (not just `gameTypeKey`) before deciding to reset; final batch upload/complete is retryable via a store-persisted `idempotencyKey` and a `retryCompletion()` path, so a failed upload no longer strands the session; results are passed to `/results` via URL query params computed before `store.reset()` | Per-task reviews couldn't see these cross-task integration gaps (timer never instantiated, sequence numbering assumed a fresh engine per page load, no upload retry path); fixed together since finding #2 (sequence) is a precondition for finding #1 (timer/MINUTES mode) actually completing a session |
| D114 | 2026-07-16 | Score Training MINUTES timer resume fixed to restore remaining time (not restart the full duration) on page reload; countdown display seeded to the full duration immediately on `init()` instead of showing `00:00` until the first tick | Second-round whole-branch review Minor findings, fixed per explicit user request rather than left as follow-ups |
| D119 | 2026-07-17 | Score Training hard-gate completion: results modal gates Back / Play again until batch POST and `PATCH COMPLETED` both succeed (`completionStatus === "succeeded"`; treats `409 SESSION_ALREADY_COMPLETED` as success); amends D90's passive-outbox pattern for Score Training so terminal session state is reached before a new session can be created; supersedes D112's results-via-query-params / dedicated `/results` page for this flow — results render as a play-page modal from a component-local snapshot | Prevents concurrent ACTIVE sessions; hard-gate is Score Training–scoped, not generalized |
| D122 | 2026-07-21 | Score Training completing visits use a client-side `pendingFinishScore` + finish-confirm modal before results/upload; Cancel restores the score into `visitInput` without committing; Confirm commits then reuses the D119 hard-gate. In-play undo pops the last committed turn (store + engine; resume-safe engine rebuild when the instance has no local visits). | Lets players correct the last score before irreversible completion upload |
| D125 | 2026-07-22 | Visit-score entry uses reusable `ScoreInputBuffer` (`modules/game`) with activation guard (`detail > 2` reject + `SCORE_INPUT_GHOST_MS` coalesce); `ScoreInput.astro` is a prop-wired shell; games own range validation and optional undo. | Root-fix ghost multi-clicks; share keypad buffer across game types |
| D138 | 2026-07-26 | One `GameEngine` contract (`modules/game/interfaces.ts`, Pattern 18) replaces five bespoke engine APIs: a `GameEngineFactory` builds every engine from a validated config snapshot bound to a `rulesetVersionKey`; the engine mints `clientKey`/`sequence`/`completedAt`, rehydrates via `create(config, prior)`, answers `wouldComplete(input)` as a pure non-mutating predicate, and `undo()` is an exact inverse of `record()` over `facts()` including any stage the record opened; one generic `events.payload.module.ts` builds every batch | Five engines shipped five APIs, three fact models and no rehydrate path, so every shared consumer (play page, store, undo, leave/abandon, hard-gate) had to special-case each game; `wouldComplete` exists because deciding completion by record-then-rollback strands the `LEG` stage 501's `record()` opens (review C1, I1–I3, ST6, P2) |
| D139 | 2026-07-26 | Engines own the fact log and derive everything else: running score, training points, hit ratios and averages are folded from `facts()` on read and never stored in a field or a fact; the game store is game-agnostic (`rulesetVersionKey` + config union + `stages`/`turns`, `_v` → 2) and replaces its whole log from `engine.facts()` instead of appending, so engine and store cannot diverge | Bob's 27 and Singles Training kept the derivation and discarded the fact, inverting "store what happened"; the Score-Training-shaped store had nowhere to put the other games' facts (review C2, ST1, ST2) |
| D140 | 2026-07-26 | Shared ruleset config schemas live in `app/src/lib/game/rulesets/` — one `.strict()` Zod object per ruleset version plus a snake_case↔camelCase codec — imported by both the Worker validator and the browser engine; the directory may import neither runtime | `lib/` is the only tree both runtimes may import; config vocabulary was otherwise translated by hand in two directions per game, and `.strict()` turns a seed/schema drift into a failed test instead of silently stripped keys (review ST3) |
| D141 | 2026-07-26 | Engine conformance mechanized: `scripts/check-game-engines.sh` fails when an `*.engine.module.ts` exports no `*EngineFactory`, never calls `registerEngineFactory`, or names a `rulesetVersionKey` with no validator in `services/rulesets/registry.ts`; `scripts/check-refinement-coverage.sh` plus `refinement-contract.ts` bind every ruleset-schema refinement to executed boundary probes — with its blind spots (delete-both edits, undeclared boundaries, refinements outside `types.ts`, floor-side weakening masked by a redundant field-level bound) stated in both files rather than implied | D105/D110/D127 precedent: the same engine defect class recurred across four PRs, and a `superRefine` was silently lost during a schema consolidation while its tests were repointed and stayed green (review P4) |
| D142 | 2026-07-26 | Persistence semantics fixed for every game: `darts.score` is the dart's actual board score, never a game-specific point value; `turns.total_score` is the sum of counted dart board scores — `0` for a void visit, never negative — so Bob's 27's full-miss penalty and Singles Training's 1/2/3 points are derived, never written | Storing game points or a negative turn total in shared columns silently corrupts every cross-game aggregation (review I5) |
| D143 | 2026-07-26 | 501 V1 rules bound to `501_V1`: the win condition is that the **finishing** dart is a double (`finishedOnDouble`), darts-at-a-double are an analytics fact only; `max_visit_score` in the ruleset config makes the score cap ruleset-owned per D15; `legs_to_win` drives one `LEG` stage per leg, so the seeded multi-leg preset is playable | `dartsOnDouble >= 1` let a visit put one dart in a double, finish on a single and still win; an unvalidated `scoreAttempted` let −100 *raise* the remaining score (review I7/I8/I4) |
| D144 | 2026-07-26 | Singles Training dart facts record **no intention at all** — both `intendedTargetNumber` and `intendedZoneKey` null, not just the zone: single, double and treble on that segment are all valid intentional outcomes, so a fabricated "minimum ring" would corrupt the D06 intended-vs-hit accuracy analysis, and a target number paired with a null zone is rejected by `chk_dart_target_consistency` regardless. Bob's 27 and Doubles Training record a genuine `DOUBLE` intent (`INNER_BULL` on the bull target); Singles' intended target is recoverable from the visit index instead, since it plays one fixed target per visit | Honesty over completeness in the intention pair — the alternative writes an intent the player never held; nulling only the zone was tried first and found unpersistable (rejected by `chk_dart_target_consistency`), corrected the same day |
| D145 | 2026-07-26 | `app/CLAUDE.md`/`AGENT.md` Non-Negotiable Rules gain a "Game engines" rule: every `*.engine.module.ts` implements the `GameEngine` contract (Pattern 18) — validated config snapshot bound to a `rulesetVersionKey`, owned `EngineFacts` log, minted `clientKey`/`sequence`/`completedAt`, `create(config, prior)` rehydration, pure `wouldComplete(input)`, `undo()` an exact inverse of `record()` over `facts()` including any opened stage — and never stores a derivable value; `scripts/check-game-engines.sh` must pass | The contract lived only in code and Pattern 18 prose; a sixth engine would have had precedent to imitate but no rule text to check against (review P2) |
| D146 | 2026-07-26 | Root `CLAUDE.md`/`AGENT.md` Hard Invariants gain an engine-only task scope rule: the spec must name the capture/input mode, stage type, and `turns`/`darts` mapping — proving the state shape can be persisted — before implementation; deferring persistence is allowed, an unpersistable shape is not | Two engines shipped keeping only a derived score and discarding the per-dart facts needed to persist them, and a third emitted an intention pair a schema CHECK rejects, none caught until the fifth PR's review (C1, C2, I1, I2) |
| D150 | 2026-07-26 | No engine hands out live internals: `state()` returns a freshly derived object and `facts()` returns detached stage/turn records (the module-level `STAGE` constant is cloned). Score Training's MINUTES timer expiry moves from a write through the returned state to an explicit `ScoreTrainingEngine.expireTimer()` | Four engines returned derived copies and one returned live state, so the same caller write drove completion in one game and silently no-opped in the other four; the shared `STAGE` constant also landed inside an Alpine `$persist` proxy (D120) |
| D151 | 2026-07-26 | `TurnFact.completedAt` is nullable and stamped when the visit resolves, not when it opens — 3rd dart for Bob's 27 / Singles, hit-or-3rd-miss for Doubles, immediately for the visit-per-record games (Score Training, 501); `undo()` clears it when it reopens a visit | `06-Spec/04-Runtime-Layer.md` defines the column as the client-observed *end* of the visit; stamping at creation recorded when the turn opened, and migration `0015` had removed the ordering check that would have caught it |
| D152 | 2026-07-26 | Pattern 18 states the contract's undo depth: unbounded, one `record()` per `undo()` call, back through rehydrated facts to an empty log, `false` when nothing remains | All five engines already behaved this way and matched `main`, but nothing said so — a sixth engine had no rule to conform to (review M-undo-depth) |
| D153 | 2026-07-26 | Ten Up One Down ships as the sixth engine on the `GameEngine` contract: `TuodConfig` (`.strict()`, matching both seeded presets key-for-key), `TuodEngine` (RECREATIONAL + QUICK_SCORE, one `EXERCISE_BLOCK`, one turn per attempt carrying the checked-out target or `0`, ladder folded from those turns), and `tuodValidator`. Success requires the finishing dart to be a double, never a count of darts thrown at one; a bust is a failed attempt. V1 ships no ladder floor, so `record()` refuses a checkout claimed on a target below 2 rather than inventing one. The three quick-score validators' shared batch rules move to `services/rulesets/quick-score.validator.ts` | `TUOD_V1` was seeded with two presets but had no schema, engine or validator, so it listed in the preset picker and could not start a session (whole-branch review, Minor) |
| D154 | 2026-07-26 | Ten Up One Down's ladder floors at 2, the minimum double-out-finishable target: `applyTuodAttempt` clamps a failure there instead of letting it fall further, so live and rehydrated sessions fold to the same target; the now-unreachable below-2 checkout guard is removed. The separate V2+ "floor at the start score (41)" variant remains deferred | A long run of failures could walk the target below any finishable double, stranding a session where only failures could ever be recorded again (product decision resolving the ruleset doc's open floor question) |
| D177 | 2026-08-01 | `ScoreTrainingConfig.duration_value` bounds changed to ROUNDS 1–100 (was 1–50) / MINUTES 3–30 (was 1–180); minutes default realigned to 5 (was 15). Setup screen exposes both as a Rounds/Timed mode radio plus one editable field, clamped client-side with a notice, and bounded server-side by the same `superRefine` | Product locked session-duration ranges for the configurable-duration feature; the old MINUTES floor of 1 was not load-bearing against the field-level `.min(1)`, so raising it to 3 makes the refinement's floor probe for MINUTES the first actually load-bearing one (see `refinement-contract.ts` blind-spot note) |
| D178 | 2026-08-01 | Score Training's "Play again" now threads the just-completed session's `durationValue` into the new session's `create` call as `overrides.duration_value`, instead of leaving the fresh session on the template's stored default | Before this fix, play-again silently reverted a custom duration (e.g. a 20-round or 12-minute session) back to the template default on every replay, contradicting the whole point of making duration configurable — a persisted-behavior change, not just new UI |
| D180 | 2026-08-02 | 501 is RECREATIONAL + QUICK_SCORE (visit totals only, no per-dart rows), so when an entered visit score would bring the leg's remaining total to exactly 0 the client cannot tell a legal double-out checkout from a bust from the number alone; `five-oh-one-play.data.ts`'s `submitVisit` gates behind a "Finished on a double?" confirm (`showDoubleConfirm`/`confirmDouble`/`denyDouble`) before recording anything | Matches `docs/game-rules/rulesets/501.md`'s Finishing section: the finishing dart, not the visit, must be a double, and the app has no per-dart fact to check that against |
| D181 | 2026-08-02 | The double-out confirm gate (D180) tests a direct `remaining - score === 0` predicate, not `engine.wouldComplete()` | For 501, `wouldComplete` (`five-oh-one.engine.module.ts`) answers whether a visit wins the *match* (`legsWon` reaches `legsToWin`), not whether it wins the current *leg* — using it would skip the confirm on every non-final-leg checkout. The distinction lives only as a code comment on `wouldComplete` today; the next X01-family game (301/701 variants, `docs/game-rules/rulesets/501.md`'s Later Versions) inherits the identical trap with no rule text to check against |
| D183 | 2026-08-02 | The double-out confirm (D180) is now three actions — Cancel (`cancelCheckout`, restores the score to the keypad, records nothing), Bust/miss (`denyDouble`, unchanged), Yes (`confirmDouble`) — instead of two, and `submitVisit`'s gate now also requires `checkoutPathFor(remaining) !== null` so the seven bogey numbers and 171-180 skip the dialog entirely and record straight as a bust. "Yes" no longer always records immediately: it calls the pure `engine.wouldComplete({ scoreAttempted, finishedOnDouble: true })` (D181's match-win, not leg-win, predicate) and only records right away when the checkout merely wins a leg; when it would end the match, it opens a second confirm (`showMatchFinishConfirm`/`confirmMatchFinish`/`cancelMatchFinish`) and defers recording until the player confirms, since `recordVisit` uploads and `PATCH COMPLETED`s the session as soon as it sees a match win, and `undoVisit` is guarded off once `finished` is set | The prior two-button gate had no way to back out of a mistyped score, and clicking "Yes" on a match-winning visit recorded and uploaded immediately with no way back — a fat-finger there could not be undone. User-approved redesign from the final whole-branch review, approved 2026-08-02 |

### D189 — Engines branch on VISUAL_BOARD input mode; 501 bust visibility becomes mode-scoped
Status: Accepted · Date: 2026-08-05
Decision: `EngineInputMode` distinguishes capture depth inside 501 and Score Training: under ANALYTICS + VISUAL_BOARD, each engine records dart-level visits (`classify(x, y)`'s hit target/zone/score/coordinates folded onto every dart, no game-specific point derived from it), while QUICK_SCORE keeps recording turn-total-only visits with no dart rows, unchanged. `isVisualBoardCapture(captureModeKey, inputModeKey)` (`app/src/services/rulesets/visual-board.validator.ts`) is the single predicate both the Worker validator and the engines key on. The Worker re-derives every submitted dart from its own coordinate via the shared `classify()` and refuses the batch on any disagreement with the client's claim — the client's zone/target/score are computed for immediate feedback but never trusted as authority.
Reason: The QUICK_SCORE 501 bust-vs-scoreless-visit ambiguity (`06-Spec/04-Runtime-Layer.md`'s known limitation) is a capture-depth problem, not a 501-specific one: full dart rows make the two cases distinguishable by construction — counted `0` against thrown non-zero darts — so the fix belongs to input mode, not to a schema change or a 501-only special case.
Consequences: `06-Spec/04-Runtime-Layer.md`'s known-limitation section is retitled to scope it to QUICK_SCORE and gains a "Retired for VISUAL_BOARD sessions" note. `DartZoneKey`'s `INNER_SINGLE`/`OUTER_SINGLE` split (D191) is a precondition — without banded singles, a visual dart landing in either band would score correctly but classify to an unbanded zone the intended-vs-hit accuracy analysis could not distinguish by ring side. A VISUAL_BOARD session's busted visit persists `turns.total_score = 0` alongside dart rows carrying their real non-zero board scores (the `total_score` bust carve-out, `06-Spec/04-Runtime-Layer.md`) — that divergence is the fact the mode-scoped fix depends on. QUICK_SCORE sessions remain unfixable: completed gameplay is immutable and no coordinate exists to recover.

### D197 — `validateConfig` must admit every mode pair its `validateBatch` handles
Status: Accepted · Date: 2026-08-08
Decision: `fiveOhOneValidator.validateConfig` and `scoreTrainingValidator.validateConfig` accept `ANALYTICS + VISUAL_BOARD` as well as `RECREATIONAL + QUICK_SCORE`, via the shared `isQuickScoreOrVisualBoardCapture` predicate in `services/rulesets/quick-score.validator.ts`. Both had hard-rejected anything but quick score while their own `validateBatch` already routed visual-board turns to `validateVisualBoardTurns`. A ruleset validator's config gate and its batch gate must admit the same set of mode pairs; the two halves are one statement of what the engine supports, not two independent checks.
Reason: `validateConfig` runs at session creation, before any batch exists. D189 wired the batch path only, so the visual capture path could be validated but never entered — every attempt to start a 501 or Score Training session in `ANALYTICS + VISUAL_BOARD` was rejected before the engine was reached. Nothing in the branch's original plan covered this; it was found by the session-creation guard's implementer and confirmed by the controller. Widening `validateConfig` is what makes the capability declaration (D196) and the app-mode setting (D195) usable end to end rather than a switch that turns everything off.
Consequences: the predicate is deliberately decoupled from `RULESET_CAPABILITIES` — the validators keep their own mode statement and the capability constant mirrors it, so the two can be cross-checked instead of one silently defining the other. The pair remains asymmetric by design for the other four rulesets: `TUOD_V1` is quick-score only and `SINGLES_V1` / `BOBS27_V1` / `DOUBLES_TRAINING_V1` are `RECREATIONAL + DETAILED_DARTS`, none of which have a visual path. A future ruleset gaining a mode must change both halves and the capability declaration in the same edit, or a parity test fails: `capability-seed-parity.test.ts` covers `RULESET_CAPABILITIES` against seed `0007`, and `capability-validator-parity.test.ts` covers it against every ruleset's `validateConfig` in both directions — a declared pair its validator refuses, and an undeclared pair its validator accepts. (`scripts/check-game-engines.sh` is not a third: it asserts only that a registered engine's ruleset key appears in `RULESET_CAPABILITIES`, and its own header disclaims pair-level parity.)

### D198 — Engines dispatch on the input's shape, not on the session's stored mode
Status: Accepted · Date: 2026-08-09
Supersedes: D189
Decision: `record()`, `wouldComplete()` and `undo()` in `five-oh-one.engine.module.ts` and `score-training.engine.module.ts` branch on what they are handed, never on the constructor's `inputMode` field. A visit total (`number` / `FiveOhOneVisitInput`) takes the quick-score path; a `DartObservation` takes the dart path, discriminated by `hitZoneKey`. `undo()` branches on the last turn's shape (`darts.length > 0`), which is the shape `record()` actually wrote. `inputMode` survives on the constructor for persistence and capability code but governs no engine behaviour. D189's capture-depth model is unchanged — only the mechanism that selects it.
Reason: With dispatch keyed on the field, a QUICK_SCORE-tagged engine handed a `DartObservation` called `classify(undefined, undefined)`, which returns `{zoneKey: "INNER_SINGLE"}` with no target and no score and throws nothing — a silently corrupt dart row. Two reachable paths produced that divergence: setup pages that hardcoded the mode pair, and `playAgain()` rebuilding a replay engine with the factory default. Input shape is the one authority that cannot disagree with the input.
Consequences: The two input paths coexist inside one session, so a player may enter a visit on the keypad and the next on the board. `recordVisitTotal` therefore refuses a keypad total while a dart visit is open (`completedAt === null`) rather than writing across it — refusing loudly beats writing wrong. The mixed log stays undoable to depth because turn shape is an invariant of the write path: `recordVisitTotal` always writes `darts: []`, `recordDart` always writes `darts.length >= 1`, and rehydration is a plain JSON mirror that round-trips shape faithfully. A future method keyed on `inputMode` would silently disagree with its own `record()` — the field is dead and should be removed under its own decision.

### D203 — The engine's `inputMode` field is removed, not merely unused
Status: Accepted · Date: 2026-08-10
Supersedes: D198
Decision: `EngineInputMode` and every trace of it are gone: the third constructor parameter on `FiveOhOneEngine` and `ScoreTrainingEngine`, the third parameter on `GameEngineFactory.create` and both factories, `engineInputMode()` in `session-mode-resolution.ts`, and the `EngineInputMode` type itself. `create(config, prior?)` is the whole contract. D198's shape-dispatch rule is unchanged and is now structural rather than conventional — there is no mode on an engine for a future method to disagree with.
Reason: D198 left the field on the constructor for persistence and capability code and recorded that it "is dead and should be removed under its own decision". It governed nothing, and `astro check` reported it as unused on every run, so the branch shipped two permanent warnings that trained everyone to read a non-empty diagnostic list as clean. Removing the parameter without removing its call sites is what actually broke the build (`ts(2554)`, expected 1-2 arguments but got 3), which is the failure mode of a half-dead field: it stays cheap to keep and expensive to touch.
Consequences: Four tests were DELETED rather than re-pointed — one in `five-oh-one-play.data.test.ts`, three in `score-training-play.data.test.ts` — because their sole assertion was which mode reached `factory.create`, and that argument no longer exists. Per D148 the alternative would have been re-pointing them at a different input to keep them green, which is exactly the failure that rule exists to catch. This drops the only executable guard on D200 at the engine-construction seam; D200's live surface is now the board gate in `BoardInputPanel.astro`, which is `.astro` markup and unit-tested by nothing (D101). The 35 engine tests that passed `"VISUAL_BOARD"` as a third argument kept their bodies and assertions — only the now-nonexistent argument was dropped — and two `undo()` describe titles naming `inputMode` were re-pointed at the same guarantee they always tested, the fact log's shape.

### D207 — Bob's 27's per-hit value is the double's board value, not its face value
Status: Accepted · Date: 2026-08-12
Decision: `pointValueOf` in `bobs27.engine.module.ts` returns the target's **board value** — `boardScore(target.number, "DOUBLE")` (2× its number) for D1–D20, unchanged `config.bullHitValue` (50, already board-value) for the bull. A hit on D16 adds 32 to the running score, not 16; a full-miss visit at D18 subtracts `missPenaltyMultiplier × 36`, not 18.
Reason: The engine shipped adding face value (`target.number`), matching only the non-canonical `docs/game-rules/rulesets/bobs-27.md`'s own worked examples — no `decisions/**` block ever recorded that formula as a considered decision. The owner clarified during the Phase 1 planning session (`docs/superpowers/specs/2026-08-12-bobs27-frontend-design.md`) that standard Bob's 27 scores the double's actual board value, matching how `darts.score` (D142) already recorded each dart.
Consequences: `bobs27.engine.module.test.ts`'s score-delta assertions and the ruleset doc's worked examples were rewritten to match (same PR). `darts.score`/`turns.total_score` were already correct under D142 and are unaffected — this decision only changes `Bobs27State.score`, a derived, never-persisted value. No config schema change.

### D210 — Training order mode: target_order frozen into the config snapshot at session creation; bull leads High→Low, shuffles fully in Random, always re-minted on Play Again
Status: Accepted · Date: 2026-08-14
Decision: Singles Training and Doubles Training gain an identical editable `order_mode` setting (`LOW_TO_HIGH` | `HIGH_TO_LOW` | `RANDOM`), resolved client-side (`lib/game/target-order.ts`) into a concrete 21-value `target_order` array (1–20 plus `25` as the BULL sentinel) once at session creation and copied into the immutable `SinglesConfig`/`DoublesTrainingConfig` snapshot — never re-resolved on undo or resume. On `HIGH_TO_LOW`, BULL **leads** (`BULL, 20, 19, …1`) for both games, not "ends on bull" as Doubles Training's pre-existing non-canonical doc had claimed. On `RANDOM`, all 21 targets (1–20 and BULL) shuffle together via Fisher–Yates, so BULL can land anywhere. Both engines' completion checks move from "is this target BULL" to "is this the 21st (last) target in `config.targetOrder`" — index-based, since BULL is no longer always last. Play Again always calls `targetOrderFor(priorConfig.orderMode)` again rather than reusing the just-finished session's array, so a Random session gets a fresh shuffle every replay.
Reason: The user asked for the setting to be identical across both games and for a session's random order to survive a correction via undo without reshuffling — undo replays the engine's reducer against `facts()`, so the order it reads must be stable across that replay, and the existing config-snapshot-copy-once mechanism (already how every other editable setting works, e.g. D177/D178's `duration_value`) already guarantees exactly that without a new persisted-fact field. Bull's placement (lead vs. trail vs. shuffled) and Play Again's reshuffle-vs-reuse behavior were both genuine alternatives weighed with the product owner rather than obvious defaults.
Consequences: `board-progression.module.ts`'s `numbersPath`/`doublesPath` gained an optional explicit-order parameter, defaulting to the original fixed ascending array when omitted, so Bob's 27's two zero-argument call sites are unaffected. `applyDoublesTrainingDart` gained a required `config` first parameter (was state-only), and `DoublesTrainingEngine`'s constructor now retains `config` as a field instead of discarding it — both engines are now genuinely config-driven, closing a gap D144/D153's precedent had left open for Doubles Training specifically. `runPlayAgain` (`play-lifecycle.ts`) gained an optional `buildOverrides` hook (`PlayAgainOverrides<TConfig>`, `lib/game/types.ts`) that, when supplied, sends `config.overrides` on the replay's `POST /api/sessions` call and reassigns `$store.game.configSnapshot`; omitted, behavior is byte-for-byte unchanged, so every other game's Play Again call is untouched. `docs/game-rules/rulesets/doubles-training.md` corrected in the same change — its Order rows and Progress section previously said High→Low "ends on bull," which never matched this decision's actual convention.

### D216 — Ten Up One Down ships its frontend fan-out; `createPresetSetupController` gains a third opt-out
Status: Accepted · Date: 2026-08-20
Decision: TUOD's frontend lands on top of its already-shipped engine/validator/DB layer (D153): bespoke `tuodSetup`/`tuodPlay` `.data.ts` controllers (mirroring `score-training-{setup,play}.data.ts`'s D88 reconciliation, `SegmentTimer` wiring, and finish-confirm gate), `TuodSetupForm.astro`/`TenUpOneDown.astro`/`TenUpOneDownResults.astro`, the two routed pages, and `games-visibility.ts`/`register-route-data.ts` wiring. `createPresetSetupController` now has three opt-outs, each for a distinct reason: 501 and Score Training bypass it for custom starting-score/duration logic (pre-existing); TUOD bypasses it because the factory assumes exactly one seeded preset per game (`this.presets[0]`) and TUOD has two (10 Rounds, 10 Minutes) the player must pick between. `tuodSetup`'s `start()` sends the picked preset's `configurationTemplateId` as `config.source: "template"` with no `overrides` — every field but session length is locked. `games-visibility.test.ts`'s ANALYTICS assertion no longer means "every carded game": TUOD_V1 (`RULESET_CAPABILITIES.TUOD_V1 = [QUICK_SCORE]`, no `VISUAL_BOARD` pair) is the first QUICK_SCORE-only card, visible under RECREATIONAL and under ANALYTICS only while it has an active session.
Reason: TUOD was seeded with two configuration-template presets and had a working engine, but no page could reach it — the games list never showed a card, so no session could be created despite the engine, validator, and DB layer all being complete since D153. This closes that gap with the smallest possible surface: no engine, validator, or schema change, matching the approved design spec (`docs/superpowers/specs/2026-08-20-tuod-frontend-design.md`).
Consequences: `docs/architecture/07-Frontend/09-Adding-A-Game.md`'s "engine-only skips" note no longer names TUOD as its example — no carded ruleset is presently in that state, so the note is phrased generically until a future ruleset re-enters it. `scripts/check-game-wiring.sh` requires every registered ruleset to be fully wired or fully absent in a single commit (half a row fails either half), so all five implementation tasks (setup controller, setup page, play controller, play page/results, wiring) landed in one commit rather than the plan's original one-commit-per-task shape.

### D217 — Every quick-score checkout is confirmed through one dialog that also captures the visit's dart counts
Status: Accepted · Date: 2026-08-20
Decision: `DoubleCheckoutConfirm.astro` is replaced by `CheckoutConfirm.astro`, shared by 501, 121 and TUOD. It has two actions — Confirm and Cancel — and two option rows — darts to finish, darts at a double — whose choices come from `checkoutDartOptions(remainingScore, maxDarts)` (`modules/game/checkout-darts.module.ts`), which reads the shortest route off the existing checkout chart: a score needing `n` darts offers `n..maxDarts` to finish and `1..maxDarts-n+1` at a double, so 41 offers 2|3 and 1|2 while 170 offers one answer to each. The shortest route is preselected, so Confirm is always reachable in one tap. `FiveOhOneVisitInput`, `OneTwentyOneVisitInput` and `TuodAttemptInput` gain optional `dartsUsed`/`dartsAtDouble`; all three engines reject a claim the chart contradicts and `wouldComplete` answers false for exactly those claims rather than throwing. TUOD's play screen drops its binary Checked out / Missed buttons for the shared `ScoreInput` keypad: a typed total equal to the current target opens the dialog, every other total is a failed attempt. The old dialog's Bust / miss action is gone: the typed total already reports the outcome, since the dialog opens only on a total that finishes exactly, and Cancel returns that total to the keypad.
Reason: The two binary buttons made TUOD the only quick-score game without a keypad, and none of the three games recorded how a checkout was actually thrown — the one visit whose dart count is not derivable from `maxDartsPerTurn`. Asking for it at the moment the player already has to answer "was it a double?" costs no extra dialog. Narrowing the options to the chart's routes is what keeps the question answerable in one tap rather than a free 1-2-3 guess.
Consequences: The counts are **not persisted** — a quick-score turn still carries `darts: []`, so no migration and no schema change, and nothing derived from the fact log (`dartsThrownCount`, the three-dart average) moves. Their only live use is validation, which is real but modest: an impossible claim is refused at the engine boundary instead of being silently accepted. Dropping Bust / miss also drops the one way to report a visit that scored exactly the remaining score without finishing on a double — a real 501 bust; the player must record it as some other total until a bust affordance exists on the keypad itself. Persisting them as dart rows so checkout quality becomes queryable is deferred, and is the change that would make the capture pay for itself. TUOD's `recordAttempt` takes a `TuodAttemptInput` rather than a `checkedOut` boolean and `pendingAttempt` holds that input, so the finish-confirm gate replays exactly what was deferred; the existing TUOD play tests were re-pointed at the same guarantees through the new signature, not re-scoped.

### D219 — Ten Up One Down joins the dual-capture set (ANALYTICS + VISUAL_BOARD)
Status: Accepted · Date: 2026-08-20
Decision: `TuodEngine` gains a second `record()`/`undo()`/`wouldComplete()` shape for `DartObservation`, dispatching on input shape per D198, never on stored mode — mirrors `OneTwentyOneEngine`'s dual-shape dart path, simplified because TUOD has exactly one visit per attempt and one `EXERCISE_BLOCK` stage for the whole session (no per-round stage bookkeeping to add). `tuodValidator.validateConfig` now accepts `ANALYTICS + VISUAL_BOARD` via `isQuickScoreOrVisualBoardCapture`, and `validateBatch` dispatches to `validateVisualBoardTurns` under it. `RULESET_CAPABILITIES.TUOD_V1` becomes `[QUICK_SCORE, VISUAL_BOARD]`, mirrored into seed `0007` and its verification script. `TenUpOneDown.astro` renders `BoardInputPanel` and hides the keypad under `VISUAL_BOARD`, exactly like `OneTwentyOne.astro`/`FiveOhOne.astro`; the D217 `CheckoutConfirm` dialog stays QUICK_SCORE-only, bypassed entirely under board capture the same way 501/121 already bypass their own keypad confirm.
Reason: TUOD (D153/D216/D217) was the only ruleset left declaring `RECREATIONAL + QUICK_SCORE` alone — 501, Score Training and 121 had already paired it with `ANALYTICS + VISUAL_BOARD` (D189/D198, `2026-08-15-121-shanghai-atc-analytics-design.md`). Doing the same for TUOD was also the chosen way to resolve the ruleset doc's own stated known limitation (bust indistinguishable from a scoreless miss): real per-dart facts under `VISUAL_BOARD` make the two recomputable from the fact log alone, with no new field — an explicit design goal for this work, not an incidental side effect.
Consequences: `docs/game-rules/rulesets/ten-up-one-down.md`'s Known Limitations section is now scoped to `QUICK_SCORE` and gains a "Retired for VISUAL_BOARD sessions" note, matching D189's wording for 501. No `v_*` view yet surfaces the newly-derivable bust/miss distinction — that remains future work. F10 (the ladder's missing ceiling) is untouched, an unrelated open finding. `capabilities.test.ts`'s "rejects visual board for a game with no visual engine path" assertion (written when TUOD was the one ruleset without a board path) and `games-visibility.test.ts`'s TUOD-specific active-session carve-out test are both removed as no-longer-true special cases, not re-pointed at a different guarantee — the games-visibility generic "never hides a game with an active session" test already covers TUOD once it is ANALYTICS-capable. The verification script `database/verification/0007_capability_seed_checks.sql` also had its row-count assertions corrected to the actual 17 → 18 (not the previously-assumed 14 → 15): the script had already drifted stale before this task, undercounting three rows other rulesets' own `VISUAL_BOARD` additions had already added to the seed without a matching update here.

### D220 — Seats live in the configuration snapshot; the active seat is always derived
Status: Accepted · Date: 2026-08-21
Decision: A session's participants become an ordered `seats` array (`{ participantRef, displayName, sideKey, participantTypeKey }`) written into `exercise_configurations.configuration` at create time, in the same transaction as the participant rows. No new table: a guest is a `participants` row with a name and no cross-session identity. `TurnFact` gains a required `participantRef` that the engine mints exactly as it mints `clientKey`/`sequence`/`completedAt`, so one session's log holds several throwers; `buildEventsBatch` reads it per turn instead of stamping one ref across the whole batch. The ACTIVE seat is never stored — the shared `modules/game/seat-rota.module.ts` derives it from the fact log, offsetting the visits already thrown in the open stage from that stage's own starting seat. Every engine declares a static `stageOwnership` (`SHARED` for an X01 leg holding every seat's interleaved visits, `PER_SEAT` for one stage per seat per round) so the one rota serves both shapes. `FiveOhOneState` splits into the three scopes it actually has: `remainingScore` per seat, `legsWon` per `sideKey`, `status` per session.
Reason: The schema was already multiplayer-ready — `participants`, `turns.participant_id` and `validateBatchReferences` all existed — so the only thing preventing a second thrower was `buildEventsBatch(participantRef, facts)` stamping one identity onto every turn. Two alternatives were weighed and rejected: composing N single-seat sub-engines (each engine would need to know how to merge its siblings' stages, and an X01 leg is genuinely shared, not mergeable), and a full contract reshuffle passing a seat into `record()` (a caller could then disagree with the engine about whose throw it is, and undo would need seat logic of its own). Folding wins per `sideKey` rather than per seat is what lets a future 2v2 land with no state change: sides already exist, only the pairing does not.
Consequences: Every one of the nine engines is touched, because making `participantRef` required breaks all nine typechecks at once — no smaller unit compiles. Eight of them declare `PER_SEAT` and stamp seat 0; only 501 is wired for rotation, and `session-seats.service.ts` refuses a second seat for any other ruleset so no participant is persisted that nothing can throw for. `applyFiveOhOneVisit` is removed in favour of `foldFiveOhOneState(facts, config)`, which the engine and the play page now share — the play page previously carried its own duplicate `foldLegState` replay that could drift. Its guarantees moved rather than vanishing: the bust matrix, score cap and double-out cases now test `resolveFiveOhOneVisit` (exported for the purpose) and the engine directly. `Seated<TConfig>` is a conditional type, because three rulesets' snapshots are `Record<string, never>` and intersecting one with a seat array narrows `seats` to `never` — a type no value can inhabit. `record()` also had to re-derive state AFTER opening the next leg's stage: it previously returned a state folded before the push, which reported the winning seat at 0 and the others un-reset. Bull-up (deciding leg 1's starter by a throw at the bull) and per-participant capture depth are deferred; no UI sends more than one seat yet, so every session the existing screens create is still solo.

### D229 — Score Training's `record()`/`wouldComplete()` guard against a decided 1v1 match, scoped to `isMatchDecided()` rather than `isComplete()`
Status: Accepted · Date: 2026-08-22
Decision: `ScoreTrainingEngine.recordVisitTotal`/`recordDart` now throw before any fact-log mutation, and `wouldComplete` now answers `false`, once a private `isMatchDecided()` reads true — `deriveState().seats.length > 1 && status !== "IN_PROGRESS"`. This mirrors `TuodEngine`'s existing completion guard (`recordDart`/`rejectionReason`/`wouldCompleteDart` all check `isComplete()`) but is deliberately narrower: `isMatchDecided()` never trips for a solo (1-seat) session, where `isComplete()` would.
Reason: Code review flagged that `recordVisitTotal`/`recordDart` had no completion guard at all — with `winningSideKey` computed as an uncapped `reduce` sum over every closed turn per seat (no budget cap), a stray extra `record()` call after both 1v1 seats finish their shared ROUNDS budget can inflate one seat's `totalScore` past the other's and flip the already-decided outcome. The literal fix (guard on `isComplete()`, matching TUOD verbatim) was tried first and broke a real, passing production test (`score-training-play.data.test.ts`'s "drives a MINUTES session to completion once the timer expires"): a solo MINUTES session's `isComplete()` can read `true` from `expireTimer()` alone, once at least one visit is already recorded — before the one remaining visit `confirmFinish` still legitimately has to record, per `submitVisit`'s own documented design (`wouldComplete` gates the finish confirm; the deferred visit is recorded exactly once, by `confirmFinish`, after the player agrees). A 1v1 match carries no equivalent risk: it is ROUNDS-only (`durationType` never `MINUTES` for `seats.length > 1`), so `status` only turns terminal as the direct result of the very `record()` call that reaches the last seat's own budget, never ahead of it — unlike `timerExpired`, an external signal `expireTimer()` can set independently of any `record()` call.
Consequences: `score-training.engine.module.ts`'s `isComplete()` is unchanged and untouched by this guard; `isMatchDecided()` is a new private method, not exported. Three regression tests were added to `score-training.engine.module.test.ts` (`describe("ScoreTrainingEngine — 1v1 completion guard")`) reproducing the exact corruption scenario: a 2-seat, single-round match completes, then a further `record()`/`wouldComplete()` call on either seat is rejected rather than silently mutating the fact log. No existing test was re-pointed at a different input; the full `app/` suite (2636 tests) and `bash scripts/check-game-engines.sh` both pass unchanged. `TuodEngine` itself is untouched — whether its own blanket `isComplete()` guard has the identical MINUTES-mid-completing-visit gap this decision found for Score Training (no test currently exercises "record once before expiry, then again after expiry" for TUOD) is noted as a new finding (F21), not fixed here.

### D230 — Three win-condition categories, generalized `SEAT_CAPS`, and `activeSeat()`'s completion predicate land 1v1 play across the eight non-501 engines
Status: Accepted · Date: 2026-08-22
Decision: `app/src/modules/game/match-outcome.module.ts` adds three pure, generic winner functions — `eliminationWinner({sideKey, failed}[])`, `raceWinner({sideKey, finished}[])`, `scoreCompareWinner({sideKey, completed, metric}[], direction)` — one per win-condition shape a ruleset's session naturally ends with. Bob's 27 is **elimination** (first seat to bust loses, the other wins). 121 is **race-to-finish** (first seat to check out 170 wins, match ends immediately). Around the Clock, TUOD, Shanghai, Score Training, Singles Training and Doubles Training are **score-compare** (both seats always finish their own session; once both are complete, the best derived metric wins — Around the Clock inverted to fewest-darts, Shanghai layering its existing instant-win-on-Shanghai on top as a race-shaped exception ahead of the score-compare fallback). `session-seats.service.ts`'s single-value `MULTI_SEAT_RULESET = "501_V1"` gate is replaced by a `SEAT_CAPS: Record<string, number>` map — `501_V1` stays at 4, the eight other rulesets (`BOBS27_V1`, `121_V1`, `AROUND_THE_CLOCK_V1`, `TUOD_V1`, `SHANGHAI_V1`, `SCORE_TRAINING_V1`, `SINGLES_V1`, `DOUBLES_TRAINING_V1`) get 2, and any ruleset absent from the map still defaults to 1 (reject), exactly as before the map existed. `seat-rota.module.ts`'s `activeSeat()` gains an optional 4th parameter, a completion predicate `(seat: SeatFact) => boolean` defaulting to `() => false`; only `AroundTheClockEngine` passes a real one, because a miss costs it an extra visit and its two seats can therefore finish in a different number of visits — every other engine's fixed-turn-count shape, or match-ending-early shape, means the default pure-alternation behavior already attributes turns correctly.
Reason: Each ruleset's session ends by a genuinely different rule (immediate elimination vs. immediate race vs. play-out-and-compare), so one generic "first to X" helper could not express all three without re-scattering per-engine special cases back into the engines — the same trap D138 already fixed once for the engine contract itself. The single-ruleset `MULTI_SEAT_RULESET` gate was sized for 501 alone; raising the cap for eight more rulesets one `if` at a time would have re-scattered the same seat-count logic `SEAT_CAPS` now centralizes as data instead. `activeSeat()`'s pure turn-count alternation silently mis-attributed Around the Clock's turns once one seat finished its circuit before the other — the one non-uniform-turn-count shape among the eight — so the fix is a predicate parameter on the shared module rather than a bespoke rota inside that one engine.
Consequences: `docs/game-rules/rulesets/{bobs-27,121,around-the-clock,ten-up-one-down,shanghai,score-training}.md` each carry a one-line 1v1 win condition in the game's own terms; `doubles-training.md` already carried one from its own engine task. `docs/game-rules/rulesets/singles-training.md` does not yet state its 1v1 win condition in prose (still reads "Multiplayer / online multiplayer" under Later Versions) despite `SINGLES_V1` shipping a real score-compare win condition (highest `totalPoints`) — logged to `FINDINGS.md` rather than fixed here, since it falls outside this doc-only task's named file list. `docs/architecture/04-Architecture-patterns.md` Pattern 18 and `docs/architecture/06-API/04-Endpoint-Contracts.md`'s `POST /sessions` section now document the win-condition categories and the `SEAT_CAPS` table respectively. See `docs/superpowers/specs/2026-08-22-single-opponent-seat-remaining-engines-design.md` for the full per-engine design — its own title reads "Remaining 7 Engines" while its scope line and `SEAT_CAPS` both list eight rulesets; a pre-existing naming quirk in that spec, not corrected here.

### D231 — TUOD and Score Training also pass `activeSeat()` a real completion predicate; only Around the Clock's does work
Status: Accepted · Date: 2026-08-22 · Supersedes: D230 — on this one point only (which engines PASS a completion predicate). Every other clause of D230 — the three win-condition categories, `SEAT_CAPS`, and the parameter's existence and default — stands unchanged.
Decision: D230 states that "only `AroundTheClockEngine` passes a real one" to `activeSeat()`'s optional 4th parameter. That is wrong as written. `foldTuodState` (`app/src/modules/game/tuod.engine.module.ts`) and `foldScoreTrainingState` (`app/src/modules/game/score-training.engine.module.ts`) both call the 4-argument form too, each passing a predicate that reads its own seat's `seatCompletesAt` result. The correct statement is: three engines pass a predicate — Around the Clock, TUOD and Score Training — and only Around the Clock's changes the answer. The other five 1v1 engines (Bob's 27, 121, Shanghai, Singles Training, Doubles Training) omit the argument entirely and take the `() => false` default.
Reason: D230's underlying architectural point was right — Around the Clock is the one engine whose seats can genuinely diverge, because a miss costs it an extra visit, so its predicate is the only one that ever redirects a turn. TUOD's and Score Training's seats share one fixed ROUNDS budget under lockstep alternation, so their predicates are structurally no-ops: the seat `activeSeat()` returns is identical with or without them, proven by `app/tests/modules/game/seat-rota.module.test.ts`'s own no-op case. They are passed anyway so the fold stays correct if either ruleset ever gains a non-uniform per-seat budget — a deliberate choice D230 recorded as an absence. The literal claim then propagated into three engine doc comments and `docs/architecture/04-Architecture-patterns.md`'s Pattern 18, where a reader checking "does my engine need a predicate?" against the source would find the docs and the code disagreeing.
Consequences: `tuod.engine.module.ts`, `score-training.engine.module.ts` and `seat-rota.module.ts` doc comments corrected to state that a predicate IS passed and why it is a no-op; `04-Architecture-patterns.md`'s Pattern 18 paragraph corrected and pointed here. D230's block is untouched, per the append-only rule — this decision is the correction of record. Three regression tests lock the corrected claim: the two engines' folds hand the final round to the seat that still has one (`tuod.engine.module.test.ts`, `score-training.engine.module.test.ts`), and `activeSeat()` returns the same seat with or without a predicate under a uniform budget (`seat-rota.module.test.ts`).

### D232 — Engine turn-log and seat-derivation boilerplate lives in two shared pure modules, not once per engine
Status: Accepted · Date: 2026-08-23
Decision: The mechanics every engine repeated verbatim after the `MultiSeatState<TSeat>` conversion move into two new pure modules under `app/src/modules/game/`. `turn-log.module.ts` owns everything that touches the `TurnFact`/`DartFact` log: `cloneTurns`, `sumDartScores`, `dartsThrownBy`, `openOrCreateTurn(turns, stageClientKey, participantRef, isReusable)` (the reuse rule stays the caller's, because a visit closes on its 3rd dart, on the dart that hits its target, or only for the seat that opened it), `appendCompletedTurn`, `openVisit`, `resolveObservation`/`appendResolvedDart` for coordinate capture, `appendObservedDart`/`doubleTargetIntent` for ring capture, and the three undo shapes an engine can have — `undoLastDart` (dart-only logs), `undoLastUnit` (mixed keypad/board logs), `undoStagedTurn` (mixed logs whose engine also opens stages). `seat-state.module.ts` owns per-seat derivation: `foldSeatStates(turns, seats, initial, applyDart)`, `activeSeatState`, `otherSeatsComplete`, `durationSeatComplete`, `completedByIndex`. `match-outcome.module.ts` gains `scoreCompareOutcome(seats, direction, soloStatus)`, the status-plus-winner composition all five score-compare engines wrapped around `scoreCompareWinner` identically. On the setup side, `participantsFromGuests` joins `participantsFromSeats` in `session-mode-resolution.ts` and `addTypedGuest` moves to `guest-list.ts`.
Reason: D230's conversion applied one recipe to eight engines, and the recipe's mechanical half was copied eight times with it — `npx fallow` went from main's 70 clone groups / 14.3% duplicated to 95 groups / 18.6%, over its 15% gate, so CI failed on the branch. The half that is genuinely per-ruleset (what a dart does to a seat, when a visit resolves, which metric decides the match) stays in each engine; only the half with no ruleset content in it was lifted. Every extracted function is pure or mutates only the array it is handed, so an engine's observable behaviour is unchanged — the engine tests were re-run unmodified after each extraction as the proof.
Consequences: `npx fallow` exits 0 again: 76 clone groups, 14.8% duplicated, 0 files above the health threshold. Adding a ninth engine now composes these helpers rather than copying a neighbour, and `docs/architecture/04-Architecture-patterns.md` Pattern 18's contract is unchanged — the modules sit under it, not beside it. The remaining duplication is deliberate and logged as F27: the `*-play.data.ts` lifecycle family (left alone because the Play Again reseating fix had just hardened it) and a 268-line whole-class structural clone between Score Training and TUOD that has no extractable blocks left. The 14.8% figure clears the 15% gate by a thin margin, so a future addition can fail it without introducing duplication of its own. The health breach that failed alongside the duplication was NOT `match-outcome.module.ts`'s fan-in — fallow's "start with <file>" line names its top refactoring target, not the failing item — it was `recordTap` in `singles-training-play.data.ts`, whose seat lookup pushed it to cyclomatic 10 / CRAP 31.6; extracting its tap-to-observation mapping cleared it without a `.fallowrc.jsonc` exception.

### D235 — TUOD busts a visit early once its last permitted dart cannot land on any double
Status: Accepted · Date: 2026-08-26
Decision: `tuod.engine.module.ts`'s `visitOutcome()` gains a third bust condition alongside overshoot / remaining-exactly-1 / reached-0-without-a-double: when exactly one dart remains in the visit and the target still remaining is odd (and greater than 1), the visit busts immediately instead of waiting for that last dart to be thrown. Every double scores an even number, so an odd remainder can never be checked out on the last dart regardless of where it lands. `settleVisit()` and `wouldCompleteDart()` both now pass a `dartsRemaining` argument (`maxDartsPerTurn` minus darts already thrown in the visit) into the shared `visitOutcome()` so the rule is written once.
Reason: Issue #161 reported that missing dart 2 for an odd remainder (e.g. a target of 41, two singles leaving 39) still made the player throw a mathematically pointless 3rd dart before the visit closed. This is a TUOD-specific ladder-game UX choice, not a general darts-scoring rule — 501 and 121 share the same `visitOutcome`-shaped bust logic verbatim and are deliberately left unchanged, since a real 501/121 leg still requires all darts to be physically thrown regardless of whether checkout remains mathematically possible.
Consequences: A visit that goes bust on dart 2 now stamps `completedAt` and closes without a 3rd dart ever being recorded — `TuodEngine.record()`/`recordDart()` simply never receive it, since the board UI's own finish-confirm gate (`wouldCompleteDart`) already reflects the same rule. Two regression tests were added to `tuod.engine.module.test.ts` (`describe("TuodEngine.record — dart-by-dart (VISUAL_BOARD)")` and `describe("TuodEngine.wouldComplete — dart path, pure")`) covering the odd-remainder-after-dart-2 bust and confirming dart 1 alone never busts early. No existing test was re-pointed at a different input.

### D236 — TUOD's 1v1 round count is player-configurable, mirroring Score Training's `duration_value` override (D177/D178)
Status: Accepted · Date: 2026-08-26
Decision: `TuodSetupForm.astro`'s 1v1 branch (`guests.length > 0`) replaces the static "1v1 plays 10 Rounds — timed mode is solo-only" notice with a free-typed rounds `Input` (`x-model.number="durationValue"`), matching `ScoreTrainingSetupForm.astro`'s own guested branch exactly. `tuod-setup.data.ts` gains `durationValue`/`clampNotice` state, seeded from the ROUNDS preset's own `duration_value` on `init()`; `start()` clamps a guested session's typed value into a new `clampTuodRounds()` (1–100, `tuod-duration.ts`) and threads it into `createSession`'s `config.overrides.duration_value`, exactly as Score Training's `start()` already does. A solo session is untouched: no override is sent, and the preset's own `duration_value` still applies unmodified.
Reason: Issue #161 reported that 1v1 was hard-locked to exactly 10 rounds with no way to change it, while the notice explaining why timed mode is unavailable added nothing a player could act on. TUOD already forces `durationType` back to ROUNDS once a guest is added (`forceRoundsIfGuested`, unchanged by this decision — timed 1v1 is still out of scope, see `2026-08-22-single-opponent-seat-remaining-engines-design.md`); the round *count* was the only genuinely fixed value left, and the app already has an identical, working mechanism for exactly this (D177/D178) one game over.
Consequences: `tuod-duration.ts` is a new, separately-tested module (`tuod-duration.test.ts`) rather than a shared helper with `score-training-duration.ts`, since TUOD only ever clamps ROUNDS (1v1 is ROUNDS-only) while Score Training clamps both ROUNDS and MINUTES — a shared module would carry an unused MINUTES branch for TUOD. `npx fallow` flags the resulting `durationValueOf`/clamp-and-override shape as a new clone group across `five-oh-one-setup.data.ts`/`score-training-setup.data.ts`/`tuod-setup.data.ts`; duplication moves from 14.2% to 14.3%, still under the 15% gate (`npx fallow` exits 0). Solo TUOD setup gains no new control — it still only toggles between the two fixed ROUNDS/MINUTES presets, unchanged from before this decision.

### D237 — One shared `accuracyDisplay()` helper replaces 5 duplicated hit-rate rounding implementations
Status: Accepted · Date: 2026-08-27
Decision: `app/src/lib/game/play-visit-stats.ts` gains `accuracyDisplay(hits, darts): string`, formatting a hits/darts percentage to exactly 2 decimal places (`"0.00%"` when `darts === 0`). Around The Clock's, Bob's 27's, Shanghai's, Doubles Training's and Singles Training's `*-play.data.ts` result-computation code all call it instead of their own inline `Math.round`/`toFixed` arithmetic. Documented as Pattern 20 in `04-Architecture-patterns.md`.
Reason: Issue #173 reported Doubles Training's accuracy rounding to multiples of 10% — a symptom of `Math.round((hits/darts)*100)` against a small denominator, not a miscalculation. Four of the five hit-rate call sites used that whole-number rounding; only Around the Clock already formatted to 2 decimals, via its own locally-scoped `accuracyLabel`. Fixing Doubles Training alone would have left 4 near-identical, independently-drifting implementations in place — the same shape of duplication D232 already found and consolidated once for engine turn-log/seat-derivation mechanics.
Consequences: All 5 games now render exactly 2 decimal places for their hit-rate percentage, including the zero-darts fallback (previously an inconsistent bare `"0%"` in 3 of the 5 call sites, now `"0.00%"` everywhere). No field was renamed and no `.astro` template changed — every result modal already rendered its field's string value as-is. `app/tests/lib/game/play-visit-stats.test.ts` gained direct coverage for `accuracyDisplay`; the five call sites' own tests were updated to the new 2-decimal string values, not re-pointed at different inputs.
