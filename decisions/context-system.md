<!--
status: canonical
scope: decisions/context-system
read-when: why a docs/context-map/CLAUDE.md/skill/gate choice was made
load-when: docs, context map, CLAUDE.md, skill, gate, check script, knowledge graph, CI, deploy, Prettier, format, husky, pre-commit, branch, PR, workflow, AGENT.md
depends-on: none
related: decisions/architecture.md
updated: 2026-09-13
-->

| # | Source | Decision | Rationale |
| - | ------ | -------- | --------- |
| D50 | P31–35 | Architecture-first: docs are source of truth; implementation conflicting with docs is incorrect; targeted fixes only, never regeneration | Longevity without debt |
| D51 | Cont. session | Design-gate docs `05-Database/07`–`09` are immutable historical records with superseded-decision tables | Immutable history applies to docs too |
| D52 | 2026-07-11 | Context routing system: root `CLAUDE.md` router + `00-Context-Map.md` packs; spec split into `06-Spec/` chapters; raw conversation logs removed after distillation into this ledger; mandatory Context Maintenance protocol for every agent | Token efficiency + guaranteed context completeness |
| D93 | 2026-07-14 | Adopt graphify for a committed AST-only codebase knowledge graph (`graphify-out/graph.json`); consumed via `graphify` CLI documented in `CLAUDE.md`; auto-rebuilt by local git hooks (`graphify extract . --code-only`); no LLM backend (no API cost); semantic/docs extraction and MCP deferred | Early, low-cost shared code map before the codebase grows |
| D94 | 2026-07-14 | Repository restructure: single `docs/` tree (`docs/architecture/`, `docs/superpowers/`), executable SQL promoted to top-level `database/`, ledger at repo root, master context retired (git history only), root `README.md` added | Kill the `architecture/docs/architecture` stutter and the duplicate doc roots |
| D96 | 2026-07-14 | Branch-integration rule: task branches land on `main` via PR at task completion; divergence is a defect; completion gate item 7 verifies it | 2026-07-14 review found `main` ~50 commits stale with zero open PRs |
| D102 | 2026-07-15 | No git worktrees: task branches are checked out directly in the main working copy (`git checkout -b <branch>`), never under `.worktrees/`; `.worktrees/` removed from `.gitignore` | Worktree-per-task left multiple stale worktrees with uncommitted, unrecoverable work sitting undiscovered outside normal branch review |
| D107 | 2026-07-16 | Self-learning gate added to the Context Maintenance protocol (step 8): rule sharpenings discovered mid-task require explicit user approval before being written, never applied unilaterally | Formalizes the propose-then-confirm pattern this design itself was built through |
| D109 | 2026-07-16 | `docs/game-rules/` introduced as the non-canonical home for pre-spec, human-authored game/routine/trivia rule descriptions (rulesets, routines, trivia, authoring templates); wired into root `CLAUDE.md`, `00-Context-Map.md`, `10-Database-Agent-Guide.md`, and `docs/CLAUDE.md` for agent discoverability; deliberately exempt from the canonical status/registration rules `check-context-map.sh` enforces for `docs/architecture/` | Raw domain material existed with no discovery path for agents; wiring it in without promoting it to canonical status keeps architecture-first authority intact while making the input to future game-type/routine work findable |
| D110 | 2026-07-16 | `CLAUDE.md`/`AGENT.md` mirroring, previously a documented-but-unenforced fact (Context Map, 2026-07-15), made an explicit Context Maintenance protocol step and mechanically enforced via new `scripts/check-agent-mirrors.sh` (local + CI, alongside `check-context-map.sh`/`check-file-locations.sh`); the new script immediately caught a real pre-existing drift (`app/AGENT.md` had fallen behind `app/CLAUDE.md` since PR #25), fixed in the same change | User-directed: mirror drift should never be possible to land silently, matching the D105 precedent of converting discipline-only rules into enforced ones |
| D113 | 2026-07-16 | `app/CLAUDE.md`/`AGENT.md` Non-Negotiable Rules gain an explicit `tsconfig.json` paths ↔ `vitest.config.ts` alias sync requirement | Self-learning gate: Task 18 discovered `@lib` was declared in `tsconfig.json` but absent from `vitest.config.ts`, undetected until a test tried a genuine (non-`vi.mock`) import — user approved documenting the drift risk |
| D133 | 2026-07-23 | Context-integrity guards mechanized: `scripts/check-doc-links.sh` validates every markdown link + path-like backtick reference across the canonical doc set (alias/base-aware, bare-identifier-skipping, `DECISIONS.md` history-refs and `docs/superpowers/` excluded), and `scripts/check-context-budget.sh` fails when the Context Map's per-file `~tokens` (strict) or per-pack `~Budget` (best-effort sum) drift >20%/>30% from a chars/4 estimate. Both added to Context-Maintenance step 5 and `checks.yml`. Same audit corrected two stale references (Alpine-Patterns `Timer`→`SegmentTimer`, Shared-Conventions barrel examples) and de-duplicated the always-loaded root `CLAUDE.md` (Forbidden Actions no longer restates Hard Invariants) | Linking staleness and budget drift both slipped past every existing check — mechanize them per the D105/D110/D127 precedent, and stop the always-loaded router paying for redundancy |
| D134 | 2026-07-23 | Retired `099-engineering-workflow-and-decision-framework.md`: it was a compressed duplicate of `03-Engineering-Workflow.md`; Context Map workflow pack / Authority Order #7 / `docs/CLAUDE.md` routing / architecture README now point at `03` alone; PR template checklist authority cites `03` while the actionable checkboxes remain inlined in `.github/pull_request_template.md` | Dual load wasted tokens for no unique authority; checklist already lived in the PR template |
| D135 | 2026-07-24 | Production app deploy is CI-only: reusable `quality.yml` (structure + format/`astro check`/fallow/`npm test`) on PRs and again on `main`, then `deploy.yml` builds and `wrangler deploy`s after GitHub Environment `production` approval; local `deploy.sh` / stale `wrangler.toml` retired; Neon migrate stays out of deploy | Prevents unformatted/broken code reaching Cloudflare; one gate definition avoids PR/deploy drift; Environment approval keeps a human gate without local deploy scripts |
| D136 | 2026-07-24 | Prettier enforced locally via husky + lint-staged pre-commit (`app/` prepare → repo-root `.husky/`) and mandatory agent/pre-PR `npm run format` + clean `format:check`; CI remains fail-only `format:check` with no Actions auto-format | PRs were failing solely on format drift; catch at commit and before PR instead of only in CI |
| D147 | 2026-07-26 | Root `CLAUDE.md`/`AGENT.md` Hard Invariants gain a stacked-branch cap: at most one open task branch may target another task branch; a third stacked branch means the first must land or the work merges into one branch | A four-deep stack (`main ← #41 ← #42 ← #43 ← #48`) caused merges to land child-into-parent instead of onto `main`, silently leaving `main` with only one of four engines (review P1) |
| D157 | 2026-07-28 | `.husky/pre-commit` runs the 7 existing structural `check-*.sh` gates (file-locations, agent-mirrors, astro-class-composition, astro-conventions, game-engines, refinement-coverage, type-barrels) in addition to Prettier lint-staged | These gates only ran in CI, after push; violations were caught in PR review instead of at commit time, same latency pattern D136 already fixed for Prettier |
| D158 | 2026-07-28 | New `scripts/check-alias-sync.sh` fails when `tsconfig.json`'s `compilerOptions.paths` and `vitest.config.ts`'s `resolve.alias` diverge (allowlisting `@styles` as CSS-only); `@icons`/`@layouts` added to `vitest.config.ts` to close the real gap the gate found | Closes D113's exact failure mode (an alias declared in one config but not the other, invisible until a genuine non-mocked import breaks) with a standing gate instead of a one-off fix |
| D159 | 2026-07-28 | New `scripts/check-constraint-mirror.sh` requires every live `chk_*` CHECK constraint on `exercise_stages`/`turns`/`darts` to have a `// MIRRORS: chk_x` anchor comment in the shared batch schema (`app/src/pages/api/sessions/types.ts`); a companion `constraint-mirror.test.ts` executes the boundary agreement | Mechanizes D149 (constraints mirrored once, in the batch schema) the same way `check-refinement-coverage.sh` mechanizes refinement coverage — manifest-matched, not text-scanned |
| D160 | 2026-07-28 | New `scripts/check-no-inline-comments.sh` fails on a `//` or non-JSDoc `/* */` comment lexically inside a function/method body under `app/src/**/*.ts` (JSDoc `/**` above a declaration stays exempt, matching the existing "JSDoc above the declaration" rule); found and fixed 3 pre-existing violations (`register-stores.ts`, two in `score-training-setup.data.ts`) that had shipped under the same rule as prose only | The rule existed since app/CLAUDE.md's TypeScript comments section but was never mechanically checked; the gate's first real run caught real violations, confirming the prose-only gap |
| D162 | 2026-07-28 | New `checks.yml` job `branch-stack-cap` fails a PR whose base branch is itself the head of another open PR targeting a non-default branch | Mechanizes D147 (at most one open branch may target another) against live PR state, which a local script cannot see reliably |
| D163 | 2026-07-28 | New `checks.yml` job `test-repointing-heuristic` posts a non-blocking PR comment when a changed `app/tests/**` assertion literal has no changed `describe`/`it` title in the same diff | Nudges human review toward D148's exact failure shape (a dropped constraint hidden behind a quietly repointed test) without hard-failing legitimate test updates |
| D164 | 2026-07-28 | New skill `.claude/skills/context-maintenance/SKILL.md` holds the 8-step Context Maintenance procedure verbatim; root `CLAUDE.md`/`AGENT.md` keep only the invariant and a pointer to the skill | Procedure prose was loaded on every session regardless of relevance; skills load on demand |
| D165 | 2026-07-28 | New skill `.claude/skills/validate-app/SKILL.md` holds the `validate:app` sequence and mid-task gate condition; `app/CLAUDE.md`/`AGENT.md` keep only the bare command and a pointer | Same context-budget rationale as D164, scoped to `app/` tasks |
| D167 | 2026-07-28 | New skill `.claude/skills/run-all-gates/SKILL.md` dispatches the right `check-*.sh` scripts and `validate:app`/database checklist by changed area (`app/`, `database/`, `docs/`) and reports each result explicitly | Which gate applies to a given change was previously a memory burden; this makes it a lookup, and gives `verification-before-completion` a concrete command to point at |
| D168 | 2026-07-28 | Split `branch-stack-cap`/`test-repointing-heuristic` out of `checks.yml` into new `.github/workflows/pr-gates.yml` with an unfiltered `pull_request: types: [opened, synchronize, reopened]` trigger; `checks.yml` keeps only the `quality` job on `branches: [main]`. Also dropped `--depth=1` from the test-repointing job's `git fetch` (it grafted the base tip as parentless, breaking the three-dot diff once `main` advanced past the branch point) | `checks.yml`'s original `branches: [main]` filter meant both PR-state jobs only ran for PRs targeting `main`, so `branch-stack-cap` could never evaluate a PR targeting a stacked non-main branch — the exact D147 incident shape — making the root `CLAUDE.md` claim that D147 is "mechanically enforced on every PR" false (final whole-branch review) |
| D169 | 2026-07-28 | `.husky/pre-commit` gains `set -e`; previously its two top-level statements were not `&&`-joined, so a failing `npx lint-staged` did not stop the hook and the hook's exit code was only the final structural-gate chain's | Prettier failures no longer blocked commits, silently regressing D136's "catch format issues at commit" guarantee (final whole-branch review) |

### D184 — The decision ledger is a router plus domain files, not one document
Status: Accepted · Date: 2026-08-02
Decision: `DECISIONS.md` holds only front-matter, the authority note, the Source key, a routing table, the Deferred list, and the rules for adding a decision. All decisions live in `decisions/**`, one file per ownership domain, each carrying `load-when` / `depends-on` / `related` front-matter.
Reason: At 163 decisions / 65 KB the single file cost every task the entire project history; a frontend task loaded 27 API and 14 database decisions it never read.
Consequences: Decision IDs stay frozen (`D01`–`D183`) because ~180 citations reference them by ID, not location, so the split required no citation edits. Decisions from D184 on use a block format with Reason/Consequences; the 163 migrated rows keep their original terse table form permanently, since back-filling rationale nobody recorded would mean inventing it. `scripts/check-decision-ids.sh` enforces uniqueness, no-disappearance, `Supersedes:` resolution, and a row-free router.

### D185 — Graph freshness is CI-owned; the local CLI is optional
Status: Accepted · Date: 2026-08-03
Decision: Knowledge-graph freshness is owned by CI, not by each agent/clone. `.github/workflows/graph.yml` rebuilds `graphify-out/graph.json` on every push to `main` (`GRAPH_REFRESH_STRICT=1`) and opens/updates a PR carrying the rebuild, since `main` is protected and the workflow cannot push directly to it; on pull requests it posts a non-blocking delta comment instead. Installing the `graphify` CLI locally remains optional, for `graphify query/path/explain` while working, and is no longer a per-clone requirement.
Reason: `scripts/refresh-graph.sh` exited 0 whenever the CLI was absent, so any clone that skipped the manual install silently skipped the rebuild too — four consecutive branches (score-training configurable-duration, bottom-nav iPhone Home Screen fix, 501-recreational-v1, decision-ledger-split) shipped knowledge-graph-staleness disclosures instead of a refreshed graph. By the time this was corrected, the committed graph had drifted to 2,321 nodes / 2,762 links against 2,872 / 4,085 on a clean rebuild, and its `built_at_commit` (`391d2dd`) was no longer reachable in history.
Consequences: `GRAPH_REFRESH_STRICT=1` makes a broken CI install fail the workflow loudly instead of no-op-ing the way the local default does. Delta comparison must normalise `built_at_commit` away as the sole volatile field, or every run would register as dirty even with no real content change. A hard-blocking PR gate on graph freshness was considered and rejected: it would make graphify CLI availability a prerequisite for contributing at all, which is the exact assumption that had already failed to hold across four branches.

### D186 — Opening the graph-refresh PR depends on a repo setting; the workflow degrades instead of failing
Status: Accepted · Date: 2026-08-05 · Refines: D185
Decision: `graph.yml`'s `refresh` job treats PR creation as best-effort. It always rebuilds, commits and force-pushes `chore/graph-refresh`; if `POST /pulls` returns 403 "GitHub Actions is not permitted to create or approve pull requests" it emits a warning plus a run summary carrying the branch, a `compare` link and the delta, and finishes green. Any other error from that call still fails the job. Full automation requires *Settings → Actions → General → Workflow permissions → "Allow GitHub Actions to create and approve pull requests"*, which is off by default on GitHub.
Reason: measured on the first real post-merge run (run 30978978985, 2026-08-05). Every step succeeded — rebuild, the normalised gate, the commit, the force-push — and only the PR-open step 403'd, because `GITHUB_TOKEN` cannot open pull requests unless that repo setting is enabled. D185 asserted the job "opens/updates a PR" without recording that this is contingent on a setting the workflow cannot control.
Consequences: with the setting off, the refreshed graph still lands on `chore/graph-refresh` and needs a one-click manual PR; the run stays green so `main` is not painted red on every merge, since a permanently-red required check is noise that gets ignored — the same not-noticing that let the graph go stale for four branches. The cost is that the pending branch is visible only in the run summary, so the setting is the preferred fix and the fallback is the safety net. Failing the run was rejected for the noise reason; silently succeeding with no summary was rejected because the waiting branch would be invisible.

### D194 — A gate that can silently find nothing is not a gate
Status: Accepted · Date: 2026-08-08
Decision: `scripts/check-context-map.sh` pins `LC_ALL` to an available UTF-8 locale before its migration-range check, and warns loudly when none exists. That check is also narrowed: `decisions/**` is excluded from it entirely, and lines naming seeds are skipped.
Reason: the range pattern matches a U+2013 en-dash. Under the POSIX/C locale that character is three bytes, its continuation bytes consume the pattern's `.?` slots, and every range stops matching — so the check passed by finding nothing rather than by finding nothing wrong. CI runs in a UTF-8 locale and did match, which is how six stale ranges reached PR #73 after the gate had been reported green locally on every run of the branch. A checker whose failure mode is silence is worse than no checker: it converts an unverified claim into a reported pass.
Consequences: the exclusions resolve a real contradiction between two gates. `check-decision-ids.sh` hashes every migrated decision row so it can never be edited, while this check demanded that D21's `chain 0001–0016` follow the chain forward — both could not hold, and the ledger is the side that must not move, because a decision records what was true when it was made. Seed ranges are skipped because seeds carry their own numbering that the migration chain max says nothing about; `docs/architecture/README.md`'s `seeds 0001–0006` was being reported as a stale migration range. Any future gate whose matching depends on non-ASCII text must pin its locale the same way, or it inherits this failure mode.

### D213 — The context map routes; it does not carry the record
Status: Accepted · Date: 2026-08-19
Decision: `00-Context-Map.md` is reduced to a router (context packs, authority order, maintenance protocol). The canonical file inventory moves to `00-File-Inventory.md`, loaded only when a pack falls short; version history, per-task `docs/superpowers/**` rows and Current Implementation State move to `00-Context-Map-History.md` (`status: historical`), which no task loads. `AGENT.md` stops mirroring `CLAUDE.md` and becomes a fixed pointer stub, with `scripts/check-agent-mirrors.sh` inverted to assert the stub. The `Tool Allowances & Restrictions` sections are deleted from all six `CLAUDE.md` files.
Reason: the map was 37.6k tokens and the root `CLAUDE.md` protocol opened it at the start of every task, while the packs it routes to budget 2–18k — the router cost 2–10x the payload. 28.5k of it (76%) was provenance: a single 75k-character version-history line, 57 rows describing point-in-time specs and plans, and drift-prone status prose. No gate ever wanted them — `check-context-map.sh` check 4 only requires top-level `docs/architecture/*.md` files be registered. The six `AGENT.md` files were byte-identical copies enforced identical by a gate, taxing every rule edit twice. The tool-allowance prose bound nothing: Claude Code enforces tool access through `.claude/settings.json`, and the prose was already contradicted by practice, restricting `mcp__github__*` in a repo whose workflow is issue-to-PR.
Consequences: the per-session context floor drops from ~39.3k to ~3.0k tokens. Nothing is deleted — every relocated token still exists in an unloaded file. Four gate scripts had to move with the split: `check-context-map.sh` registers against the inventory, `check-context-budget.sh` reads packs from the map and file rows from the inventory, `check-doc-links.sh` skips the path-backtick pass for `status: historical` documents (the carve-out `decisions/**` already had, for the identical reason), and `check-agent-mirrors.sh` asserts the stub instead of the mirror. No new gate script was created, so `.github/workflows/quality.yml` and `.husky/pre-commit` are unchanged. Future context growth lands in the history archive, which is written on every task and read on none. The graph rule in root `CLAUDE.md` — instructing agents to query a CLI that is absent from the session container — is knowingly left standing for a later change.

### D214 — A finding is logged, not fixed; acting on one needs permission
Status: Accepted · Date: 2026-08-19 · Supersedes: D107
Decision: Anything an agent notices that its task was not asked to change is appended to a new root `FINDINGS.md` and named in the completion report, never fixed in the same pass; acting on a finding requires explicit user permission and becomes its own task on its own branch. The rule is a root `CLAUDE.md` Hard Invariant, the log is shape-guarded by `scripts/check-findings-log.sh` (gate 16, in `.husky/pre-commit` and `quality.yml`), and `context-maintenance` step 8 becomes the Findings gate. Findings are deleted when resolved, and ids come from a `highest-issued:` high-water mark rather than a scan.
Reason: an agent that found a defect mid-task had three bad options and no good one — widen the diff past its assignment, mention it in chat where the record dies with the session, or park it in `DECISIONS.md`'s Deferred list, where five deliberately-unbuilt features and a known-broken script were filed under one heading. Five live contradictions were sitting unrecorded in the repo when this was written, every one found incidentally during earlier tasks. D107's self-learning gate already encoded propose-then-confirm but only for `CLAUDE.md`/`AGENT.md` rule sharpenings, and half its target went dead when D213 reduced every `AGENT.md` to a pointer stub; a stale doc or a bug outside scope is not a rule sharpening, so it never fired for any of the five.
Consequences: the per-session context floor rises from ~3.2k to ~3.4k tokens — the only floor increase in the change, accepted because a non-negotiable rule belongs where every task already reads. `FINDINGS.md` cannot grow into a provenance blob the way the pre-D213 context map did, because resolved entries are deleted rather than restatused; the cost is that a finding's history after deletion is git history alone, plus a `decisions/**` entry where the fix embodied a real choice. The gate proves the log's shape and that every cited evidence path still resolves; it cannot prove an agent logged instead of fixed, and no script can — the invariant carries that obligation. The log ships seeded with F1–F5 and none of them fixed, F3's one-word correction included: a rule that yields to convenience on its first pass is read as advisory forever after.

### D223 — The type gate fails on hints; done means 0 errors, 0 warnings, 0 hints
Status: Accepted · Date: 2026-08-21
Decision: `npm run check` runs `astro check --minimumFailingSeverity hint`, so any hint-level diagnostic — an unused import, an unread parameter — exits non-zero, and `.github/workflows/quality.yml`'s Type gate invokes that script instead of calling `npx astro check` directly. The definition of done for `app/` work is that every step of `validate:app` exits zero, `npx fallow` included, and the type gate reports 0 errors, 0 warnings, 0 hints. The bar is stated in `app/CLAUDE.md` and the `validate-app` skill; the fix for a hint is to delete the dead declaration, never to suppress it.
Reason: `astro check`'s default `--minimumFailingSeverity error` printed hints and then exited zero, so three unused declarations had accumulated across `src/` and `tests/` while every task truthfully reported the gate green. Reporting a diagnostic that cannot fail anything trains agents to read the summary line as decoration. CI compounded it by running the bare binary rather than the package script, so hardening the script alone would not have reached the pipeline. The same pass fixed `npx fallow`, red on the baseline (F12) because `foldFiveOhOneState` breached the cognitive-complexity ceiling: two gates that could not go green had made `validate:app` unrunnable end to end, which is the condition under which a completion bar stops being one.
Consequences: dead code cannot accumulate behind a passing gate. Every future task pays the cost of deleting unused declarations before it can claim done, and an agent removing the last use of an import must remove the import in the same pass. `--minimumSeverity` stays at its default so hints are still printed; only the failing threshold moved. The 71 clone groups `fallow dupes` reports (14.9% duplication) remain non-gating: `duplicates.threshold` is unset, `fallow dupes` exits zero on its own, and the deliberate `*-play.data.ts` duplication D215 accepted is untouched by this decision.

### D255 — Doc comments document the declaration, not its decision history
Status: Accepted · Date: 2026-09-03
Decision: `app/CLAUDE.md`'s comment rule (§Comments, scope widened from `app/src/**/*.ts` to also cover `.astro` frontmatter) now bars restating decision history inside a JSDoc/block comment — no decision id, phase number, prior version, or architecture-doc paragraph narrated in prose. A comment states the declaration's contract and any genuinely non-obvious constraint; rationale and alternatives-weighed stay in `docs/architecture/**`/`decisions/**`, cited by a short pointer.
Reason: the DartBot level-picker implementation plan (`docs/superpowers/plans/2026-09-03-dartbot-level-picker.md`) drafted JSDoc blocks that restated the design spec's rationale in full — the same pattern already visible across the codebase (`guest-list.ts`, `OpponentChooserModal.astro`, `BoardMagnifier.astro`), where a doc comment doubles as an architecture essay. The existing rule only said "put necessary detail in JSDoc," which had no ceiling and rewarded exactly this.
Consequences: existing long-form comments already in the codebase are not retroactively rewritten by this decision — the rule governs new and touched comments going forward. A comment that only points at a doc/decision id is now correct where a full restatement was previously the norm; `01-Principles.md`'s Architecture First still owns *why* code exists, comments own *what* a declaration does.

### D273 — Discovered work moves from `FINDINGS.md` to GitHub issues
Status: Accepted · Date: 2026-09-13 · Supersedes: D214
Decision: `FINDINGS.md` and its three support scripts (`scripts/check-findings-log.sh`, `scripts/check-plan-findings-closure.sh`, `scripts/check-plan-findings-closure-hook.sh`) are deleted, along with their wiring in `.husky/pre-commit`, `.claude/settings.json`, and `.github/workflows/quality.yml`. Anything an agent notices that its task was not asked to change is now filed as a GitHub issue (`discovered-work` label plus one `type:*` and one `severity:*` label) per the new `.claude/skills/capturing-discovered-work/SKILL.md`, mirrored for human use by `.github/ISSUE_TEMPLATE/discovered-work.yml`. D214's underlying rule is unchanged: never fixed in the same pass, always surfaced, acting on one needs explicit user permission and its own task/branch. The 31 findings open at the time of this decision (F15–F94) were migrated verbatim to GitHub issues #280–#310, preserving their Claim/Evidence/Impact/Proposed content under the new skill's Problem/Why It Matters/Proposed Fix headings.
Reason: `FINDINGS.md`'s append-then-delete high-water-mark log had no duplicate detection, no cross-referencing, no assignment, and no state beyond Open/Raised — every one of which GitHub issues already provide for free. The file's own gate (`check-findings-log.sh`) could only prove shape (front matter, field completeness, id uniqueness, live evidence paths); it could never prove an agent logged instead of fixed, the same limitation D214 already named as unfixable by any script. A companion gate (`check-plan-findings-closure.sh`/its `PostToolUse` hook) existed solely to keep a plan's "Closes FINDINGS.md F\<id\>" claim honest — a problem that disappears entirely once closure is "the GitHub issue is closed," a state GitHub already tracks natively.
Consequences: the per-session context floor drops by `FINDINGS.md`'s own ~12.1k tokens (never loaded by a task, so the floor effect is on `00-File-Inventory.md`'s row list, not the always-loaded root `CLAUDE.md`, whose one-line invariant is now slightly longer instead). The record moves outside the repository: reading "what's currently open" now means querying GitHub (`label:discovered-work`) rather than opening one file, a real cost traded for issue search, labels, and cross-linking the flat file had no way to offer. Historical citations of `FINDINGS.md`/specific `F<id>`s already written into accepted `decisions/**` blocks, `08-DartBot.md`'s append-only version-history log, and existing code comments are left exactly as they were — they are provenance for decisions already made, not pointers into a mechanism that still needs to resolve. `app/CLAUDE.md`'s pre-commit gate count drops from 14 to 13.

### D287 — A degradation nobody can see is an outage; the graph fallback files an issue
Status: Accepted · Date: 2026-09-17 · Supersedes: D186 · Refines: D185
Decision: `graph.yml`'s blocked-PR fallback opens or updates a tracking issue (`graph: refresh PR could not be opened`, labelled `discovered-work`/`type:infrastructure`/`severity:high`, keyed on a `<!-- graph-refresh-blocked -->` body marker and matched on both that marker and the bot author) instead of only writing a run summary, and closes that issue on the next run that does open a PR. `issues: write` is added to the `refresh` job. The repo settings *Allow GitHub Actions to create and approve pull requests* and *Allow auto-merge* are both enabled, so the documented path is: CI rebuilds, opens the PR and arms auto-merge pinned to `SQUASH`; the owner approves; it squash-merges. The ruleset's approval requirement is not waived and the graph refresh is not exempted from it.
Reason: D186's fallback was correct about not painting `main` red and wrong about where the signal goes. A `core.warning` plus a run summary is read by nobody: the committed graph reached 692 commits and six weeks of staleness — every subsystem in `app/src/modules/exercise/`, `training/`, `dartbot/` and migrations `0024`–`0032` missing from the map agents are told to consult *first* — while every graph run reported success (#345). Absence from the graph reads as absence from the repo, so the failure mode was agents confidently answering from a map of a codebase that no longer existed.
Consequences: the blocked state is now visible in the issue list, where work is already tracked, and costs `main` nothing — the run still finishes green and any non-403 error still fails the job, both per D186. The fallback's issue lookup is scoped to the `discovered-work` label, so an issue whose labels are stripped by hand goes unmatched and the next run opens a duplicate. The fallback was merged *before* the repo settings were flipped, so its blocked branch executed at least once against the real API rather than being assumed correct from the docs; it then fired for real (issue #373) and self-closed on the first successful open (PR #374). Auto-merge arming is measured only in its failing form so far: the first armed attempt was rejected with "merge commits are not allowed on this repository" because `main`'s ruleset requires linear history, which is why the call is pinned to `SQUASH`; no refresh has produced a graph delta since that pin, so the success path is reasoned, not observed, and the next real delta is its first test. One human approval per refresh remains, which is the price of not making a bot a ruleset bypass actor — direct-commit-to-`main` was the considered alternative and was rejected on least privilege.

### D289 — The graph refresh lands by a human merge; a bot cannot satisfy required signatures
Status: Accepted · Date: 2026-09-17 · Refines: D287
Decision: `graph.yml` no longer arms auto-merge on the refresh PR. It rebuilds, force-pushes `chore/graph-refresh`, opens or updates the PR, writes a notice naming the PR, and stops there; the PR body states that landing it is a manual merge and why. Root `CLAUDE.md`'s freshness bullet says the same. The `required_signatures` rule is not relaxed, the bot is not added as a bypass actor, and no signing key is introduced for CI.
Reason: measured on PR #379. `enablePullRequestAutoMerge` succeeded and the PR still reported "Merging is blocked — Commits must have verified signatures": the `Main branch protection` ruleset (active since 2026-07-10) carries `required_signatures`, `GET /pulls/379/commits` returns `verified: false, reason: "unsigned"` for the `github-actions[bot]` commit, and the ruleset's only bypass actor is the repository owner. D287 recorded the SQUASH pin as making auto-merge work once armed; that inference was wrong, and it was wrong in the direction D287 itself warned against — a decision block overstating automation. Approval was never the blocker, so the earlier framing ("needs one approval") misattributed the cost.
Consequences: landing a refresh is one deliberate merge by a bypass actor, which is what already happened for PRs #368, #374 and #379 — the change is that the workflow and the docs now say so instead of advertising an automation that cannot complete. Three alternatives were weighed and rejected: an SSH signing key in Actions secrets (a private key to hold and rotate, for one saved click); `createCommitOnBranch`, which GitHub signs itself but would carry a 7.2 MB `graph.json` base64-encoded through GraphQL; and adding the bot as a bypass actor, which is per-ruleset rather than per-rule and would also exempt it from the pull-request and linear-history requirements. The 403 fallback from D287 is untouched: a PR that cannot be opened at all still files a tracking issue, and that remains the case this mechanism actually needs to catch.

### D293 — Worktrees are allowed again, with landing as the cleanup trigger
Status: Accepted · Date: 2026-09-17 · Supersedes: D102
Decision: task branches may be worked in a git worktree under `.claude/worktrees/` or checked out directly in the main working copy. The root `CLAUDE.md` No-git-worktrees invariant, `app/CLAUDE.md`'s restatement and `writing-plans`' context note are replaced by the permissive rule; `.claude/worktrees/` is gitignored, and a worktree is removed once its branch lands.
Reason: the repo moved from a customized copy of the superpowers skills to the official plugin, whose skills (`using-git-worktrees`, `executing-plans`, `subagent-driven-development`, `finishing-a-development-branch`) assume worktree isolation and carry their own cleanup step. D102 banned worktrees because worktree-per-task left stale worktrees holding uncommitted, unrecoverable work outside branch review — a gap the plugin's `finishing-a-development-branch` now closes by removing the worktree only after a merge succeeds. Keeping the ban meant every plugin skill had to be locally contradicted, which is the condition issue #356 records.
Consequences: the plugin's skills run unmodified, and parallel agents can hold separate checkouts instead of serialising on one working copy. D102's failure mode returns as a live risk: nothing mechanically proves a landed branch's worktree was removed, so `git worktree list` showing a worktree whose branch is merged is the signal to prune. Issue #356 (plugin `executing-plans` contradicts the invariant) is resolved by this decision rather than by editing the plugin.

### D311 — Superpowers process skills come from the official plugin, never vendored
Status: Accepted · Date: 2026-09-18
Decision: the eight vendored copies of Superpowers process skills under `.claude/skills/` (`brainstorming`, `executing-plans`, `finishing-a-development-branch`, `subagent-driven-development`, `systemic-debugging`, `using-superpowers`, `verification-before-completion`, `writing-plans`) are deleted — 26 files, 4,481 lines. The official plugin `superpowers@claude-plugins-official` is declared in `.claude/settings.json` (`extraKnownMarketplaces` + `enabledPlugins`) and is the sole source of those skills, invoked under their `superpowers:` prefix. `.claude/skills/` now holds only this repo's own five skills (`capturing-discovered-work`, `context-maintenance`, `graphify`, `run-all-gates`, `validate-app`). The two project deviations that lived inside the vendored copies move into `CLAUDE.md` files, where the authority order already puts them above any skill: the always-Option-2 branch finish into root `CLAUDE.md`'s new §Superpowers Skills, and the `app/` red→green→refactor cycle into `app/CLAUDE.md`'s §Test-Driven Development, which had cited the vendored copy twice. Two further deviations are dropped as redundant — `writing-plans`' worktree note restates a Hard Invariant, and its plan-save path was never a deviation at all (identical upstream).
Reason: the copies were forked from an older Superpowers and had drifted from the installed v6.3.0 in ways nobody chose — measured against the installed version: `subagent-driven-development` 710 diff lines, `finishing-a-development-branch` 306, `brainstorming` 181, `systemic-debugging` 52, `verification-before-completion` 34, `executing-plans` 22, `writing-plans` 15, `using-superpowers` 14. `systemic-debugging` was a misspelt directory whose `name:` front-matter already read `systematic-debugging`, so it shadowed the plugin's skill from a different path; its entire diff was Prettier table reformatting, no repo content. Every plugin update widened the gap silently and nothing in the repo could detect it: none of the eight was registered in `00-File-Inventory.md`, so no gate script ever read them. A vendored copy is only worth its maintenance cost when the fork is deliberate; five of the eight carried no repo-specific content at all. An earlier attempt at this removal (2026-09-16, uncommitted on a stale base in a sibling worktree) was lost when its work was not landed — this decision records the change on `main`'s lineage so it cannot be silently restored again.
Consequences: plugin skills now update without repo changes, and the repo can no longer pin a version — a breaking upstream change to `finishing-a-development-branch`'s option numbering would silently invalidate root `CLAUDE.md`'s override, which names "Option 2" by number. The plugin's `using-git-worktrees` skill returns and plugin `executing-plans` step 1 points at it; D293 already allows worktrees under `.claude/worktrees/`, so this is consistent rather than contradictory. A clone that never fetches the marketplace gets no process skills at all, where before they were in-tree — accepted because `.claude/settings.json` now declares the marketplace, making the fetch automatic rather than manual. The per-session context floor is unchanged: skills load on invocation, not at session start. Prose references to the deleted copies are re-pointed at the prefixed plugin names in `.claude/skills/run-all-gates/SKILL.md`, `00-File-Inventory.md`'s `app/CLAUDE.md` row, `05-Database/10-Database-Agent-Guide.md` and `docs/game-rules/README.md`; historical mentions inside `docs/superpowers/plans/**` and `00-Context-Map-History.md` are left as written, being provenance rather than live pointers.

### D312 — Repo deltas are paired with plugin skills, and the repo bends where the plugin is right
Status: Accepted · Date: 2026-09-18 · Supersedes: D293 (worktree path only) · Refines: D311
Decision: each deviation from a `superpowers` plugin skill is carried in a *paired* file named by a four-row table in root `CLAUDE.md`, never as prose the reader must remember to apply and never as a copy of upstream text. The pairs: `finishing-a-development-branch` → the new `.claude/skills/finishing-a-dart-branch` skill (Step 4's menu skipped, Option 2 every time, `run-all-gates` standing in for "the test suite"); `test-driven-development` → `app/CLAUDE.md` §Test-Driven Development; `verification-before-completion` → `run-all-gates`; `executing-plans` and `subagent-driven-development` → `capturing-discovered-work`, which is preserved as-is — incidental findings become GitHub issues and are never fixed in the same pass. Where the plugin was right and the repo was not, the repo moves instead: worktrees live under `.worktrees/` (gitignored) rather than `.claude/worktrees/`, and `superpowers:brainstorming` runs unmodified — it writes its design doc to `docs/superpowers/specs/` and commits it without asking, the single named exception to the "do not commit unless the user asks" invariant. `docs/superpowers/specs/` is declared non-canonical in `00-Context-Map.md` on the same terms as `docs/game-rules/`.
Reason: D311 removed the forks but left both deviations as prose in `CLAUDE.md` files, which is the weakest possible coupling for the branch-finish rule: `finishing-a-development-branch` says "Present the menu exactly as written" and "Integration is your human partner's decision" — emphatic text arriving in-context long after the line that overrides it. A paired skill fires at the same moment as the skill it modifies and costs one description line until invoked. The worktree path was a measured defect, not a preference: the plugin's Step 6 claims cleanup only for worktrees under `.worktrees/` or `worktrees/` and leaves everything else to "the host environment", so D293's `.claude/worktrees/` meant the plugin never removed a worktree it created while the invariant said one left behind is a defect — six were live when this was written, four of them stale. The brainstorming commit carve-out follows the same logic in the other direction: the blanket no-commit rule was written before the plugin owned the spec artifact, and intercepting a skill's own output is exactly the local contradiction D311 set out to stop. `app/CLAUDE.md`'s TDD section was also mis-paired, citing `verification-before-completion` when the plugin ships a `test-driven-development` skill that is the actual counterpart.
Consequences: a plugin skill can now be read on its own without silently violating a repo rule, because the override travels with it. The pairing table is a maintenance surface: an upstream rename breaks a row, and nothing mechanically checks the four names resolve — the same version-pinning gap D311 already accepted, now with four rows instead of one prose block. `finishing-a-dart-branch` is a skill in `.claude/skills/` that shadows nothing: the name is distinct, so both it and the plugin skill stay addressable, and it carries only the delta, so there is no upstream text to drift. Two worktree homes coexist until the pre-D312 worktrees land — `.gitignore` covers both, and the older path is not migrated by hand because those worktrees hold other branches' work. Specs now enter the repo without a gate having seen them; they are historical material, already registered as one inventory row for the tree rather than per file, and rank below every numbered item in the authority order.

### D314 — Accidental whole-file loads are denied at the tool boundary, and the skill tree is registered
Status: Accepted · Date: 2026-09-18
Decision: `.claude/settings.json` denies `Read` on the four paths nothing should ever load entire — `graphify-out/graph.json`, `.worktrees/**`, `.claude/worktrees/**`, and the `drizzle-kit`-generated `app/src/db/schema.ts` — and sets `claudeMdExcludes` so a `CLAUDE.md` inside a worktree copy is not auto-loaded beside the real one. A new gate, `scripts/check-skill-pointers.sh`, asserts that every `.claude/skills/*/SKILL.md` and every `.claude/rules/*.md` carries a row in `00-File-Inventory.md`, and that every `superpowers:<name>` named in a live tracked Markdown file resolves under the installed plugin cache. It is wired into `run-all-gates` §Always run, `quality.yml`'s structure job and `.husky/pre-commit`. `check-doc-links.sh`'s scan set gains `.claude/rules/*.md`.
Reason: D311 found that none of the eight vendored skill copies was registered in the inventory, so no gate script ever read them and every upstream drift landed unobserved; the registration assertions close that for the files the repo owns. The denies address a different failure — not drift but volume: `graphify-out/graph.json` is 7.7 MB, roughly 1.9M tokens read whole, while root `CLAUDE.md` invites exactly that read; a worktree is a second full copy of the repo; and `schema.ts` is regenerated output whose authority is `database/migrations/`. Grep over the graph is untouched, because grep is what the manual actually wants. The gate lands before the `.claude/rules/` tree exists so that tree is born covered — a gate landing after the tree it checks has nothing to say about how that tree was built.
Consequences: `superpowers:` pointer resolution is a local-only check. CI installs no plugins, so it prints `SKIP` and passes there; making it hard-fail would leave a permanently red check CI cannot fix, which is the "advisory gate" failure mode in a different costume. `claudeMdExcludes` is ships-and-hopes: `strings` on binary 2.1.236 confirms the identifier exists in the build, which is not confirmation that this key name and glob syntax are honoured — an unrecognised settings key is ignored, so the downside of being wrong is that it silently does nothing rather than that it breaks something. The same uncertainty is load-bearing for the path-scoped-rules branch, which is why that branch owes a live probe before it commits to six files. `.claude/skills/**/*.md` is not yet in the link gate's scan set: widening it today fails on 8 dangling `references/*.md` pointers in `graphify/SKILL.md` — inherited from a fork that copied the file without upstream's `references/` directory — and the next branch deletes that file, so the widening travels with the deletion rather than fixing text twice.

### D315 — The knowledge-graph skill is a lookup skill, and the graph is never read whole
Status: Accepted · Date: 2026-09-18
Decision: `.claude/skills/graphify/SKILL.md` (678 lines) is deleted and replaced by `.claude/skills/graph-lookup/SKILL.md` (~40 lines, one tested query recipe) plus `.claude/skills/graph-lookup/references/building.md`. The new `description:` triggers only on locating a named entity in this repository and explicitly excludes building. Root `CLAUDE.md`'s "or read it directly" clause is removed and its install pointer moves from `app/CLAUDE.md` to `references/building.md`; `app/CLAUDE.md`'s §Knowledge Graph becomes a two-line pointer, 145 lines → 133. `check-doc-links.sh` gains `.claude/skills/**/*.md`.
Reason: a skill's `description:` is its trigger, and this one — "any question about a codebase, its architecture, file relationships, or project content" — was the broadest in the repo, so a nine-step build pipeline loaded on routine questions. It was also unused: `.github/workflows/graph.yml` installs `graphifyy[sql]` and runs `scripts/refresh-graph.sh`, which hardcodes `graphify update .`; no path in this repo reads the skill. The deleted text described the upstream tool's full surface — chunked subagent extraction, Neo4j/FalkorDB/GraphML exports, Obsidian vaults, video transcription — none of which this repo runs. Separately, "or read it directly" invited a whole-file read of 7.7 MB, roughly 1.9M tokens; grep and the Python recipe return a handful of lines from the same file, which is what the instruction was always reaching for.
Consequences: the skill no longer documents how to *build* a graph from scratch, only how to query the one that exists — correct here, where builds are CI's, and a real loss for anyone wanting to point graphify at an unrelated folder, who should install the upstream plugin rather than read a repo skill. The rename shadows nothing: `graphify` the CLI, `graphifyy` the package, `graphify-out/` the directory, `scripts/refresh-graph.sh` and `.github/workflows/graph.yml` all keep their names, and only the skill moved. Mentions of the old skill path inside `docs/superpowers/**`, `docs/braindump/`, `00-Context-Map-History.md` and `decisions/**` are left as written, being provenance rather than live pointers — the same carve-out D311 made. Deleting the file also unblocked `check-doc-links.sh` over `.claude/skills/**`: its 8 `references/*.md` pointers named files this repo never had, inherited from a fork that copied `SKILL.md` without upstream's `references/` directory, and they were the only thing the widened gate failed on.

### D316 — Directory rules load with the directory; app/CLAUDE.md keeps only what is app-wide
Status: Accepted · Date: 2026-09-18
Decision: five directory guides are added — `app/src/services/`, `app/src/components/`, `app/src/stores/`, `app/src/modules/training/` and `app/tests/` — each with the mandatory `AGENT.md` pointer stub, joining the two that already existed under `app/src/`. The `GameEngine` contract moves to `.claude/rules/game-engines.md`, scoped by `paths: ["**/*.engine.module.ts", "app/src/modules/game/**"]`. `app/CLAUDE.md` loses the style non-negotiables, the Alpine and store clauses of the Handbook line, both engine Non-Negotiables, the Astro upstream link list and two TDD detail bullets — 145 lines to 104, counting the graphify trim in D315. Every relocated rule is deleted from `app/CLAUDE.md` in the same commit that adds it elsewhere. `scripts/check-doc-links.sh` derives its `CLAUDE.md`/`AGENT.md` scan set from `git ls-files` instead of a hardcoded list.
Reason: `app/CLAUDE.md` was loaded in full on every `app/` task while roughly a third of it applied to one directory. The trigger already existed and was already proven here — `database/CLAUDE.md` opens by telling the reader which context pack to load, and fires only when `database/` is touched. What the repo lacked was coverage: two narrow directories had guides while `services/`, `components/`, `stores/`, `modules/` and `tests/` — where most work happens — had none. Extending a mechanism every gate already understands beat introducing a second one for the same job, which is why the research report's nine `.claude/rules/` files became one: four of the nine duplicated directory guides that already exist. The single glob is not a preference — `*.engine.module.ts` spans `modules/game/` and `modules/training/exercises/`, so no directory boundary expresses it.
Consequences: what moves is decided by when a rule must fire, not by how much text it saves. Test *placement* stays in `app/CLAUDE.md` because a directory guide loads on read and placement is needed before the first read; the red→green→refactor procedure stays for the same reason, and remains the sole definition (D99). That principle caps the reachable saving well above the research report's ~55–60 line target and above this branch's own spec figure of ~85–90; the measured landing point is 104 lines, and chasing a smaller number would mean deleting rules that genuinely are app-wide. `.claude/rules/game-engines.md` rests on a mechanism confirmed by a live probe before this branch committed to it — `strings` on binary 2.1.236 shows the identifiers exist, which is not evidence of semantics; the probe put a marker behind `paths: ["**/*.engine.module.ts"]`, saw it arrive in a fresh session that read `bobs27.engine.module.ts` and not in one that read `lib/utils/route-class.ts`, and the documented fallback — rehoming the contract into `app/src/modules/game/CLAUDE.md` and accepting the duplication the glob avoided — went unused. Seven directory guides now exist under `app/`, each a maintenance surface: a rule that belongs in two of them is a signal it belongs in `app/CLAUDE.md` instead. Nothing here is mechanically provable — no gate can assert that a guide was read when its directory was opened. The honest measure is `/context` before and after, which is an observation, not a test.

### D318 — A decision id is derived against `origin/main`, and re-derived before the PR opens
Status: Accepted · Date: 2026-09-18
Decision: `scripts/next-decision-id.sh` derives the next free id against fetched `origin/main` as well as the working tree, counting as taken the ids that plans under `docs/superpowers/plans/` reserve before their block exists, and `context-maintenance` step 3 requires re-running it immediately before opening a PR. `DECISIONS.md`'s "How to add a decision" names the script as the derivation, keeping the raw `git grep` visible as what it runs. A collision caught late is fixed with `scripts/renumber-decision.sh`, which refuses only the rewrite that would touch `main`'s own block — the id being live on `main` is the case it exists for. No new CI job is added.
Reason: issue #386 reported PRs #381 and #382 each declaring an id that `main` had since issued to a different decision, and asked whether the check belongs pre-merge. It already is: `check-decision-ids.sh` runs from `quality.yml` on every `pull_request` to `main`, and `actions/checkout` resolves that event to the merge ref, so the uniqueness check already sees branch and `main` combined — which is how both collisions surfaced. The gap was never a missing gate; it was that both ids were correct when derived and stale by the time they landed, and that renumbering meant hand-editing every citation across `CLAUDE.md`, `00-File-Inventory.md`, `.gitignore` comments and the PR body.
Consequences: two branches in flight can still derive the same id — nothing local can see an unmerged branch's ledger. That race stays the CI gate's job; what changes is that fixing it is one command instead of a manual sweep. The ledger stays append-only: a renumbered id is the same block under a free number, never an edit to someone else's decision.
