<!--
status: canonical
scope: decisions/frontend/astro
read-when: why an .astro component/routing/prerender choice was made
load-when: .astro, component, prerender, routing, layout, cn(), props, frontmatter
depends-on: decisions/frontend/architecture.md
related: decisions/frontend/style.md
updated: 2026-08-02
-->

| # | Source | Decision | Rationale |
| - | ------ | -------- | --------- |
| D79 | 2026-07-14 | Frontend handbook 0.1.0: prerender-default on Cloudflare (`output: 'server'` + per-route prerender) | Honest adapter model; fast shells |
| D80 | 2026-07-14 | Middleware protected-prefix allowlist; v1 public = `/login` only | HTML nav gate; extensible public list |
| D92 | 2026-07-14 | `.astro` component conventions (`05-Astro-Components.md`): fixed frontmatter order; class placement decided by change-trigger (static→`class`, build-time→frontmatter, runtime→`:class`, recurring→`@layer components`); `cn()` = `twMerge(clsx())` is the sole class-composition helper | One canonical component shape; minimal variation |
| D97 | 2026-07-15 | Prerendered protected route shells are public-by-design (Cloudflare serves static assets before the Worker/middleware runs, verified via `wrangler dev` probe); the JWT-gated API remains the sole real authorization boundary, not the navigation gate | `run_worker_first` would front every static asset through the Worker for a purely cosmetic benefit (an anonymous visitor sees an empty shell either way, since data is never server-rendered) |
| D116 | 2026-07-17 | Converted `results/index.astro`'s non-trivial inline `x-data` (getters over `URLSearchParams`) to a `scoreTrainingResults()` factory in new `lib/game/score-training-results.data.ts`, registered in `register-route-data.ts` | Per PR review and this project's established `.data.ts` convention: only the trivial `{open: false}` shape is allowed inline |
| D121 | 2026-07-21 | Game **play** routes use `GameLayout` (exit chrome + confirm modal, no `BottomNav`). In-game Leave → `appendBatch` when turns exist, then `PATCH ABANDONED`, then `store.reset()` + `/games`. Browser close/refresh is not abandon — `$persist` + D88 resume. | Separates resume-friendly persistence from explicit session close for statistics |
| D123 | 2026-07-21 | Prettier is the `app/` formatter (`singleAttributePerLine`); `.astro` template comments are `{/* */}` only; CI enforces `x-show`↔`x-cloak` and bans template `<!-- -->` via `scripts/check-astro-conventions.sh`; TS inline body comments banned under `app/src/**/*.ts` (JSDoc-above; agent-enforced) | FOUC-safe Alpine markup, consistent authoring, mechanical drift prevention |
| D124 | 2026-07-21 | Portable `Modal.astro` shell + `ConfirmDialog.astro` preset in `components/ui/`; parent owns visibility; Alpine expression props for dismiss/cancel/confirm; all dialog actions use `Button.astro`; `dismissible` opt-out for hard gates (finish-confirm, continue-session). ExitModal and ContinueSessionModal compose these primitives. | One overlay contract; stop duplicating backdrop/panel markup |
| D127 | 2026-07-22 | Build-time Astro class composition must use `cn()`; `class:list` and `.filter(Boolean).join` banned in `app/src/**/*.astro`, enforced by `scripts/check-astro-class-composition.sh` in Context Maintenance + GitHub `checks` workflow | Prevents new UI primitives from regressing on `05-Astro-Components.md` |
| D128 | 2026-07-22 | Astro components forward leftover attributes as `...props` / `{...props}` — never `...rest` / `{...rest}` | One consistent rest-spread name across UI primitives |
| D170 | 2026-07-29 | `Button.astro`'s `loadingExpr` prop defaults to `"typeof loading !== 'undefined' && loading"`, not the bare `"loading"` originally shipped — an out-of-scope Alpine expression reference throws inside Alpine's evaluator and resolves falsy (hides the element), it does not evaluate `undefined` as truthy as first assumed. The unsafe default silently blanked `ConfirmDialog`'s Cancel button inside `ExitModal.astro` (rendered in `GameLayout.astro`'s bare `x-data` scope, which never declares `loading`) — this, not the Score Training play page's Retry button, was issue #49's actual "Ending session modal … title not showing" | Final whole-branch review executed the real Alpine build against the actual DOM shape and found the design spec's diagnosis of Alpine's undefined-variable behavior was factually wrong, misdirecting the original fix at the wrong component |
| D171 | 2026-07-29 | `Button.astro`'s built-in loading spinner (D138-adjacent UI convention, not a game-engine decision) must not be paired with a call site's own manually-wired `iconAfter`/`iconBefore` loading icon keyed on the same variable — `login/index.astro` and `LogoutButton.astro` had both, rendering two overlapping spinners once the built-in one shipped; their manual `LoadingIcon` slots were removed in favor of the built-in one | Task-scoped reviews for the spinner addition and the button-misusage fixes each saw only one half of this collision; only visible at whole-branch review |
| D173 | 2026-07-29 | `apple-mobile-web-app-capable` meta tags + `app/public/manifest.json` (`scope`/`start_url: "/"`, `display: "standalone"`) declare the app as an installable PWA | No manifest or meta tags existed; "Add to Home Screen" produced an unconfigured Safari Web Clip that could break navigation out to Safari on an auth redirect |
| D179 | 2026-08-01 | `BaseLayout.astro`'s body-child wrapper div uses `h-full max-h-full` (was `h-dvh max-h-dvh`), matching the `h-full` pattern `AppLayout`/`GameLayout` already use for their own wrappers | D174's `padding-top: env(safe-area-inset-top)` on `body` shrinks `body`'s border-box content area, but this nested div re-asserted its own full `100dvh` height regardless, overflowing `body`'s clipped bottom edge by exactly the top inset — invisible in a Safari tab (inset resolves 0 outside standalone display) but clipping `BottomNav` off-screen on an installed iOS Home Screen app, where the inset is non-zero |
