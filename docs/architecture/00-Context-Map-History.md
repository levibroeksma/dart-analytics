<!--
status: historical
scope: context-map version history and point-in-time task records
read-when: never during a task — provenance only
updated: 2026-09-03
-->

# Context Map History

> Append-only. `context-maintenance` writes its version entry here, not
> into `00-Context-Map.md`. Nothing in this file is authority; path
> references may name files since renamed or deleted, which is why
> `scripts/check-doc-links.sh` skips the path-backtick pass for
> `status: historical` documents.

---

# Version History

> **Version:** 1.42.0 (2026-09-03 — open-findings-cleanup: three small, independent findings closed. F15 (partial): `SplitScoreboardHalf.astro`'s root `cn()` call drops the dead `flex-1` class — its parent `SplitScoreboard.astro` is `display: grid`, so `flex-*` never applied and height was already grid-row-stretched; F15's own text trimmed to the still-open 8-file `h-full`/`flex-1` audit, with a note that this file's half is fixed so a future reader doesn't re-discover the same dead class. F58: backfilled three missing `00-Context-Map-History.md` Version entries (1.35.1-1.35.3, inserted chronologically between the existing 1.35.0 and 1.36.0) for `dartbot-setup-wiring-fixes`, `alpine-reactivity-fold-fixes`, and `preview-seat-scoping-fixes`, each of which had shipped (via PR #224) without the mandatory context-maintenance Version History step — doc-only, no code change, since all three plans' commits were already on this branch. F59: new `scripts/check-plan-findings-closure.sh` mechanically checks every `Closes FINDINGS.md F<id>` claim in a plan file against that same file's own text for a matching deletion step (exit 1 + stderr message on mismatch); wired as a `PostToolUse` hook (`scripts/check-plan-findings-closure-hook.sh`, added via `.claude/settings.json`, matcher `Write|Edit`) that filters to `docs/superpowers/plans/*.md`, blocks (exit 2) on a mismatch, and prints a non-blocking reminder when a plan's text names a `scripts/*.sh` path (not mechanically verifiable, since that would require executing the plan's own proposed diff). The gate script's own Step 2 acceptance fixture (a plan claiming `Closes FINDINGS.md F999` with no deletion step, expected to fail) surfaced a genuine bug during this task's own execution, not deferred as a finding since it blocked the task's own verification: the deletion-detection regex's `clos\w*` alternative matched the word "Closes" inside the claim sentence itself ("Closes FINDINGS.md F999"), so the claim doubled as its own deletion evidence and the gate silently passed malformed input — fixed by masking out the claim's own matched span before scanning for deletion evidence. Plan: `docs/superpowers/plans/2026-09-03-open-findings-cleanup.md`. `00-File-Inventory.md` rows added for the two new scripts; `FINDINGS.md` row refreshed from ~2.7k to ~2.2k (`check-context-budget.sh` had drifted 23% after F58/F59's deletions) and its closure/addition history extended with F60 (play-data-lifecycle-dedup, recorded but not yet reflected in this row before this task) and this plan's own F15(partial)/F58/F59 closures. No `DECISIONS.md` entry — no architectural alternative weighed; every fix follows the finding's own proposed correction. Validation: `cd app && npx astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; `npx fallow` 0 above threshold (maintainability 92.0, 12.2% duplication — unchanged from F60's already-recorded baseline, this plan touched no `app/src` runtime file); `npm test` 3308/3308 pass across 159 files (no runtime source file changed — Tasks 1-3 touch `.astro`, `.md`, and new top-level `scripts/*.sh` files only); `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh` all pass. `db:status`/`db:migrate`/`db:introspect` not run — no migration or schema file touched.)

> **Version:** 1.41.0 (2026-09-03 — play-data-lifecycle-dedup: closes F27's play-data half. `five-oh-one-play.data.ts`/`one-twenty-one-play.data.ts`/`score-training-play.data.ts`/`tuod-play.data.ts` each reimplemented `abandonAndExit`/`back`/`uploadAndCompleteSession`/`playAgain`/`currentFacts` near-identically instead of adopting `play-lifecycle.ts`'s already-shared generic functions (the other 5 rulesets — Bob's 27, Singles/Doubles Training, Shanghai, Around the Clock — already had). `currentFacts` was exported then reverted to module-private once Tasks 2-4 removed every external call site (fallow flagged the now-dead export). `playAbandonAndExit` gains an optional `onAbandoned?: () => void`, called on the success path right before store reset/redirect, so 121/Score Training/TUOD's `timer.stop()` has somewhere to attach. `playUploadAndCompleteSession` gains an optional `resolveFinalState?: () => ReturnType<TEngine["state"]> | null` (default: the exact prior `context.engine?.state() ?? null`), since all 4 rulesets read final state through their own store-fold-based `this.state()` instead — preserving each one's completion-retry-with-no-live-engine path, the exact regression the plan's own investigation flagged as the trap in a naive pass-through. `runPlayAgain` gains two optional callbacks, `resetLocalState?: () => void` and `afterEngineReady?: (engine: TEngine) => void` — both deliberately parameterless-of-context (the plan's own draft typed them `(context) => void`, which does not type-check: `PlayLifecycleContext<TConfig,TEngine,TResults>` erases each ruleset's own extra fields like `pendingCheckoutScore`/`timer`/`scoreInput`, so a callback receiving it as a parameter cannot read or write them without a cast — callers close over their own `this` instead, the same pattern Task 2's `onAbandoned` already used). All four extensions are additive and optional; the 5 existing callers pass none of the new arguments and needed no edit. `five-oh-one-play.data.ts`'s own `buildResultsSnapshot` reads `winningSideKey`/`legsWonFor` off the page context directly rather than off a `finalState` value (unlike the other 3 rulesets' local snapshot builders) — a second plan-draft mismatch, corrected by wrapping it (`() => buildResultsSnapshot(this)`) rather than passing it straight through. `one-twenty-one-play.data.ts`'s `resetForReplay` and Score Training's/TUOD's `finalScoreTrainingState`/`finalTuodState` are deleted, each provably equivalent to the shared function plus a callback or `this.state()` alone. TUOD's own pre-existing asymmetry (`playAgain` never called `scoreInput.clear()`, unlike 501/121/Score Training) is preserved exactly, not normalized. Plan: `docs/superpowers/plans/2026-09-03-play-data-lifecycle-dedup.md`; design: `docs/superpowers/specs/2026-09-03-play-data-lifecycle-dedup-design.md`. `npx fallow`'s total duplication percentage rose rather than fell against the plan's own 11.4% pre-task baseline (12.2% after) — every clone group the plan targeted (3-/4-way spans across the 4 files centred on `uploadAndCompleteSession`/`abandonAndExit`/`playAgain`) is gone, but removing that literal duplication let fallow's near-miss detector bridge a much longer already-similar span between `score-training-play.data.ts` and `tuod-play.data.ts` specifically (105 to 531 shared lines) — the same "whole-file near-twin, not extractable blocks" pattern F27 already left out of scope for those two rulesets' engine modules, now visibly present in their play-data files too; logged as **F60** rather than acted on. `dead-code` issues: 0 (fallow's own scan; the `currentFacts` dead-export flag it raised mid-task is the reason for the export/revert above). Validation: `cd app && npm test` 3308/3308 pass across 159 files (run after every task, not only at the end, per the plan's own fragility warning about the Play Again path); `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; `npx fallow` 0 above threshold (maintainability 92.0, no numeric duplication ceiling configured in `.fallowrc.jsonc`); `check-findings-log.sh` passes (F27 deleted, F60 added). No `DECISIONS.md` entry — every extension mirrors an already-established generic-function pattern (Task 2's `onAbandoned` shape reused for Task 4's two new callbacks), no new alternative weighed. `db:status`/`db:migrate`/`db:introspect` not run — no migration or schema file touched.)

> **Version:** 1.40.0 (2026-09-02 — process-gate-improvements: five independent governance/gate findings closed (F5, F38, F42, F43, F50), plus one new finding raised (F59) and one doc-only investigation recorded. `scripts/check-context-map.sh`'s migration-range check (lines 51-68) flips from an exclusion list (`grep -iv 'seed'`, skip any line naming "seed") to a label-adjacency positive requirement: `grep -hoiE 'migrations?[^0-9A-Za-z]{0,20}0001.?[–-].?.?[0-9]{4}'` only checks a range sitting directly after the literal word "migration(s)" (F5). The plan's own given regex (a simpler line-level "does this line contain 'migration'" check) broke on two real lines in `docs/architecture/README.md` the plan's own baseline note never accounted for — a Version History note pairing "migration/seed ranges" under one shared label, and a sentence stating a migration range and a seed range together ("Migrations `0001`–`0021` and seeds `0001`–`0007` implement it."); the label-adjacency form correctly checks only the genuine migration claim on each line and skips the seed half, and also still fixes F5's original bug (a seed range phrased without the literal word "seed" no longer misreads as a migration range). That same sentence's now-exposed stale claim was corrected to `0001`–`0023`/`0001`–`0013`, since it is a live current-state claim, not a historical record. `scripts/check-game-engines.sh` gains a new "Resumable ruleset version wiring" section (after its existing per-module loop): for any engine module naming more than one `rulesetVersionKey`, its paired `*-play.data.ts` file must reference every one of those keys literally, or the gate fails — the exact structural gap that let SHANGHAI_V2 ship without its play page's resume logic ever being updated (F43); proven by reintroducing that exact bug on a throwaway local edit (gate correctly failed) before reverting it. `09-Adding-A-Game.md`'s touch list gains a note on the `<code-slug>-play.data.ts` row naming this new enforcement. F38 (historical spec discrepancy — nothing to fix, by its own text) and F50 (`session.service.ts`'s `buildSeatPlan` already has the early-return-per-branch shape F50 said it needed; `npx fallow` confirmed clean) closed doc-only, both already-resolved findings the plan named as pure FINDINGS.md deletions. F42 (why `fallow`'s duplication gate never flagged the pre-cleanup bust/checkout and `otherSeatsComplete` duplication) investigated on a throwaway branch (`throwaway/f42-fallow-investigation`, discarded after): hand-reconstructed the pre-fix duplication (5 bust/checkout sites across `five-oh-one`/`one-twenty-one`/`tuod` engine modules; 3 `otherSeatsComplete`-shaped inline folds across `tuod`/`score-training`) and ran `npx fallow dupes` in both default (`mild`) and `--near` mode. Not a threshold/size gap — several reconstructed clones were smaller than duplicates the gate already reports elsewhere in this codebase; it did catch an exact same-file repeat and one close cross-file pair, but never unified the whole family, because each hand-copied site's own local variable/field names and destructuring shape were enough to defeat token-based matching across files even under `--near`. Recorded as a new "Fallow Duplication Detection — Known Limitation" section in `06-Test-Strategy.md` (no prior fallow section existed there to append to, contrary to the plan's assumption). Plan: `docs/superpowers/plans/2026-09-02-process-gate-improvements.md`. Global Constraints named this plan as closing all five findings, but only Tasks 2/3/6 (F38/F50/F42) carried an actual FINDINGS.md deletion step; Tasks 1 and 4 fixed F5's and F43's underlying defects with no corresponding deletion step named anywhere in the plan — both deleted directly in Task 6 once the gap was noticed running the full gate suite, and the gap itself logged as **F59**, the same failure category F50 already named for the DartBot phase-plan series recurring in a different plan series. `00-File-Inventory.md` rows refreshed: `07-Frontend/06-Test-Strategy.md` (~0.7k → ~1.3k, new fallow section) and `FINDINGS.md` (~4.1k → ~2.7k after five closures and one addition; closure/addition history extended). No `DECISIONS.md` entry — both fixes are mechanical (a script regex, a new structural-gate section mirroring an already-established per-module-loop pattern), no new alternative weighed. Validation: `cd app && npm test` 3308/3308 pass across 159 files; `npx fallow` 0 above threshold (maintainability 92.0); `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh` and all ten `app/`-scoped structural gates pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session, the established D193 precedent — no migration touched).)

> **Version:** 1.39.0 (2026-09-02 — scoring-stats-correctness: four independent stats/results correctness fixes in the Score Training / Shanghai results path, closing F20, F34, F35, F37. `foldShanghaiState` (`shanghai.engine.module.ts`) gains `seats.length === 1 ? null : raceWinner(...)`, matching `foldOneTwentyOneState`'s already-shipped guard — a solo session that ends on a Shanghai now reports `winningSideKey: null` instead of leaking its own `sideKey` (F20). `visitScoreBandCounts` (`play-visit-stats.ts`)'s top-band check changes from `score === 180` to `score >= 180`, matching the other three bands' own "highest threshold" contract (F34). New exported `completedVisitsTotal(turns)` in `play-visit-stats.ts`, alongside `highestVisitScore`; `score-training-play.data.ts`'s `statsFor` swaps `total: seat.totalScore` for `total: completedVisitsTotal(seatTurns)`, so an open visit's running score no longer contradicts the other seven per-seat stats when a results snapshot is taken mid-visit (F35). Two new 1v1 regression tests (decided finish, tie) confirm the reshaped `ScoreTrainingResultsSnapshot`'s top-level `winningSideKey`/`status` — pure test coverage, no source change, both passed immediately as expected (F37). Plan: `docs/superpowers/plans/2026-09-02-scoring-stats-correctness.md`. Two plan-authoring gaps found and fixed in the same task, both direct consequences of the plan's own specified fixes rather than out-of-scope changes: (1) the plan's Task 1 Step 5 named three stale `winningSideKey: "A"` assertions in `shanghai-play.data.test.ts` (lines 309, 394, 514) as all solo-session fixtures needing the null update; the third (514) is actually a 1v1 fixture ("ends the whole match instantly on either seat's Shanghai, naming that seat the winner") where seat A legitimately still wins under the new 2-seat-only guard, so it was left unchanged — updating only 309 and 394. (2) Task 2's `>= 180` fix, run against the full suite, broke an unrelated pre-existing test in `five-oh-one-play.data.test.ts` ("uploads the batch, completes the session, and snapshots per-seat results") whose 461-point visit fixture asserted `oneFortyPlus: 1`/`oneEighties: 0` under the old equality-check bug; `visitScoreBandCounts` is shared across every engine, and 501 (unlike Score Training) has no `max_visit_score` ceiling stopping a >180 visit total, so the plan's own Task 2 Impact note ("inert today... the engine cannot produce one") held only for Score Training, not 501 — the fixture's expected band counts were swapped to `oneFortyPlus: 0`/`oneEighties: 1` to match the corrected contract. `ShanghaiResults.astro`'s solo short-circuit, `one-twenty-one.engine.module.ts`, every seeded template's `max_visit_score`, and Score Training's persisted-mirror retry path are all unchanged, per the plan's own Non-goals. `00-File-Inventory.md`'s `FINDINGS.md` row refreshed from ~5.9k to ~4.1k (`check-context-budget.sh` had drifted 42% from this plan's four closures) and its closure history extended with F32/F33 (preview-seat-scoping-fixes), F31 (alpine-reactivity-fold-fixes), and F20/F34/F35/F37 (this plan) — F32/F33/F31 were never recorded when those three earlier plans actually landed (see F58, logged this task). No `DECISIONS.md` entry — each fix mirrors an already-established pattern (121's own guard, the other three bands' own `>=`, the other seven stats' own `completedVisits` filter), no new alternative weighed. Validation: `cd app && npx vitest run` 3308/3308 pass across 159 files; `npx fallow` 0 above threshold (maintainability 92.0); `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; `npm run format:check` clean; `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh` and all ten `app/`-scoped structural gates pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session, the established D193 precedent — no migration touched).)

> **Version:** 1.38.0 (2026-09-02 — tuod-hardening: three independent TUOD fixes closing F10, F18, F21. `applyTuodAttempt` (`tuod.engine.module.ts`) gains `MAX_FINISHABLE_TARGET = 170` (the highest three-dart double-out total), clamping the success branch symmetrically with the existing `MIN_FINISHABLE_TARGET` floor on the failure branch, so the checkout ladder can no longer climb onto a target no double can ever finish (F10). `FINDINGS.md`'s F18 closed doc-only: its claimed live-stats banner in `TenUpOneDownResults.astro` was already replaced by `SinglePlayerSummary`/`ComparisonSummary` in a prior commit, confirmed by grep before deletion. New private `TuodEngine.isMatchDecided()` mirrors `ScoreTrainingEngine.isMatchDecided()` (D229): `rejectionReason`, `recordDart`, and `wouldCompleteDart` now gate on it instead of the public `isComplete()`, narrowing engine-level record()-blocking to 2-seat (1v1) sessions whose score-compare outcome is settled. A solo session is exempt entirely — not just around MINUTES timer-expiry, the plan's stated F21 scenario — because `isComplete()`'s MINUTES reading (`timerExpired && attempts >= 1`) can turn true mid-session before the one finishing attempt is recorded, and the plan's own design deliberately relocates solo completion enforcement to `tuod-play.data.ts`'s existing `finished`/`showFinishConfirm` gate rather than duplicating it in the engine. This is a wider exemption than the plan's Task 3 text anticipated: it broke three pre-existing engine-level tests asserting a solo ROUNDS-complete session refuses further `record()`/reads `wouldComplete() === false` — both engine and dart-input paths. Rather than weaken those assertions to keep them green (CLAUDE.md's test-integrity invariant forbids re-pointing a test at a different input just to pass), they were updated to assert the new, intentional engine contract (record() no longer throws; wouldComplete() recomputes rather than short-circuiting), and a new `tuod-play.data.test.ts` case was added confirming `recordAttempt` still no-ops once `finished` is true — the guarantee's new, sole enforcement point. `isComplete()` itself, `ScoreTrainingEngine`, `checkout-path.module.ts`, and TUOD's solo-only MINUTES capture restriction are all unchanged. Plan: `docs/superpowers/plans/2026-09-02-tuod-hardening.md`. Two plan-authoring gaps beyond the plan's own text, both fixed in the same task since they were either a literal input error the task's own test needed corrected or the direct, foreseeable consequence of the task's own specified design change: Task 1's own test fixture recorded a 168-checkout with `dartsUsed: 1`, which `checkoutDartsRejection` rejects outright (168 needs at least 3 darts) — corrected to `dartsUsed: 3`. Task 3's `isMatchDecided()` as specified exempts every solo session unconditionally (not narrowly scoped to the MINUTES-expiry race), the three broken pre-existing tests described above. `00-File-Inventory.md`'s `FINDINGS.md` row refreshed from ~7.9k to ~5.9k (`check-context-budget.sh` had drifted 33%, itself triggered by this and the immediately preceding plan's finding closures shrinking the file) and its closure history extended with F44 (prior plan) and F10/F18/F21 (this one). No new `FINDINGS.md` entries. No `DECISIONS.md` entry — mirrors D229's already-established pattern, no new alternative weighed. Validation: `cd app && npm test` 3301/3301 pass across 159 files; `npx fallow` 0 above threshold (maintainability 92.0); `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; `npm run format:check` clean; `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh` and all ten `app/`-scoped structural gates pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session, the established D193 precedent — no migration touched).)

> **Version:** 1.37.0 (2026-09-02 — board-dart-bull-double-checkout: 501 and 121's VISUAL_BOARD checkout logic now recognizes a closing dart on the inner bull as a double, matching the fix TUOD already shipped for issue #207 — closes F44. Both games share `checkout-bust.module.ts`'s `resolveCheckoutAttempt(remaining, thrown, endedOnDouble)`, unchanged; each has two call sites computing `endedOnDouble` from a dart's zone key, and all four previously tested only `=== "DOUBLE"`. `five-oh-one.engine.module.ts`: `settleVisit` (line 453) and `dartChecksOutFinalLeg` (line 511) now OR in `hitZoneKey`/`resolved.zoneKey === "INNER_BULL"`. `one-twenty-one.engine.module.ts`: `settleVisit` (line 501) and `wouldCompleteDart` (line 640) get the same OR. Two regression tests added per file — a `record()`-based checkout test for `settleVisit` and a `wouldComplete`/`wouldComplete(dart)` prediction test for the other call site — each proven red against the exact line it fixes before the fix landed. No change to `resolveCheckoutAttempt` itself, to QUICK_SCORE capture (checkout there is an explicit user-supplied double flag, not a derived zone key), or to TUOD's already-fixed `visitOutcome`. Plan: `docs/superpowers/plans/2026-09-02-board-dart-bull-double-checkout.md`. Validation: `cd app && npm test` 3296/3296 pass across 159 files; `npx fallow` 0 above threshold (maintainability 92.0); `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh` and all ten `app/`-scoped structural gates pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session, the established D193 precedent — no migration touched). No new `FINDINGS.md` entries. No `DECISIONS.md` entry — a mechanical fix mirroring an already-established pattern, no new decision made.)

> **Version:** 1.36.0 (2026-09-02 — doc-only-corrections: six independent, targeted doc/comment fixes closing F9, F28, F30, F46, F51, F53, no behavior change anywhere. `09-Adding-A-Game.md` gains a note that `check-game-wiring.sh` runs pre-commit on every commit, not just the final one, so a new game's frontend touch list can't be split across sequential per-file commits (F9). `00-File-Inventory.md`'s `context-maintenance/SKILL.md` row drops its hardcoded "8-step" count and its `decisions/frontend/alpine.md` row drops its hardcoded decision count, matching `decisions/frontend/style.md`'s topic-list-only style (F28, F30). `07-Style-Guide.md`'s `.control` Primitives row stops naming `Checkbox.astro`/`Radio.astro`, which don't exist, and now says to apply the class directly to a raw `<input>` (F46). `session-mode-resolution.ts`'s `participantsFromSeats` JSDoc is repointed from the deleted `seatsFromParticipants` to the `toSeatFacts` function that replaced it, with a covering-test-file annotation added to `session-mode-resolution.test.ts` to satisfy D224 (F51). This file's own "Current Implementation State" table gains a disclaimer pointing to the Version History above as the authoritative current-state source, rather than a rewrite of its stale rows (F53). Plan: `docs/superpowers/plans/2026-09-02-doc-only-corrections.md`. Validation: `cd app && npx vitest run tests/lib/game/session-mode-resolution.test.ts` 21/21 pass; `scripts/check-doc-links.sh`, `check-context-map.sh`, `check-context-budget.sh`, `check-agent-mirrors.sh`, `check-file-locations.sh`, `check-findings-log.sh`, `check-test-coverage.sh` all pass. No new `FINDINGS.md` entries. No `DECISIONS.md` entry — no alternative was weighed, each fix was the finding's own proposed correction.)

> **Version:** 1.35.3 (2026-09-02 — preview-seat-scoping-fixes: backfilled Version History entry for a plan whose own context-maintenance pass (`fbec686`) skipped it — closes F32, F33. `singles-training-play.data.ts`'s `previewSegmentsFor` computed its round index from `turns.length` (global turn count across both seats), the same seat-unscoped bug issue #166 found in Shanghai — fixed to derive the round from a count of turns filtered to the last turn's own `participantRef` (`c56c9f0`, "scope Singles Training's preview target to the throwing seat's own round"). `around-the-clock-play.data.ts`'s `previewSegments()` filtered `$store.game.turns` by `state.activeParticipantRef` before computing the preview, but `seat-rota.module.ts`'s `activeSeat` rotates to the next thrower the instant a turn closes — before the 1.5s reveal timer even starts — so during that window the preview read the *next* seat's (usually empty) turn history instead of the seat whose darts were fading out; fixed to scope by the last turn's own `participantRef` instead of the rotated active seat (`31413a2`, "scope Around the Clock's preview to the just-closed turn's own seat"). Same fix direction both times, adapted to each file's own `previewSegmentsFor` signature — no shared helper extracted. Plan: `docs/superpowers/plans/2026-09-02-preview-seat-scoping-fixes.md`. Validation: covered by PR #224's aggregate run before merge — `cd app && npx vitest run` 3289/3289 pass; `npx fallow` 0 above threshold; `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; all local gate scripts `OK`. No new `FINDINGS.md` entries from this plan itself. No `DECISIONS.md` entry — mirrors the already-established Shanghai preview-seat-scoping fix (`docs/superpowers/plans/2026-08-27-shanghai-preview-seat-scoping.md`), no new alternative weighed.)

> **Version:** 1.35.2 (2026-09-02 — alpine-reactivity-fold-fixes: backfilled Version History entry for a plan whose own context-maintenance pass (`c30b733`) skipped it — closes F31. Six `*-play.data.ts` files' `state()` read `this.engine?.state()` — a plain, non-reactive class instance — instead of folding `$store.game`'s own Alpine-tracked `stages`/`turns` fields, so `x-text`/`x-show` expressions never re-rendered on a recorded dart (the same defect TUOD and 501 had already fixed). Score Training and Shanghai swapped `state()` to call each engine module's already-exported `foldXState`. Doubles Training, Around the Clock, and Singles Training each had a private module-level fold function exported, then `state()` swapped to call it directly. Bob's 27 needed a real extraction: `foldBobs27State` pulled out of `Bobs27Engine.deriveState()`'s inline logic into its own exported function, then `state()` swapped to call it. Each affected `*-play-data.test.ts`'s pre-existing "per-seat accessors" test had stubbed `engine.state()` directly — a fixture shape the new store-folding `state()` no longer reads — re-pointed at the same guarantee (correct seat selected among several / an unknown seat returns `""`) via `$store.game.recordFacts` instead of being deleted. Plan: `docs/superpowers/plans/2026-09-02-alpine-reactivity-fold-fixes.md`; PR: #224 ("Fold Alpine reactivity for six play controllers", merged 2026-09-02T22:03:48Z). Validation: `cd app && npx vitest run` 3289/3289 pass; `npx fallow` 0 above threshold; `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; all local gate scripts (context map, doc links, context budget, agent mirrors, file locations, findings log, test coverage, Astro class composition/conventions, game engines, refinement coverage, type barrels, alias sync, constraint mirror, no-inline-comments, style tokens, game wiring) `OK`. No new `FINDINGS.md` entries. No `DECISIONS.md` entry — a mechanical fix mirroring TUOD/501's already-established fold pattern, no new decision made.)

> **Version:** 1.35.1 (2026-09-02 — dartbot-setup-wiring-fixes: backfilled Version History entry for a plan whose own context-maintenance pass (`f90583e`) skipped it — closes F45, F54, F55, F56, F57. F54: `fiveOhOneSetup()`/`FiveOhOneSetupContext` gained the `bot`/`addBot`/`removeBot`/`showOpponentChooser` state and methods `PresetSetupContext` already had, and `start()` now calls `participantsFromGuests(this.guests, this.bot)`, so the 501 setup screen's DartBot chooser can actually seat a bot (`26b6a72`). F45: both 121 and Singles Training setup always submitted their V2 ruleset key regardless of guest count, so a 1v1 session could never be created for either — 121's `start()` now resolves `121_V1` for a guested session via new `resolveStartPreset`/`resolveStartOverrides` helpers (extracted after the guest-aware branching pushed the function's CRAP score over the app-wide `maxCrap:30` gate; `e6d2982`, `655a88e`), and Singles Training's shared `createPresetSetupController` widened its `rulesetVersionKey` option to accept a per-context resolver function so `singlesTrainingSetup()` can resolve `SINGLES_V1` once guested (`4747bf9`). F55: `playFoldBotQuickScoreVisit` looped on `isComplete()` (whole-match completion) alone, so a bust or non-final-leg checkout that closed the bot's current visit before the third dart went unnoticed — the loop opened a new, fabricated visit and returned that instead of the bot's real one; fixed to stop the loop once the visit it just recorded closes (`301958a`). F56: `toSeatFacts`/`composeSeatFacts` each guarded their `DARTBOT` branch on type-plus-payload, silently falling through to `PLAYER`/`GUEST` when the `dartbot` payload was absent — the two-collapses-in-opposite-directions failure mode `08-DartBot.md` names as actively dangerous; both now throw instead of mislabeling the seat when the payload is unexpectedly missing (unreachable today — `buildSeatPlan` always attaches it — but no longer silently wrong if that ever changes) (`409799a`). F57: `08-DartBot.md`'s scatter-model paragraph reworded to describe `covarianceRotationDegrees` as a fixed per-player technique bias rather than a target-wire-relative rotation, matching what the code actually computes (doc-only; the constant is 0 for every level today, so this was latent, not observable) (`612dd75`). Plan: `docs/superpowers/plans/2026-09-02-dartbot-setup-wiring-fixes.md`; PR: #224 ("Fold Alpine reactivity for six play controllers", merged 2026-09-02T22:03:48Z — its diff also carries this plan's and preview-seat-scoping-fixes' commits, landed on the same branch before that PR opened). Validation: covered by PR #224's aggregate run before merge — `cd app && npx vitest run` 3289/3289 pass; `npx fallow` 0 above threshold; `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints; all local gate scripts `OK`. No new `FINDINGS.md` entries from this plan itself. No `DECISIONS.md` entry — each fix follows an already-established sibling pattern (`PresetSetupContext`'s bot fields, the other three engines' bot-visit fold, the type-discriminant-first guard shape), no new alternative weighed.)

> **Version:** 1.35.0 (2026-09-02 — DartBot phase 7 (X01Strategy): opponent mode ships on `501_V1`, the first ruleset whose bot needs a real decision — checkout routing — rather than a dictated target. `RULESET_DARTBOT.501_V1` flips to `true` (`capabilities.ts`), so a bot seat is now offered on the 501 setup screen (`FiveOhOneSetupForm.astro` gains the same two-line `allowDartbot={supportsDartbot("501_V1")}` pattern `Bobs27SetupForm.astro` established at phase 4). `SkillProfile` (`modules/dartbot/types.ts`) gains `decisionQuality: number` (D-D's decision axis), populated 7→100 across the fifteen `LEVEL_SKILL_TABLE` levels. New `app/src/modules/dartbot/strategy/x01.strategy.module.ts` exports `chooseTarget(view: X01View, decisionQuality)`: below a 30-point threshold it always fires at treble 20 (`08-DartBot.md` §Decision degrades too's "low" tier); at or above it, it aims at the first step of `view.checkoutPath` — the caller's own `checkoutPathFor(remaining)` result, re-derived fresh before every dart so a miss that changes the remaining score self-corrects on the very next call with no visit-scoped state anywhere, never falls back to treble 20 only when no route exists (a bogey number, or above 170). `playFoldBotQuickScoreVisit`'s `throwDart` parameter widens from `() => DartObservation` to a new `BotQuickScoreThrower<TState> = (state: TState) => DartObservation` (`lib/game/types.ts`), the QUICK_SCORE fold's first real change since phase 6 built it ahead of a live consumer — recorded as D253 (`decisions/game-engine.md`), since D252 had predicted no such change. `five-oh-one-play.data.ts` gains `botThrowing: boolean` and `maybeRunBotVisit()`, wired from `init`, the VISUAL_BOARD `commitDart` tail, and `recordVisit`'s tail; it branches on `this.$store.game.inputModeKey === "QUICK_SCORE"` — VISUAL_BOARD reuses `playRunBotVisualBoardVisit` unchanged (mirrors Bob's 27), QUICK_SCORE calls the widened `playFoldBotQuickScoreVisit` with the concrete `fiveOhOneEngineFactory` (not the type-erased `getEngineFactory` registry, so `TState` infers as `FiveOhOneState` with no cast) and then `recordVisit(fold.totalScore, fold.totalScore === remainingBefore)` — a checkout being provably the only path to `totalScore === remainingBefore` under this engine's own bust matrix. `undoVisit` branches to `undoToActiveSeat` when a bot seat exists, otherwise the prior inline undo logic, mirroring Bob's 27's phase-6 shape. New engine-level contract test `app/tests/modules/dartbot/harness/five-oh-one-dartbot-seat.contract.test.ts` drives a full 1v1 match to `WON` with fixed human keypad visits and real bot darts, proving both `FiveOhOneInput` variants coexist inside one `SHARED` leg — the phase's own gate. Design: `08-DartBot.md` (already written, phase 5); plan: `docs/superpowers/plans/2026-09-02-dartbot-7-x01-strategy.md`. Seven plan-authoring gaps found and fixed in the same task, none logged as findings since each was an adjacent edit the task's own acceptance tests genuinely required, not an incidental discovery of a pre-existing unrelated defect: (1) Task 1's own code snippet wrote `501_V1: true,` as a bare object key, invalid JS since the key starts with a digit and isn't a valid identifier or numeric literal — quoted to `"501_V1": true,`, matching the file's own existing `RULESET_CAPABILITIES` convention. (2) Two pre-existing test fixtures (`aim-resolver.module.test.ts`, `throw-engine.module.test.ts`) broke once `decisionQuality` became a required `SkillProfile` field, a fallout Task 2's own file list never named — fixed by adding `decisionQuality: 100` to each's local `SkillProfile` literal. (3) Task 3's own "parses a plain-number single step" test fixture set `checkoutPath: ["T20", "20", "D20"]`, but `chooseTarget` only ever reads `checkoutPath[0]` — the fixture as written would have exercised treble parsing, contradicting its own name — corrected to `["20", "T20", "D20"]`. (4) Task 5's own `maybeRunBotVisit` code checked `this.$store.game.captureModeKey === "QUICK_SCORE"`, but `captureModeKey` only ever holds `"RECREATIONAL"`/`"ANALYTICS"` (`capabilities.ts`'s own `ModePair` shape) — the QUICK_SCORE/VISUAL_BOARD value lives on the sibling `inputModeKey` field, exactly as this same file's own `statsFor` already reads it — the plan's condition as written would never have taken the QUICK_SCORE branch; fixed to check `inputModeKey`. (5) Two pre-existing service tests (`session-seats.service.test.ts`, `session.service.test.ts`) asserted `501_V1` as their example of a ruleset `RULESET_DARTBOT` does not admit, broken once Task 1 flipped the flag — repointed to `SCORE_TRAINING_V1`, still excluded. (6) Task 7's own contract-test config snippet omitted `checkIn`/`checkOut`, both required fields on `FiveOhOneSnapshot` — added `STRAIGHT_IN`/`DOUBLE_OUT`. (7) Task 5's own test sketch asserted `turns.at(-1)!.participantRef === HUMAN_REF` immediately after the bot's own visit with no subsequent human turn recorded, which is false by construction regardless of correctness — the plan's own "control returned" comment matches Bob's 27's actual phase-6 precedent test, which checks `activeParticipantRef` instead — corrected to match. `08-DartBot.md` (0.7.0 → 0.8.0; `updated:` 2026-09-02): the play-loop dependency-table row's "no live consumer yet" clause removed (now false) and a new phase-7 row added; the speculative `GameView contracts` `X01View` example (`{ remaining, dartsLeftInTurn, opponentRemaining }`) replaced with the shipped shape (`{ remaining, checkoutPath }`) — phase 7's own gate needed neither field, both deferred to a later phase if a strategy ever needs them. `00-File-Inventory.md` rows updated: `08-DartBot.md` ("phases 1–6" → "phases 1–7", "phase 7+" → "phase 8+", ~23.1k → ~23.9k), `capabilities.ts` ("three ruleset versions" → "four"), `play-lifecycle.ts` (D253 named alongside D252). No new `FINDINGS.md` entries. Validation: `npm test` 3264/3264 across 159 files, `npx fallow` exits 0 (maintainability 92.0, 0 above threshold), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh`/`check-decision-ids.sh` and all ten `app/`-scoped structural gates pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this phase added no migration). Manual in-browser verification could not be exercised either, the same D193 root cause extended to auth as phases 4 and 6 already established: a human with a working local/Neon environment must play a real 501 match against a seated `DARTBOT` opponent under both `VISUAL_BOARD` and `QUICK_SCORE` capture before merge and confirm — the bot throws automatically with a visible pause; the match-finish confirm dialog never opens for the bot's own dart; undo mid-bot-turn returns control to the human; a `QUICK_SCORE` bot visit's board never shows bot dart coordinates, since none exist to show.)

> **Version:** 1.34.0 (2026-09-01 — DartBot phase 6 (The Play Loop): builds the mechanism `08-DartBot.md` §The Play Loop specifies as one thing seen from four angles, generically in `play-lifecycle.ts`, and wires it onto Bob's 27 — its first live consumer. `undoToActiveSeat(context, participantRef)` pops visits until the named seat is active again (always at least once, so a solo session's undo button never goes dead), replacing a bare `undo()` for any session carrying a bot seat. `playRunBotVisualBoardVisit(context, botParticipantRef, throwDart, wait?)` drives the bot's whole visit — one real `playCommitDart` per dart, looping while the engine's own active seat stays the bot's — behind two required re-entrancy guards: `context.botThrowing` (a second concurrent call for the same trigger is a no-op) and a post-`wait(pacing.preThrowMs)` active-seat re-check before recording (abandons the throw if `undoToActiveSeat` moved the active seat away during the delay — this is the load-bearing guard; `botThrowing` only stops two *overlapping* loops from both reaching it at once). `playFoldBotQuickScoreVisit(factory, config, facts, throwDart, dartsPerVisit)` feeds a bot's darts into a throwaway scratch instance of the same ruleset and returns only `{ totalScore, dartsThrown }` — proven against `scoreTrainingEngineFactory` at the engine level, with no live `QUICK_SCORE` page consumer yet (no ruleset `RULESET_DARTBOT` admits is `QUICK_SCORE`-only until phase 7 adds the flag to `501_V1`). `Bobs27PlayContext` gains `botThrowing: boolean` and `maybeRunBotVisit()`; `bobs27-play.data.ts` wires the real phase 1–3 throw pipeline (`skillProfileForLevel`, `createDartRng`, `chooseTarget`, `throwDart`) into a `throwBotDart()` adapter whose `dartIndex` is re-derived from the fact log on every call (never held on a closure, so an undone bot visit re-throws identically from the same seed), and calls `maybeRunBotVisit()` from `init`/`commitDart`/`undoVisit`; `undoVisit` itself now branches to `undoToActiveSeat` when a `DARTBOT` seat is present and falls through to the unchanged `playUndoVisit` otherwise, so a guest 1v1 session is untouched. No `DartBot` class exists or was needed — the shipped phase 1–3 functions compose directly, exactly as `play-dictated-session.ts`'s test harness already showed for a solo bot. Design: `08-DartBot.md` §The Play Loop (already written, phase 5); plan: `docs/superpowers/plans/2026-09-01-dartbot-6-play-loop.md`. Decision: D252 (`decisions/game-engine.md`) records the two reusability choices — an injected `BotDartThrower` rather than a `DartBot`-class or `modules/dartbot/*` dependency in `play-lifecycle.ts`, and the fold's ruleset-agnostic `{ totalScore, dartsThrown }` return shape. Two plan-authoring gaps beyond the plan's own text, both fixed in the same task rather than left to trip CI later: `BotQuickScoreFold` was written as an inline `export type` inside `play-lifecycle.ts` (Task 3's own code block), which `scripts/check-type-barrels.sh` rejects — moved to `types.ts` and imported back via a relative `./types` import, matching every other exported type in the file; and the plan's own D248-numbering note ("re-check ... in case a concurrently-landed phase already claimed D248") undersold the risk — the correct next id, derived from a repo-wide (not single-file) grep across all of `decisions/**.md`, was D252, not D248 or D249 or D250, three ids having landed on `main` from concurrent PRs (#205, #213, and one more) since this branch's checkout; caught by `scripts/check-decision-ids.sh` and corrected before commit. `00-File-Inventory.md` rows updated: `08-DartBot.md` ("phases 1–5" → "phases 1–6", "phase 6+" → "phase 7+", ~22.9k → ~23.1k), `decisions/game-engine.md` (45 → 46 decisions, D252 named, ~17.4k → ~21.9k — this was the token-budget gate's actual failure, `check-context-budget.sh` having flagged 20% drift before this edit), `bobs27-play.data.ts` and `play-lifecycle.ts` (both description rows extended to name the new bot-play-loop exports). No new `FINDINGS.md` entries. Validation: `npm test` 3247/3247 across 157 files, `npx fallow` exits 0 (maintainability 92.0, 0 above threshold), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh`/`check-decision-ids.sh` and all ten `app/`-scoped structural gates plus `check-constraint-mirror.sh` pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this phase added no migration). Manual in-browser verification (Task 6 Step 2 — seating a bot on Bob's 27, confirming the automatic throw and undo-crosses-the-bot behavior) could not be exercised either: the dev server serves pages at 200, but every session-creating API call returns `401 UNAUTHORIZED` with no Neon Auth backend configured in this sandbox — the same D193 root cause extended to auth, exactly as phase 4's own manual-verification note already established for the setup-screen chooser. A human with a working local/Neon environment must run through Bob's 27 with a seated `DARTBOT` opponent before merge and confirm: the bot throws automatically with a visible pause; undo mid-"thinking" does not record the pending dart; one undo press after the bot's visit returns control to the human.)

> **Version:** 1.33.0 (2026-09-01 — DartBot phase 5 (Participant Write Path): closes `08-DartBot.md`'s write-path deferral by proof rather than by new code. New contract test `app/tests/modules/dartbot/harness/bobs27-dartbot-seat.contract.test.ts` drives a real `DARTBOT`-typed 1v1 seat through `Bobs27Engine` using the already-shipped throw pipeline (phases 1–3: `throw-engine.module.ts`, `skill-profile.module.ts`, `dictated.strategy.module.ts`) and confirms `buildEventsBatch` carries the bot's own `participantRef` through unchanged for every bot turn. `session.service.test.ts` gains a second test proving `appendBatch` persists a `DETAILED_DARTS` turn attributed to a `DARTBOT` participant exactly like any other seat's, reaching `insertBatchRecords` via the same generic `validateBatchReferences`/`resolveBatchStructure` path every participant type already used. `database/verification/0023_owner_scoped_dart_view_checks.sql` gains a third fixture participant (`DARTBOT`, `participant_type_id = 3`, `player_id IS NULL`) with its own turn and dart, extending the existing PLAYER/GUEST checks to prove a bot's dart is likewise excluded from both `v_dart_analytics` and `v_dart_locations` while `v_game_replay` deliberately still returns all three participants' turns; the anti-vacuity guard's expected count rises from 6 to 8. No source file under `app/src/` changed — direct reading of the write path (`events.payload.module.ts`, `session.service.ts`, `session-seats.service.ts`, `three-dart.validator.ts`, `quick-score.validator.ts`) confirmed none of it branches on participant type, so phase 4's `DARTBOT`-typed `SeatFact` (D220) and migration `0023`'s owner-scoped view join (D222) already covered a bot seat without change. `08-DartBot.md`'s dependency table row for "The DartBot write path" flips from "still deferred"/"`DARTBOT` is explicitly refused at the create contract" (both now false since phase 4 shipped) to "shipped"; version bumped 0.5.0 → 0.6.0, `updated:` front-matter to 2026-09-01. `DECISIONS.md` §Deferred no longer lists "DartBot participants (2026-07-12)" — the second and last half of that deferral, the guest half having been struck earlier. Design: `docs/superpowers/specs/2026-09-01-dartbot-v1-delivery-design.md`; plan: `docs/superpowers/plans/2026-09-01-dartbot-5-participant-write-path.md`. Context maintenance found `database/verification/0023_owner_scoped_dart_view_checks.sql` itself had never been registered in `00-File-Inventory.md`, a pre-existing gap dating to the script's original creation alongside migration `0023` (2026-08-21) — not this phase's doing, but this phase's own Task 5 step named updating that file's entry, so the missing row was added rather than logged as a separate finding (adjacent edit the named step required, not incidental discovery). `08-DartBot.md`'s own inventory row updated to "phases 1–5 ... are built ... phase 6+ (play loop, ghost mode, 501 opponent) remains design-only", its char/4 estimate unchanged (~22.9k, growth too small to shift the rounded bucket). No new `FINDINGS.md` entries — unlike phase 4, nothing in this phase's own plan tripped a gate or left a stale reference. No `DECISIONS.md` decision block — this phase confirms D220/D222 already cover a `DARTBOT`-typed seat; it weighs no new alternative. Validation: `npm test` 3230/3230 across 157 files, `npx fallow` exits 0 (maintainability 92.0, 0 above threshold), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-agent-mirrors.sh`/`check-file-locations.sh`/`check-findings-log.sh`/`check-test-coverage.sh` and all ten `app/`-scoped structural gates plus `check-constraint-mirror.sh` pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this phase added no migration). The extended `database/verification/0023_owner_scoped_dart_view_checks.sql` itself likewise could not run in this sandbox for the same D193 reason — a human with Neon access must run `psql "$DATABASE_URL" -f database/verification/0023_owner_scoped_dart_view_checks.sql` before merge and confirm all 9 checks (8 view-driven + the anti-vacuity guard) read PASS.)

> **Version:** 1.32.0 (2026-09-01 — DartBot phase 4 (Seat Admission): admits `DARTBOT` as a third participant type through the seat-creation path, wired write-to-read: `ParticipantInput`/`ParticipantRef` (`pages/api/sessions/types.ts`) widen to `"PLAYER" | "GUEST" | "DARTBOT"` plus an optional `level` (1–15) and a returned `dartbot: { level, seed, levelSource: "MANUAL" }` payload; `SeatFact` (`rulesets/types.ts`) becomes a discriminated union with a `DARTBOT` branch carrying that same payload; `SeatPlan`/`CreateSessionResult` (`services/types.ts`) carry it server-side. New `generateBotSeed()` (`lib/id.ts`, Web Crypto `Uint32Array`, mirroring `generateId()`'s RNG) mints the seed; new `RULESET_DARTBOT`/`supportsDartbot()`/`DEFAULT_BOT_LEVEL = 8` (`rulesets/capabilities.ts`) is the shared frontend/backend admission policy, following the existing `RULESET_CAPABILITIES` placement precedent rather than living beside `SEAT_CAPS` as the architecture doc phrases it (`session-seats.service.ts` is backend-only; the setup screens need to read the same map). `session-seats.service.ts`'s `rejectSeatRequest` gates a bot seat on `supportsDartbot`; `composeSeatFacts` gains the `DARTBOT` branch. `session.service.ts`'s `loadCreateSessionLookups` resolves the `DARTBOT` participant-type id; `buildSeatPlan` mints level+seed and always sets `displayName: "DartBot"` server-side, ignoring a spoofed request value, mirroring how the `PLAYER` seat's name is already copied server-side. `session-mode-resolution.ts`'s `seatsFromParticipants()` — the "coerce anything-not-GUEST to PLAYER" two-way collapse the architecture doc names as the anti-pattern to remove — is deleted, replaced by a private `toSeatFacts()` shared by `startSessionInput`/`reseatSnapshot`; `participantsFromSeats`/`participantsFromGuests` gain a `DARTBOT` branch, `participantsFromGuests` a new optional `bot` parameter that wins over any guest (mutually exclusive by construction). `guest-list.ts` gains `addBotOpponent()`, mirroring `addTypedGuest()`'s single-opponent-slot refusal in both directions (a bot seat blocks a guest add and vice versa); `setup-controller.ts` gains `bot`/`showOpponentChooser` state and `addBot`/`removeBot`, `start()` now sends `participantsFromGuests(this.guests, this.bot)`. New `OpponentChooserModal.astro` — the setup screen's existing add-opponent button (`AddGuestButton.astro`) becomes a two-step Guest/DartBot chooser rather than a new screen; `GuestSection.astro` renders the seated bot's avatar (target icon) and remove badge; `UserSection.astro` forwards a new `allowDartbot` prop. **Scope decision, disclosed not silent:** `RULESET_DARTBOT` admits only `AROUND_THE_CLOCK_V1`/`BOBS27_V1`/`DOUBLES_TRAINING_V1` — 3 of the 5 rulesets `08-DartBot.md` names as already playing `DictatedStrategy` — because Shanghai V2 and Singles Training V2 can never create *any* 2-seat session today (F45, already open, explicitly deferred, not this task's to fix); only those three setup forms (`AroundTheClockSetupForm.astro`/`Bobs27SetupForm.astro`/`DoublesTrainingSetupForm.astro`) pass `allowDartbot={supportsDartbot(...)}`. Design: `docs/superpowers/specs/2026-09-01-dartbot-v1-delivery-design.md`; plan: `docs/superpowers/plans/2026-09-01-dartbot-4-seat-admission.md`. Execution found two plan-authoring gaps beyond the plan's own text, both logged as new findings rather than fixed silently: Task 4's `SeatFact` widening left `singles-training-results-title.ts`'s inline seat-shape param (`participantTypeKey: "PLAYER" | "GUEST"`) structurally incompatible, requiring a widen to `"PLAYER" | "GUEST" | "DARTBOT"` the plan never named (fixed in-scope, since Task 4's own typecheck step required it); and Task 7's `buildSeatPlan` code, committed verbatim, failed `npx fallow`'s health gate (cyclomatic 10, CRAP 31.6) — fixed by replacing its three interleaved ternaries with one early return per participant-type branch, same behavior — logged as **F49**, the second consecutive DartBot-series plan to trip this exact gate (F48, phase 2). Also logged **F50**: `participantsFromSeats`'s own docstring still names the just-deleted `seatsFromParticipants`. `00-File-Inventory.md` rows for `capabilities.ts`, `guest-list.ts`, `session-mode-resolution.ts`, `session-seats.service.ts`, and `08-DartBot.md` (its "no DartBot code exists yet" claim was already stale from phases 1–2 landing first) updated to match. Validation: `npm test` 3213/3213 across 150 files, `npx fallow` exits 0 (maintainability 92.0, 0 above threshold), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-findings-log.sh`/`check-test-coverage.sh` and all ten `app/`-scoped structural gates pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this change touches no schema). Manual UI verification (Task 11) could only confirm both `/games/bobs27/setup` and `/games/501/setup` render at 200 with the new markup present in the raw HTML — full interactive click-through (opening the chooser, seating a bot) could not be exercised because every setup page redirects to `/login` before Alpine hydrates, this sandbox having no Neon Auth backend configured either (the same D193 precedent, extended to auth). No `DECISIONS.md` entry — the one open design question (`RULESET_DARTBOT`'s placement) was resolved by existing precedent and stated explicitly in the plan's own Global Constraints, not left for the executor to weigh.)

> **Version:** 1.31.0 (2026-09-01 — DartBot phase 1 (Throw Engine): five pure-function modules under `app/src/modules/dartbot/` — `rng.module.ts` (seeded `mulberry32` PRNG keyed by `(seed, dartIndex)`, never a running instance-held stream), `skill-profile.module.ts` (hand-set `LEVEL_SKILL_TABLE` prior for levels 1–15, D-E production-data re-fit deferred as a data edit to the same table), `aim-resolver.module.ts` (`ThrowIntent` + `SkillProfile` → aim `BoardPoint` via the shared `zoneCentroid()`, radially offset by `bedOffsetMm`), `throw-engine.module.ts` (aim + anisotropic Gaussian scatter with heavy-tail outlier draw + wire bounce-out + `classify()` → `BotThrow`), plus `interfaces.ts`/`types.ts` barrels. `docs/architecture/07-Frontend/02-Folder-Structure.md` (0.2.2 → 0.2.3) registers `modules/dartbot/` in the authoritative tree, widens the `.module.ts` suffix row, and adds `.strategy.module.ts`. Design: `docs/superpowers/specs/2026-09-01-dartbot-v1-delivery-design.md`; plan: `docs/superpowers/plans/2026-09-01-dartbot-1-throw-engine.md`. Execution found two plan-authoring gaps beyond the plan's own text: the plan's example imports (`@lib/game/board/types`, `@modules/dartbot/types`) reached past their area's raised barrel, which `scripts/check-type-barrels.sh` rejects — resolved to `@lib/types`/`@modules/types` per the existing raising rule, and the new `dartbot/interfaces.ts`/`dartbot/types.ts` barrels themselves needed raising into `app/src/modules/interfaces.ts`/`types.ts`, a step no task in the plan named. Validation: `npm test` 3159/3159 across 147 files, `npx fallow` exits 0 (maintainability 92.0), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-context-map.sh`/`check-doc-links.sh`/`check-context-budget.sh`/`check-decision-ids.sh`/`check-findings-log.sh` all pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this change touches no schema). No `DECISIONS.md` entry — no alternative was weighed, the plan's own values were transcribed as specified.)

> **Version:** 1.30.0 (2026-09-01 — three new architecture docs registered: `08-DartBot.md` (simulated opponent design — seat layer, play loop, delivery phases; no DartBot code exists yet), `09-training-routines.md` (proposed training routine/exercise/exercise-engine architecture, extending `GameEngine`/ruleset/configuration-snapshot), `10-trivia.md` (standalone checkout-trivia flashcard quiz, reusing `checkout-path.module.ts`, explicitly not integrated with `09-training-routines.md`). Docs-only: rebased a pre-written branch onto current `main`, added the missing `status:`/`scope:`/`read-when:`/`updated:` front-matter `09-training-routines.md` lacked, registered all three in `00-File-Inventory.md` with chars/4 token estimates. No `04-Architecture-patterns.md`/`DECISIONS.md` change — nothing here alters an existing pattern or invariant. Validation: `check-context-map.sh` and `check-context-budget.sh` both pass.)

> **Version:** 1.29.0 (2026-08-28 — Shanghai V2 Target Needed: new `SHANGHAI_V2` ruleset version adds a Normal/Hard difficulty toggle (`ShanghaiV2Config`) alongside the untouched `SHANGHAI_V1`, following the exact `121_V2` precedent — one shared `ShanghaiEngine` class via two `GameEngineFactory` registrations and a `difficultyOf()` config normalizer, `shanghaiV2Validator` registered in `services/rulesets/registry.ts` in the same commit as the engine change (`app/CLAUDE.md`-mandated), `SHANGHAI_V2: [DETAILED_DARTS, VISUAL_BOARD]` in `capabilities.ts`. Under Hard, a round landing zero darts in the active number's single/double/treble halves the seat's running total (round-half-up); a round with any hit is never halved. `shanghai-setup.data.ts` moves to `SHANGHAI_V2` and injects the player's chosen `difficulty` via `createPresetSetupController`'s `configOverrides` hook, reusing V1's existing empty preset rather than seeding a second `configuration_templates` row (a second row would leave `presets[0]` unable to disambiguate — see D245). `docs/game-rules/rulesets/shanghai.md` gains a "Target Needed (V2 — implemented)" subsection. New `database/seeds/0012_shanghai_v2_game_engine_reference.sql` (`ruleset_versions` row only) plus two appended `seeds/0007` capability rows and `database/verification/0012_shanghai_v2_capability_checks.sql`; `database/README.md`'s Seed Order and Verification Scripts table updated. Two pre-existing tests needed count bumps the plan didn't anticipate, closed in the same branch: `tests/scripts/verify-db.test.ts` (the new verification script must be named in `database/README.md`) and `tests/lib/game/rulesets/capability-validator-parity.test.ts`'s hardcoded ruleset count (10 → 11). Decision: D245. Design: `docs/superpowers/specs/2026-08-28-shanghai-v2-target-mode-design.md`; plan: `docs/superpowers/plans/2026-08-28-shanghai-v2-target-mode.md`. Validation: `npm test` 3029/3029 across 138 files, `npx fallow` exits 0 (maintainability 91.9), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-game-engines.sh`/`check-game-wiring.sh`/`check-decision-ids.sh` all pass; `db:status`/`db:migrate`/`db:introspect` and the new verification script could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent); the setup-screen toggle was confirmed rendering correctly (Normal/Hard options, NORMAL default, hint caption) via a direct HTTP fetch of the rendered page, but full in-browser gameplay smoke testing (session creation, live halving) needs the same live database and was not run.)

> **Version:** 1.28.0 (2026-08-28 — engine-findings-cleanup: closed F39, F40, F41 (D241), the three mechanical duplication pockets D240's own audit found and deliberately deferred. `turn-log.module.ts` gains `isDartObservationInput<T>(input: T | DartObservation): input is DartObservation`, replacing the identical private guard 501, 121, and TUOD each declared (Score Training's own differently-shaped guard stays local); `exerciseBlockStage(): StageFact`, replacing the identical `const STAGE: StageFact = {...}` literal Around the Clock, Bob's 27, Doubles Training, Score Training, Shanghai, and Singles Training each declared (each file keeps its own `const STAGE = exerciseBlockStage();`, so every `{ ...STAGE }`/`STAGE.clientKey` call site is untouched, and each file's now-unused `StageFact` type import was removed); `turnsBeforeVisit(turns, visit): TurnFact[]`, extracting the shared "slice up to the visit, then refold" step 121's `seatBeforeVisit` and TUOD's `targetBeforeVisit` both performed before calling their own different `foldXxxState`. 501's `remainingBeforeVisit` was deliberately left alone — a genuinely different running-total technique, not a 3rd copy — and `04-Architecture-patterns.md` Pattern 18 (1.8.1 → 1.8.2) now documents the two-technique split as intentional. Design: `docs/superpowers/specs/2026-08-28-engine-findings-cleanup-design.md`; plan: `docs/superpowers/plans/2026-08-28-engine-findings-cleanup.md`. Validation: `npm test` 2889/2889 across 137 files, `npx fallow` exits 0 (14.5% duplication, down from 14.8%), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-decision-ids.sh`/`check-doc-links.sh`/`check-context-map.sh`/`check-findings-log.sh`/`check-game-engines.sh`/`check-context-budget.sh` all pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this change touches no schema))

> **Version:** 1.27.0 (2026-08-27 — engine-duplication-cleanup: an audit of all 9 `*.engine.module.ts` files against Pattern 18 and the D232 extraction found two remaining pockets of hand-duplicated logic and folded them into new/generalized shared modules (D240). `app/src/modules/game/checkout-bust.module.ts` (new) gains `resolveCheckoutAttempt(remainingBefore, scored, endedOnDouble)`, the double-out bust/checkout rule 501, 121, and TUOD each hand-wrote at 5 call sites total (`resolveFiveOhOneVisit`, `FiveOhOneEngine.settleVisit`/`dartChecksOutFinalLeg`, `resolveOneTwentyOneVisit`, `OneTwentyOneEngine.settleVisit`/`wouldCompleteDart`, TUOD's `visitOutcome`), each ruleset's own extra bust condition (121's final-visit unreachable-remainder rule, TUOD's odd-remainder-with-one-dart-left rule) staying local. `seat-state.module.ts`'s `otherSeatsComplete` gains a third `isComplete: (seat: TSeat) => boolean` parameter so TUOD and Score Training (no `status` field, duration-budget completion) can call it instead of reimplementing the identical inline fold; the 3 existing `status`-based callers (Around the Clock, Singles Training, Doubles Training) pass `(seat) => seat.status === "COMPLETE"` explicitly. Documented as an addition to Pattern 18 in `04-Architecture-patterns.md` (1.8.0 → 1.8.1). Comparing the 3 touched engines against their `docs/game-rules/rulesets/*.md` source material during the same audit found staleness systemic across all 9 non-canonical raw-notes files, corrected in the same branch: 6 files' Features table still said "Multiplayer: TBD" past their 2026-08-22 1v1 ship date; Singles/Doubles Training misfiled their shipped 1v1 win condition under "Later versions (V2+)" instead of Objective; `501.md`'s Known-limitations/Open-questions described the bust-rate limitation as fully open when VISUAL_BOARD capture already resolves it; `121.md` had no Capture section and didn't document its final-visit early-bust rule. Four smaller duplication pockets noticed during the same audit — `isDartObservation` hand-duplicated 3x, six identical literal `STAGE` constants, two unconsolidated "state before this visit" techniques, and why `fallow`'s duplication gate did not flag any of this before the fix — were deliberately left unpursued and logged as F39-F42. Design: `docs/superpowers/specs/2026-08-27-engine-duplication-cleanup-design.md`; plan: `docs/superpowers/plans/2026-08-27-engine-duplication-cleanup.md`. Validation: `npm test` 2869/2869 across 137 files, `npx fallow` exits 0, `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `check-decision-ids.sh`/`check-doc-links.sh`/`check-context-map.sh`/`check-findings-log.sh`/`check-game-engines.sh` all pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this change touches no schema))

> **Version:** 1.26.0 (2026-08-27 — issue #173 accuracy-formatting fix: extracted `accuracyDisplay(hits, darts)` into `app/src/lib/game/play-visit-stats.ts`, the single shared formatter for every ruleset's hit-rate percentage (always 2 decimal places, `"0.00%"` at zero darts), replacing 5 independent implementations across `around-the-clock-play.data.ts`, `bobs27-play.data.ts`, `shanghai-play.data.ts`, `doubles-training-play.data.ts`, `singles-training-play.data.ts` — 4 of which previously rounded to a whole number via `Math.round`, the root cause of Doubles Training's reported "rounds to multiples of 10%" symptom. Documented as Pattern 20 in `04-Architecture-patterns.md` (version 1.6.1 → 1.7.0) and recorded as `decisions/game-engine.md` D237. `00-File-Inventory.md`'s `04-Architecture-patterns.md` row updated to name Pattern 20. Validation: `npm test` 2828/2828 across 135 files, `npx fallow` exits 0 (14.3% duplication, under the 15% gate), `astro check --minimumFailingSeverity hint` 0 errors/0 warnings/0 hints, `format`/`format:check` clean, `check-decision-ids.sh`/`check-doc-links.sh`/`check-context-map.sh`/`check-findings-log.sh` all pass; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — the established D193 precedent; this change touches no schema)

> **Version:** 1.25.0 (2026-08-26 — DB verification script drift closed: F7 (narrowed `database/verification/0008_shanghai_capability_checks.sql`, `0009_121_capability_checks.sql`, `0010_around_the_clock_capability_checks.sql` to their own game's row plus the shared "no undeclared session" check, dropping each script's stale full-table exact-count assertion — full bidirectional parity against `capabilities.ts` already lives in `0007_capability_seed_checks.sql` alone, which re-verifies the whole table on every run since its seed file is the single running ledger; 0008 included even though F7's evidence named only 0009/0010, since it carried the identical stale-count defect), F11 (reworded `0007_capability_seed_checks.sql`'s stale "fixed 9-row VALUES list" comment to describe what the guard checks without naming a specific count), F13 (added `database/verification/0023_owner_scoped_dart_view_checks.sql` — a PLAYER + GUEST fixture proving `v_dart_analytics`/`v_dart_locations` return only the owning player's dart while `v_game_replay` deliberately returns both, per migration `0023`'s own comment). Verification-script-only change set, no `app/` or migration/seed files touched. Validation: `scripts/check-context-map.sh`, `scripts/check-findings-log.sh`, `scripts/check-doc-links.sh` pass; the four edited/new `.sql` files could not be executed against a live database in this sandbox (no `DATABASE_URL`, established D193 precedent) — `npm run db:verify` still needs to run against the real Neon database before merge)

> **Version:** 1.24.0 (2026-08-23 — setup/ruleset-doc sibling consistency closed: F8 (dropped the redundant "V1 seeds exactly one configuration preset" JSDoc line from five preset setup-data modules — `around-the-clock-setup.data.ts`, `bobs27-setup.data.ts`, `one-twenty-one-setup.data.ts`, `shanghai-setup.data.ts`, `singles-training-setup.data.ts` — `doubles-training-setup.data.ts` needed no edit and `createPresetSetupController` (`setup-controller.ts:27`) remains the single source of that fact), F26 (added a "### Variants — Multiplayer (1v1)" subsection with the 1v1 win-condition line to `docs/game-rules/rulesets/singles-training.md`, mirroring `doubles-training.md`'s existing wording/placement). Comment-only + one non-canonical doc addition, no runtime behavior change. Validation: `npx tsc --noEmit` clean, affected setup-data Vitest suites pass unchanged (53/53), `check-doc-links.sh` and `check-findings-log.sh` pass)

> **Version:** 1.23.0 (2026-08-23 — governance/context-doc drift closed: F1 (dropped 3 unusable `gh` CLI entries from `.claude/settings.json`'s allowlist), F3 (purged 4 hardcoded decision-id/count references from `DECISIONS.md` — the 3 the plan named plus a fourth, line 10's summary sentence, the same defect the plan's task list missed — leaving only the derive command as source of truth), F4 (deleted the dead `.worktrees/` line from `.graphifyignore`), F6 (corrected `00-File-Inventory.md`'s two stale "byte-identical AGENT.md mirror" rows to describe the D213 pointer-stub gate), F14 (see the 1.22.0 entry immediately below — belated registration of the guest-player-501-setup-ui spec/plan). Doc/config-only change set, no `app/` or `database/` files touched. Validation: `check-context-map.sh`, `check-doc-links.sh`, `check-context-budget.sh`, `check-agent-mirrors.sh`, `check-file-locations.sh`, `check-findings-log.sh`, `check-decision-ids.sh` all pass; `.claude/settings.json` confirmed valid JSON. `00-File-Inventory.md:235`'s `context-maintenance` row still says "8-step procedure" though the skill file has 9 steps — out of this task's named scope, logged as a new finding rather than fixed here)

> **Version:** 1.22.0 (2026-08-23 — belated registration: `docs/superpowers/specs/2026-08-21-guest-player-501-setup-ui-design.md` (2026-08-21, commit `7e355d3`) and its plan `docs/superpowers/plans/2026-08-21-guest-player-501-setup-ui.md` (2026-08-21, commit `a66cd9f`) never got a row here when they landed — F14. Added here as a belated registration of a previously-completed task; no code or other doc content changes with this entry.)

> **Version:** 1.21.0 (2026-08-22 — whole-plan review fixes for the single-opponent-seat-remaining-engines plan, six findings closed on the same branch. (1) **Play Again was broken for every replayed 1v1 match across all nine games**: `createSession` was called with no `participants`, so the replacement session got one seat, and the new engine was built from the config captured BEFORE the store was reseated — every dart in the replayed match carried the finished session's `participantRef`s, which the batch endpoint rejects as `BATCH_REFERENCE_MISSING`. New `participantsFromSeats` (`session-mode-resolution.ts`, the inverse of `seatsFromParticipants`) derives the request from the finished match's own seats and returns `undefined` for a solo list; `runPlayAgain` plus the five bespoke `playAgain` copies (Bob's 27, 121, TUOD, Score Training, 501) now send it and build their engine from the reseated snapshot. Solo replays are byte-identical to before, asserted per game. (2) 121's `winningSideKey` gained the `seats.length === 1 ? null :` guard every other engine carries — a solo checkout at 170 had reported its own side as winner (the same defect F20 logs for Shanghai, which stays open: its `raceResult ?? compareResult` shape makes the guard's placement a judgment call). (3) `foldAroundTheClockState` made module-private — exported with no consumer anywhere, which was F17 and the whole of `npx fallow`'s dead-code failure; fallow now reports only `dupes` (F16) and `health` (F25), and F17 is deleted. (4) The plan's own spec and plan registered in this file's Task Records table. (5) Three engine doc comments and Pattern 18 corrected: `foldTuodState` and `foldScoreTrainingState` DO pass `activeSeat()` a real completion predicate (verified against the 4-argument call sites), structurally a no-op under their uniform ROUNDS budget — D230 claimed only Around the Clock passes one at all, corrected by the new append-only D231 rather than an edit. (6) Pattern 18's score-compare list gained Singles Training and Doubles Training; `06-API/04-Endpoint-Contracts.md`'s "other seven rulesets" corrected to eight, matching its own table. Validation: `npm test` 2673/2673 across 131 files, `npx tsc --noEmit` clean, `astro check --minimumFailingSeverity hint` 0/0/0, `format:check` clean, all fourteen structural gates green; `npx fallow` still exits non-zero on F16/F25 alone, both out of scope here; `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL` in this sandbox, the established D193 precedent))

> **Version:** 1.20.0 (2026-08-22 — Task 21 (final task) closes out the single-opponent-seat-remaining-engines plan: docs, decision ledger, full validation, context maintenance. Six ruleset docs (`bobs-27`, `121`, `around-the-clock`, `ten-up-one-down`, `shanghai`, `score-training`) each gain a one-line 1v1 win condition in the Objective section; `doubles-training.md` already carried one from its own engine task, `singles-training.md` does not yet — outside this task's named file list, logged as new finding F26. `04-Architecture-patterns.md` Pattern 18 gains a "Win conditions (1v1 and beyond)" subsection naming the three categories (elimination/race-to-finish/score-compare), `match-outcome.module.ts`'s three functions, and `activeSeat()`'s new completion-predicate parameter, cross-referencing `2026-08-22-single-opponent-seat-remaining-engines-design.md`. `06-API/04-Endpoint-Contracts.md`'s `POST /sessions` section gains the `SEAT_CAPS` table (501_V1 → 4, eight other rulesets → 2, default 1) and generalizes the old single-`501_V1`-special-case seat-rejection wording. `decisions/game-engine.md` gains D230 recording the win-condition model, `SEAT_CAPS`, and the completion-predicate parameter as one append-only block. This File Inventory's rows for `04-Architecture-patterns.md`, `06-API/04-Endpoint-Contracts.md` and `decisions/game-engine.md` (32 → 38 decisions) updated to match. Full `validate:app` run: `db:status`/`db:migrate`/`db:introspect` could not run (no `DATABASE_URL`/Neon credentials in this sandboxed session — an environment gap, not a code defect); `npm test` (2652/2652), `astro check --minimumFailingSeverity hint` (0/0/0), all ten structural `check-*.sh` gates relevant to this change, and `format`/`format:check` all passed clean. `npx fallow` (also part of the completion bar) still fails — pre-existing F16 (duplication) and F17 (dead `foldAroundTheClockState` export), plus a newly-observed health-threshold breach on `match-outcome.module.ts` (9-importer fan-in), logged together as new finding F25 since this task's own scope is docs + validation only, no source changes.)

> **Version:** 1.19.0 (2026-08-22 — split-scoreboard nested-blur fix: `SplitScoreboard.astro`'s outer grid carried its own `glass` (backdrop-filter blur) class wrapping `SplitScoreboardHalf.astro` → `SinglePlayerDisplay.astro`, whose own card already carries `glass` — a nested backdrop-filter stack unique to the split (2-seat) path, since single-player mode has only the one `glass` layer. Reported as a name/score overlap on a real iPhone 12 Pro in production that did not reproduce in desktop-simulated mobile viewports or in this environment's Chromium-only Playwright at matching dimensions (390×various heights down to 600px) with realistic seat names — consistent with a WebKit-specific nested-`backdrop-filter` compositing/stacking bug rather than a layout/sizing bug, since no other component in this codebase nests `glass` inside `glass`. Fix: dropped the redundant outer `glass` from `SplitScoreboard.astro` — the seat cards already provide the glass surface the user sees; visually unchanged in Chromium. Root cause could not be directly confirmed against real WebKit in this sandboxed environment — flagged to the user for on-device re-verification)

> **Version:** 1.18.0 (2026-08-21 — first `IconBtn.astro` call sites: `AddGuestButton.astro`'s dashed add-guest circle and `GuestSection.astro`'s per-guest remove badge moved off `Button.astro`'s `icon` prop onto `IconBtn.astro`, per owner request. Visuals unchanged (`AddGuestButton` keeps its `border-4` override via `class`; the remove badge keeps its Alpine `:aria-label` override alongside a static `ariaLabel="Remove guest"` to satisfy `IconBtn`'s required prop, same trick the prior `Button.astro` call site relied on). `.fallowrc.jsonc`'s `IconBtn.astro` `entry` allowlist line from 1.17.0 removed — the component now has real call sites and is reachable without it. Other icon-only `Button.astro` call sites (`GameLayout.astro`'s exit button, `BoardInputPanel.astro`'s undo/bounce-out, `LogoutButton.astro`) remain untouched, still out of scope)

> **Version:** 1.17.0 (2026-08-21 — new `IconBtn.astro` icon-only button primitive: always a true circle via `aspect-square` + `rounded-full` regardless of caller-supplied padding, `border-3` base width across all five variants (`primary`/`secondary`/`ghost`/`error`/`dashed`), zero built-in padding, one default slot, required `ariaLabel` — no text fallback exists. Reuses `Button.astro`'s existing `.btn-*` CSS rules from `global.css`, no new CSS. Component only, per owner decision — migrating existing icon-only `Button.astro` call sites to it is an explicit follow-up, not part of this change; `08-Component-Inventory.md` gains one new row; `.fallowrc.jsonc` gains an `entry` allowlist line since the component has no call sites yet)

> **Version:** 1.16.0 (2026-08-21 — split-scoreboard leg-pill placement: `SinglePlayerDisplay.astro` gains an `above` slot rendered inside the card, before the big number; `SplitScoreboardHalf.astro` moves the seat's leg-wins pill from the header row into that slot (centered, directly above the score/target number) and centers the standalone seat name above the card, per owner request)

> **Version:** 1.15.0 (2026-08-21 — split-scoreboard active-seat indicator: replaced the accent dot next to the seat name with an accent-colored border on the active seat's `SinglePlayerDisplay` card (new `activeExpr` prop swaps `border-border`/`border-accent` via a reactive Alpine `:class` ternary rather than a static class, avoiding a same-category class-coexistence race), and the inactive seat's whole column now dims to `opacity-90`, per owner request)

> **Version:** 1.14.0 (2026-08-21 — split-scoreboard overflow fix: shipped `SplitScoreboardHalf.astro` overlapped its own header row on shorter viewports because `SinglePlayerDisplay.astro`'s `text-7xl` big number, sized for the single-seat full-width layout, didn't fit the split view's narrower/shorter halves — reproduced with a throwaway Astro route rendering the real component tree under Playwright at reduced viewport heights (no `DATABASE_URL` for a real 2-seat session, so `/api/auth/get-session` was routed to hang rather than resolve, keeping the client-side auth guard from redirecting to `/login` before the screenshot). Fixed by giving `SinglePlayerDisplay.astro` a `size` prop (`lg`/`sm`, default `lg`, single-seat callers unaffected) and passing `size="sm"` from `SplitScoreboardHalf.astro`; the per-seat avatar icon dropped from the half's header row per owner request, leaving just the name and its active-dot; `AddGuestButton.astro`'s dashed-outline border width changed from `border-6` to `border-4` per owner request. `08-Component-Inventory.md` rows updated for `SinglePlayerDisplay.astro`'s new prop and `SplitScoreboardHalf.astro`'s avatar-free header)

> **Version:** 1.13.0 (2026-08-21 — guest-play UI fixes + split scoreboard: 501's add-guest button hardened (`Button.astro`'s new `dashed` variant, `border-6`, full-opacity `border-accent`, no `!`; `addGuest()` caps at 1 guest, closing the "3 guests" cap-mismatch gap against the shipped 1v1 UI) and its remove-badge icon sizing fixed (icon-driven `size-4`, not a fixed oversized box); `scripts/check-style-tokens.sh` now bans the Tailwind important modifier in both forms — prefix (`!utility`, D175) and suffix (`utility!`, D175's own sanctioned v4 form) — after auditing every remaining `!`-suffixed class in the repo and confirming `cn()`'s `twMerge` merge ordering already resolved every one without it (D226, supersedes D175's suffix endorsement); new shared `SplitScoreboard.astro`/`SplitScoreboardHalf.astro` render a two-seat scoreboard (per-seat avatar/name/active-dot, leg-wins pill, checkout chips, leg dot pager), each half driven by build-time Alpine-expression-string props; `five-oh-one-play.data.ts` grows six seat-parameterized getters (`remainingScoreFor`/`checkoutHintFor`/`averageFor`/`previousScoreFor`/`dartsThrownThisLegFor`/`legsWonFor`) alongside the existing no-arg getters, which now delegate to the active seat — fixing a pre-existing bug where `average()`/`previousScore()` read the whole match's turns unfiltered by seat; `FiveOhOne.astro` branches on `state()?.seats.length` to pick single vs. split rendering, single-seat markup unchanged; wiring the remaining 8 engines is deferred to a follow-up plan, since none can reach 2 seats yet)

> **Version:** 1.12.0 (2026-08-21 — test-coverage gate: new `scripts/check-test-coverage.sh` + `scripts/check_test_coverage.py` as the fourteenth `.husky/pre-commit` gate and eighteenth `quality.yml` structure gate, failing any change set that edits a runtime `.ts` file under `app/src/` or `app/scripts/` without editing a test that imports it; coverage resolved through `app/tsconfig.json`'s alias table rather than filename mirroring, because `app/src/lib/utils/is-nav-active.ts` is covered off-mirror by `app/tests/utils/is-nav-active.test.ts`; exemptions for `.d.ts`, own-runtime-declaration-free re-export barrels and type-only edits all derived rather than listed, with `drizzle-kit introspect` output the one path-keyed entry; no per-file silencer, deliberately; proven against seven probes before wiring (source alone, source with its test, uncovered source, pure barrel, generated file, non-TS change, off-mirror legacy path); the `structure` CI job now checks out with `fetch-depth: 0` so the merge base exists; writing the gate found the hole it was built for — `ParticipantInput` had shipped with no test parsing it, closed by the new `app/tests/pages/api/sessions/types.test.ts`; `app/CLAUDE.md` and the `run-all-gates` skill updated from 13 structural gates to 14; D224 recorded)

> **Version:** 1.11.0 (2026-08-21 — gate hardening: `npm run check` now runs `astro check --minimumFailingSeverity hint`, so hint-level diagnostics fail the build, and `.github/workflows/quality.yml`'s Type gate calls that script instead of the bare `npx astro check` it had been invoking, which would otherwise have kept CI on the old threshold; the three unused declarations the old threshold had allowed to accumulate (`FiveOhOneSnapshot` in `app/src/lib/game/five-oh-one-play.data.ts`, `EngineFacts` in the around-the-clock engine test, and `missObservationFor`'s unread `state` parameter in the shanghai engine test) deleted, taking the gate to 0 errors / 0 warnings / 0 hints, proven to bite by re-introducing one hint and confirming a non-zero exit; `npx fallow` returned to green by decomposing `foldFiveOhOneState` (cognitive 16 against a ceiling of 15) into `legVisitsOf` / `foldLeg` / `seatStatesOf`, closing and deleting finding `F12` by explicit user permission; `app/CLAUDE.md` and the `validate-app` skill now state the zero-hint bar as the definition of done; D223 recorded. `fallow dupes` stays non-gating at 71 clone groups / 14.9%, which D215's deliberate `*-play.data.ts` duplication dominates.)

> **Version:** 1.10.0 (2026-08-19 — consistency: `createPresetSetupController` (`app/src/lib/game/setup-controller.ts`) adopted by six games and `createThreeDartValidator` (`app/src/services/rulesets/three-dart.validator.ts`) adopted by five, both behaviour-preserving with all 819 existing tests passing unmodified; new `docs/architecture/07-Frontend/09-Adding-A-Game.md` carrying the 26-file / 9-tree touch list, `bobs27` as the named reference exemplar, the route-slug/code-slug rule and the two setup-controller opt-outs, with the "New game (full stack)" pack repointed at it instead of carrying a partial fan-out inline; new `scripts/check-game-wiring.sh` as the thirteenth pre-commit gate and seventeenth `quality.yml` structure gate, proven against six break-fixtures before wiring; root `CLAUDE.md`'s Knowledge Graph section reworded to consult the committed `graphify-out/graph.json` rather than an absent CLI, closing and deleting finding `F2` by explicit user permission; D215 recorded)

> **Version:** 1.9.0 (2026-08-19 — governance: new root `FINDINGS.md` log for defects noticed but deliberately not fixed (append-then-delete lifecycle, `highest-issued:` high-water-mark ids, seeded with F1–F6 and none of them fixed); new root `CLAUDE.md` Hard Invariant making log-don't-fix non-negotiable and permission-gated, the only per-session floor increase in the change (~3.2k → ~3.4k, measured); new `scripts/check-findings-log.sh` as the twelfth pre-commit gate and sixteenth `quality.yml` structure gate, proven to fail on seven malformed fixtures before landing; `context-maintenance` step 8 rewritten from Self-learning gate to Findings gate, absorbing D107 — a rule sharpening is a finding whose subject is a rule; `DECISIONS.md` Deferred gains an explicit boundary line and sheds the `check-context-map.sh` false positive to F5; D214 recorded, superseding D107)

> **Version:** 1.8.0 (2026-08-19 — context diet: `00-Context-Map.md` reduced from 37.6k to ~1.7k tokens as a pure router; canonical file inventory extracted to `00-File-Inventory.md` (~8.4k); version history, the 57 per-task `docs/superpowers/**` rows and Current Implementation State extracted to this file; `07-Frontend/08-Component-Inventory.md` added and wired into the frontend packs; two packs added (issue-driven UI polish, new game full stack) and every existing pack budget recomputed from `chars/4`; all six `AGENT.md` files reduced to pointer stubs with `scripts/check-agent-mirrors.sh` inverted to assert the stub; `.claude/skills/{emil-design-eng,apple-design,animation-vocabulary,improve-animations,review-animations,using-git-worktrees}` and `using-superpowers/references/` deleted (2,020 lines, unreferenced or contradicting the No-git-worktrees invariant), with the dangling worktree references in `writing-plans`, `subagent-driven-development` and `executing-plans` and the dead Platform Adaptation section in `using-superpowers` repaired; `Tool Allowances & Restrictions` deleted from all six `CLAUDE.md` files (114 lines, non-binding); `scripts/check-context-map.sh`, `scripts/check-context-budget.sh` and `scripts/check-doc-links.sh` re-pointed at the new shape. Decision: D213. Per-session context floor ~39.3k → ~3.0k. No `app/` source changed, so `validate:app` did not apply. Deferred to later specs: the flagging/self-learning system, templates, and the unfollowable `graphify query` rule.)

> **Version:** 1.7.51 (2026-08-19 — analytics-mode target section too small fix, owner-reported (#131): every ANALYTICS + `VISUAL_BOARD` play screen that also renders the darts-preview strip (`VisitPreview.astro` — Bob's 27, Singles Training, Doubles Training, Shanghai, Around the Clock) could shrink its `SinglePlayerDisplay` target/score section well below the intended 40% height, while 501/Score Training/121 (no `VisitPreview`, keypad-only capture) rendered correctly, per the owner's own two confirming comments. Root cause: `SinglePlayerDisplay`'s `max-h-2/5 h-full` class only ever set a *ceiling* — a `min-h-0` from the component's own base classes still won, so in a CSS flex-column whose total content (target + the extra `VisitPreview` strip + `BoardInputPanel`'s viewport-relative `w-[min(100%,60vh)]` dartboard) exceeded the available height, the browser's flexbox shrink algorithm gave the target *zero* weight (its `flex-1` resolves to `flex-basis: 0%`, so `flex-shrink`'s scaled factor — `flex-shrink × flex-basis` — is `0`, meaning it cannot shrink further and is left at its already-shrunk 0-basis size) while `VisitPreview`/the board (nonzero `flex-basis`, from auto/content sizing) absorbed none of it either, since nothing forced them to give the target headroom back. Fixed on both ends: (1) the five `VisitPreview`-rendering interfaces' `SinglePlayerDisplay` gained `min-h-2/5` alongside the existing `max-h-2/5` (`app/src/components/layout/games/interfaces/Bobs27.astro`, `SinglesTraining.astro`, `DoublesTraining.astro`, `Shanghai.astro`, `AroundTheClock.astro`) — min = max pins it to exactly 40%, so the CSS clamping pass now floors it there and pushes any remaining deficit back onto the flexible siblings; 501/Score Training/121 (`FiveOhOne`/`ScoreTraining`/`OneTwentyOne.astro`) were left untouched, already correct per the owner's confirmation. (2) `BoardInputPanel.astro`'s board is no longer sized off `w-[min(100%,60vh)]` (a viewport-relative *width*, invisible to the flex layout's actual leftover height): its outer `x-show` wrapper is now `flex min-h-0 flex-col items-center gap-3` and the board's own square wrapper is `flex-1 min-h-0 max-h-[60vh] max-w-full aspect-square` — a real flex participant whose rendered size (not just its layout box) now derives from whatever height the column's flex algorithm actually leaves it, still capped at 60vh for the no-deficit case. No engine/schema/API/store change — pure CSS layout fix over already-rendered facts, shared by all 8 play screens (`BoardInputPanel.astro`) plus the 5 affected interfaces; no new `decisions/**` entry, same precedent as 1.7.35/1.7.48/1.7.49/1.7.50's display-only fixes. No test change — `.astro` markup variant/sizing logic is untested per D101, and the full suite is unaffected by a pure class/layout edit (confirmed: still 122 files/2361 tests, 0 regressions); `astro check` 0 errors (5 pre-existing hints, unchanged baseline); `npm run build` prerenders all 21 routes cleanly, rendered HTML spot-checked to confirm the new classes land (`min-h-2/5 max-h-2/5 h-full` on Bob's 27's target, unchanged `max-h-2/5 h-full` on 501's); all 15 gate scripts pass; `db:status`/`db:migrate`/`db:introspect` and the manual browser smoke test could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed, verified instead via the build's prerendered/rendered HTML output as above); branch `claude/issue-131-9orq5u` not yet merged to `main`, no PR opened as of this task; prior 1.7.50 (2026-08-19 — logo header-size regression fix (owner-reported): homepage header `<LogoLockup>` in `app/src/pages/index.astro`, moved into the header by 1.7.48's #126 fix, had its class changed from `size-40` to `h-8 w-auto` — but the SVG asset (`assets/logo-lockup.svg`) carries its own inline `style="height:auto"` on the root element, which always beats a stylesheet class for `height` (inline style wins the cascade), so `h-8` silently never applied; meanwhile `w-auto` did override the SVG's `width="1em"` presentation attribute, leaving neither axis with a real driving dimension — the logo rendered at a near-zero, "barely a mm high" size. Fixed by driving size from `width` alone (`class="w-24"`, 96px), letting the SVG's own baked-in `height:auto` compute a correctly proportioned ~33px height from its 230:80 viewBox — mirrors how the pre-move `size-40` happened to work (its height class was likewise silently overridden; only its width class was ever load-bearing). No schema/API/store/engine change; pure single-class CSS fix, no new `decisions/**` entry, same precedent as 1.7.35/1.7.49. Full suite 122 files/2361 tests unchanged (no new test — CSS-only display fix, D101); `astro check` 0 errors (5 pre-existing hints, unchanged baseline); `npx fallow` exits 0 (0 above threshold, maintainability 91.9); all 14 applicable gate scripts pass; `db:status`/`db:migrate`/`db:introspect` and manual browser smoke test could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed; verified instead via `astro build`'s prerendered HTML output showing the corrected `class="w-24"` on the rendered `<svg>`); branch `claude/issue-logo-header-size` not yet merged to `main`, no PR opened as of this task; prior 1.7.49 — 2026-08-19 — two independent owner-triaged bug fixes shipped together on one branch: #128 (121 active score reverts to 121 instead of climbing to the new target after a checkout) — `one-twenty-one-play.data.ts`'s `remainingInAttempt()`/`visitsThisAttempt()` folded only `turnsInCurrentRound()` (the just-opened round's turns, empty immediately after a checkout) through `foldRoundState`, which starts from `initialOneTwentyOneState()`'s hardcoded `START_TARGET` (121) — so the instant a round closed, before its first visit, the hero score read back the stale 121 instead of the newly climbed target. Fixed by folding `$store.game.turns` (the whole session) instead, matching `currentTargetLabel()`'s own already-correct whole-session fold — `applyOneTwentyOneVisit`'s reducer already resets `remainingInAttempt` to the new target on every checkout when run over the full history, so no per-round scoping was needed at all; the now-dead `turnsInCurrentRound()` helper removed from both the factory and `OneTwentyOnePlayContext`. #129 (dartboard hover highlights the wrong ring) — `global.css`'s `.dartboard-group .segment:hover .single, .segment:hover .multiplier` and `.segment:hover path` rules keyed `:hover` off the parent `<g class="segment">`, which wraps all four rings (inner/outer single, treble, double) for one number — hovering any one ring (e.g. the inner single) fired `:hover` on the shared group and lit up every ring in it, including the treble, on every number. Fixed by moving `:hover` onto the specific `<path>` under the pointer (`.segment .single:hover`, `.segment .multiplier:hover`, `.segment path:hover`) so only the ring actually under the cursor highlights. No schema/API/store/engine change across either fix — pure display-layer corrections over already-recorded facts and already-existing markup; no new `decisions/**` entry, same precedent as 1.7.35's Bob's 27 bug triage. Full suite 122 files/2361 tests (1 new: a checkout-climb `remainingInAttempt`/`visitsThisAttempt`/`currentTargetLabel` regression test for #128, confirmed failing against the pre-fix code before the fix landed); `astro check` 0 errors (5 pre-existing hints, unchanged baseline); `npx fallow` exits 0 (0 above threshold, maintainability 91.9); all 15 gate scripts pass; `db:status`/`db:migrate`/`db:introspect` and the manual browser smoke test could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed); branch `claude/issue-128-129-fixes` not yet merged to `main`, no PR opened as of this task; prior 1.7.48 — 2026-08-15 — three independent owner-triaged GitHub issue fixes shipped together on one branch, per the 1.7.42 precedent: #112 (uppercase non-analytics game inputs) — `SinglesRecreationalInput.astro`'s three literal-text tap-input labels (`Bull`/`Bullseye`/`Miss`) uppercased to `BULL`/`BULLSEYE`/`MISS`; the design spec and plan only named the first two, `Miss` was found during exploration and folded into Task 1's edit since it's explicitly named in issue #112's own wording (`isBullVisit()` left untouched — a method name, not display text). #113 (Singles Training session-summary stats) — `singles-training-play.data.ts`'s `uploadAndCompleteSession` widened its `resultsSnapshot` builder from `{points}` alone to also fold `misses`/`singles`/`doubles`/`trebles`/`hitPercentage` from `$store.game.turns` via the file's existing `countZoneKey` + `*_COUNT_ZONE_KEYS` helpers (already used by the play-screen's own live-stat getters, shipped 1.7.37) — `hitPercentage` follows `bobs27-play.data.ts`'s `computeStats()` precedent (`Math.round((hits/darts)*100)+'%'`, `"0%"` guard on zero darts); type widened in `lib/game/types.ts`; `SinglesTrainingResults.astro` gains five new `StatRow`s (Darts missed, Singles hit, Doubles hit, Trebles hit, Hit percentage). #114 (setup pages back button) — `AppLayout.astro` gains an optional `showBackButton` prop (default `false`, so its other 4 consumers — home, profile, statistics, games index — are unaffected), rendering a top-left icon-only `Button` navigating to `/games` via `chevron-down.svg` rotated 90° (reuse-by-rotation, the same trick `InfoSection.astro` already applies to the same icon for its own collapsed-state chevron — no new icon asset); wired into all 8 setup pages (`501`/`bobs27`/`121`/`singles-training`/`doubles-training`/`score-training`/`shanghai`/`around-the-clock`). No engine/schema/API change across all three — pure display-layer fixes over already-recorded facts and already-existing components; no new `decisions/**` entry, per the plan's own Global Constraints. Full suite 117 files/2333 tests (1 new: a zero-darts `"0%"`-guard case; the existing BULL-visit-completion test's assertion widened in place), 0 regressions; `astro check` 0 errors; `npx fallow` exits 0 (0 above threshold, maintainability 91.9); 14 gate scripts pass (5 always-run + 9 app-specific); `db:status`/`db:migrate`/`db:introspect` and the manual browser smoke test could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed); design spec and implementation plan registered in File Inventory below; branch `claude/issues-112-113-114-fk5ast` not yet merged to `main`, no PR opened as of this task; prior 1.7.47 — 2026-08-15 — 121/Shanghai/Around the Clock gain the `ANALYTICS + VISUAL_BOARD` capability pair (board-tap coordinate capture) alongside their existing capture mode, closing the gap 1.7.46 flagged as deferred (design spec `docs/superpowers/specs/2026-08-15-121-shanghai-atc-analytics-design.md`, plan `docs/superpowers/plans/2026-08-15-121-shanghai-atc-analytics-plan.md`). Shanghai and Around the Clock needed only capability/validator/seed/frontend wiring — both engines already carried real `locationX`/`locationY` into dart facts, so `capabilities.ts` widened `SHANGHAI_V1`/`AROUND_THE_CLOCK_V1` to `[DETAILED_DARTS, VISUAL_BOARD]` and both validators were rewritten to the `isDetailedDartsOrVisualBoardCapture` dispatch shape `singles-training.validator.ts` already uses. 121 needed a real engine change since it records whole-visit totals, not darts: `OneTwentyOneEngine` gains a dual-shape `OneTwentyOneInput = OneTwentyOneVisitInput | DartObservation` `record()`/`undo()`/`wouldComplete()` dispatch mirroring `FiveOhOneEngine`, deliberately NOT a blind copy — 501 has no per-round visit cap, so folding an open (still-being-thrown) turn through its reducer only ever touches `remainingScore`, safe; 121's `applyOneTwentyOneVisit` treats every turn it folds as a FINISHED visit and always increments `visitsThisAttempt` on a non-checkout, so folding a still-open turn's partial total through it would prematurely count an unfinished visit against the round's 3-visit budget. `deriveState()` is split accordingly (`deriveClosedState` for closed turns, an open turn's running total only overlaid onto `remainingInAttempt` for live display), and `recordDart`'s round-stage-opening check gates on `settleVisit`'s own `resolved: boolean` return, not merely `visitsThisAttempt === 0`, to avoid a false positive on a fresh round's very first dart. `capabilities.ts` widened `"121_V1"` to `[QUICK_SCORE, VISUAL_BOARD]`; its validator rewritten to `five-oh-one.validator.ts`'s dispatch shape (`isQuickScoreOrVisualBoardCapture`/`validateQuickScoreTurns`/`validateVisualBoardTurns`). Seed `0007` gains three rows (`SHANGHAI_V1`/`121_V1`/`AROUND_THE_CLOCK_V1` × `ANALYTICS`+`VISUAL_BOARD`, 11→14 triples) and `database/verification/0007_capability_seed_checks.sql` updated in lockstep, unexecuted in this container (no `DATABASE_URL`, D193) — this closes the exact seed-verification gap 1.7.46 flagged and deliberately left untouched (the live 14-row seed now matches the verification script's own count). All three play-data modules (`shanghai-play.data.ts`/`around-the-clock-play.data.ts`/`one-twenty-one-play.data.ts`) gain the `let self`/`boardInputData` spread/`recordDart` wiring; Shanghai/Around the Clock additionally override `boardInputData`'s default `visitMarkers()` with `playVisitMarkers` (reveal-then-clear, matching Singles/Doubles Training's 1.7.46 precedent) — 121 does not, matching `five-oh-one-play.data.ts`'s own precedent of no reveal-then-clear concept. `Shanghai.astro`/`AroundTheClock.astro`/`OneTwentyOne.astro` gate their existing recreational input behind `x-show="$store.game.inputModeKey !== 'VISUAL_BOARD'"` and render `BoardInputPanel` (reused byte-for-byte unchanged) alongside it. Several plan-authoring bugs caught and fixed during execution, none architectural: two collateral test suites the plan never scheduled (`capabilities.test.ts`'s hardcoded `capableRulesets("ANALYTICS","VISUAL_BOARD")` list, and `shanghai-setup.data.test.ts`/`around-the-clock-setup.data.test.ts`'s "falls back to the declared pair" tests, which had used `ANALYTICS + VISUAL_BOARD` as their undeclared-pair fixture — now genuinely declared, both re-pointed at `RECREATIONAL + QUICK_SCORE`, the same fixture `bobs27-setup.data.test.ts`'s equivalent test already uses, per D148's re-point-at-the-same-guarantee rule); two `one-twenty-one.validator.test.ts` cases omitted `captureModeKey`/`inputModeKey` on a validator now genuinely dual-mode; and several `visual board capture`/`wouldComplete` engine test fixtures were dart-arithmetic errors, not engine bugs — landing exactly on a live remaining of 1 after two darts (an immediate bust under the same rule `five-oh-one.engine.module.ts`'s `settleVisit` already applies, not a display bug) and attempting a cap-target checkout via a bull dart (only a genuine `DOUBLE` `hitZoneKey` ever checks a board visit out, matching 501's own `settleVisit` — bull can never finish a board-captured visit in this codebase, at either ruleset), all fixed by choosing arithmetic consistent with the codebase's own established bust/checkout rules rather than by changing engine behavior. `astro check` also surfaced a real gap `npm test` alone could not: `hiddenTimer` needed initializing in `shanghai-play.data.ts`/`around-the-clock-play.data.ts`'s returned object literal, not just declared on the widened `ShanghaiPlayContext`/`AroundTheClockPlayContext` types — a type-only diff type-checks fine at the declaration site but fails at every `as ShanghaiPlayContext`/`as AroundTheClockPlayContext` test cast, since the field is genuinely absent from the object being cast. Full suite 117 files/2332 tests, 0 regressions; `astro check` 0 errors (2 new warnings, `one-twenty-one.engine.module.ts`'s unused `config` field, same pre-existing pattern as `around-the-clock.engine.module.ts`/`shanghai.engine.module.ts`'s own empty-config engines); `npx fallow` exits 0 (0 above threshold, maintainability 91.9); all 14 gate scripts pass; `db:status`/`db:migrate`/`db:introspect`, the seed-0007 live verification script, and the manual browser smoke test could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed); no new `decisions/**` entry — every pattern applied here is already decided (D198 shape-dispatch, D196 capability declaration, D211 `play-lifecycle.ts` generalization), per the plan's own Global Constraints; design spec and implementation plan registered in File Inventory below; branch `claude/analytics-dartboard-input-ji763d` not yet merged to `main`, no PR opened as of this task; prior 1.7.46 — 2026-08-15 — Singles/Doubles Training gain the `ANALYTICS + VISUAL_BOARD` capability pair (board-tap coordinate capture) alongside their existing `RECREATIONAL + DETAILED_DARTS` keypad capture, mirroring Bob's 27's already-shipped pair (design spec `docs/superpowers/specs/2026-08-15-singles-doubles-training-analytics-design.md`, plan `docs/superpowers/plans/2026-08-15-singles-doubles-training-analytics-plan.md`). `SinglesTrainingEngine`/`DoublesTrainingEngine.record()` bugfix: dart facts now carry the observation's real `locationX`/`locationY` instead of always `null`, the same class of bug D189/Bob's 27 Phase 2 fixed for their own engines. `capabilities.ts` widened `SINGLES_V1`/`DOUBLES_TRAINING_V1` to `[DETAILED_DARTS, VISUAL_BOARD]`; both validators rewritten to the `isDetailedDartsOrVisualBoardCapture` dispatch `bobs27.validator.ts` already uses, with a dartless-turn rejection now applying under both capture modes; seed `0007` gains both `('SINGLES_V1'|'DOUBLES_TRAINING_V1', 'ANALYTICS', 'VISUAL_BOARD')` rows (9→10→11 triples) and `database/verification/0007_capability_seed_checks.sql` updated in lockstep, unexecuted in this container (no `DATABASE_URL`, D193) — its pre-existing gap versus the live 12-row seed (`SHANGHAI_V1`/`121_V1`/`AROUND_THE_CLOCK_V1` untracked) is left untouched, per minimal-diffs. Rather than re-forking Singles/Doubles Training into bespoke play-data modules a second and third time the way Bob's 27 did, `play-lifecycle.ts` (`playCommitDart`/`playUndoVisit`/`runPlayAgain`) is generalized: an additive `hiddenTimer` field on `PlayLifecycleContext` plus a mode-gated reveal-then-clear branch, and a new shared `playVisitMarkers` helper extracted from Bob's 27's own override (D211, `decisions/frontend/architecture.md`, supersedes D209's exclusion clause). Both play-data modules (`singles-training-play.data.ts`/`doubles-training-play.data.ts`) gain the `let self`/`boardInputData` spread/`recordDart`/`visitMarkers` wiring `bobs27-play.data.ts` already uses. `SinglesTraining.astro`/`DoublesTraining.astro` gate their recreational tap-row input behind `x-show="$store.game.inputModeKey !== 'VISUAL_BOARD'"` and render `BoardInputPanel` (reused byte-for-byte unchanged) alongside it, mirroring `Bobs27.astro`'s precedent. Full suite 117 files/2295 tests, 0 regressions; `astro check` 0 errors/0 warnings (6 pre-existing hints, unchanged baseline); `npx fallow` exits 0 (0 above threshold); `db:status`/`db:migrate`/`db:introspect`, the seed-0007 live verification script, and the manual browser smoke test could not run in this container (no `DATABASE_URL`/`.env`, per the established D193 precedent — flagged rather than claimed); design spec and implementation plan registered in File Inventory below; branch `claude/singles-doubles-analytics-4qzc9j` not yet merged to `main`, no PR opened as of this task; prior 1.7.45 — 2026-08-15 — Around the Clock v1 shipped end-to-end on one branch: single-player fixed 21-target path (1→20 then BULL) where any hit (single/double/treble) advances the target immediately, mid-visit — unlike Shanghai/Singles Training/121's fixed-target-per-visit shape, a lucky 3-dart visit can clear multiple numbers; BULL (either ring) ends the session immediately, even mid-visit with darts still in hand, no bust/fail condition in v1. `AroundTheClockEngine` (`modules/game/around-the-clock.engine.module.ts`) reuses `board-progression.module.ts`'s existing 21-target `numbersPath()`/`targetAt()`, with a bespoke `isAroundTheClockHit` for the BULL case since `isHitOn()`'s own BULL branch requires `INNER_BULL` only (doubles-path semantics) — this game accepts either ring; `applyAroundTheClockDart` advances `targetIndex` on every hit before checking the 3-dart visit boundary, and short-circuits to `COMPLETE` on a BULL hit regardless of `dartsThisVisit`, so a turn can close permanently short of 3 darts (still stamped `completedAt`, same as any other resolving dart) — the one real mechanical difference from every prior engine. `intendedTargetNumber`/`intendedZoneKey` stay `null` on every dart, same convention as Singles Training/Shanghai: even though the active target can now change mid-visit, it remains exactly recoverable by replaying `facts()` through the reducer up to that dart. Frontend reuses `SinglesRecreationalInput.astro`/`VisitPreview.astro` unmodified (DETAILED_DARTS-only capture, Singles Training precedent over Bob's 27's dual-mode) and `play-lifecycle.ts` for its full lifecycle; play data's `previewSegmentsFor` is simpler than Shanghai's — no target-number comparison needed, since every tap this UI renders is already relative to whichever target was active the instant it was thrown, so a non-MISS dart is always a hit — and its `uploadAndCompleteSession` closes over `$store.game.turns` (captured before any store reset) to fold `{turns, hits, totalDarts}` at completion time, since the engine's own `state()` carries no hit-count fields; BULL-ring tap mapping follows the Singles Training precedent (SINGLE→OUTER_BULL, DOUBLE→INNER_BULL, TREBLE blocked). New seed `database/seeds/0010_around_the_clock_game_engine_reference.sql` covers `game_types`/`ruleset_versions`/`configuration_templates` only, its capability row appended to `seeds/0007_ruleset_version_capabilities.sql`'s running ledger (same convention as Shanghai's `0008`/121's `0009`), with a new `database/verification/0010_around_the_clock_capability_checks.sql` and `database/README.md`'s Seed Order/verification-script table updated for `0010`. Tasks 1 (config schema) and 2 (engine/validator/capability, one commit) landed as planned, `scripts/check-game-engines.sh`'s atomicity gate satisfied the same way as the Shanghai/121 precedent. Two plan-quality gaps caught and fixed while executing this final task, neither scheduled by any task in the plan: neither `aroundTheClockSetup` nor `aroundTheClockPlay` was wired into `register-route-data.ts`'s `Alpine.data()` calls by Task 4's setup-flow commit — the same class of gap the 121 v1 rollout hit for its own two Alpine factories — without it neither route's `x-data` resolves at runtime; both now registered together in this task's commit. Task 1's `AroundTheClockConfigData` (`z.infer` of the empty config schema) was flagged by `npx fallow`'s dead-code check as an unused type export, same root cause as `ShanghaiConfigData`'s own removal — `AroundTheClockSnapshot` has no fields to map from it, unlike every other ruleset's `*Snapshot`, so nothing ever reads it; dropped, matching the Shanghai precedent exactly (121 never added the export at all). Full suite 117 files/2267 tests, 0 regressions; `astro check` 0 errors/0 warnings (6 pre-existing hints, unchanged baseline); `npx fallow` exits 0 (0 dead-code issues after the fix; 55 pre-existing duplication clone groups, same shared-shape debt already registered against Shanghai/121/Doubles Training's setup/play modules, under threshold); all 11 structural gate scripts pass; `db:status`/`db:migrate`/`db:introspect`/`db:seed`/`db:verify` and the seed-0010/0007 live verification script could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed); no new `decisions/**` entry — built entirely from already-decided patterns (Pattern 18 engine contract, D196 capability declaration, the `SinglesRecreationalInput.astro`/`VisitPreview.astro` reuse precedent), per the plan's own Global Constraints; design spec and implementation plan registered in File Inventory below; branch `claude/around-the-clock-v1-4xtisb` not yet merged to `main`, no PR opened as of this task; prior 1.7.44 — 2026-08-15 — 121 v1 shipped end-to-end on one branch: single-player X01-style checkout ladder from 121 to 170, double-out, 3 visits (9 darts) per attempt, fail-rule "stay" (a failed attempt retries the same target with a fresh budget; a checkout below 170 climbs the target by one; a checkout at 170 wins the session). `OneTwentyOneEngine` (`modules/game/one-twenty-one.engine.module.ts`) reuses the previously-unused `ROUND` `StageTypeKey`, one per attempt, mirroring 501's per-leg `LEG` stage model exactly, and adds `remainingInAttempt` to `OneTwentyOneState` — a field the design spec's own state sketch omitted, needed for the same reason `FiveOhOneState.remainingScore` exists (a pure per-visit fold needs a running remainder), not a new architectural pattern. Play data (`one-twenty-one-play.data.ts`) is bespoke, mirroring `five-oh-one-play.data.ts`'s double-confirm + session-finish-confirm dialog shape — deliberately not `play-lifecycle.ts`, already out of scope per D209's `GameEngine<DartObservation, unknown>` constraint, the same reason 501/Score Training stay out. New seed `database/seeds/0009_121_game_engine_reference.sql` covers `game_types`/`ruleset_versions`/`configuration_templates` only, its capability row appended to `seeds/0007_ruleset_version_capabilities.sql`'s running ledger (same convention as `0008`'s Shanghai row), with `database/README.md`'s Seed Order and verification-script table updated for `0009`. Tasks 1 (config/capability) and 2 (engine/validator/seed) landed as planned two commits, `scripts/check-game-engines.sh`'s atomicity gate satisfied by folding engine+validator+registry+seed into Task 2's single commit, matching the Shanghai precedent. Four plan-quality defects caught and fixed while executing, none of them architectural: the plan's own engine and play-data tests climbed the ladder to 170 via a fixed `scoreAttempted: 2` per visit instead of the actual per-target checkout amount (needed `scoreAttempted: target`) — a checkout only fires when the entered score equals the attempt's live remainder, so a constant score can never check out past the first target; a companion play-data test asserted a bust off `scoreAttempted: 41` where the setup visit actually left a remaining score of 80 (needed `81`, mirroring the sibling test that already had this right), and two more play-data tests needed an explicit `store.game.recordFacts(engine.facts())` after driving the engine directly, since `remainingInAttempt()`/the deferred-checkout assertion both read the reactive store mirror, not engine-internal state — all copy/typo-class test-authoring slips, not implementation bugs, only caught by actually running the tests against the real code rather than trusting the plan's pre-written expected output. `capabilities.test.ts`'s hardcoded ruleset-key array and `database/README.md`'s own Seed Order/verification table were both missing a `121_V1`/`0009` entry no task in the plan scheduled a step for. Neither `oneTwentyOneSetup` nor `oneTwentyOnePlay` was wired into `register-route-data.ts`'s `Alpine.data()` calls by any plan step, even though `07-Frontend/03-Alpine-Patterns.md` already documents the requirement — without it neither route's `x-data` resolves at runtime; both now registered. Full suite 113 files/2194 tests, 0 regressions; `fallow` clean (0 above threshold, maintainability 92.0); `db:status`/`db:migrate`/`db:seed`, the seed-0009/0007 live verification script, and the manual browser smoke test could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed); no new `decisions/**` entry — built entirely from already-decided patterns (Pattern 18 engine contract, D196 capability declaration, D209's `play-lifecycle.ts` scope), per the plan's own Global Constraints; design spec and implementation plan registered in File Inventory below; prior 1.7.43 — 2026-08-14 — Shanghai v1 shipped end-to-end on one branch: new solo round-by-round target game, rounds 1–20 fixed ascending, Shanghai instant win on single+double+treble of the round's own number in one visit (any order), otherwise runs to round 20 for a total-score finish. `ShanghaiEngine` (`modules/game/shanghai.engine.module.ts`) reuses `board-progression.module.ts`'s existing 21-target `numbersPath()`, simply never advancing past index 19 (BULL unreachable); its `ShanghaiState.dartsThisVisit` stores each visit's raw hit zone (or `null`) rather than a precomputed single/double/treble flag, so a Shanghai is derived on the visit's 3rd dart, matching "store facts, derive meaning." `ShanghaiConfig` is a genuinely empty `.strict()` schema (v1 has zero editable settings) — `ShanghaiConfigData` was dropped after being flagged as an unused export by `fallow`'s dead-code check, since `ShanghaiSnapshot` has no fields to map from it, unlike every other ruleset's `*Snapshot`. Frontend reuses `SinglesRecreationalInput.astro`/`VisitPreview.astro` unmodified (DETAILED_DARTS-only capture, matching the Singles Training precedent over Bob's 27's dual-mode rollout); setup/play data modules mirror Singles Training's shape, including `play-lifecycle.ts` reuse. New seed `database/seeds/0008_shanghai_game_engine_reference.sql` covers `game_types`/`ruleset_versions`/`configuration_templates` only — its capability row is instead appended to `seeds/0007_ruleset_version_capabilities.sql`'s own VALUES list (0007 is the single running ledger every ruleset's capability rows are appended to, confirmed by its own git history, e.g. the Bob's 27 `ANALYTICS`+`VISUAL_BOARD` addition; `tests/lib/game/rulesets/capability-seed-parity.test.ts` and `verify-db.test.ts`'s README-registration check both caught this gap in the plan, which had drafted a duplicate insert in `0008` instead), with `database/README.md`'s Seed Order and verification-script table updated for `0008`. Tasks 3 (engine) and 4 (server-side validator) landed as one commit, not the plan's separate two: `scripts/check-game-engines.sh`'s pre-commit gate requires a `rulesetVersionKey` to resolve in both `modules/game/engine.registry.ts` and `services/rulesets/registry.ts` atomically. `fallow`'s duplication gate (44 clone groups on this branch, up from `main`'s pre-existing 24) and its 2 pre-existing duplicate-export findings are unchanged baseline debt — confirmed by running `fallow` against a clean `main` checkout in a scratch clone — not a regression this branch introduced; the one genuinely new dead-export finding (`ShanghaiConfigData`) was fixed. Full suite 109 files/2131 tests, 0 regressions; `db:status`/`db:migrate`/`db:introspect` and the seed-0008/0007 verification script could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed); no new `decisions/**` entry — built entirely from already-decided patterns (Pattern 18 engine contract, D196 capability declaration, D208/D209 `play-lifecycle.ts` reuse), per the plan's own Global Constraints; design spec and implementation plan registered in File Inventory below; prior 1.7.42 — 2026-08-14 — Three independent owner-triaged issue fixes shipped together on one branch, personal-best/#99 deferred: #102 (jumpy interface) — `AppModeForm.astro` and `pages/games/index.astro` each gain an outer `x-show="!$store.settings.loading"`/`x-cloak` gate plus a sibling `animate-pulse` skeleton, mirroring the existing `loadingReconciliation`/`ReconciliationBlocked.astro` pattern already in the codebase; no store/API change — `settings.store.ts`'s `loading` flag already flips synchronously at the top of `init()`. #95 (Bob's 27 results modal) — `Bobs27Results.astro`'s title copy changes LOST→"Game over!" WON→"Winner!" (was "Lost"/"Won"); `bobs27-play.data.ts`'s `computeStats()` gains two derived fields, `doubleHitRatio` (`"H/T"` string, on-target-equality count over total darts, reusing the same `hitTargetNumber === intendedTargetNumber && hitZoneKey === intendedZoneKey` check `doublesPathPreviewSegments` already uses) and `highestNumberReached` (`doublesPathTargetLabel(targetAt(doublesPath(), state.targetIndex))`, reusing the exact helper `currentTargetLabel()` already calls) — both flow through `resultsSnapshot`'s type in `bobs27-play.data.ts`/`types.ts` and render via two new `StatRow`s; the "New high score!" personal-best pill is explicitly deferred, owner decision — no PB concept exists anywhere in the app yet and would need a new stored best-score comparison. #88 (finish game modal) — 501's `ConfirmDialog` usage drops its non-default `confirmLabel="Finish match"` prop, falling back to the component's own documented `"Confirm"` default, the only non-default confirm-button label in the app. No schema, API, store, or engine change across all three; pure UI-layer reuse of already-documented patterns — no new `decisions/**` entry. Full suite 105 files/2042 tests, 0 regressions; `db:status`/`db:migrate`/`db:introspect` and the manual dev-server D101 verification steps could not run in this container (no `DATABASE_URL`, no Neon credentials, per the established D193 precedent — flagged rather than claimed); design spec and implementation plan registered in File Inventory below; prior 1.7.41 — 2026-08-14 — Training order mode: Singles/Doubles Training gain an identical editable Low→High/High→Low/Random `order_mode` setting, resolved once client-side (new `lib/game/target-order.ts`) into a 21-value `target_order` array copied into the immutable config snapshot (D210, `decisions/game-engine.md`) so undo/resume never reshuffles. `SinglesConfig`/`DoublesTrainingConfig` widened with `target_order` (`.superRefine` permutation check, inlined per-schema rather than on a shared sub-schema — required by `scripts/check-type-barrels.sh` and `scripts/check-refinement-coverage.sh`'s nearest-preceding-export attribution, ~13 pre-existing test fixtures updated in lockstep); `board-progression.module.ts`'s `numbersPath`/`doublesPath` gain an optional explicit-order parameter (Bob's 27's zero-arg call sites unaffected); both engines' completion checks move from BULL-kind to index-based (target index 20, the 21st/last); `applyDoublesTrainingDart` gains a required `config` parameter and `DoublesTrainingEngine` now retains its config field (previously discarded, D144-era gap closed); `runPlayAgain` (`play-lifecycle.ts`) gains an optional `buildOverrides` hook (`PlayAgainOverrides<TConfig>`) so Play Again mints a fresh shuffle every replay under Random, byte-for-byte unchanged for every other caller that omits it; setup UI reuses the `Toggle` primitive (D175-era pattern); seed presets (`0002`/`0003`) gain `target_order`, Doubles Training's row switched `DO NOTHING`→`DO UPDATE` (split into its own INSERT so Bob's 27's row keeps `DO NOTHING`); Doubles Training's non-canonical doc corrected — it previously said High→Low "ends on bull," this ships it leading with bull instead, matching Singles Training's convention. `TargetOrderMode` ended up declared in `lib/game/types.ts`, not `target-order.ts` as the implementation plan's Task 1 first drafted it — `scripts/check-type-barrels.sh` requires an exported type to live in its folder's `types.ts` barrel, caught while executing the plan. Full suite 105 files/2042 tests, 0 regressions; design spec and implementation plan registered in File Inventory below; prior 1.7.40 — 2026-08-13 — D209 lifecycle dedup, owner-requested after this PR's CI "Stale-usage gate" (`npx fallow`) failed: the branch's duplication percentage (13.2%) had crossed the threshold that gates its exit code, up from `main`'s own 11.4% baseline, traced to the `doubles-training-play.data.ts`/`singles-training-play.data.ts` lifecycle overlap D208 had deferred. New `lib/game/play-lifecycle.ts` (`playInit`/`playRetryReconciliation`/`playCommitDart`/`playUndoVisit`/`playUploadAndCompleteSession`/`playBack`/`playAbandonAndExit`/`runPlayAgain`), generic over `<TConfig, TEngine extends GameEngine<DartObservation, unknown>, TResults>` via the new `PlayLifecycleContext` type in `lib/game/types.ts` (type-raised per D103/D115/D156 — an inline declaration was caught by `scripts/check-type-barrels.sh` and moved); each ruleset file keeps only its `resumeEngine`, `resultsSnapshot`-builder, and `instanceof`-narrowing callbacks. Bob's 27/501/Score Training stay out (different `VISUAL_BOARD`/`ScoreInputBuffer` shapes). Behavior-preserving: both consumer test suites pass unmodified, plus 18 new direct tests for the shared module against a ruleset-agnostic fake engine; full suite 104 files/2009 tests. Also fixed two unrelated, pre-existing `fallow` dead-code false positives (`ConfigurationPresetData`/`SessionActiveData` duplicate-export findings against the sanctioned two-barrel type-raising pattern) via `.fallowrc.jsonc`'s `ignoreExports` — fallow's own documented mechanism for an intentional namespace-barrel API, after an inline `fallow-ignore-next-line` comment was tried and found ineffective for this finding type. `npx fallow` now exits 0 (10.9% duplication, under `main`'s 11.4% baseline; 0 dead-code issues). decisions/frontend/architecture.md D209 records the scope reversal over D208's "wait for a fourth sample" deferral; `play-lifecycle.ts` registered in File Inventory below; prior 1.7.39 — 2026-08-13 — Task 8 dedup, owner-requested after the branch's final whole-branch review found byte-identical duplication: new `app/src/lib/game/doubles-path-play.ts` (`doublesPathTargetLabel`, `doublesPathPreviewSegments`, `doublesPathObservation` — the BULL/DOUBLE hit-vs-miss `DartObservation` builder for the shared numeric doubles path both Bob's 27 and Doubles Training walk) extracted out of `bobs27-play.data.ts`/`doubles-training-play.data.ts`'s three near-duplicate helpers (`targetLabel`/`previewSegmentsFor`/the inline `recordTap` observation ternary), each file's own copies deleted and their three call sites repointed at the shared module; byte-identical `Bobs27RecreationalInput.astro`/`DoublesTrainingRecreationalInput.astro` merged into one `DoublesPathRecreationalInput.astro` (`git mv` + delete), both interfaces' imports updated. Behavior-preserving refactor: both play-data test suites pass unmodified (53 tests combined) and the new module's own 8 pass (61 total across all three); full suite 103 files/1991 tests, 0 regressions. `singles-training-play.data.ts` untouched (different target path, out of scope, per the task brief). No schema change; scope fixed by the owner at review time, decisions/frontend/architecture.md D208 records the fallow dupe-count baseline (38 clone groups pre-existing on main, 42 after — the delta is the doubles-training-play.data.ts/singles-training-play.data.ts lifecycle overlap left deliberately out of this dedup's scope, per D182's own "revisit when a third game lands" clause) and the deferred remainder; `doubles-path-play.ts` registered in File Inventory below; prior 1.7.38 — 2026-08-13 — Doubles Training v1 frontend shipped on top of its already-complete engine/validator/capability/seed layer (frozen, no engine/validator/capability/schema change): games-index card (`DOUBLES_TRAINING_V1`, RECREATIONAL-only, no `ANALYTICS` pair, same precedent as `SINGLES_V1`); setup page (`doubles-training-setup.data.ts`/`DoublesTrainingSetupForm.astro`, zero editable settings, mirrors Singles Training's shape); play page (`doubles-training-play.data.ts`, registered in File Inventory below) — tap input (Undo/Miss/target-labelled hit button, no board-input branch since this ruleset never enters `VISUAL_BOARD`), 3-dart visit preview (`previewSegments()`), live Hits/Misses totals read directly off `DoublesTrainingEngine.state().outcomes` rather than re-derived from dart facts (so a full-miss visit counts as exactly one miss, not three); results modal (`DoublesTrainingResults.astro`, "Session complete" heading, no win/loss framing, same precedent as `SinglesTrainingResults.astro`). Final whole-branch review found no Critical issues and two Important findings — both about duplication the plan's own mirror-sibling-files instruction produced (the recreational input component byte-identical to Bob's 27's, and ~30 lines of play-data logic encoding the doubles-path hit-zone invariant) — which the owner asked to fix in-branch rather than defer; resolved by the dedup documented above (1.7.39); `doubles-training-setup.data.ts`/`doubles-training-play.data.ts` registered in File Inventory below; prior 1.7.37 — 2026-08-13 — Singles Training play-screen UI refinement, owner-requested over the just-shipped frontend: `SinglesRecreationalInput.astro`'s tap row restyled from one divided row into two stacked rows (`divide-y`) — row 1 keeps the number-target S/D/T buttons but each now shows the current target in its label (`'S' + currentTargetLabel()` etc., safe because that branch only renders when `!isBullVisit()`, where the label is provably always a plain number string) or, on the BULL visit, the unchanged `Bull`/`Bullseye` pair with no third slot; row 2 is now always `Undo`/`Miss` as two equal-width buttons (previously grouped with S/D/T in one row). `singles-training-play.data.ts` gains four session-total getters — `missCount()`/`singleCount()`/`doubleCount()`/`trebleCount()` — each classifying every dart's `hitZoneKey` across all turns (not just the current visit), exhaustive over `DartZoneKey`'s 8 members: `MISS`→miss; `SINGLE`/`INNER_SINGLE`/`OUTER_SINGLE`/`OUTER_BULL`→single; `DOUBLE`/`INNER_BULL`→double; `TREBLE`→treble — the bull fold-in deliberately mirrors `trainingPointsFor`'s existing scoring equivalence rather than introducing a second classification. `SinglesTraining.astro`'s stat `<dl>` gains four `StatRow`s (Misses/Singles/Doubles/Trebles) directly under Target, reading the new getters. All three task reviews came back clean (no Critical/Important findings); Minor, non-blocking notes only (a small `SINGLE_COUNT_ZONE_KEYS`/`SINGLE_ZONE_KEYS` duplication, four independent full-session scans instead of one combined pass — inconsequential at realistic dart counts). No schema/decision-ledger change — mechanical UI refinement of the already-decided `SINGLES_V1` capability layer, per the design spec and plan's own Global Constraints; design spec and implementation plan registered in File Inventory below; prior 1.7.36 — 2026-08-13 — Singles Training frontend shipped on top of its already-complete engine/validator/capability layer: games-index card (`SINGLES_V1`, RECREATIONAL-only, no `ANALYTICS` pair — `games-visibility.test.ts`'s previously-universal analytics-visibility assumption corrected to exclude it, the first carded-ruleset exception); setup page (`singles-training-setup.data.ts`/`SinglesTrainingSetupForm.astro`, zero editable settings, mirrors Bob's 27's shape); play page (`singles-training-play.data.ts`, registered in File Inventory below) — target-aware recreational input (`SinglesRecreationalInput.astro`: S/D/T/Miss on number targets, Bull/Bullseye/Miss on the BULL visit, one shared `recordTap(ring)`), a locally-duplicated `trainingPointsFor` (the engine doesn't export its own — a documented, accepted seam across the module boundary), and a total-points-only results modal (`SinglesTrainingResults.astro`, static "Session complete" heading, no darts-thrown/win-loss); `Bobs27VisitPreview.astro` generalized in place to `VisitPreview.astro` (`git mv`, zero content change) and reused by both games rather than duplicated. Final whole-branch review found no Critical/Important defects; two Minor robustness gaps fixed post-review in `singles-training-play.data.ts` — `previewSegments()` could throw on a null engine (e.g. a stale foreign session mid-reconciliation) instead of returning placeholders, and `recordTap("TREBLE")` on the BULL visit silently recorded `INNER_BULL` instead of being rejected. One test-brief defect caught during implementation and fixed with controller approval: the `undoVisit` test's literal assertion (`turns[0].darts` empty) contradicted the already-shipped engine's real `undo()` (which pops an empty turn rather than leaving one) — corrected to match, per the precedent already established in `five-oh-one-play.data.test.ts`. No schema/decision-ledger change — mechanical frontend extension of D196/D198/D195/D206-adjacent patterns, per the design spec and plan's own Global Constraints; design spec and implementation plan registered in File Inventory below; prior 1.7.35 — 2026-08-12 — Bob's 27 owner-reported bug triage, three issues: (1) "Could not start the session" starting an analytics-mode Bob's 27 game diagnosed as not a code defect — `resolveSessionModePair`/`supportsMode`/`bobs27Validator` all correctly admit `ANALYTICS`+`VISUAL_BOARD`, and seed `0007` already declares the `('BOBS27_V1','ANALYTICS','VISUAL_BOARD')` row with `ON CONFLICT DO NOTHING` — but migration `0020`'s `fk_sessions_capability` rejects the session insert until that seed row actually lands in the real Neon database, which per the existing operator handoff (`docs/superpowers/handoffs/2026-08-12-bobs27-visual-board-capability-checklist.md`, unchanged) still needs `npm run db:seed` run against it; no code change — root cause traced and confirmed via the FK dependency chain, not reproduced against a live database (none in this container, D193). (2) `Bobs27VisitPreview.astro`'s miss-segment icon was `delete.svg` (a delete-key/backspace glyph, also `ScoreInput.astro`'s digit-delete icon) instead of a plain miss cross — new `app/src/icons/cross.svg` added and swapped in; `delete.svg` itself untouched, still correct for `ScoreInput.astro`. (3) `Bobs27.astro`'s `SinglePlayerDisplay` had the hero number and its progress-slot `StatRow` inverted — the hero (`isTarget`) showed `currentTargetLabel()` labelled "Target" and the `StatRow` showed `currentScore()` labelled "Score"; flipped so the hero now shows `currentScore()` (`isTarget={false}`, labelled "Score") and the `StatRow` shows `currentTargetLabel()` labelled "Target", matching the reporter's expectation that the prominent number is the running score and "Target" names the actual target. No schema/decision-ledger change — pure display/asset corrections over existing Phase 4 code, no new pattern; `cross.svg` is a leaf icon asset, not separately registered in File Inventory (icons aren't inventoried individually, per the `check.svg`/`delete.svg` precedent); prior 1.7.34 — 2026-08-12 — Bob's 27 Phase 4 (final phase): play page. New `app/src/lib/game/bobs27-play.data.ts` (`bobs27Play()`, registered `Alpine.data("bobs27Play", ...)`) mirrors `five-oh-one-play.data.ts`'s game-loop shape minus all checkout/bust confirm-dialog machinery — Bob's 27 has no bust/double ambiguity, so `recordTap(hit)` (recreational) and `recordDart(observation)` (board) both funnel into one `commitDart` that records → mirrors → checks `isComplete()` → uploads; new `Bobs27VisitPreview.astro` (3 hit/miss/placeholder segments + "D1 D2 D3" caption, reading a new `previewSegments()` method), `Bobs27RecreationalInput.astro` (Undo/MIS/current-target tap row), `Bobs27.astro` (assembles `SinglePlayerDisplay` + the preview + both mode-gated inputs, `BoardInputPanel` reused byte-for-byte unchanged), `Bobs27Results.astro` (Won/Lost off `Bobs27State.status` — no literal `result()` method exists on `Bobs27Engine`, so `state().status` captured into `resultsSnapshot` at completion time is the source of truth); `pages/games/bobs27/play/index.astro` replaces Phase 3's static `AppLayout` placeholder with the real `GameLayout` shell (fixing that placeholder's dead Exit button in the process — `@confirm-exit.window="abandonAndExit()"` now wired). The board's 1.5s reveal-then-clear (hide a resolved visit's markers/preview 1.5s after its 3rd dart, only under `VISUAL_BOARD`) lives entirely in `commitDart`/`undoVisit`, overriding `boardInputData`'s own `visitMarkers()` after spreading it (object-literal key order). Final whole-branch review found and fixed one Critical bug the plan's own Task 1 code had introduced: `previewSegmentsFor` marked *any* on-board dart lacking `hitZoneKey === "MISS"` as a hit, so an off-target board dart (any zone besides the exact double/bull) rendered a false green checkmark — fixed by comparing the dart's actual `hitTargetNumber`/`hitZoneKey` against its own `intendedTargetNumber`/`intendedZoneKey` (what `Bobs27Engine.record()` already stamps from the target thrown at), the same equivalence `isHitOn` uses. Same review found resuming an already-terminal engine (e.g. a completion upload that saved the fact log but failed to mark the session COMPLETED) left `finished = false` with a live, throwing tap row — `init()` now checks `engine.isComplete()` and routes into the same completion path `commitDart` uses; `commitDart`'s `engine.record()` call also gained a defensive try/catch mirroring `five-oh-one-play.data.ts`'s established pattern. Same review flagged that the spec's Task 4 wording named only a target display, not a score display, for the play screen (`SinglePlayerDisplay`, target = current double/BULL) — surfaced to the product owner rather than silently added or silently omitted; decision: add it now, via a new `currentScore()` method + `SinglePlayerDisplay`'s existing `progress` slot (the same pattern `FiveOhOne.astro` already uses), not deferred. A Minor timer-leak (`commitDart` could overwrite `hiddenTimer` without clearing a still-pending one when two visits resolve within 1.5s) was fixed with a `clearTimeout` guard; its own regression test initially failed to actually guard the fix (both the leaked and replacement timer converged on the same end assertion) and was tightened to assert the intermediate state. No schema/decision-ledger change — mechanical extension of D198 (shape-dispatch)/D201 (marker lingering), per the design spec's cross-phase notes; this is the fourth and final phase, so the whole `docs/superpowers/specs/2026-08-12-bobs27-frontend-design.md` rollout is now complete. Phase 3's plan row updated (this branch, complete) → (merged); this phase's plan doc registered in File Inventory; prior 1.7.33 — 2026-08-12 — Bob's 27 Phase 3: setup page + routing. `games-visibility.ts` gains a `BOBS27_V1` card (`href: "/games/bobs27/setup"`); new `bobs27-setup.data.ts` (`Bobs27SetupContext` in `types.ts`, registered `Alpine.data("bobs27Setup", ...)` in `register-route-data.ts`) mirrors `five-oh-one-setup.data.ts` minus score/legs — V1 has zero editable settings, so `start()` sends `config: { source: "template", templateRef }` with no `overrides` key at all, and picks the ruleset's single seeded preset at `presets[0]`; new `Bobs27SetupForm.astro` (`SetupShell` + `UserSection` + `InfoSection` only, no settings block) and `pages/games/bobs27/setup/index.astro` (mirrors the *real* `pages/games/501/setup/index.astro`, which inlines its reconciliation-failed markup rather than using the `ReconciliationBlocked` component the design spec's prose names — 501's play page is the only real consumer of that component); new `pages/games/bobs27/play/index.astro`, a deliberate static placeholder (`AppLayout`, no `x-data`, no gameplay) so the setup flow has a non-404 landing route ahead of Phase 4. Final whole-branch review caught a real spec-acceptance gap the plan itself introduced: `visibleGames` filtered on the *exact* capture+input mode pair via `supportsMode`, but the app's mode picker (`AppModeForm.astro`) only ever sets `RECREATIONAL`+`QUICK_SCORE` or `ANALYTICS`+`VISUAL_BOARD` — never `BOBS27_V1`'s `RECREATIONAL`+`DETAILED_DARTS` — so its card never rendered for a recreational-mode player even though `resolveSessionModePair`'s own-first-pair fallback (Phase 2) already started such a session correctly; fixed with a new `supportsCaptureMode(rulesetVersionKey, captureModeKey)` in `capabilities.ts` (additive, `supportsMode`/`capableRulesets` untouched) and `visibleGames`/`games-index.data.ts`'s `isVisible` re-keyed to capture mode alone, which as a side effect makes every carded ruleset visible under both real app modes and let `games-visibility.test.ts`'s "declared card order" test go back to deriving its expectation from `GAME_CARDS` directly; `games-index.data.test.ts` and `capabilities.test.ts` updated in step (new `supportsCaptureMode` describe block) — exact-pair enforcement for session creation itself is untouched (`supportsMode` in `session.service.ts`). Same review also had the Task 4 play placeholder swap `GameLayout` for `AppLayout`: `GameLayout`'s Exit modal dispatches a `confirm-exit` window event only the real play pages listen for, so on the placeholder route "Leave" did nothing and the modal's abandon copy was false (nothing to abandon yet) — `AppLayout` has no exit affordance, matching the empty page. No schema/decision-ledger change — mechanical extension of D196/D197/D198/D201, per the design spec's cross-phase notes; the capture-mode fix is a bug fix to this phase's own plan, not a new architectural pattern. Deferred, not fixed: `bobs27-setup.data.ts` is a third near-verbatim copy of the reconciliation/session-creation scaffolding already duplicated between `five-oh-one-setup.data.ts` and `score-training-setup.data.ts` — a shared-factory extraction was flagged but correctly kept out of this branch (touches two other games' setup modules); this phase's plan registered in File Inventory; `docs/superpowers/specs/2026-08-12-bobs27-frontend-design.md`'s Phase 3 section is the authority for what shipped; prior 1.7.32 — 2026-08-12 — Bob's 27 Phase 2: `capabilities.ts` declares `BOBS27_V1: [DETAILED_DARTS, VISUAL_BOARD]` (was `DETAILED_DARTS` only); seed `0007` gains the mirrored `('BOBS27_V1', 'ANALYTICS', 'VISUAL_BOARD')` row (8→9 triples) and its verification SQL (`database/verification/0007_capability_seed_checks.sql`) updated in lockstep, unexecuted in this container (no `DATABASE_URL`, D193) — operator handoff `docs/superpowers/handoffs/2026-08-12-bobs27-visual-board-capability-checklist.md` added; `bobs27.validator.ts` now accepts and routes both mode pairs (`isDetailedDartsOrVisualBoardCapture` mirroring `isQuickScoreOrVisualBoardCapture`'s shape), and — after the whole-branch review found the shared `validateVisualBoardTurns` delegation let a dartless (0-dart) turn through, a shape no legitimate Bob's 27 client can produce in either capture mode since every visit is exactly 3 darts hit-or-miss — the dart-presence check was moved ahead of the mode dispatch so it applies under `VISUAL_BOARD` too, at the owner's explicit direction; `Bobs27Engine.record()` bugfix: dart facts now carry the observation's real `locationX`/`locationY` instead of always `null`; `resolveSessionModePair`'s fallback generalized from a hardcoded `RECREATIONAL + QUICK_SCORE` constant to `RULESET_CAPABILITIES[rulesetVersionKey][0]`, since `BOBS27_V1` never declared `QUICK_SCORE` and the old fallback would have handed a Bob's 27 setup page a pair its own engine can't play; no schema/decision-ledger change — mechanical extension of D196/D197/D198, per the design spec's cross-phase notes; design spec and both phase plan docs (previously unregistered — a Phase 1 gap backfilled here) plus this phase's plan and handoff registered in File Inventory; `docs/superpowers/specs/2026-08-12-bobs27-frontend-design.md`'s Phase 2 section is the authority for what shipped; prior 1.7.31 — 2026-08-12 — Bob's 27 Phase 1 scoring correction: `pointValueOf` in `bobs27.engine.module.ts` now adds the hit double's board value (2× its number, e.g. D16 = +32) instead of face value, matching how `darts.score` (D142) already records the dart; `bobs27.engine.module.test.ts`'s score-delta assertions rewritten to match; `docs/game-rules/rulesets/bobs-27.md`'s Scoring section, worked examples and Open Questions note corrected in lockstep; D207 (`decisions/game-engine.md`, supersedes nothing — no prior decision had recorded the face-value formula) appended; `decisions/game-engine.md`'s File Inventory row corrected 29→31 decisions (recount, was already stale before this task) and `DECISIONS.md`'s counts/ID-gap note bumped 186→187, highest `D206`→`D207`; no schema/API/persisted-value change — `Bobs27State.score` is derived and never persisted (D139); prior 1.7.30 — 2026-08-11 — 501 extended setup config: starting score is now editable at setup — a `Toggle` picker (301/501/701/Custom) plus a conditional custom `Input` (2–999, default 101) alongside the existing firstTo-legs `Input`, both fields with their own clamp-notice `<p role="status">`; new `app/src/lib/game/five-oh-one-starting-score.ts` (`clampFiveOhOneStartingScore`, mirroring `five-oh-one-legs.ts`'s clamp shape) and `five-oh-one-setup.data.ts`'s state widened (`startingScoreOption`, `startingScoreValue`, `scoreClampNotice`; existing `clampNotice` renamed `legsClampNotice`) and its `start()` override payload now carries `starting_score` alongside `legs_to_win` — no API/schema/seed change, `FiveOhOneConfig`'s existing `.min(2)` floor and the generic override-merge path already cover it; `FiveOhOneSetupForm.astro` moved `components/layout/games/` → `components/layout/games/setup/` and rebuilt on Score Training's shared setup components (`SetupShell`, `UserSection`, `InfoSection`, `SettingSectionShell`, `Toggle`, `Input`), dropping the old locked-settings `<dl>` for `InfoSection`'s description, and its page caller (`pages/games/501/setup/index.astro`) drops the `title`/`description` props it used to pass; `components/layout/games/interfaces/FiveOhOne.astro`'s three play-page stat tiles reordered and relabeled (3 dart avg → Previous score → Darts), a pure markup change over the existing `average()`/`previousScore()`/`dartsThrownThisLeg()` reads; `docs/game-rules/rulesets/501.md` brought in sync in four places — Features table's "Alternate start scores (301, 701, …)" row TBD→v1, the Identity paragraph naming the custom 2–999 value alongside 301/701, the Config & presets table's Start score row flipped from "Shown, locked" to "Editable" with the 301/501/701/custom range, and the Later-versions Variants bullet for other X01 start scores deleted (it now ships in V1, contradicting the flipped Features row otherwise); no `decisions/**` entry added — reusing Score Training's setup components for a second setup form applies `app/CLAUDE.md`'s existing "reuse existing UI components before hand-rolling markup" rule and continues the un-decisioned `InfoSection.astro` move/reuse precedent one commit prior, not a new pattern; `five-oh-one-starting-score.ts` and the moved `FiveOhOneSetupForm.astro` registered in File Inventory; prior 1.7.29 — two owner-requested profile-page follow-ups: `AppModeForm.astro`'s inline `<h2>`+`<p>` heading/explanation replaced with `components/ui/InfoSection.astro`, mirroring `HandednessForm.astro`'s #84 pattern; `HandednessForm.astro`'s RIGHT/LEFT option labels gain `text-sm` so the label text never wraps to a second line inside the `flex-1` option card at narrow widths; prior 1.7.28 — 2026-08-11 — two owner-reported follow-up issues fixed: `board-input.module.ts`'s magnifier placement no longer re-decides which side of the pointer it sits on during a drag — `resolveMagnifierSide` now runs once, on press, and `clampMagnifierPlacement` (new, only repositions along the current side) runs on every `move`, so the magnifier can no longer visibly jump to the opposite side mid-gesture even though it still slides to stay on-screen (#83, `MagnifierSide` type raised to `types.ts` per the type-barrel gate); `HandednessForm.astro`'s RIGHT/LEFT options now render on one line (radiogroup wrapper `flex gap-2`, each option `flex-1`) and its title+explanation moved into `components/ui/InfoSection.astro` (moved there from `components/layout/games/setup/`, generalized to render `title` verbatim instead of baking in a " rules" suffix, with a new optional `id` prop so `HandednessForm`'s `aria-labelledby` still resolves) — `ScoreTrainingSetupForm.astro`, `InfoSection`'s other caller, updated its import path and passes `"Scoring Training rules"` explicitly to keep its rendered heading unchanged (#84); prior 1.7.27 — 2026-08-11 — PR review fix: `BoardInputPanel.astro`'s Undo/Bounce-out `<button>`s replaced with `components/forms/Button.astro` (`variant="secondary" icon` / `variant="ghost"` + `glass`), the reusable primitive this should have used from the start; `app/CLAUDE.md`/`AGENT.md`'s Style non-negotiables gain an explicit "reuse existing UI components before hand-rolling markup" rule, exempting the pre-existing `role="radio"` option-button pattern (`AppModeForm.astro`, `HandednessForm.astro`) a shared `Button` cannot express; prior 1.7.26 — four owner-reported board-UI issues fixed: `board-geometry.module.ts`'s `trebleInner` corrected 97→99mm so the treble ring is 8mm wide like the double ring (#79 — the double ring already matched the 162/170 regulation width; the outer-single/inner-single gap the issue also flagged is real-board-accurate and deliberately left alone), with `dartboard.svg`/`DartBoard.astro`'s 20×2 affected segment paths regenerated in lockstep and `board-geometry.module.test.ts`/`miss-margin.module.test.ts`'s hardcoded treble-centroid coordinates shifted to match; `BoardMagnifier.astro`'s resolved-read label gains a `glass` background for legibility (#81); `BoardInputPanel.astro`'s Undo/Bounce-out buttons reordered (Undo left, Bounce out right), sized equally (`flex-1` each) and Bounce out gains `glass` (#80); a new `boardInput` Alpine store (`stores/board-input.store.ts`, one `$persist` `handedness` field) is wired into `board-input.data.ts`'s `freshController`, replacing the `boardInput()` call's implicit `"RIGHT"` default so the magnifier opens on the player's actual throwing-hand side instead of only auto-flipping at the viewport edge, with a new profile-page `HandednessForm.astro` writing the store (#78, D206 — local `$persist` chosen over `player_settings` since this is a per-device rendering preference, not gameplay data); 501's `averageThisLeg`/`previousScoreThisLeg` renamed `average`/`previousScore` and re-scoped to read `$store.game.turns` (the whole match) instead of `turnsInCurrentLeg()`, fixing both stats resetting to zero/`—` at every leg boundary — `dartsThrownThisLeg` stays leg-scoped, which was already correct (#68); prior 1.7.25 — 2026-08-10 — owner-requested visual polish on the merged board UI: `BoardMagnifier.astro`'s resolved read now prints above the circle at `text-xs` (was below, `text-sm`) via a new pure `magnifierLabelStyle()` mirroring `magnifierAnchorStyle`/`magnifierBoardStyle` (D204, supersedes D199's layout clause); `--dartboard-*` tokens in `global.css` recoloured from the sky-blue theme to a real board's black/cream single beds and red/green multiplier rings, and `.dartboard-group .segment text` fixed to read the already-defined `--dartboard-label` token instead of `--accent` (D205, `decisions/frontend/style.md`); both verified by rendering the actual compiled component in a headless browser — the label's measured bounding box sits above the circle's, and each zone's computed `fill` matches the intended hue; prior 1.7.24 — 2026-08-10 — the engine's dead `inputMode` field is removed outright (D203, supersedes D198): the third parameter is gone from both engine constructors, `GameEngineFactory.create` and both factories, along with `engineInputMode()` and the `EngineInputMode` type; removing only the constructor parameter is what broke the build with `ts(2554)`, and `astro check` now reports 0 errors/0 warnings/0 hints where it had carried 2 permanent unused-field warnings; four tests whose sole assertion was which mode reached `factory.create` were DELETED rather than re-pointed (D148), which drops the last executable guard on D200 at that seam — D200 now lives only in `BoardInputPanel.astro`'s gate, which nothing unit-tests (D101); 35 engine tests kept their bodies with just the vanished argument dropped, and two `undo()` describe titles were re-pointed at the fact-log-shape guarantee they always tested; prior 1.7.23 — 2026-08-09 — visual board UI defect fixes from the owner's first play-through: the magnifier froze at the press position because `BoardMagnifier.astro` bound at the pure controller, whose closure-held state Alpine cannot observe — `boardInputData()` now publishes a reactive `board: BoardView` mirror re-synced after every controller call (D202); landed darts are drawn as markers via a new `DartBoard.astro` slot, showing the most recent turn's located darts so a finished visit's grouping survives until the next visit's first dart (D201); the keypad is hidden while a visual session is played and `BoardInputPanel.astro` carries its own `Undo dart`, reversing D199's keypad clause at the owner's direction; `07-Frontend/00-Overview.md` and `07-Style-Guide.md` updated with the mirror rule and the `x-for`-cannot-live-inside-`<svg>` hazard; `ScoreInput.astro` gains `{...props}` forwarding so the gate can be applied at the call site; browser-verified by driving the real Alpine build with a pointer drag — the owner's end-to-end play-through and the `v_dart_locations` query remain outstanding, no database in this container; prior 1.7.22 — 2026-08-09 — visual board UI plan 3 shipped: press-drag-release capture with a live magnifier wired into both the 501 and Score Training play pages; `board-input.module.ts` (transform/placement/state machine), `board-input.data.ts` (DOM bridge), `session-mode-resolution.ts`, `DartBoard.astro`, `BoardMagnifier.astro` and the shared `BoardInputPanel.astro` registered in File Inventory; `07-Frontend/00-Overview.md` gains a Visual Board Input section and `07-Style-Guide.md` gains pointer-tracked-surface rules (`touch-none`, `fixed` overlays, magnifier placement, the object-vs-string `:style` hazard) plus two accessibility rules; D198 (engines dispatch on input shape, not the stored mode — supersedes D189, `decisions/game-engine.md`) and D199 (press-drag-release with a magnifier over tapping, `decisions/frontend/astro.md`) appended; three defects found and fixed during execution that no unit test caught — a keypad total silently corrupting a dart row via `classify(undefined, undefined)`, `undo()` left keyed on the stored mode after `record()` moved to shape dispatch, and Alpine's string-form `:style` wiping the magnifier's `--mag-zoom`; the live play-through and `v_dart_locations` query remain owner-side, no database in this container; prior 1.7.21 — 2026-08-08 — visual board capability & settings plan 2 documented: migration chain bumped to `0001`–`0021` and seeds to `0001`–`0007` across root `CLAUDE.md`/`AGENT.md`, `database/CLAUDE.md`/`AGENT.md`/`README.md`, `docs/CLAUDE.md`/`AGENT.md`, `06-Database-Specification.md`, `10-Database-Agent-Guide.md`, `11-Neon-Integration.md`, `docs/architecture/README.md` and this file, closing the open `scripts/check-context-map.sh` gate failure recorded against `0018`; `03-Migrations.md` gains `0019`/`0020`/`0021` sections incl. the three-stage migrate→seed→migrate apply order; `06-Spec/01-Reference-Layer.md` gains `ruleset_version_capabilities`; `06-Spec/03-Player-Layer.md`'s `player_settings` moves from deferred to shipped and its two mode FKs are corrected to migration `0017`; `v_player_settings` registered in `06-Spec/05-Read-Model-Layer.md` and `05-Views.md`; `GET`/`PATCH /api/players/me/settings` contracted in `04-Endpoint-Contracts.md` →1.3.0 and routed in `00-Overview.md` →1.6.0; D195 (settings endpoints ship, supersedes D60's deferral clause, `decisions/api.md`), D196 (capability declared twice — cross-runtime constant + seeded table + composite FK — and keyed on ruleset version, `decisions/architecture.md`) and D197 (`validateConfig` must admit every mode pair its `validateBatch` handles, `decisions/game-engine.md`) appended; `player_settings endpoints` dropped from the Deferred list; capability/settings/games-filter code and the new migrations, seed and verification scripts registered in File Inventory — no real database in this container, so `0019`/`0020`/`0021` and seed `0007` ship unapplied and `app/src/db/schema.ts`'s `vPlayerSettings` block is hand-written rather than introspected, awaiting owner ratification; prior 1.7.20 — `scripts/check-context-map.sh` pins a UTF-8 locale for its migration-range check, which had been silently matching nothing under this container's POSIX locale and so reporting green while six ranges were stale; the check now also skips `decisions/**` (frozen by `scripts/check-decision-ids.sh`) and seed ranges; `0001`–`0018` corrected in root `CLAUDE.md`/`AGENT.md`, `06-Database-Specification.md`, `10-Database-Agent-Guide.md`, `11-Neon-Integration.md`; D194 (`decisions/context-system.md`) records it; prior 1.7.19 — visual board capture core plan 1 is complete: `npm run db:verify` returns `ALL 11 CHECKS PASSED` against the Neon dev branch, matching the local PostgreSQL 16 results exactly, so no operator-checklist item remains open; prior 1.7.18 — operator-checklist steps 3–5 turned into a committed, rollback-safe SQL script, `database/verification/0018_visual_board_checks.sql`, run by `npm run db:verify` (`app/scripts/verify-db.ts`) so the checks need no local `psql` — this project has no local PostgreSQL server (D24), so its client binaries are absent too; D193 (`decisions/database.md`) records why; the checklist now invokes it and records the operator's Neon migrate+seed run; prior 1.7.16 — visual board capture core verified against a real PostgreSQL 16 cluster: migration `0018`'s angle expression could never have applied (`MOD()` has no `double precision` overload), fixed in place with a `::NUMERIC` cast and guarded by `app/tests/db/migration-numeric-typing.test.ts`; `drizzle-kit introspect` run for real, confirming the hand-written `locationX`/`locationY`/`chk_dart_location_pair` and registering `v_dart_locations` in `schema.ts`; operator checklist steps 1 and 3–5 verified and the handoff rewritten to say so; D192 (`decisions/database.md`) records the run; `06-Spec/05-Read-Model-Layer.md` gains the NUMERIC-arrives-as-string note; `app/db/` (dbmate's schema dump) git-ignored; prior 1.7.15 — visual board branch rebased onto main after PR #72 merged; the branch's draft `D187` (single is two stored bands) renumbered to `D191`, main having already issued `D187` for the Alpine closure rule — `DECISIONS.md` counts and ID-gap note updated to 171 decisions, highest `D191`; prior visual board capture core plan 1, Task 15: migration range bumped to `0001`–`0018` throughout (`06-Spec/04-Runtime-Layer.md`, `06-Spec/05-Read-Model-Layer.md`, `05-Views.md`, `03-Migrations.md`, this file, `database/README.md`, `10-Database-Agent-Guide.md`, `docs/architecture/README.md`, `database/CLAUDE.md`/`AGENT.md`) closing the open `scripts/check-context-map.sh` gate failure recorded against `0017`; `06-Spec/04-Runtime-Layer.md` gains the shipped `location_x`/`location_y` shape, the ANALYTICS+VISUAL_BOARD capture-depth pairing, the QUICK_SCORE-scoped 501 bust limitation retitle + VISUAL_BOARD retirement note, and the `total_score` bust carve-out; `v_dart_locations` registered in `06-Spec/05-Read-Model-Layer.md` and `05-Views.md`; `board-geometry.module.ts`, `miss-margin.module.ts`, `visual-board.validator.ts` and seeds `0005`/`0006` registered in File Inventory; D188 (dart coordinates shipped, `decisions/database.md`) and D189 (mode-scoped bust visibility, `decisions/game-engine.md`) appended; operator handoff `docs/superpowers/handoffs/2026-08-05-visual-board-operator-checklist.md` added and registered — no real database in this container, so migrations `0017`/`0018` and seeds `0005`/`0006` ship unapplied; prior 1.7.14 — 2026-08-07 — D187: Alpine closure rule for UI module instances — an Alpine factory driving a `modules/ui/*` class keeps the instance in the factory closure, never on `this.*`, since Alpine's deep-proxied reactive state throws when an ES private field is read through the Proxy (renumbered from a draft D185 during rebase onto main, which had already taken D185/D186 for the graph-CI-ownership decisions via PR #70); Setup Toggle integration design/plan registered; `03-Alpine-Patterns.md` →0.2.3; prior 1.7.13 — D186: `graph.yml`'s PR creation is contingent on the repo's "Allow GitHub Actions to create and approve pull requests" setting, off by default; the job now degrades to a pushed `chore/graph-refresh` branch plus a run summary instead of failing, measured on the first real post-merge run; `graph.yml` File Inventory row updated; prior 1.7.12 — graph freshness moved to CI: `.github/workflows/graph.yml` + `scripts/graph-delta.py` registered in File Inventory; `graphify-out/graph.json` row updated to state it is CI-maintained (still `generated`, still never hand-edited); D185 records the decision, and the corresponding local-refresh/completion-report language dropped from root+`app` `CLAUDE.md`/`AGENT.md`, the `context-maintenance` skill, the `validate-app` skill (which still told agents to record the warning and stage the graph — it is `app/CLAUDE.md`'s named sole validation procedure, so it would have produced a fifth staleness disclosure), `app/README.md`, and this file's Knowledge graph conventions row; prior 1.7.11 — final whole-branch review fixes for the decision-ledger split: D135/D136 re-filed database.md→context-system.md (counts 12/28, deploy/Prettier/format/husky added to its `load-when` + router row); PWA/manifest/icon/safe-area added to frontend/astro.md + frontend/style.md `load-when` + router rows; "Why was X decided?" pack-row regex-skip fixed (single `~2.5k` cell, real ~2.3k–5.8k range moved to prose); Authority Order now names `decisions/**` explicitly; `01-Rendering-Strategy.md`'s stale `DECISIONS.md` citation row repointed at the 3 domain files that actually hold D79/D80/D88/D97/D98/D172; decision-ledger migration tooling (split-decisions.sh, verify-decision-split.sh, decision-map.txt, decision-front-matter.txt) registered as spent/historical; `scripts/check-decision-ids.sh` gains row-hash and registration checks (`scripts/decision-row-hashes.tsv` added); prior 1.7.10 — `scripts/check-decision-ids.sh` registered under Cross-cutting mechanical guards: durable id-integrity gate for the split ledger, position-anchored against darts `D<n>` notation; prior 1.7.9 — decision ledger split registered: `DECISIONS.md` is now a router, its 163 decisions live in 10 `decisions/**` domain files; File Inventory's single `DECISIONS.md` row replaced with 11 rows (router + 10 domain files, each with its own `~Nk`); Context Packs "Why was X decided?" row repointed at the router + only the domain file(s) a task needs; prior 1.7.8 — 501 recreational v1 spec/plan + checkout-path module registered)

---

# Current Implementation State

> This table is maintained best-effort and can lag; the Version History
> section above is the authoritative current-state source when they
> disagree.

| Area | Status |
| ---- | ------ |
| Domain model v1.0 | Frozen |
| Migrations | `0001`–`0021` complete; `0015` time-semantics constraints, `0016` replay/overview rebuild + `v_configuration_presets` (2026-07-13); `0017` dart location columns (`chk_dart_location_pair`) + two `player_settings` FKs, `0018` `v_dart_locations` (2026-08-05); `0019` `ruleset_version_capabilities`, `0020` `fk_sessions_capability` (needs seed `0007` first — apply order is migrate→seed→migrate), `0021` `v_player_settings` (D195/D196, 2026-08-08). `0019`–`0021` and seed `0007` ship **unapplied** — no database in this container |
| Seeds | `0001` reference data, `0002` default templates (Singles/501 presets corrected to V1 scope; both TUOD presets verified against `TuodConfig` — unchanged), `0003` `BOBS27` + `DOUBLES_TRAINING` reference data (2026-07-26), `0004` Score Training minutes preset realigned to 5 (2026-07-31), `0005` `VISUAL_BOARD` input mode, `0006` `INNER_SINGLE`/`OUTER_SINGLE` dart zones (D191, 2026-08-05), `0007` the 9 ruleset-version capability triples (8 originally, D196, 2026-08-08; +1 `BOBS27_V1`/`ANALYTICS`/`VISUAL_BOARD`, 2026-08-12) |
| Game engines | All six (Score Training, Bob's 27, Singles Training, Doubles Training, 501, Ten Up One Down) on the `GameEngine` contract (D138–D141, D153): config-driven, fact-log-owning, rehydratable; six ruleset validators registered; guarded by `scripts/check-game-engines.sh` (which also asserts every registered engine declares a capability) + `scripts/check-refinement-coverage.sh`. Each ruleset version's supported mode pairs are declared once in `app/src/lib/game/rulesets/capabilities.ts` and mirrored by seed `0007`; 501, Score Training and (as of 2026-08-12) Bob's 27 accept `ANALYTICS + VISUAL_BOARD` at session creation as well as in batch validation (D196/D197, 2026-08-08); `Bobs27Engine.record()` now threads the observed dart's `locationX`/`locationY` onto the persisted fact instead of hardcoding `null`, required for board-tap coordinate capture (2026-08-12). Review fixes D149–D152: batch request schema mirrors the write path's column CHECKs, `state()`/`facts()` return derived copies, `completedAt` stamped at visit resolution, undo depth documented (2026-07-26) |
| Database spec | `06-Database-Specification.md` v2.2.0 — split into `06-Spec/` chapters (2026-07-11) |
| Database handbook | `00`–`11` complete |
| API docs | v1 frozen; contracts `00`–`04`; error boundary (D131) + `SESSION_ALREADY_ACTIVE` single-active guard (D132); `03`→1.7.0 type-vs-value barrel rule (D156, 2026-07-26); `00`→1.4.0, `02`→1.3.0, `03`→1.6.0, `04`→1.2.0 (2026-07-22); prior: `01` frozen at 1.0.0, `02`→1.2.0, `03`→1.5.0 (2026-07-16/17); hardening `00`→1.3.0, `04`→1.1.0 (2026-07-13); auth proxy `00`→1.5.0, `02`→1.4.0 (D172, 2026-07-29); player settings `00`→1.6.0, `04`→1.3.0 (D195, 2026-08-08) |
| Application code | Auth middleware with route-class 401/403 handling + API error boundary (D131); frozen envelope/error helpers; player provisioning (D76); `POST /api/sessions` server-guards single-active (D132); logout flow (`signOut`, `LogoutButton`) complete; Score Training first-deploy write/read subset live (S1); same-origin Neon Auth proxy (`/api/auth/*`) fixes iOS standalone-PWA login (D172, 2026-07-29); `GET`/`PATCH /api/players/me/settings` with the Alpine settings store and profile `AppModeForm`, `createSession` rejecting an undeclared mode pair, and the games page filtered by app mode with banner + empty state (D195/D196, 2026-08-08); Singles Training's frontend shipped on top of its already-complete engine layer — games-index card, setup page, and play page with target-aware recreational input and a total-points results modal (2026-08-13) |
| Frontend docs | `03-Alpine-Patterns.md` →0.2.3 — toggle in UI registration list + anti-pattern for OOP instances on Alpine data (D187, 2026-08-07); prior `07-Style-Guide.md` →0.2.2 — Tailwind v4 utility syntax section (suffix `!important`, arbitrary negatives) before Anti-patterns (D175, 2026-07-31); `10`→0.1.6 — same rule condensed into §12 Styling + checklist (2026-07-31); prior 0.2.1 — top safe-area inset noted alongside `h-dvh` (D174, 2026-07-29); prior 0.2.0 — sky/glass/`surface`/`foreground` visual contract; legacy `bg-bg`/`text-fg`/old surface-badge-nav API retired (2026-07-22); Handbook `02`→0.2.1, `03`→0.2.1, `04`→0.1.1, `10`→0.1.3, overview `00`→0.3.4 — shared `session-recovery.ts` decision table (D118) + Score Training hard-gate completion / play-page results modal (D119, supersedes D112 for this flow) (2026-07-17); prior: prerender-default, Alpine factory, client auth gate (D98), auto-cleanup recovery, completed-batch outbox + `_v` store guard, `.astro` authoring conventions; prerendered protected shells decided public-by-design, JWT-gated API is the real boundary (D97, 2026-07-15); tests live under `app/tests/` (never colocated), `.astro` variant logic stays inline in frontmatter (D101, 2026-07-15); type/interface barrel-raising universal, no `.ts` outside `lib/`/`pages/api/`, centralized error mapping, self-learning gate (D103–D107, 2026-07-16); original `07-Style-Guide.md` 0.1.0 (D108, 2026-07-16) |
| Knowledge graph | graphify AST-only `graphify-out/graph.json` committed; freshness is CI-owned via `.github/workflows/graph.yml` (D185, 2026-08-03); canonical refresh via `scripts/refresh-graph.sh` (`graphify update .`), optional locally; CLI + hooks documented in root/app `CLAUDE.md` (2026-07-15) |
| Drizzle schema (`app/src/db/schema.ts`) | **Known deviation, awaiting owner ratification (2026-08-08):** `app/src/db/CLAUDE.md` says the file is generated by `drizzle-kit introspect` and must never be hand-edited. The `vPlayerSettings` entry was hand-written because introspect needs a live database and there is none in this container. It was verified against migration `0021` column-for-column and follows the file's existing view conventions, so the content is believed right and the violation is procedural. It must be diffed against a real introspect run before merge — if it disagrees, the next real run silently rewrites it and the mismatch becomes a runtime error rather than a type error (the same situation `locationX`/`locationY` hit in plan 1, D192). `0019`/`0020` were likewise never introspected |
| DB connection contract | `DATABASE_URL` = pooled (tooling), `DATABASE_URL_UNPOOLED` = direct (Worker runtime); `DATABASE_URL_POOLED` retired — user-verified against real `neonctl link` output (D95, 2026-07-15) |

---

---

# Task Records

> One row per point-in-time spec, plan or handoff under `docs/superpowers/`.
> Provenance only — these describe what a past task decided, never what is
> currently true. Canonical outcomes live in the architecture docs and
> `decisions/**`.

| File | Answers | Status |
| ---- | ------- | ------ |
| `docs/superpowers/specs/2026-07-25-game-engine-review-design.md` | Game engine PR review: findings C1/C2, I1–I8, M1–M6, P1–P4, ST1–ST7 — closed except **P1** (branch carries no PR against `main` yet — pending this task's completion) and **P3** (knowledge graph stale; `graphify` CLI absent in this environment); other resolutions are canonical in Pattern 18, `07-Frontend/04-Modules-And-OOP.md`, `06-Spec/04-Runtime-Layer.md` and `DECISIONS.md` D138–D144 (2026-07-26) | historical |
| `docs/superpowers/plans/2026-07-25-game-engine-contract-hardening.md` | The 14-task plan closing the findings; P1 and P3 remain open as of Task 14 (2026-07-26) | historical |
| `docs/superpowers/specs/2026-07-29-ios-web-app-auth-design.md` | iOS Home Screen web app auth: same-origin `/api/auth/*` proxy design, cookie-rebinding rules, PWA manifest scope (D172, D173) (2026-07-29) | historical |
| `docs/superpowers/plans/2026-07-29-ios-web-app-auth.md` | The 9-task plan implementing that spec, incl. the Neon Auth trusted-origin prerequisite (2026-07-29) | historical |
| `docs/superpowers/handoffs/2026-07-29-ios-auth-pre-merge-checklist.md` | Operator checklist: manual Neon-console trusted-origin registration + iOS Safari verification, neither doable in-session (2026-07-29) | historical |
| `docs/superpowers/specs/2026-07-29-ios-safe-area-top-inset-design.md` | iOS standalone web app top safe-area inset fix: `body` pads `env(safe-area-inset-top)` alongside existing `h-dvh`/`overflow-hidden` (D174) (2026-07-29) | historical |
| `docs/superpowers/plans/2026-07-29-ios-safe-area-top-inset.md` | The 2-task plan implementing that spec, incl. the manual iOS verification handoff (2026-07-29) | historical |
| `docs/superpowers/handoffs/2026-07-29-ios-safe-area-verification-checklist.md` | Operator checklist: manual iOS-device verification of the safe-area fix, not doable in-session (2026-07-29) | historical |
| `docs/superpowers/specs/2026-07-31-tailwind-v4-utility-syntax-gate-design.md` | Tailwind v4 utility syntax gate design: ban prefix-important (`!utility`) and leading-dash arbitrary negatives (`-prop-[…]`) in favor of `utility!` / `prop-[-…]` (D175) (2026-07-31) | historical |
| `docs/superpowers/plans/2026-07-31-tailwind-v4-utility-syntax-gate.md` | The task plan implementing that spec: `scripts/check-style-tokens.sh` extension + Style Guide/agent-guide/mirror/context-map/decision updates (2026-07-31) | historical |
| `docs/superpowers/specs/2026-07-31-logo-lockup-svg-design.md` | Outlined logo lockup SVG design: Michroma outlines, generator script, home/login adoption (2026-07-31) | historical |
| `docs/superpowers/plans/2026-07-31-brand-lockup-and-app-icons.md` | The 5-task plan: dartboard favicon/PWA icons, BaseLayout links, logo-lockup generator + UI swap, context touch-up (2026-07-31) | historical |
| `docs/superpowers/specs/2026-08-01-501-recreational-v1-design.md` | 501 recreational v1 design: setup/play flow mirroring Score Training, double-out confirm gate, checkout-path lookup, leg-scoped progress stats (2026-08-01) | historical |
| `docs/superpowers/plans/2026-08-01-501-recreational-v1.md` | The 10-task plan implementing that spec: checkout-path module, shared-component reuse fixes, setup/play data factories, play UI, validation pass (2026-08-01) | historical |
| `docs/superpowers/specs/2026-08-07-setup-toggle-integration-design.md` | Setup Toggle integration design: `modules/ui` Toggle class, closure-held Alpine factory, `x-model`/`x-modelable` binding to `scoreTrainingSetup` (2026-08-07) | historical |
| `docs/superpowers/plans/2026-08-07-setup-toggle-integration.md` | The 4-task plan implementing that spec: Toggle module + types barrel, `lib/ui` Alpine factory, setup form/`$watch`/submit wiring, docs + verification (2026-08-07) | historical |
| `docs/superpowers/specs/2026-08-05-visual-board-input-design.md` | Visual board input design: tap-to-throw capture, shared `classify(x, y)` geometry, spatial facts goal, split into three plans (2026-08-05) | historical |
| `docs/superpowers/plans/2026-08-05-visual-board-capture-core.md` | Plan 1 (this branch, complete): board-geometry/miss-margin modules, migrations `0017`/`0018`, seeds `0005`/`0006`, `INNER_SINGLE`/`OUTER_SINGLE` bands (D191), both engines' visual paths, `v_dart_locations` (2026-08-05) | historical |
| `docs/superpowers/plans/2026-08-05-visual-board-capability-and-settings.md` | Plan 2 (this branch, complete): capability constant + table + seed + composite FK, registry/session-creation guards, `v_player_settings`, settings endpoints/client/store, profile `AppModeForm`, games-page mode filter; executed with three corrections — the DETAILED_DARTS rulesets are RECREATIONAL not ANALYTICS, the settings service returns `ServiceResult` instead of throwing, and an unplanned task 6b widened `validateConfig` (D197) (2026-08-08) | historical |
| `docs/superpowers/plans/2026-08-05-visual-board-ui.md` | Plan 3 (this branch, complete): screen-to-millimetre transform, magnifier placement, press-drag-release state machine, `DartBoard`/`BoardMagnifier`/`BoardInputPanel`, the Alpine bridge, both play pages, a11y + docs; executed with four unplanned corrections — a parity guard for the inlined SVG copy (4b), setup pages that hardcoded the mode pair (7b), shape-based `record()` dispatch (7c) and the matching `undo()` (7d) (2026-08-09) | historical |
| `docs/superpowers/handoffs/2026-08-05-visual-board-operator-checklist.md` | Operator checklist: apply migrations `0017`/`0018` + seeds `0005`/`0006`, `drizzle-kit introspect` diff against the hand-written `schema.ts` coordinate columns, `chk_dart_location_pair` rejection check, `v_dart_locations` angle-convention check, bust-vs-scoreless divergence check — none doable in-session, no database in this environment (2026-08-05) | historical |
| `docs/superpowers/specs/2026-08-12-bobs27-frontend-design.md` | Bob's 27 recreational + analytics frontend design: 4 sequential phases (scoring fix, `ANALYTICS + VISUAL_BOARD` capability, setup page, play page), each its own plan/branch, executed one at a time (2026-08-12) | historical |
| `docs/superpowers/plans/2026-08-12-bobs27-phase1-scoring-fix.md` | Phase 1 (merged): `pointValueOf` corrected to add the hit double's board value instead of face value; tests, ruleset doc and decision ledger (D207) updated in lockstep (2026-08-12) | historical |
| `docs/superpowers/plans/2026-08-12-bobs27-phase2-visual-board-capability.md` | Phase 2 (merged): `BOBS27_V1` gains `ANALYTICS + VISUAL_BOARD` (`capabilities.ts`, seed `0007`, `bobs27.validator.ts`); `Bobs27Engine.record()` location-passthrough bugfix; `resolveSessionModePair`'s fallback generalized from a hardcoded pair to a ruleset's own first declared pair; a dartless-turn tightening for Bob's 27's `VISUAL_BOARD` batch validation, added after the whole-branch review flagged that the shape (legitimate for 501/Score Training's keypad fallback, D199) has no legitimate Bob's 27 producer (2026-08-12) | historical |
| `docs/superpowers/plans/2026-08-12-bobs27-phase3-setup-routing.md` | Phase 3 (merged): games-page card, `bobs27Setup` setup-page data module + form (zero editable settings), a static play-route placeholder ahead of Phase 4; final whole-branch review caught and fixed a spec-acceptance gap the plan itself introduced — `visibleGames`'s exact-pair filter never showed the card under the app's actual `RECREATIONAL` setting, fixed via a new capture-mode-only `supportsCaptureMode` helper (2026-08-12) | historical |
| `docs/superpowers/plans/2026-08-12-bobs27-phase4-play-page.md` | Phase 4 (this branch, complete — final phase): `bobs27-play.data.ts` game loop, visit preview, recreational tap input, board input's 1.5s reveal-then-clear, Won/Lost results modal, replaces Phase 3's play-route placeholder; final whole-branch review found and fixed a Critical board-mode false-hit bug the plan's own code introduced, a resumed-terminal-session silent break, a timer-leak, and (per an explicit product decision) added a running-score display the spec's wording had omitted (2026-08-12) | historical |
| `docs/superpowers/handoffs/2026-08-12-bobs27-visual-board-capability-checklist.md` | Operator checklist: re-apply seed `0007` (idempotent) against Neon so the new `BOBS27_V1`/`ANALYTICS`/`VISUAL_BOARD` row exists before Phase 3/4's UI can create such a session; not doable in-session, no database in this environment (2026-08-12) | historical |
| `docs/superpowers/specs/2026-08-13-singles-training-frontend-design.md` | Singles Training frontend design: games-index card, setup page (zero editable settings), target-aware play-page input (S/D/T/Miss on number targets, Bull/Bullseye/Miss on the BULL visit), 3-dart visit preview, total-points-only results; flags the `trainingPointsFor` duplication seam (2026-08-13) | historical |
| `docs/superpowers/plans/2026-08-13-singles-training-frontend.md` | The 10-task plan implementing that spec: setup/play data modules, `VisitPreview.astro` generalization, target-aware input row, interface/results components, route wiring, verification; final whole-branch review found no Critical/Important issues, two Minor robustness fixes applied post-review (2026-08-13) | historical |
| `docs/superpowers/specs/2026-08-13-singles-training-input-stats-design.md` | Owner-requested UI refinement over the shipped Singles Training play screen: `SinglesRecreationalInput.astro`'s tap row becomes a two-row grid (row 1 target-labeled S/D/T or Bull/Bullseye, row 2 Undo/Miss), and four session-total stat rows (Misses/Singles/Doubles/Trebles) added below Target; no engine/validator/capability change (2026-08-13) | historical |
| `docs/superpowers/plans/2026-08-13-singles-training-input-stats.md` | The 4-task plan implementing that spec: session hit-count getters (`missCount`/`singleCount`/`doubleCount`/`trebleCount`, exhaustive over `DartZoneKey`), the two-row button rewrite, the four stat rows, final verification; task reviews clean, no Critical/Important findings (2026-08-13) | historical |
| `docs/superpowers/specs/2026-08-13-doubles-training-frontend-design.md` | Doubles Training v1 frontend design: games-index card, setup page (zero editable settings), play page (tap input, visit preview, live hit/miss totals only, no board input — `DOUBLES_TRAINING_V1` declares only RECREATIONAL + DETAILED_DARTS), results modal; mirrors the Singles Training precedent (RECREATIONAL-only, no `ANALYTICS` pair), not Bob's 27's multi-phase rollout (2026-08-13) | historical |
| `docs/superpowers/plans/2026-08-13-doubles-training-frontend.md` | The 7-task plan implementing that spec: type contracts, games-index card, setup data+page, play data+page, results modal, final validation; executed with an 8th unplanned task (dedup, see 1.7.38/1.7.39 above and D208) added after the final whole-branch review flagged mirror-caused duplication and the owner asked for it to be fixed in-branch (2026-08-13) | historical |
| `docs/superpowers/specs/2026-08-14-training-order-mode-design.md` | Training order mode design: editable Low→High/High→Low/Random `order_mode` setting shared identically by Singles and Doubles Training, resolved to a 21-value `target_order` array once at session creation and copied into the immutable config snapshot so undo/resume never reshuffles; bull leads on High→Low, is fully shuffled in on Random, and Play Again always mints a fresh order (2026-08-14) | historical |
| `docs/superpowers/plans/2026-08-14-training-order-mode.md` | The 12-task plan implementing that spec: shared `target-order.ts` helper, config schema widening (`target_order` field + `TARGET_ORDER_VALUES`/`isValidTargetOrder` refinement, ~13 pre-existing fixtures updated), order-aware `numbersPath`/`doublesPath`, both engines' index-based completion, both play pages reading the session's own order, a generic `runPlayAgain` `buildOverrides` hook, the setup UI Toggle, seed data, and doc corrections — including fixing Doubles Training's non-canonical doc, which had wrongly said High→Low "ends on bull" instead of leading with it; `TargetOrderMode` ended up declared in `lib/game/types.ts` rather than `target-order.ts` as the plan's Task 1 first drafted it, since `scripts/check-type-barrels.sh` requires exported types to live in the folder's `types.ts` barrel (2026-08-14) | historical |
| `docs/superpowers/specs/2026-08-14-issues-102-95-88-design.md` | Design closing GitHub issues #102 (games-index/app-mode-picker settings-load flash), #95 (Bob's 27 results modal: "Game over!"/"Winner!" titles, new double-hit-ratio and highest-number-reached stats; personal-best pill explicitly deferred, owner decision), and #88 (501 finish-match dialog button falls back to `ConfirmDialog`'s own default label) (2026-08-14) | historical |
| `docs/superpowers/plans/2026-08-14-issues-102-95-88.md` | The 6-task plan implementing that spec: Bob's 27 `computeStats()` TDD (double hit ratio, highest number reached), results-modal title/stat-row markup, 501 confirm-label prop removal, settings-loading skeleton gates on `AppModeForm.astro` and `pages/games/index.astro` mirroring the existing `loadingReconciliation`/`ReconciliationBlocked.astro` pattern, final validation; no new pattern, file-location rule, or architectural decision — pure reuse of existing conventions (2026-08-14) | historical |
| `docs/superpowers/specs/2026-08-14-shanghai-v1-design.md` | Shanghai v1 design: round-by-round target game, rounds 1–20 fixed ascending (resolved over the source ruleset doc's internal contradiction), Shanghai instant win on single+double+treble of the round's own number in one visit, DETAILED_DARTS-only capture (Singles Training precedent, not Bob's 27's dual-mode), raw per-dart zone array reducer state (`dartsThisVisit`) so a Shanghai is derived on the visit's 3rd dart rather than tracked eagerly (2026-08-14) | historical |
| `docs/superpowers/plans/2026-08-14-shanghai-v1.md` | The 11-task plan implementing that spec: seed (game type/ruleset/preset, capability row folded into `seeds/0007`'s running ledger rather than a duplicate insert), config schema (genuinely empty `.strict()` — no `ShanghaiConfigData`, unused since the snapshot has no fields to map), engine, server-side validator, games-index card, shared type contexts, setup/play data modules (reusing `play-lifecycle.ts` and, unmodified, `SinglesRecreationalInput.astro`/`VisitPreview.astro`), play UI, final gates; Tasks 3/4 (engine + validator) committed together since `scripts/check-game-engines.sh`'s pre-commit gate requires both registrations atomically; `database/README.md`'s verification-script table and Seed Order list updated for `0008` (2026-08-14) | historical |
| `docs/superpowers/specs/2026-08-14-121-v1-design.md` | 121 v1 design: single-player X01-style checkout ladder from 121 to 170, double-out, 3 visits (9 darts) per attempt, fail-rule "stay" (miss the budget, retry the same target) — a checkout below the cap climbs the target by one, a checkout at 170 wins the session (2026-08-14) | historical |
| `docs/superpowers/plans/2026-08-14-121-v1-implementation.md` | The 6-task plan implementing that spec: config schema + capability declaration, engine + server validator + DB seed (one commit, same `scripts/check-game-engines.sh` atomicity constraint as Shanghai), games-index card, setup flow, play flow, final gates. `OneTwentyOneEngine` reuses the previously-unused `ROUND` `StageTypeKey`, one per attempt, mirroring 501's per-leg `LEG` stage model; `OneTwentyOneState` gained a `remainingInAttempt` field the spec's own sketch omitted (mechanical application of the already-canonical fact-log-fold pattern `FiveOhOneState.remainingScore` established, not a new decision). Play data is bespoke (`one-twenty-one-play.data.ts`, double-confirm + session-finish-confirm dialogs mirroring `five-oh-one-play.data.ts`), deliberately not `play-lifecycle.ts` — already out of scope per D209's own `GameEngine<DartObservation, unknown>` constraint, same reason 501/Score Training stay out. Four plan-quality bugs caught and fixed during execution, none of them architectural: the plan's own engine/play-data tests climbed the ladder to 170 via a fixed `scoreAttempted: 2` per visit instead of the actual per-target checkout amount (`target`), and one play-data test asserted a bust off `scoreAttempted: 41` where the setup visit left a remaining score of 80, not 41 (needed `81`, mirroring the sibling test that already got this right) — both are copy/typo-class test-authoring errors, not implementation bugs, caught by running the tests rather than trusting the plan's pre-written expected output; `capabilities.test.ts`'s hardcoded ruleset-key array and `database/README.md`'s Seed Order/verification-script table were both missing a `121_V1`/`0009` entry the plan never scheduled a step for; and neither `oneTwentyOneSetup` nor `oneTwentyOnePlay` was wired into `register-route-data.ts`'s `Alpine.data()` calls, a step no task in the plan named even though `03-Alpine-Patterns.md` already documents the requirement — without it neither route's `x-data` resolves at runtime. `db:status`/`db:migrate`/`db:seed` and the seed-0009/0007 live verification script could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed), nor could the manual browser smoke test that depends on a live session-creation API; `database/README.md`'s Seed Order and verification-script table updated for `0009` (2026-08-15) | historical |
| `docs/superpowers/specs/2026-08-15-around-the-clock-v1-design.md` | Around the Clock v1 design: fixed 21-target path (1→20 then BULL), any hit (single/double/treble) advances the target immediately, mid-visit — the classic pub-rule shape, not the fixed-target-per-visit shape existing engines use; BULL (either ring) ends the session immediately, even mid-visit; `intendedTargetNumber`/`intendedZoneKey` stay null on every dart, same convention as Singles Training/Shanghai, since the active target is still exactly recoverable by replaying `facts()`; DETAILED_DARTS-only capture, `SinglesRecreationalInput.astro`/`VisitPreview.astro` reused unmodified (2026-08-15) | historical |
| `docs/superpowers/plans/2026-08-15-around-the-clock-v1.md` | The 5-task plan implementing that spec: config schema, engine + server validator + capability (one commit, same `scripts/check-game-engines.sh` atomicity constraint as Shanghai/121), DB seed (`0010`, capability row appended to `0007`'s ledger), setup flow, play flow. `AroundTheClockEngine` advances `targetIndex` on every hit before the 3-dart visit-boundary check and short-circuits to `COMPLETE` on a BULL hit regardless of `dartsThisVisit`, so a turn can close permanently short of 3 darts; play data's `previewSegmentsFor` needs no target-number comparison (every tap is already relative to the target active at throw time) and its `uploadAndCompleteSession` closes over `$store.game.turns` to fold `{turns, hits, totalDarts}`, since the engine's `state()` carries no hit-count fields; BULL-ring tap mapping follows the Singles Training precedent (SINGLE→OUTER_BULL, DOUBLE→INNER_BULL, TREBLE blocked). One plan-quality gap caught and fixed during the final task, the same class the 121 v1 rollout hit: neither `aroundTheClockSetup` nor `aroundTheClockPlay` was wired into `register-route-data.ts`'s `Alpine.data()` calls by the setup-flow task, a step no task in the plan named — without it neither route's `x-data` resolves at runtime; both registered together in the play-flow task's commit. `db:status`/`db:migrate`/`db:seed`/`db:verify` and the seed-0010/0007 live verification script could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed); `database/README.md`'s Seed Order and verification-script table updated for `0010` (2026-08-15) | historical |
| `docs/superpowers/specs/2026-08-15-singles-doubles-training-analytics-design.md` | Singles/Doubles Training analytics (VISUAL_BOARD) capture design: gives both rulesets the `ANALYTICS + VISUAL_BOARD` capability pair alongside their existing `RECREATIONAL + DETAILED_DARTS` keypad capture, mirroring Bob's 27's already-shipped pair; generalizes `play-lifecycle.ts` rather than re-forking bespoke play-data modules a second/third time (2026-08-15) | historical |
| `docs/superpowers/plans/2026-08-15-singles-doubles-training-analytics-plan.md` | The 9-task plan implementing that spec: engine location-fact fixes (both engines now carry the observed dart's real `locationX`/`locationY`), capability/validator/seed additions for both rulesets, `play-lifecycle.ts` generalization (`hiddenTimer`, mode-gated reveal-then-clear, new shared `playVisitMarkers`, D211 supersedes D209), play-data wiring (`recordDart`/`visitMarkers`/`boardInputData` spread), frontend board panel, decision ledger + context maintenance. Execution surfaced fixtures the plan itself didn't schedule: `capabilities.test.ts`'s hardcoded `capableRulesets("ANALYTICS","VISUAL_BOARD")` list needed both new ruleset keys appended, and `singles-training-setup.data.test.ts`/`doubles-training-setup.data.test.ts`'s "falls back to the declared pair" tests had used `ANALYTICS + VISUAL_BOARD` as their undeclared-pair fixture — now genuinely declared, both re-pointed at `RECREATIONAL + QUICK_SCORE` (the same undeclared pair `bobs27-setup.data.test.ts`'s equivalent test already uses), re-pointed at the same guarantee rather than a different one. `play-lifecycle.test.ts`'s own reveal-timer tests needed the `fetchActiveSessions` mock-override pattern (setting `$store.game.inputModeKey` directly before `playInit` doesn't survive reconciliation's `setSessionModes` overwrite). Full suite 117 files/2295 tests, 0 regressions; `astro check` 0 errors/0 warnings (6 pre-existing hints, unchanged baseline); `npx fallow` exits 0 (0 above threshold); `db:status`/`db:migrate`/`db:introspect`, the seed-0007 live verification script, and the manual browser smoke test could not run in this container (no `DATABASE_URL`/`.env`, per the established D193 precedent — flagged rather than claimed) (2026-08-15) | historical |
| `docs/superpowers/specs/2026-08-15-121-shanghai-atc-analytics-design.md` | 121/Shanghai/Around the Clock analytics (VISUAL_BOARD) capture design: gives all three rulesets the `ANALYTICS + VISUAL_BOARD` capability pair alongside their existing capture mode — Shanghai/Around the Clock mirror `singles-training.validator.ts`'s dispatch shape (no engine coordinate fix needed, both already carry real coordinates into dart facts), 121 needs a real engine change since it records whole-visit totals, not darts: a dual-shape `record()`/`undo()`/`wouldComplete()` mirroring `FiveOhOneEngine`, adapted for 121's per-round visit cap (2026-08-15) | historical |
| `docs/superpowers/plans/2026-08-15-121-shanghai-atc-analytics-plan.md` | The 6-task plan implementing that spec: capability/validator/seed layer for all three rulesets (Task 1), Shanghai/Around the Clock board-input frontend wiring (Tasks 2–3), 121 engine dual-shape dart support via TDD (Task 4) — `deriveClosedState`/`deriveState` split so an open board visit's partial total never prematurely advances `visitsThisAttempt`, plus a `settleVisit`-resolved gate on the round-stage-opening check to avoid a false positive on a fresh round's first dart — 121 board-input frontend (Task 5), full regression + context maintenance (Task 6). Execution surfaced plan-authoring bugs the plan itself didn't anticipate: two collateral test suites (`capabilities.test.ts`'s hardcoded `capableRulesets` list, `shanghai-setup.data.test.ts`/`around-the-clock-setup.data.test.ts`'s "falls back to the declared pair" tests using `ANALYTICS + VISUAL_BOARD` as their now-invalid undeclared-pair fixture, re-pointed at `RECREATIONAL + QUICK_SCORE`) needed fixes the plan never scheduled; two `one-twenty-one.validator.test.ts` cases omitted `captureModeKey`/`inputModeKey` on a now-dual-mode-pair validator; and several `visual board capture`/`wouldComplete` engine test fixtures were dart-arithmetic errors — landing exactly on a live remaining of 1 (an immediate bust under the same rule `five-oh-one.engine.module.ts` already applies) or attempting a checkout via a bull dart (only a genuine `DOUBLE` zone key ever checks a board visit out, matching 501's own `settleVisit`) — all fixed by choosing arithmetic consistent with the codebase's own established bust/checkout rules, not by changing engine behavior. `astro check` also caught a real gap `npm test` alone did not: `hiddenTimer` needed initializing in `shanghai-play.data.ts`/`around-the-clock-play.data.ts`'s returned object, not just declared on the type. Full suite 117 files/2332 tests, 0 regressions; `npx fallow` exits 0; `db:status`/`db:migrate`/`db:introspect`, the seed-0007 live verification script, and the manual browser smoke test could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed) (2026-08-15) | historical |
| `docs/superpowers/specs/2026-08-15-issues-112-113-114-design.md` | Design closing three independent, small, owner-triaged GitHub issues on one branch, per the 1.7.42 precedent: #112 (uppercase `Bull`/`Bullseye` recreational tap-input labels), #113 (Singles Training session-summary modal gains hit/miss/percentage stats already tracked live), #114 (all 8 setup pages gain a top-left back button to `/games` via an `AppLayout` prop) (2026-08-15) | historical |
| `docs/superpowers/plans/2026-08-15-issues-112-113-114.md` | The 6-task plan implementing that spec: #112's text edit, #113's TDD `resultsSnapshot` widening + `StatRow` additions, #114's `AppLayout` prop + 8-page wiring, full validation. Execution found one plan-authoring gap: `SinglesRecreationalInput.astro`'s `Miss` label (line 74) was in scope for #112 per the issue's own wording but named in neither the spec nor the plan's Task 1 (only `Bull`/`Bullseye` were) — folded into Task 1's edit at execution time (2026-08-15) | historical |
| `docs/superpowers/specs/2026-08-15-profile-display-darts-config-design.md` | Editable display name and darts/weight config on the profile page design: `DisplayNameForm`/`DartsConfigForm`, `profile.store.ts`, `PATCH /api/players/me/settings` extension (2026-08-15) | historical |
| `docs/superpowers/plans/2026-08-15-profile-display-darts-config.md` | The 8-task plan implementing that spec (DB/service/API/client/store/UI + validation); shipped as PR #118 (merged) | historical |
| `docs/superpowers/specs/2026-08-16-profile-settings-inline-edit-design.md` | Redesign of the shipped profile settings UI: owner rejected the always-open three-form layout post-merge, asked for a plain `h2` "Player settings" grouping display name/darts/weight/handedness with each value shown as text + pencil-icon edit button (2026-08-16) | historical |
| `docs/superpowers/plans/2026-08-16-profile-settings-inline-edit.md` | The 5-task plan implementing that redesign: `pencil.svg` icon, `SettingRow.astro`, `HandednessForm.astro`'s `InfoSection` wrapper stripped, `PlayerSettingsCard.astro`, `app/src/pages/profile/index.astro` restructure + `DisplayNameForm`/`DartsConfigForm` deletion. Full suite 122 files/2356 tests, 0 regressions; `astro check` 0 errors/0 warnings; manual dev-server smoke test of the served markup confirmed heading structure, pencil-edit rows, `x-model`/`x-model.number` bindings, and the un-wrapped radiogroup; `db:status`/`db:migrate`/`db:introspect` and the live-browser interaction pass could not run in this container (no `DATABASE_URL`, per the established D193 precedent — flagged rather than claimed) (2026-08-16) | historical |
| `docs/superpowers/specs/2026-08-18-121-analytics-mode-badge-design.md` | Issue #121 design: replace `/games`' full-width analytics-mode text block with a small `Badge.astro` pill ("analytics", accent/blue variant) inline with the page `h1`; new portable `components/ui/Badge.astro` primitive built as the style guide's already-prescribed but never-built `.badge`/`.badge-accent` replacement; badge text size deliberately left out of the base component, passed by callers via `class` (2026-08-18) | historical |
| `docs/superpowers/plans/2026-08-18-121-analytics-mode-badge-plan.md` | The 3-task plan implementing that spec: `Badge.astro` creation, `pages/games/index.astro` wiring (text block removed, `h1`/badge flex row added), context maintenance. Execution found one plan gap: `Badge.astro`'s first draft didn't spread leftover props (`...props`/`{...props}`, the `app/CLAUDE.md` non-negotiable), so the call site's `x-show`/`x-cloak` never reached the rendered `<span>` — caught via a manual dev-server fetch of `/games`, fixed in its own commit before the wiring commit (2026-08-18) | historical |
| `docs/superpowers/specs/2026-08-18-122-profile-settings-cards-design.md` | Issue #122 design: five `/profile` polish fixes — "Player settings" `h2` moved inside `PlayerSettingsCard`'s bordered card; `SettingRow`/Handed-row label→value spacing widened `mt-1.5`; `HandednessForm` button copy `Right-handed`/`Left-handed` → `Right`/`Left`; `AppModeForm` wrapped in its own `bg-tab-card` card and its `InfoSection` (a second, nested card) dropped in favor of a plain inline heading + short static description, since nesting two `bg-tab-card` cards would violate the style guide's "one surface level per visual block" rule; description text shortened to the analytics/recreational essentials (2026-08-18) | historical |
| `docs/superpowers/plans/2026-08-18-122-profile-settings-cards-plan.md` | The 4-task plan implementing that spec: title/spacing in `PlayerSettingsCard.astro`+`SettingRow.astro`, `HandednessForm.astro` copy, `AppModeForm.astro` card+description rewrite, context maintenance. No plan gaps found during execution — manual dev-server fetch of `/profile` confirmed all five fixes render as designed (2026-08-18) | historical |
| `docs/superpowers/specs/2026-08-19-governance-design.md` | Spec 2 of the three-spec agent-context program: a finding is logged, not acted on. Establishes one rule (a root `CLAUDE.md` Hard Invariant, permission required to act on any incidental discovery), one log (`FINDINGS.md`, `DECISIONS.md`'s block shape with the opposite lifecycle — resolved entries are deleted, so ids come from a `highest-issued:` mark rather than a scan) and one gate (`scripts/check-findings-log.sh`, shape and evidence-liveness only, with its inability to prove an agent logged rather than fixed stated plainly). Absorbs D107's self-learning gate rather than running it alongside. Ships seeded with the five live contradictions found while writing it and fixes none of them — the restraint is load-bearing, not an omission (2026-08-19) | historical |
| `docs/superpowers/plans/2026-08-19-governance.md` | The 8-task plan implementing that spec: gate script first and proven to bite on seven malformed fixtures (Task 1), the seeded log (Task 2), pre-commit/CI/run-all-gates wiring (Task 3), the Hard Invariant (Task 4), the step-8 rewrite (Task 5), the Deferred boundary + D214 (Task 6), context registration (Task 7), and a closing task that logs findings surfaced during execution rather than fixing them (Task 8) (2026-08-19) | historical |
| `docs/superpowers/specs/2026-08-19-consistency-design.md` | Consistency (Spec 3 of 3): the measurement behind one shape per game — six byte-identical preset setup controllers and five three-dart validators — the two extractions, the exemplar doc, the wiring gate, and `play.data.ts` deliberately left alone (D215) (2026-08-19) | historical |
| `docs/superpowers/plans/2026-08-19-consistency.md` | The 12-task plan executing that spec, correcting four of its details against the compiler and the repo: the self-typed context is a TS2456 circular alias, `overrides` belongs to `createSession`'s config, the validator builder needs a third required `dartlessIssue`, and the touch list is 26 files (2026-08-19) | historical |
| `docs/superpowers/specs/2026-08-20-guest-player-x01-design.md` | Guest player + multi-seat X01 design: seats as an ordered list in the configuration snapshot (no new table, a guest is a `participants` row), the active seat always derived from the fact log, a shared seat rota serving both stage shapes, and X01 wins folded per `sideKey` so a future 2v2 needs no state change. 1–4 seats, one per side in v1; bull-up and per-participant capture depth deferred (2026-08-20) | historical |
| `docs/superpowers/plans/2026-08-20-guest-player-x01-implementation.md` | The 8-task plan implementing that spec. Execution merged Tasks 2 and 3 into one commit — making `TurnFact.participantRef` required forces every engine to read `config.seats`, which forces every config reaching an engine (the store's included) to be seated, so no smaller unit typechecks. Three plan gaps found and fixed: `Seated<T>` had to become a conditional type because three snapshots are `Record<string, never>` (an intersection narrows `seats` to `never`); `record()` derived its returned state before opening the next leg's stage, reporting the winning seat at 0 and the others un-reset; and the seat-rejection test's regex did not match the plan's own message. Migration `0023` is written but NOT applied — no `DATABASE_URL` in this container, per the D193 precedent (2026-08-21) | historical |
| `docs/superpowers/specs/2026-08-21-guest-play-ui-fixes-and-split-scoreboard-design.md` | Design closing three shipped 501 guest-add UI defects (faint dashed outline, backwards icon sizing, missing 1v1 cap) plus a repo-wide ban on the Tailwind important modifier (both forms) and a shared two-seat split scoreboard proven on 501, the only engine that can reach 2 seats today (2026-08-21) | historical |
| `docs/superpowers/plans/2026-08-21-guest-play-ui-fixes-and-split-scoreboard.md` | The 4-task plan implementing that spec: setup-page fixes (`Button.astro`'s `dashed` variant, `addGuest()` 1v1 cap), the important-modifier ban (`scripts/check-style-tokens.sh` `IMPORTANT_MODIFIER` check, D226), the split scoreboard shell + seat-parameterized `five-oh-one-play.data.ts` getters wired into `FiveOhOne.astro`, context maintenance. Execution found one plan-code gap: the two Alpine `x-for` blocks embedded inside `{cond && (...)}` JSX-expression regions needed `:key`/`:class` passed via a spread object (`{...{ ":key": "part" }}`), matching `CheckoutConfirm.astro`'s established pattern — literal colon-attributes are invalid JSX syntax inside an embedded expression block, caught by Prettier's JSX parser on first commit attempt and fixed before recommitting (2026-08-21) | historical |
| `docs/superpowers/specs/2026-08-22-single-opponent-seat-remaining-engines-design.md` | Single opponent seat (1v1, 2 seats max) design for the eight non-501 engines — Bob's 27, 121, Around the Clock, TUOD, Shanghai, Score Training, Singles Training, Doubles Training. Three win-condition categories (elimination / race-to-finish / score-compare) behind one shared `match-outcome.module.ts`, every engine's flat state converted to `MultiSeatState<TSeat>` folded per `participantRef`, `activeSeat()` gaining an optional completion predicate for Around the Clock's variable per-seat visit count, and `session-seats.service.ts`'s single `MULTI_SEAT_RULESET` gate generalized to a `SEAT_CAPS` map. Out of scope: 2v2 for these eight (501 alone keeps 1–4 seat prep), DartBot, bull-up, reusable guest rosters, per-participant capture depth, TIMED-duration 1v1 for TUOD/Score Training. Its title reads "Remaining 7 Engines" while its own scope line and `SEAT_CAPS` both list eight rulesets — a naming quirk noted in D230, not a scope difference (2026-08-22) | historical |
| `docs/superpowers/plans/2026-08-22-single-opponent-seat-remaining-engines.md` | The 21-task plan implementing that spec: the shared `match-outcome.module.ts` and `activeSeat()` predicate plus the `SEAT_CAPS` map first, then one engine-and-frontend pair per ruleset (engine + validator + seat cap in one commit each, per `scripts/check-game-engines.sh`'s atomicity rule), closing with docs, D230 and full validation. A final whole-plan review found six defects fixed on the same branch: Play Again created every replayed session with no `participants`, replaying a 1v1 match solo and building the new engine from the pre-reseat config (stale `participantRef`s → `BATCH_REFERENCE_MISSING` on upload); 121 lacked the solo `winningSideKey` guard every other engine carries; `foldAroundTheClockState` shipped exported with no consumer; three doc comments and D230 claimed only Around the Clock passes `activeSeat()` a completion predicate when TUOD and Score Training pass one too (D231 corrects the record); Pattern 18's score-compare list omitted Singles and Doubles Training; and `04-Endpoint-Contracts.md` said "seven rulesets" above a table of eight (2026-08-22) | historical |
| `docs/superpowers/specs/2026-08-27-engine-duplication-cleanup-design.md` | Engine-duplication-cleanup design: F1 (`checkout-bust.module.ts`'s `resolveCheckoutAttempt`, the double-out bust/checkout rule duplicated 5x across 501/121/TUOD) and F2 (`otherSeatsComplete`'s completion-predicate generalization so TUOD/Score Training stop reimplementing it inline), plus 4 `docs/game-rules/rulesets/*.md` corrections found comparing those 3 engines against their source material. Explicitly deferred: `isDartObservation` duplicated 3x, six identical `STAGE` constants, two "state before this visit" techniques, and why `fallow` didn't flag any of this (2026-08-27) | historical |
| `docs/superpowers/plans/2026-08-27-engine-duplication-cleanup.md` | The 12-task plan implementing that spec: `checkout-bust.module.ts` + wiring into 501/121/TUOD (Tasks 1-4), `otherSeatsComplete` generalization + wiring into TUOD/Score Training (Tasks 5-7), a `validate:app` checkpoint (Task 8), then the 4 doc corrections (Tasks 9-12). Self-review caught one placeholder violation in Task 4's original Step 1 (a hedge/conditional test-addition step), rewritten to an unconditional concrete test before execution. Execution found two gaps beyond the plan itself: the D224 test-coverage gate required a covering-test touch for each of the 3 `otherSeatsComplete` caller files Task 5 modified, not just `seat-state.module.ts` (the plan under-scoped this task's Files/Test block), closed by adding one real integration test per caller; and `astro check` caught a type error in Task 2's own added test (`FiveOhOneGameEngine` narrows to `FiveOhOneVisitInput`, which excludes the `DartObservation` shape the test records), fixed by casting to the `FiveOhOneEngine` class instead — both fixed in their own small commits (2026-08-27) | historical |
| `docs/superpowers/specs/2026-08-28-engine-findings-cleanup-design.md` | Engine-findings-cleanup design closing F39 (`isDartObservationInput` extraction), F40 (`exerciseBlockStage()` builder extraction), and F41 (`turnsBeforeVisit` slice-helper extraction) — the three duplication pockets D240's own audit found and deferred. All three exports land in `turn-log.module.ts`; 501's `remainingBeforeVisit` explicitly stays out of scope as a genuinely different technique, documented instead of unified (2026-08-28) | historical |
| `docs/superpowers/plans/2026-08-28-engine-findings-cleanup.md` | The 8-task plan implementing that spec: `isDartObservationInput` + wiring into 501/121/TUOD (Tasks 1-2), `exerciseBlockStage()` + wiring into the 6 single-block engines (Tasks 3-4), `turnsBeforeVisit` + wiring into 121/TUOD (Tasks 5-6), the Pattern 18 doc note (Task 7), final validation + context maintenance (Task 8). Execution found one gap beyond the plan: wiring `exerciseBlockStage()` into the 6 engines left each file's `StageFact` type import unused (its own literal's type annotation had been the only use), caught by `astro check` and fixed by removing the now-dead import from each of the 6 files in the same commit (2026-08-28) | historical |
| `docs/superpowers/specs/2026-09-01-dartbot-v1-delivery-design.md` | DartBot v1 delivery design: phases the simulated opponent build (`08-DartBot.md`) into ordered, independently-shippable slices — phase 1 is the deterministic throw pipeline (aim resolution, anisotropic scatter, wire bounce-out, hand-set level curve) with no strategy layer, no `DartBot` class, no game engine wiring (2026-09-01) | historical |
| `docs/superpowers/plans/2026-09-01-dartbot-1-throw-engine.md` | The 7-task plan implementing phase 1: `docs/architecture/07-Frontend/02-Folder-Structure.md` registration (Task 1), seeded per-dart RNG (Task 2), hand-set level curve (Task 3), aim resolver (Task 4), throw engine — scatter/bounce/classify (Task 5), the deterministic snapshot test that is the phase gate (Task 6), full validation + context maintenance (Task 7). Execution found two plan-authoring gaps: the plan's own example imports reached past their area's raised type barrel (`@lib/game/board/types`/`@modules/dartbot/types` instead of `@lib/types`/`@modules/types`), and the new `dartbot/interfaces.ts`/`dartbot/types.ts` barrels needed raising into `app/src/modules/interfaces.ts`/`types.ts` — both required by `scripts/check-type-barrels.sh`, neither named as a step (2026-09-01) | historical |
| `docs/superpowers/plans/2026-09-01-dartbot-6-play-loop.md` | The 5-task plan for phase 6 (the play loop): `undoToActiveSeat` (Task 1), `playRunBotVisualBoardVisit` + the re-entrancy guards (Task 2), `playFoldBotQuickScoreVisit` (Task 3), wiring onto Bob's 27 (Task 4), validation + context maintenance (Task 5). Reconstructed 2026-09-02 (F52) from phase 6's own shipped commits (`593efc3`, `1325309`, `41d853d`, `853a417` on `main`) after `decisions/game-engine.md` D252 and this file's own 1.34.0 entry were found citing a path that had never been written — both `decisions/**` and this file are append-only, so the compliant fix was authoring the actually-cited file, verified against the real diffs, rather than rewording either citation (2026-09-02) | historical |
| `docs/superpowers/plans/2026-09-02-dartbot-7-x01-strategy.md` | The 8-task plan for phase 7 (`X01Strategy`): admit `501_V1` into `RULESET_DARTBOT` (Task 1), the `decisionQuality` skill axis (Task 2), `chooseTarget` — checkout-label parsing + the decision-tier gate (Task 3), widen `playFoldBotQuickScoreVisit`'s thrower to read live scratch-engine state (Task 4), seat the bot on 501 under both capture modes (Task 5), the setup-screen chooser (Task 6), the engine-level SHARED-leg contract test that is the phase gate (Task 7), validation + context maintenance (Task 8). See the 1.35.0 entry above for the seven plan-authoring gaps execution found and fixed (2026-09-02) | historical |
