<!--
status: canonical
scope: canonical file inventory — what each document answers
read-when: a context pack demonstrably lacks the answer
updated: 2026-09-26
-->

> Escalation target for `00-Context-Map.md`. Packs come first; open this
> only when the pack lacks the answer. History lives in
> `00-Context-Map-History.md`.

---

# File Inventory

Status: **canonical** = current truth · **historical** = preserved record, never read by default · **generated** = tool output, do not hand-edit.

## Foundation (`docs/architecture/`)

| File | Answers | Status | ~Tokens |
| ---- | ------- | ------ | ------- |
| `README.md` | Documentation philosophy and hierarchy | canonical | ~1.5k |
| `00-Context-Map.md` | The router — context packs, authority order, maintenance protocol (2026-08-19); New non-game client tool (Trivia) pack row added (2026-09-09) | canonical | ~1.9k |
| `00-File-Inventory.md` | This file — what each canonical document answers; escalation target when a pack falls short (2026-09-08) | canonical | ~26.9k |
| `00-Context-Map-History.md` | Context-map version history and point-in-time task records; provenance only, never loaded by a task (2026-08-19; claim refreshed 2026-09-19) | historical | ~111.3k |
| `01-Principles.md` | What we believe (core values + decision priorities) | canonical | ~2.1k |
| `02-System-Architecture.md` | System layers, data flows, ownership | canonical | ~1.9k |
| `03-Engineering-Workflow.md` | 10-phase change lifecycle | canonical | ~2.2k |
| `04-Architecture-patterns.md` | Recurring design patterns + anti-patterns; Pattern 18 game engine contract, incl. undo depth, derived-value returns, `completedAt` timing, and the win-condition categories/`match-outcome.module.ts`/`activeSeat()` completion predicate (2026-07-26; win-condition subsection added 2026-08-22); Pattern 20 shared accuracy/hit-rate formatting via `accuracyDisplay()` (2026-08-27); Pattern 21 exclusive score-band tallying via `visitScoreBandCounts()` (2026-08-27); Pattern 18 gains `checkout-bust.module.ts` and `otherSeatsComplete`'s completion-predicate parameter (2026-08-27, D240); Pattern 21 gains a fifth band, Pattern 18's `checkout-bust.module.ts` gains `checkoutAttemptCount` (2026-08-28, D242) | canonical | ~6.8k |
| `08-DartBot.md` | Simulated opponent architecture: seat layer plug-in, the play loop, delivery phases, board-geometry/engine-contract boundaries; phases 1–7 (throw engine, calibration, dictated strategy, seat admission, participant write path, play loop, `X01Strategy` + 501 opponent mode) are built — see `app/src/modules/dartbot/`, `app/src/lib/game/guest-list.ts`/`session-mode-resolution.ts`/`play-lifecycle.ts`/`bobs27-play.data.ts`/`five-oh-one-play.data.ts`/`shanghai-play.data.ts`/`singles-training-play.data.ts`/`one-twenty-one-play.data.ts`/`tuod-play.data.ts`/`score-training-play.data.ts`/`around-the-clock-play.data.ts`/`doubles-training-play.data.ts`, `app/src/services/session.service.ts`; phase 8+ (ghost mode) remains design-only; the play loop now covers all eight `PER_SEAT` rulesets — Shanghai, Singles Training, 121, TUOD and Score Training (2026-09-03) and Around the Clock and Doubles Training (2026-09-16) alongside Bob's 27; D-E population prior measured and closed — `population-prior.module.ts`/`scripts/dartbot-population-prior.ts`; `LEVEL_SKILL_TABLE` refit against it closes D-L — `scripts/dartbot-level-curve-refit.ts`, anchor history in `08-DartBot-Anchor-Log.md`; the level-picker average/checkout UI D-L deferred closes D-M — `allLevelSelectStats()`/`level-select-stats.module.ts`/`scripts/dartbot-level-select-stats.ts`, wired into `OpponentChooserModal.astro`; D-N recalibrates `LEVEL_SKILL_TABLE` against user-supplied level-1/level-15 guardrails, fixing D-L's degenerate level 15 — `scripts/dartbot-level-curve-recalibration.ts`, `08-DartBot-Anchor-Log.md`; D313 removes the three bias flat spots `LEVEL_SKILL_TABLE` inherited from the pre-D-L table, giving levels 7/8, 9/10 and 12/13 distinct `biasXMm`/`biasYMm` pairs — `scripts/dartbot-bias-flatspot-fix.ts`, closing issue #286 (2026-09-18) — `fitProfile()` (phase 10) still unbuilt, D-K still open (2026-09-02; Shanghai wiring 2026-09-03; Singles Training wiring closes F62, 2026-09-03; 121/TUOD/Score Training wiring 2026-09-03; D-E closed 2026-09-04; D-L closed 2026-09-04; D-M closed 2026-09-04; D-N closed 2026-09-04; SHANGHAI_V2 seat admission closes issue #237 (2026-09-07); Around the Clock/Doubles Training wiring closes issue #283, 2026-09-16) | canonical | ~26.3k |
| `08-DartBot-Anchor-Log.md` | Append-only anchor history for `LEVEL_SKILL_TABLE`'s refits (D-L, D-N) — date, anchor level(s), measured/guardrail values, spread scale/exponent, verified bands (2026-09-04) | canonical | ~1.1k |
| `09-Training/00-Overview.md` | Folder overview: what `01-Routines.md`/`02-Trivia.md` each cover, the `training/{trivia,exercises,routines}` source split (2026-09-17) | canonical | ~0.2k |
| `09-Training/01-Routines.md` | Architecture for training routines, exercises, exercise engines; configurable/adaptive training, extends `GameEngine`/ruleset/configuration-snapshot architecture (2026-09-01); §9/§10's `ExerciseEngine` contract and §8's `TrainingEngine` orchestration are now built — see `app/src/modules/training/exercises/`, `app/src/modules/training/`, `app/src/lib/training/exercises/`, `app/src/services/exercise-rulesets/` (D264, 2026-09-10); Balanced Training's orchestration/API/UI layer (service, `/api/training-sessions/*`, play page, Start button) is now built per `docs/superpowers/specs/2026-09-12-balanced-training-playable-design.md` — see `app/src/services/training-session.service.ts`, `app/src/repositories/training-session.repository.ts`, `app/src/lib/training/` (2026-09-12); Switching/Double Pattern play screens rebuilt on the game shape with a visible step countdown per `docs/superpowers/specs/2026-09-14-switching-double-pattern-play-interface-design.md` (D275, 2026-09-14); moved under `09-Training/` alongside `02-Trivia.md` (D308, 2026-09-17); §7/§20 restated as shipped — custom-routine builder, migration `0038`, `app/src/pages/training/routines/` (D305/D306/D321/D336, 2026-09-19) | canonical | ~9.7k |
| `09-Training/02-Trivia.md` | Checkout trivia architecture: standalone untimed flashcard quiz drilling checkout routes; reuses `checkout-path.module.ts`, explicitly not integrated with `09-Training/01-Routines.md` (2026-09-01); moved under `09-Training/` alongside `01-Routines.md` (D308, 2026-09-17) | canonical | ~3.4k |
| `10-Statistics/00-Overview.md` | Detailed statistics pages architecture (designed, not built): insight-section registry, capability tags, compute-site rule, common query contract (required date range, `tz`, bucket, status, context), additive metric components, planned endpoints, IndexedDB cache, play context, abandoned sessions, data layer (two base views built by `0043`, thin per-section views planned), scalability review, rollout (D364/D365/D366, 2026-09-26) | canonical | ~3.6k |
| `10-Statistics/01-Section-Catalog.md` | Shared insight-section library (site + reason per section), loose-dart and derived-intent rules, section list per game page, game-specific sections, deferred sections (D364, 2026-09-26) | canonical | ~1.8k |
| `10-Statistics/02-Replay.md` | Paginated per-session replay route and endpoint: turn pages, first-page header, caching (D365, 2026-09-26) | canonical | ~0.6k |
## Database handbook (`05-Database/`)

| File | Answers | Status | ~Tokens |
| ---- | ------- | ------ | ------- |
| `00-OVERVIEW.md` | Database philosophy and operating model | canonical | ~2.5k |
| `01-Naming-Conventions.md` | Table/index/constraint/view naming; Applied Exceptions records `exercise_configurations`' pre-format singular constraint names (`0005`), which stay as applied (2026-09-18) | canonical | ~2.3k |
| `02-Design-Rules.md` | Schema design rules, controlled denormalisation | canonical | ~2.4k |
| `03-Migrations.md` | Migration process + chain `0001`–`0042`, one `##` section per migration in the chain (backfilled through `0037`, 2026-09-19; `0039`'s own section is missing — flagged as discovered work, not fixed here); `0019`/`0020` capability table + composite FK and their migrate→seed→migrate apply order, `0021` `v_player_settings` (2026-08-08), `0022` `v_player_profile` (2026-08-15), `0023` owner-scoped dart views (2026-08-21), `0024`–`0026` the dart/visit/leg fact views (2026-09-05/06), `0027`–`0032` exercise-type generalisation with the same migrate→seed→migrate apply order as `0019`/`0020` (2026-09-10/11), `0033` LEFT JOINs the read model onto the lookups `0028`/`0029` made nullable (2026-09-16), `0034` non-game single-active index, `0035` template ruleset pin, `0036` view consumers (2026-09-17), `0037` constraint renaming (2026-09-18), `0038` custom routines (2026-09-19, applied 2026-09-19); `0039` the `v_x01_checkout_darts` view swap (2026-09-19); `0040` pins a game ruleset version on GAME exercise templates, mirroring `0035` (D339, 2026-09-20); `0041` weekly training schedules — two Template-layer tables, one active per player, RESTRICT on the routine FK (D342, 2026-09-20) | canonical | ~9.9k |
| `04-Indexes.md` | Index strategy (query-path driven) | canonical | ~2.6k |
| `05-Views/00-Overview.md` | View categories and replay rules; ten implemented views through `0024` (2026-09-05); split into a directory to host per-domain view catalogs (2026-09-06); a view carries the columns its consumer reads, and JSONB snapshot projection is allowed (D298, 2026-09-17); a repository narrows a view select's non-null columns with `nonNull()` per column, never a blanket row-array cast (D348, 2026-09-21); `v_stats_session_facts`/`v_stats_dart_facts` base views for the detailed statistics pages, built by `0043` (D364, 2026-09-26) | canonical | ~3.5k |
| `05-Views/01-General-Views.md` | `v_player_visit_facts`/`v_player_leg_facts` view contracts for career-wide statistics (2026-09-06) | canonical | ~0.7k |
| `06-Database-Specification.md` | Cross-layer invariants + index into `06-Spec/` chapters | canonical | ~2.2k |
| `06-Spec/01-Reference-Layer.md` | Lookup tables (game_types … duration_types); `ruleset_version_capabilities` and why capability is keyed on ruleset version (2026-08-08); `exercise_types`/`exercise_ruleset_versions` and the `EXERCISE_SECTION` stage type (D263, 2026-09-10) | canonical | ~2.9k |
| `06-Spec/02-Template-Layer.md` | Templates, routines, configuration presets; `exercise_type_id` discriminator, nullable `game_type_id`, `default_configuration`/`routine_steps.configuration` (D263, 2026-09-10); Phase 1 custom-routine-builder roadmap noted (2026-09-17); roadmap corrections — ownership `CHECK`, owner-aware `v_routine_execution`, stale `09-training-routines.md` path fixed (D321, 2026-09-18); `training_schedules`/`training_schedule_days` sections — weekday SMALLINT+CHECK, rest as absent row, RESTRICT on the routine FK (D342, 2026-09-20) | canonical | ~3.9k |
| `06-Spec/03-Player-Layer.md` | players, player_settings — settings shipped, read through `v_player_settings`, capture/input mode FKs added by `0017`; profile (darts equipment) shipped, read through `v_player_profile` (2026-08-15) | canonical | ~1.1k |
| `06-Spec/04-Runtime-Layer.md` | Activities, sessions, stages, turns, darts, idempotency; turn/dart score semantics, `location_x`/`location_y` shipped, VISUAL_BOARD capture depth, QUICK_SCORE-scoped 501 bust limitation + `total_score` bust carve-out (2026-08-05); generalised `exercise_sessions` (nullable game/capture pairs under two CHECKs) and `activity_configurations` (D263, 2026-09-10) | canonical | ~4.9k |
| `06-Spec/05-Read-Model-Layer.md` | View contracts (`v_*`), incl. `v_dart_locations` (2026-08-05), `v_player_settings` (2026-08-08), the owner-scoping both dart views gained in `0023` (2026-08-21), and `v_double_out_checkout_darts` for 501 checkout accuracy (2026-09-05); which `*_key`/`*_name` columns are nullable after `0033` (2026-09-16); `0036`'s `starting_score` on `v_double_out_checkout_darts` and the step-resolution columns on `v_routine_execution` (D298, 2026-09-17) | canonical | ~4.2k |
| `06-Spec/06-Relationships-and-Evolution.md` | Relationship matrix, full ERD, future expansion | canonical | ~1.7k |
| `07-Data-Model-Review.md` | Design-gate record (superseded decisions inside) | historical | ~2.3k |
| `08-Physical-Schema-Mapping.md` | Design-gate record | historical | ~2.2k |
| `09-Pre-Implementation-Review.md` | Design-gate record | historical | ~1.5k |
| `10-Database-Agent-Guide.md` | Condensed DB rules for agents; ID strategy owner; new-game-type checklist through engine registration (2026-07-26); `trg_`/`fn_` trigger naming noted, the "docs do not assume database triggers" line corrected now that `0038` is one (D334, 2026-09-19) | canonical | ~2.7k |
| `11-Neon-Integration.md` | Neon topology, branches, dbmate/drizzle workflow; `env:dev`/`env:prod` PUBLIC_ mirror; per-branch trusted origins (D172, 2026-07-29); CI applies production migrations behind a Neon-branch rehearsal, `:ci` script variants, required secrets (D288, 2026-09-17); Schema Drift on the shared `dev` branch — `db:drift`, what `dbmate status` cannot see (D333, 2026-09-19) | canonical | ~3.1k |

## API (`06-API/`) and Frontend (`07-Frontend/`)

| File | Answers | Status | ~Tokens |
| ---- | ------- | ------ | ------- |
| `06-API/00-Overview.md` | Frozen v1 API baseline: runtime, routes, auth, envelopes; `/api/auth/*` same-origin proxy (D172, 2026-07-29); `/api/players/me/settings` routed (D195, 2026-08-08); `GET /api/statistics/overview` routed (2026-09-06); activity grouping restated as shipped — one activity spans N step sessions (D301, 2026-09-17); `/execution` alias dropped unbuilt (D321, 2026-09-18); the routine reads/writes and `GET /api/exercise-templates` restated as shipped, closing #483 (D306/D321/D336, 2026-09-19); session participants restated as shipped — 1-4 seats, `GUEST`/`DARTBOT` implemented per `SEAT_CAPS`, closing #406 (D350, 2026-09-21); detailed-statistics routes planned, `trends`/`checkouts` replaced by the section route (D365, 2026-09-26) | canonical | ~4.6k |
| `06-API/01-Implementation-Strategy.md` | REST endpoints, Cloudflare + Neon constraints | canonical | ~2.1k |
| `06-API/02-Middleware-And-Layering.md` | Middleware, `locals.auth`, folder layering, API error boundary; `api-auth-proxy` route class (D172, 2026-07-29) | canonical | ~3.0k |
| `06-API/03-Shared-Conventions.md` | Envelope, headers, pagination, error registry; type-raising governs type imports, value imports exempt (2026-07-26); `X-Request-Id` emitted by the envelope builders, header contract scoped to envelope responses (D300, 2026-09-17) | canonical | ~4.1k |
| `06-API/04-Endpoint-Contracts.md` | Per-domain endpoint contracts (2026-07-22); Player Settings `GET`/`PATCH /api/players/me/settings` incl. the uncapable-pair `VALIDATION_FAILED` case (D195, 2026-08-08); `POST /sessions`' `SEAT_CAPS` table (2026-08-22); Statistics Overview contract (2026-09-06); `VISUAL_BOARD` added to the `inputModeKey` contract and capture/input pairing corrected against seed `0007`, activity grouping restated as shipped (D301, 2026-09-17); roadmap corrections — start by `routineTemplateId`, owner/description fields on the routine DTOs, `ExerciseTemplateCatalogEntry`, `NOT_FOUND`/`VALIDATION_FAILED` mapping, `/execution` dropped (D321, 2026-09-18); the three `GET /api/routines*`/`/exercise-templates` reads and the Custom Routine Write Contracts section restated as shipped, closing #483; a non-offerable `exerciseTemplateId` documented as sharing the "unknown" reason with a genuinely unknown one (2026-09-19); new "Training Schedules" section — full per-endpoint contract for `/api/schedules*`, the client-side "today" note, and `DELETE /api/routines/:routineId`'s new "routine in use" rejection (D342/D343, 2026-09-20) | canonical | ~11.7k |
| `07-Frontend/00-Overview.md` | Client integration, state ownership, handbook index (2026-07-17); routine builder paragraph restated as shipped — move-up/move-down reorder, `@alpinejs/sort` declined for now (D337, 2026-09-19) | canonical | ~3.6k |
| `07-Frontend/01-Rendering-Strategy.md` | Prerender-default, middleware, client auth gate (D98), route classes; same-origin auth client (D172, 2026-07-29); `/training` classified + `/statistics` restated as shipped (D258, #350, 2026-09-17); `/training` protected-prefix row corrected to the shipped `/training/routines/{new,detail,edit,play}` routes, replacing the retired static `/training/balanced-training` pair (2026-09-19) | canonical | ~2.4k |
| `07-Frontend/02-Folder-Structure.md` | `app/src/` tree, aliases, suffixes; cross-runtime `lib/game/rulesets/` (2026-07-26); `modules/dartbot/` + `.strategy.module.ts` registered (2026-09-01); single-route-class carve-out for Trivia (2026-09-09); nine missing `lib/`/`modules/` trees + three missing aliases added, alias table now gate-enforced (D302, #347, 2026-09-17) | canonical | ~2.4k |
| `07-Frontend/03-Alpine-Patterns.md` | Alpine factory, stores, forms, `$persist` (D120 per-field factory), recovery/hard-gate; UI module instances held off the reactive object (D187, 2026-08-07) | canonical | ~3.2k |
| `07-Frontend/04-Modules-And-OOP.md` | OOP boundary, portable UI kit, `GameEngine` contract members (derived-value returns, undo depth) + engine anti-patterns (2026-07-26); Non-Game Client Tools exception for Trivia (2026-09-09); `modules/exercise`, `modules/training`, `modules/stats` rows added (#347, 2026-09-17) | canonical | ~2.6k |
| `07-Frontend/05-Astro-Components.md` | `.astro` authoring: frontmatter order, props, class composition, slots; template `{/* */}` comments; Prettier `singleAttributePerLine` (2026-07-21) | canonical | ~2.1k |
| `07-Frontend/06-Test-Strategy.md` | Shared-mock promotion rule, full-suite-always-runs policy (2026-07-16); fallow duplication-detection known limitation, found investigating F42 (2026-09-02); rendered-SQL convention for repository write paths, D303 (2026-09-17); wrapped-store seam tested through the real store, D304 (2026-09-17) | canonical | ~1.9k |
| `07-Frontend/07-Style-Guide.md` | Sky/glass/surface visual contract: tokens, primitives, typography, motion, a11y; Tailwind v4 utility syntax section — no important modifier, either form, arbitrary negatives (D226 supersedes D175, 2026-08-21); top safe-area inset noted alongside `h-dvh` (D174, 2026-07-29); container-query sizing for variable-length content (D262, 2026-09-09); no `height` beside `flex-1` (D326, 2026-09-19); the 40% play-screen scoreboard band (D327, 2026-09-19) | canonical | ~4.4k |
| `07-Frontend/08-Component-Inventory.md` | Every shared `.astro` component, its purpose and key props; check before hand-rolling markup; `IconBtn.astro` added (2026-08-21); `StatCard.astro`/`StatCardSkeleton.astro` added (2026-09-08); `BlockedStepModal.astro` added (2026-09-16); `components/layout/training/` split into its real `routines/`/`exercises/`/`trivia/` subheadings, all three (closes #423), `RoutineCard`/`RoutineBuilder`/`RoutineStepRow`/`ExercisePicker`/`QuickSubtract` rows added, `RoutineDetail.astro` rewritten data-driven (2026-09-19); new `components/layout/home/` subheading, `WeekdayStrip.astro` row added (2026-09-22); `StatCard`/`StatCardSkeleton` rows removed with the Statistics page stat cards (2026-09-24) | canonical | ~3.9k |
| `07-Frontend/09-Adding-A-Game.md` | The 26-file fan-out a new game requires, the six shared registries that fail silently, `bobs27` as the reference exemplar, the route-slug/code-slug rule, and the three setup-controller opt-outs (2026-08-20) | canonical | ~2.1k |
| `07-Frontend/10-Frontend-Agent-Guide.md` | Condensed frontend agent rules; comment/format checklist; TS JSDoc-above convention (2026-07-21) | canonical | ~2.1k |

## SQL (`database/`)

| File | Answers | Status |
| ---- | ------- | ------ |
| `README.md` | Directory layout, apply order | canonical |
| `migrations/0001`–`0042` | Applied schema chain — never modify; `0023` scopes the two dart analytics views to the owning participant (D222, 2026-08-21); `0024` adds `v_double_out_checkout_darts`, scoped to 501 VISUAL_BOARD sessions only (D256, 2026-09-05); `0025`/`0026` add `v_player_visit_facts`/`v_player_leg_facts` for career-wide statistics (D257, 2026-09-06); `0027`–`0031` generalise the exercise layer for non-game exercises — exercise types, exercise ruleset versions, nullable game columns under two independent CHECKs, routine-step configuration, and the training configuration snapshot (D263, 2026-09-10); `0028` adds `exercise_templates.exercise_type_id` nullable (not `NOT NULL` as first written — `seeds/0002` already populates the table) and `0032` promotes it to `NOT NULL` after `seeds/0014` backfills it, the same shape `0029`/`0031` use for `exercise_sessions.exercise_type_id` (D266, 2026-09-10); `0033` LEFT JOINs every read-model view onto the lookups `0028`/`0029` made nullable, so a training exercise session is visible to statistics, history and active-session reads instead of being dropped (D284, 2026-09-16); `0035` adds `exercise_templates.exercise_ruleset_version_id` under a composite FK on `(exercise_type_id, exercise_ruleset_version_id)`, so a routine step resolves one ruleset version rather than every version of its exercise type, backfilled by `seeds/0019` (D295, 2026-09-17); `0036` recreates `v_double_out_checkout_darts` with `starting_score` and `v_routine_execution` with everything a routine step resolves from, so the statistics and routine reads run through their own read models instead of around them (D298, 2026-09-17); `0038` adds the ownership CHECK, the chain's first trigger (duration-bound, D305/D336), and the owner-aware `v_routine_execution`/`v_exercise_template_catalog` (2026-09-19); `0039` drops `v_double_out_checkout_darts` and replaces it with `v_x01_checkout_darts`, widened to 501/TUOD/121 `VISUAL_BOARD` sessions and projecting facts only — no running total — since the ladder fold now lives in the app (D338, 2026-09-19); `0040` adds `exercise_templates.game_ruleset_version_id` under a composite FK on `(game_type_id, id)` mirroring `0035`, with a one-directional CHECK (a pin names its game; a GAME template may pin nothing, as `seeds/0002`'s permanently-unpinned templates do), and recreates both routine views with `game_ruleset_version_key` (D339, 2026-09-20); `0041` adds `training_schedules`/`training_schedule_days` (one active schedule per player via a partial unique index, `day_of_week` SMALLINT+CHECK rather than a lookup table, `RESTRICT` on the routine FK) and `v_training_schedules`/`v_training_schedule_days` (D342, 2026-09-20) | canonical (applied) |
| `database/migrations/0043_stats_base_views.sql` | `v_stats_session_facts`/`v_stats_dart_facts`, the two owner-scoped base views for the detailed statistics pages, plus `idx_exercise_sessions_player_game_completed` (statistics phase 1, D364, 2026-09-26) | canonical (unapplied — lands in production on this PR's deploy) |
| `seeds/0001`, `0002` | Reference data + default templates | canonical |
| `database/seeds/0003_game_engine_reference.sql` | `BOBS27` + `DOUBLES_TRAINING` game types, features, ruleset versions, presets (2026-07-26) | canonical |
| `database/seeds/0004_score_training_minutes_preset.sql` | Score Training minutes preset realigned to 5 (2026-07-31) | canonical |
| `database/seeds/0005_visual_board_input_mode.sql` | `VISUAL_BOARD` input mode (2026-08-05) | canonical |
| `database/seeds/0006_single_band_dart_zones.sql` | `INNER_SINGLE` (id 7) / `OUTER_SINGLE` (id 8) dart zones (D191, 2026-08-05) | canonical |
| `database/seeds/0007_ruleset_version_capabilities.sql` | The 8 declared ruleset-version/capture-mode/input-mode triples, mirroring `capabilities.ts`; must run before migration `0020` (D196, 2026-08-08) | canonical |
| `database/seeds/0016_switching_double_pattern_exercise_types.sql` | `SWITCHING`/`DOUBLE_PATTERN` exercise types + their `SWITCHING_V1`/`DOUBLE_PATTERN_V1` ruleset versions — catalog rows only, mirroring the `0014`/`0015` WARM_UP split (2026-09-11) | canonical |
| `database/seeds/0017_balanced_training_routine.sql` | Second system routine, Balanced Training (4 steps, 30 minutes); reuses `0015`'s Warm-Up template via an in-place `default_configuration` `UPDATE` (fixed `durationSeconds` → proportional `weight` per phase) rather than a second template (2026-09-11) | canonical |
| `database/seeds/0019_exercise_template_ruleset_versions.sql` | Backfills `exercise_ruleset_version_id` on the three non-game system templates to their own v1 ruleset; the GAME template stays NULL. In-place `UPDATE`, the second case of the pattern `0017` established; must run before a routine using those templates can start (D295, 2026-09-17) | canonical |
| `database/seeds/0020_finishing_default_configuration.sql` | Backfills the Finishing (TUOD) system template's `default_configuration` from Balanced Training's own step override, so a user-authored routine step referencing it does not resolve to `{}`; in-place `UPDATE`, the third case of the `0017`/`0019` pattern (D336, 2026-09-19) | canonical |
| `database/seeds/0021_exercise_template_game_rulesets.sql` | Backfills the Finishing (TUOD) system template's `game_ruleset_version_id` to `TUOD_V1` (migration `0040`); in-place `UPDATE` resolved by `implementation_key`, the fourth case of the `0017`/`0019`/`0020` pattern; runs after `0040` in deploy order, and needs no staged constraint to do so — `0040`'s CHECK only requires a pin to name its game (D339, 2026-09-20) | canonical |
| `database/seeds/0023_target_scoring_exercise_type.sql` | `TARGET_SCORING` exercise type, its `TARGET_SCORING_V1` ruleset version and the "Target Scoring" system template (`{"targets":[20,19,18,25]}`, pinned to that version) — reaches the routine builder via `v_exercise_template_catalog`; no routine seeded (2026-09-23) | canonical |
| `database/seeds/0024_switching_target_scoring_exercise_type.sql` | `SWITCHING_TARGET_SCORING` exercise type, its `SWITCHING_TARGET_SCORING_V1` ruleset version and the "Switching Target Scoring" system template (`{"targets":[20,19,18]}`, pinned to that version) — reaches the routine builder via `v_exercise_template_catalog`; no routine seeded (2026-09-23) | canonical |
| `database/seeds/0025_score_threshold_exercise_type.sql` | `SCORE_THRESHOLD` exercise type, its `SCORE_THRESHOLD_V1` ruleset version and the "65 or More" system template (`{"threshold":65}`, pinned to that version) — reaches the routine builder via `v_exercise_template_catalog`; no routine seeded (2026-09-23) | canonical |
| `database/seeds/0026_warm_up_advanced_template.sql` | "Warm-Up Advanced" system template: a second `WARM_UP` template pinned to `WARM_UP_V1`, sections narrowed to 20, 3, 6, 11, then the bull — reaches the routine builder via `v_exercise_template_catalog`; no routine seeded (D359, 2026-09-23) | canonical |
| `database/seeds/0027_around_the_clock_v2_game_engine_reference.sql` | `AROUND_THE_CLOCK_V2` ruleset version under the Around the Clock game type — direction, odds first, outer single only, 1/2/3-dart difficulty with step back, untimed or 3–30 min timed run; capability rows appended to `0007` (D360, 2026-09-23) | canonical |
| `database/seeds/0028_around_the_clock_routine_templates.sql` | Three GAME `exercise_templates` pinned to `AROUND_THE_CLOCK_V2` — Around the Clock 1, 2 and 3 darts, outer single only, 10-minute default — reaching the routine builder via `v_exercise_template_catalog` (D360, 2026-09-24) | canonical |
| `database/seeds/0029_bullseye_checkout_exercise_type.sql` | `BULLSEYE_CHECKOUT` exercise type, its `BULLSEYE_CHECKOUT_V1` ruleset version and the "Bullseye Checkouts" system template (`{"startScore":81}`, pinned to that version) — reaches the routine builder via `v_exercise_template_catalog`; no routine seeded (2026-09-24) | canonical |
| `database/seeds/0030_bull_up_exercise_type.sql` | `BULL_UP` exercise type, its `BULL_UP_V1` ruleset version and the "Bull Up Practice" system template (`{}`, pinned to that version) — reaches the routine builder via `v_exercise_template_catalog`; no routine seeded (D362, 2026-09-24) | canonical |
| `database/seeds/0031_remove_default_routines.sql` | Deletes the "Standard Practice" and standalone "Warm-Up" system routines (steps first) from databases that hold them; a routine a schedule still names is kept with a NOTICE; the Warm-Up exercise template stays (D363, 2026-09-25) | canonical |
| `database/seeds/0022_routine_game_templates.sql` | Two more GAME exercise templates for the routine picker: "Score Training (timed)" pinned to `SCORE_TRAINING_V1`, "121 (timed)" pinned to game type `ONE_TWENTY_ONE`'s `121_V2` (the plan this seed was drafted from named the wrong lookup column — `'121'` is only the display name, `ONE_TWENTY_ONE` is `implementation_key` — corrected in the committed file) (2026-09-20) | canonical |
| `database/verification/0018_visual_board_checks.sql` | Rollback-safe live-database checks for the visual-board capture core: `chk_dart_location_pair`, `v_dart_locations` angles, bust divergence; run via `npm run db:verify` (D193, 2026-08-08) | canonical |
| `database/verification/0007_capability_seed_checks.sql` | Seed `0007` row count, per-triple resolution, zero undeclared `exercise_sessions` (the `0020` precondition — read its `undeclared`/`total` detail, an empty table passes trivially), parity with `capabilities.ts` (30 checks) (D196, 2026-08-08; both `VALUES` lists resynced to `capabilities.ts` and gated by `capability-seed-parity.test.ts` — D294, 2026-09-17) | canonical |
| `database/verification/0020_capability_fk_checks.sql` | `fk_sessions_capability` exists over the exact composite columns, refuses an undeclared mode combination, permits a declared one (4 checks) (D196, 2026-08-08) | canonical |
| `database/verification/0021_player_settings_checks.sql` | `v_player_settings` column set, id→key translation, no row for a player with no settings, `LEFT JOIN` preserved under NULL mode ids (7 checks) (D195, 2026-08-08) | canonical |
| `database/verification/0022_player_profile_checks.sql` | `v_player_profile` column set, configured/unconfigured player resolution, `chk_players_darts_description_not_empty`/`chk_players_darts_weight_grams_range` fire correctly (11 checks) (2026-08-15) | canonical |
| `database/verification/0023_target_scoring_seed_checks.sql` | Live-DB proof of seed `0023`: type published, v1 ruleset linked, template pinned with the default target list, catalog row present (2026-09-23) | canonical |
| `database/verification/0024_switching_target_scoring_seed_checks.sql` | Live-DB proof of seed `0024`: type published, v1 ruleset linked, template pinned with the default target sequence, catalog row present (2026-09-23) | canonical |
| `database/verification/0025_score_threshold_seed_checks.sql` | Live-DB proof of seed `0025`: type published, v1 ruleset linked, "65 or More" template pinned with `{"threshold":65}`, catalog row present (2026-09-23) | canonical |
| `database/verification/0026_warm_up_advanced_seed_checks.sql` | Live-DB proof of seed `0026`: "Warm-Up Advanced" pinned to `WARM_UP_V1`, single-number sections, both `WARM_UP` templates in the catalog (2026-09-23) | canonical |
| `database/verification/0027_around_the_clock_v2_capability_checks.sql` | Live-DB proof of seeds `0027`+`0007`: both `AROUND_THE_CLOCK_V2` mode pairs resolve, zero undeclared `exercise_sessions` (2026-09-23) | canonical |
| `database/verification/0028_around_the_clock_routine_template_checks.sql` | Live-DB proof of seed `0028`: three system GAME templates pinned to `AROUND_THE_CLOCK_V2`, six-key defaults, all offered by the catalog (2026-09-24) | canonical |
| `database/verification/0029_bullseye_checkout_seed_checks.sql` | Live-DB proof of seed `0029`: type published, v1 ruleset linked, "Bullseye Checkouts" template pinned with `{"startScore":81}`, catalog row present (2026-09-24) | canonical |
| `database/verification/0030_bull_up_seed_checks.sql` | Live-DB proof of seed `0030`: type published, v1 ruleset linked, "Bull Up Practice" template pinned with `{}`, catalog row present (2026-09-24) | canonical |
| `database/verification/0031_remove_default_routines_checks.sql` | Live-DB proof of seed `0031`: both routines and their steps gone, Warm-Up exercise template kept (2026-09-25) | canonical |
| `database/verification/0023_owner_scoped_dart_view_checks.sql` | `v_dart_analytics`/`v_dart_locations` return only the session owner's own dart, excluding both a GUEST and a DARTBOT participant's; `v_game_replay` deliberately returns all three (D222, D193; DARTBOT fixture added phase 5, 2026-09-01) | canonical |
| `database/verification/0039_x01_checkout_darts_view_checks.sql` | migration `0039`: `v_x01_checkout_darts` exists and `v_double_out_checkout_darts` does not, the view exposes every documented column, only 501/TUOD/ONE_TWENTY_ONE game types and only VISUAL_BOARD sessions appear, every row's participant belongs to the session's owning player, anti-vacuity guard (9 checks); replaces `0024`'s deleted script (D338, D193, 2026-09-19) | canonical |
| `database/verification/0036_read_model_view_consumers_checks.sql` | migration `0036`: the checkout-darts LEFT JOIN is still non-fanning and every checkout-darts row carries its own session's configuration snapshot (re-pointed at `v_x01_checkout_darts` when `0039` dropped `v_double_out_checkout_darts`, taking `0024`'s verification script with it), `v_routine_execution` exposes every column `findRoutineTemplateSteps` reads and returns all four Balanced Training steps without fan-out, each step's `exercise_type_key` is its template's own (8 checks) (D298, D193, 2026-09-17; narrowed D338, 2026-09-19) | canonical |
| `database/verification/0037_exercise_configuration_constraint_naming_checks.sql` | migration `0037`: `uq_exercise_configurations_exercise_session` and `fk_exercise_configurations_exercise_session` both exist on `exercise_configurations.exercise_session_id`, neither pre-rename name survives, anti-vacuity guard (4 checks) (D310, D193, 2026-09-18) | canonical |
| `database/verification/0038_custom_routine_checks.sql` | migration `0038`: `chk_routine_templates_player_ownership`, both deferred duration-bound triggers (30–60 min floor, exempting system routines, the zero-step case), `v_routine_execution`'s owner-aware columns, `v_exercise_template_catalog`'s four routine-composable templates plus at least one non-offerable one (11 checks; D336's own text still says "10 checks" — decisions are append-only, see D321's precedent) (D305, D306, D321, D193, 2026-09-19). Never executed against a live database — no `DATABASE_URL` reaches this environment (D193); written for a human to run via `npm run db:verify` | canonical |
| `database/verification/0040_exercise_template_game_ruleset_checks.sql` | migration `0040`: `game_ruleset_version_id` column shape, the composite FK rejects a ruleset version of another game, the CHECK rejects a pin naming no game while accepting a GAME template that pins nothing, seed `0021`'s Finishing backfill, the three routine-eligible templates each pinned, both routine views expose `game_ruleset_version_key`, seed `0022`'s two templates present (10 checks) (D339, D193, 2026-09-20). Never executed against a live database — no `DATABASE_URL` reaches this environment; written for a human to run via `npm run db:verify` | canonical |
| `database/verification/0043_stats_base_views_checks.sql` | migration `0043`: `v_stats_session_facts` owner-scopes turn/dart/score counts and derives `context_key` (STANDALONE vs ROUTINE, no fan-out), an abandoned zero-turn session zeroes every count, an ACTIVE session and a training exercise session (NULL `game_type_id`) are absent from both views, `v_stats_dart_facts` owner-scopes darts and excludes non-`VISUAL_BOARD` sessions, `idx_exercise_sessions_player_game_completed` exists, anti-vacuity guard (11 checks) (D364, D193, 2026-09-26). Run against a throwaway local PostgreSQL 16 (chain + seeds applied) — all 11 checks passed | canonical |

## Game engine code + mechanical guards

Registered for discoverability, not as reading material — the rules live in `04-Architecture-patterns.md` Pattern 18 and `07-Frontend/04-Modules-And-OOP.md`. (2026-07-26)

| File | Answers | Status |
| ---- | ------- | ------ |
| `app/src/lib/game/rulesets/types.ts` | One `.strict()` Zod config schema per ruleset version (cross-runtime) | canonical |
| `app/src/lib/game/rulesets/config-codec.ts` | snake_case wire config ↔ camelCase client snapshot | canonical |
| `app/src/lib/game/rulesets/refinement-contract.ts` | Declared accept/reject boundaries every schema refinement must hold | canonical |
| `app/src/lib/game/rulesets/capabilities.ts` | `RULESET_CAPABILITIES` + `supportsMode` / `capableRulesets` (exact pair, gates session creation) and `supportsCaptureMode` (capture mode alone, gates games-page card visibility only — 2026-08-12) — the cross-runtime code-side declaration of which capture/input mode pairs each ruleset version implements; mirrored by seed `0007` (D196, 2026-08-08); `RULESET_DARTBOT` + `supportsDartbot` + `DEFAULT_BOT_LEVEL` — which eleven ruleset versions currently admit a DartBot opponent seat, read by both the setup screens (chooser gate) and `session-seats.service.ts` (server-side gate) (2026-09-02; Singles Training admitted 2026-09-03; 121/TUOD/Score Training admitted 2026-09-03; `SHANGHAI_V2` admitted 2026-09-07, D259; `SINGLES_V3` admitted 2026-09-19, D329) | canonical |
| `app/src/lib/game/rulesets/games-visibility.ts` | Filters `GameCardDescriptor`s by the player's app mode via `supportsCaptureMode` (capture mode alone, not the exact pair — a ruleset like `BOBS27_V1` that pairs `RECREATIONAL` with `DETAILED_DARTS` instead of the app's actual `QUICK_SCORE` setting must still show a card); drives the games page's cards, banner and empty state (2026-08-08; capture-mode-only filter 2026-08-12) | canonical |
| `app/src/lib/game/games-index.data.ts` | `GAME_CARDS` — the games-page card list the visibility filter is applied to (2026-08-08) | canonical |
| `app/src/lib/game/around-the-clock-play.data.ts` | `aroundTheClockPlay()` — Around the Clock's play-page game loop for `AROUND_THE_CLOCK_V1`: the BULL-terminated `numbersPath()` progression (`targetAt()`/`isAroundTheClockHit()`), a local `replayHits()` that re-folds the seat state per dart so hit/miss (and therefore `previewSegments()`/`accuracyDisplay()`) is judged against the dart's own live target rather than `hitZoneKey` alone — a VISUAL_BOARD dart lands wherever the player touched, so zone alone cannot tell the active target from the wrong number; `init`/`retryReconciliation`/`commitDart`/`undoVisit`/`uploadAndCompleteSession`/`back`/`abandonAndExit`/`playAgain` delegate to `play-lifecycle.ts` (owns only its `resumeEngine`, `resultsSnapshot`-builder and `instanceof`-narrowing callbacks); a seated `DARTBOT` opponent throws automatically via `maybeRunBotVisit()`/`throwBotDart()`, reading its target off the shared numbers path (closes issue #283) (2026-09-16; inventory row added 2026-09-18) | canonical |
| `app/src/lib/game/bobs27-play.data.ts` | `bobs27Play()` — Bob's 27's play-page game loop (record → mirror → complete, both recreational tap and board dart paths funnel through one `commitDart`), the board's 1.5s reveal-then-clear (`visitMarkers()` override), `currentTargetLabel()`/`currentScore()`/`previewSegments()` display reads; no checkout/bust confirm dialogs (2026-08-12; target-label/preview/observation logic moved to `doubles-path-play.ts` 2026-08-13; a seated `DARTBOT` opponent now throws automatically via `maybeRunBotVisit()`/`throwBotDart()`, wired into `init`/`commitDart`/`undoVisit` — DartBot phase 6, 2026-09-01) | canonical |
| `app/src/lib/game/doubles-path-play.ts` | `doublesPathTargetLabel`/`doublesPathPreviewSegments`/`doublesPathObservation` — the target-label, visit-preview and hit/miss `DartObservation` builder for the shared BULL-terminated numeric doubles path (`doublesPath()`), deduped out of `bobs27-play.data.ts` and `doubles-training-play.data.ts` after the byte-identical duplication was flagged in review (2026-08-13) | canonical |
| `app/src/lib/game/doubles-training-setup.data.ts` | `doublesTrainingSetup()` — mirrors `singles-training-setup.data.ts`: zero editable settings, reconciles against the one seeded preset (`database/seeds/0003_game_engine_reference.sql`, id `...000010`, `{mode: EASY, order_mode: LOW_TO_HIGH}`) (2026-08-13) | canonical |
| `app/src/lib/game/doubles-training-play.data.ts` | `doublesTrainingPlay()` — Doubles Training's play-page game loop; `currentTargetLabel()`/`previewSegments()`/the tap `recordTap`'s BULL/DOUBLE mapping delegate to `doubles-path-play.ts`, and `init`/`retryReconciliation`/`commitDart`/`undoVisit`/`uploadAndCompleteSession`/`back`/`abandonAndExit`/`playAgain` delegate to `play-lifecycle.ts` (own only its `resumeEngine`, `resultsSnapshot`-builder, and `instanceof`-narrowing callbacks); `hitCount()`/`missCount()` read `engine.state().outcomes` directly (visit-level, not dart-scanned — a full-miss visit is exactly one miss, never three); synchronous `hiddenTurnKey`, no reveal-then-clear timer (`DOUBLES_TRAINING_V1` never enters `VISUAL_BOARD`); a seated `DARTBOT` opponent throws automatically via `maybeRunBotVisit()`/`throwBotDart()`, wired into `init`/`commitDart`/`undoVisit` — `throwBotDart` reads the target off `config.targetOrder`, not the default doubles path, since Doubles Training's order is configurable (2026-08-13; lifecycle delegated to `play-lifecycle.ts` 2026-08-13; DartBot wiring 2026-09-16) | canonical |
| `app/src/lib/game/play-lifecycle.ts` | `playInit`/`playRetryReconciliation`/`playCommitDart`/`playUndoVisit`/`playUploadAndCompleteSession`/`playBack`/`playAbandonAndExit`/`runPlayAgain` — the record → mirror → complete lifecycle now shared by all 9 rulesets, generic over `<TConfig, TEngine extends GameEngine<DartObservation, unknown>, TResults>` via `PlayLifecycleContext` in `lib/game/types.ts`; deduped out of `doubles-training-play.data.ts`/`singles-training-play.data.ts` after their overlap crossed `fallow`'s CI duplication threshold (D209, 2026-08-13), and adopted by `five-oh-one-play.data.ts`/`one-twenty-one-play.data.ts`/`score-training-play.data.ts`/`tuod-play.data.ts` — each keeps only its own `resultsSnapshot`-builder and `instanceof`-narrowing callbacks, plus (outside the two rulesets D309 covers) its own `resumeEngine`, plus `commitDart`/board-input logic `play-lifecycle.ts` never owned (F27 play-data half, 2026-09-03). `playAbandonAndExit`'s optional `onAbandoned?: () => void` and `playUploadAndCompleteSession`'s optional `resolveFinalState?: () => ReturnType<TEngine["state"]> | null` and `runPlayAgain`'s optional `resetLocalState?: () => void`/`afterEngineReady?: (engine: TEngine) => void` exist for that adoption — all four callbacks are parameterless-of-context by design (the generic `PlayLifecycleContext` erases each ruleset's own extra fields), so a caller closes over its own `this` rather than receiving a passed context; `undoToActiveSeat()`, `playRunBotVisualBoardVisit()` (the `botThrowing` + post-delay re-entrancy guards) and `playFoldBotQuickScoreVisit()` — the generic DartBot play-loop mechanism, wired onto Bob's 27 (D252, DartBot phase 6, 2026-09-01), then Shanghai and Singles Training, then 121/TUOD/Score Training (all 2026-09-03), then Around the Clock and Doubles Training (2026-09-16); `playFoldBotQuickScoreVisit()`'s `throwDart` widened to `(state: TState) => DartObservation` so a strategy can re-target between darts off the scratch engine's own live state, wired onto 501 (D253, DartBot phase 7, 2026-09-02); gains `resumeGameEngine<TConfig, TEngine>` — the generic form of each play-data file's own `resumeEngine`, rebuilding a ruleset's engine from the persisted store or returning null, with the caller's `instanceof` guard passed in because the registry hands back a type-erased engine (D309, 2026-09-18) | canonical |
| `app/src/lib/game/play-bot-seat.ts` | `findBotSeat`/`botDartIndex` — the DARTBOT seat lookup and that seat's own cumulative dart count, extracted from `app/src/lib/game/score-training-play.data.ts` and `app/src/lib/game/tuod-play.data.ts` where both were byte-identical. `DartbotSeat` is declared in `app/src/lib/game/types.ts`, not here, because `scripts/check-type-barrels.sh` forbids an implementation file from declaring an exported type (D309, 2026-09-18) | canonical |
| `app/src/lib/game/play-countdown.ts` | `formatRemaining`/`startCountdown`/`maybeResumeCountdown` — the MINUTES countdown mechanics, generic over any store carrying the four timer fields (`CountdownGame`) and any engine exposing `expireTimer()` (`ExpirableEngine`); extracted from the same two play-data files, where the bodies were byte-identical and only the type annotations differed. `app/src/lib/game/one-twenty-one-play.data.ts` still carries its own copy (D309, 2026-09-18) | canonical |
| `app/src/lib/game/singles-training-play.data.ts` | `singlesTrainingPlay()` — Singles Training's play-page game loop; target-aware `recordTap(ring)` (BULL visit maps SINGLE/DOUBLE → OUTER_BULL/INNER_BULL, TREBLE rejected) and a locally-duplicated `trainingPointsFor` (the engine doesn't export its own — documented seam) stay local, but `init`/`retryReconciliation`/`commitDart`/`undoVisit`/`uploadAndCompleteSession`/`back`/`abandonAndExit`/`playAgain` delegate to `play-lifecycle.ts` (own only its `resumeEngine` and `resultsSnapshot`-builder callbacks); session-total hit-count getters `missCount()`/`singleCount()`/`doubleCount()`/`trebleCount()` classifying `hitZoneKey` across all turns, exhaustive over `DartZoneKey`'s 8 members (bull `OUTER_BULL`/`INNER_BULL` fold into single/double, mirroring `trainingPointsFor`'s scoring equivalence); a seated `DARTBOT` opponent now throws automatically via `maybeRunBotVisit()`/`throwBotDart()`, wired into `init`/`commitDart`/`undoVisit` — `throwBotDart` reads the target off `config.targetOrder`, not the default path, since Singles Training's order is configurable (2026-08-13; lifecycle delegated to `play-lifecycle.ts` 2026-08-13; DartBot wiring 2026-09-03) | canonical |
| `app/src/modules/game/interfaces.ts` | `GameEngine` / `GameEngineFactory` — the contract itself | canonical |
| `app/src/modules/game/engine.registry.ts` | `rulesetVersionKey` → engine factory lookup | canonical |
| `app/src/modules/game/board-progression.module.ts` | Shared board paths and dartboard arithmetic | canonical |
| `app/src/modules/game/client-key.module.ts` | `newClientKey()` — transient batch correlation token, explicitly not an entity id (D190) (2026-08-05) | canonical |
| `app/src/modules/game/checkout-path.module.ts` | Standard 2-170 double-out checkout chart; `null` for bogey numbers (2026-08-01); `checkoutPathWithin()` returns a route that fits the darts a visit has left — the chart route when it fits, otherwise the shortest true route (D330, issue #291, 2026-09-19) | canonical |
| `app/src/modules/game/checkout-reachability.module.ts` | True double-out reachability over every legal dart value: `minimumCheckoutDarts()` (the fewest darts that can finish a score, `null` for a bogey number) and `isCheckoutReachable()`; the engine-side gate `checkout-path.module.ts`'s curated chart cannot supply (issue #247, 2026-09-07; registered 2026-09-19, issue #499) | canonical |
| `app/src/modules/game/checkout-darts.module.ts` | Which darts-to-finish / darts-at-a-double answers a checkout may claim, read off the chart, plus the rejection all three quick-score engines share (D217) (2026-08-20) | canonical |
| `app/src/modules/game/events.payload.module.ts` | The one `buildEventsBatch` for every game; reads each turn's own `participantRef` so refs vary within one batch (D220, 2026-08-21) | canonical |
| `app/src/modules/game/seat-rota.module.ts` | Pure seat derivation shared by every engine: `activeSeat` (from the fact log, never stored), `startingSeatFor`, `seatOf`; serves both `SHARED` and `PER_SEAT` stage shapes (D220, 2026-08-21) | canonical |
| `app/src/modules/game/turn-log.module.ts` | Pure turn-log mechanics every engine shares: `cloneTurns`, `sumDartScores`, `dartsThrownBy`, `openOrCreateTurn`/`appendCompletedTurn`, `openVisit`, `resolveObservation`/`appendResolvedDart`, `appendObservedDart`/`doubleTargetIntent`, and the three undo shapes (`undoLastDart`, `undoLastUnit`, `undoStagedTurn`) (2026-08-23) | canonical |
| `app/src/modules/game/seat-state.module.ts` | Pure per-seat derivation every 1v1 engine shares: `foldSeatStates` (replay the log once per seat), `activeSeatState`, `otherSeatsComplete`, `durationSeatComplete`, `completedByIndex` (2026-08-23) | canonical |
| `app/src/modules/training/exercises/interfaces.ts` | `ExerciseEngine<TState>`/`ExerciseEngineFactory` (phase-driven, `advance()`) plus `DartExerciseEngine<TState>`/`DartExerciseEngineFactory` (dart-driven, `record(observation)`) — siblings, neither built on the other nor on `GameEngine` (D264; `DartExerciseEngine` added D267, 2026-09-11) | canonical |
| `app/src/modules/training/exercises/engine.registry.ts` | `ExerciseRulesetVersionKey` → `ExerciseEngineFactory` lookup for the phase-driven family (`WARM_UP_V1`), the Warm-Up-era sibling of `dart-engine.registry.ts`; `registerExerciseEngineFactory` throws on a duplicate key, `resetExerciseEngineRegistry()` is test-only, and the map stores the type-erased `ExerciseEngineFactory<unknown, unknown>` so a concrete factory upcasts without an unsafe cast (2026-09-10; inventory row added 2026-09-18) | canonical |
| `app/src/modules/training/exercises/types.ts` | The per-engine state shapes every exercise engine derives on `state()` — `WarmUpState`, `SwitchingState`, `DoublePatternState`; none carries elapsed time or mutable engine state (the clock lives in the caller, the rest is folded from `facts()`), keeping each engine deterministic with respect to its configuration (`09-Training/01-Routines.md` §9) (2026-09-10; `SwitchingState`/`DoublePatternState` 2026-09-11; inventory row added 2026-09-18) | canonical |
| `app/src/modules/training/exercises/warm-up.engine.module.ts` | `WarmUpEngine`/factory for `WARM_UP_V1`: ordered timed sections, no dart input, no score; one flat `EXERCISE_SECTION` stage per section entered, caller-driven transitions via `advance()` (the engine owns no clock), and `resolveWarmUpPhaseDurations()` splitting the routine step's `stepDurationSeconds` proportionally to each `phase.weight` — rounded per phase, so the sum can drift a few seconds from the step total (2026-09-10; inventory row added 2026-09-18) | canonical |
| `app/src/modules/training/exercises/dart-engine.registry.ts` | `ExerciseRulesetVersionKey` → `DartExerciseEngineFactory` lookup, mirroring `engine.registry.ts` for the dart-driven engine family (`SWITCHING_V1`/`DOUBLE_PATTERN_V1`) (2026-09-11) | canonical |
| `app/src/modules/training/exercises/solo-participant.module.ts` | `SOLO_PARTICIPANT_REF` — the fixed `turns` participant every solo exercise session logs under; training routines have no seat/opponent concept to derive one from (2026-09-11) | canonical |
| `app/src/modules/training/exercises/switching.engine.module.ts` | `SwitchingEngine`/factory for `SWITCHING_V1`: cycles a fixed `targets: number[]` list dart by dart, `scoring.{single,double,treble}` for a hit on the current target, 0 for anything else including a wrong number ("outside" by omission); exported for `expireTimer()` access (2026-09-11) | canonical |
| `app/src/modules/training/exercises/target-scoring.engine.module.ts` | `TargetScoringEngine`/factory for `TARGET_SCORING_V1`: chain of hits on one target (single 1, treble 3, double a miss; outer bull 1, bullseye 3), a miss resets it and advances only a live chain, targets cycle; `targetScoringPoints`/`targetScoringTargetLabel` shared with the play page and summary (`docs/game-rules/training/exercises/target-scoring.md`, 2026-09-23) | canonical |
| `app/src/modules/training/exercises/switching-target-scoring.engine.module.ts` | `SwitchingTargetScoringEngine`/factory for `SWITCHING_TARGET_SCORING_V1`: three-target sequence, a hit advances (chain kept across a full pass), a miss resets the chain and restarts at the first target; scores through `targetScoringPoints` (`docs/game-rules/training/exercises/switching-target-scoring.md`, D357, 2026-09-23) | canonical |
| `app/src/modules/training/exercises/score-threshold.engine.module.ts` | `ScoreThresholdEngine`/factory for `SCORE_THRESHOLD_V1` ("65 or More"): free aim, no intended target; a visit is judged after its third dart and beats when its board total reaches `threshold`; an unfinished visit at expiry is not judged (`docs/game-rules/training/exercises/score-threshold.md`, D358, 2026-09-23) | canonical |
| `app/src/modules/training/exercises/bullseye-checkout.engine.module.ts` | `BullseyeCheckoutEngine`/factory for `BULLSEYE_CHECKOUT_V1` ("Bullseye Checkouts"): every visit starts at 81, darts 1–2 free aim, dart 3 intended at the inner bull; a checkout is a 31 setup plus the bullseye; an unfinished visit at expiry is not judged (`docs/game-rules/training/exercises/bullseye-checkout.md`, D361, 2026-09-24) | canonical |
| `app/src/modules/training/exercises/bull-up.engine.module.ts` | `BullUpEngine`/factory for `BULL_UP_V1` ("Bull Up Practice"): one dart per throw, each its own completed turn, always intended at the inner bull; a throw is a bullseye, an outer bull or a miss; bulls count both rings (`docs/game-rules/training/exercises/bull-up-practice.md`, D362, 2026-09-24) | canonical |
| `app/src/modules/training/exercises/double-pattern.engine.module.ts` | `DoublePatternEngine`/factory for `DOUBLE_PATTERN_V1`: cycles a fixed `patterns: number[][]` list, one point per hit double, nothing else scores; exported for `expireTimer()` access (2026-09-11) | canonical |
| `app/src/lib/game/guest-list.ts` | `addTypedGuest` — the add-a-guest modal rule every setup screen shares: at most one guest, trimmed non-blank name, modal closed on success; `addBotOpponent` — the DartBot branch of the same single opponent slot, mutually exclusive with a guest (2026-08-23; DartBot 2026-09-01) | canonical |
| `app/src/modules/game/tuod.engine.module.ts` | Ten Up One Down: the checkout ladder folded from attempt turns (2026-07-26) | canonical |
| `app/src/lib/game/board/board-geometry.module.ts` | Regulation board radii, clockwise sector order, `classify(x, y)` → `BoardHit` (target/zone/score), `zoneCentroid`; `trebleInner` corrected 97→99mm so the treble ring is 8mm wide like the double ring (2026-08-05; radius fix 2026-08-11) | canonical |
| `app/src/lib/game/board/miss-margin.module.ts` | Distance + bearing from a dart's landing point to its declared zone's centroid; deliberately outside SQL — see `v_dart_locations` (2026-08-05) | canonical |
| `app/src/modules/game/board-input.module.ts` | Visual board input, pure: `screenToBoard` (viewport px → board mm via the SVG's inverse screen CTM), `boardPxPerMm`, `resolveMagnifierSide`/`clampMagnifierPlacement` (handedness+edge-flip side resolved once per gesture on press, clamped per move so it never swaps sides mid-drag — #83, 2026-08-11), `magnifierPlacement` (single-call wrapper of both, D199, 2026-08-09), `boardInput` press-drag-release state machine | canonical |
| `app/src/lib/game/board-input.data.ts` | Alpine/DOM bridge for the board: pointer events → controller, a fresh controller per press so a resize or rotation between gestures is picked up; reads `$store.boardInput.handedness` into each fresh controller (D206, 2026-08-11) | canonical |
| `app/src/stores/board-input.store.ts` | `boardInput` Alpine store: one `$persist` `handedness` field, local-only rather than routed through `player_settings` (D206, 2026-08-11) | canonical |
| `app/src/lib/game/session-mode-resolution.ts` | `resolveSessionModePair` (player settings + ruleset capability → the pair a new session is created with), `participantsFromSeats`, `participantsFromGuests` (the setup screen's start-time twin of `participantsFromSeats`, now also seating a DartBot opponent), and `startSessionInput` (the store payload both setup pages send, seats composed into the snapshot via the private `toSeatFacts`, which admits `PLAYER`/`GUEST`/`DARTBOT` — `seatsFromParticipants` was deleted, its `PLAYER` fallback was the two-way-collapse anti-pattern DartBot admission had to remove), shared by both play pages (2026-08-10; seats 2026-08-21; `participantsFromGuests` 2026-08-23; DartBot 2026-09-01) | canonical |
| `app/src/lib/game/setup-controller.ts` | `createPresetSetupController` — the preset setup skeleton five games share (Bob's 27, Shanghai, Around the Clock, Singles Training, Doubles Training); one seam (`configOverrides`) for the two training games' target order; 501, Score Training, 121 and TUOD deliberately opt out with hand-written setup contexts (2026-08-19; consumer list corrected against the code, issue #284, 2026-09-18) | canonical |
| `app/src/lib/game/five-oh-one-starting-score.ts` | `FIVE_OH_ONE_STARTING_SCORE_NOTICE` + `clampFiveOhOneStartingScore` — floors and clamps the custom 501 starting-score input to the inclusive 2–999 bound; a non-finite/blank input clamps to the custom field's stated default of 101, not the bare minimum of 2; mirrors `five-oh-one-legs.ts`'s clamp shape (2026-08-11) | canonical |
| `app/src/components/ui/DartBoard.astro` | Presentational dartboard SVG, millimetre `viewBox`, `role="img"` + `aria-label`; optional `boardRef` opts one instance into the controller's `x-ref` (2026-08-09) | canonical |
| `app/src/components/ui/BoardMagnifier.astro` | Zoomed inset following the fingertip: a second clipped `DartBoard`, crosshair, live resolved read with a `glass` background for legibility; `aria-hidden`, `fixed`, zoom scaled to the displayed board (D199, 2026-08-09; glass label 2026-08-11) | canonical |
| `app/src/components/ui/InfoSection.astro` | Collapsible info panel (heading + chevron-toggled description); `title` rendered verbatim, optional `id` for a caller's `aria-labelledby`; moved from `components/layout/games/setup/` so profile-page forms could reuse it outside the games domain (#84, 2026-08-11) — `HandednessForm.astro` reused it until the 2026-08-16 inline-edit redesign moved it under a plain-heading card; `AppModeForm.astro` still does | canonical |
| `app/src/components/layout/games/BoardInputPanel.astro` | The board block both play pages mount: mode gate, `touch-none` pointer surface, magnifier `x-if` guard, equal-size Undo (left) / `glass` Bounce out (right) actions built from `components/forms/Button.astro`; declares no `x-data` of its own (2026-08-09; button layout 2026-08-11; Button.astro reuse 2026-08-11) | canonical |
| `app/src/components/layout/games/setup/FiveOhOneSetupForm.astro` | 501's setup form, rebuilt on Score Training's shared setup components (`SetupShell`, `UserSection`, `InfoSection`, `SettingSectionShell`, `Toggle`, `Input`): starting-score `Toggle` (301/501/701/Custom) + conditional custom-value `Input`, `legsToWin` `Input`, each with its own clamp-notice `<p role="status">`; moved from `components/layout/games/` (old flat file removed), dropping the old locked-settings `<dl>` in favor of `InfoSection`'s description (2026-08-11) | canonical |
| `app/src/components/forms/HandednessForm.astro` | Profile-page throwing-hand picker — roving-tabindex radiogroup writing `$store.boardInput.handedness` directly; RIGHT/LEFT options laid out on one line (`flex` row, `flex-1` each, `text-sm` label so the option text never wraps) (D206, 2026-08-11; single-line + InfoSection reuse #84, 2026-08-11); `InfoSection` wrapper and outer `<section>` stripped, radiogroup now labeled directly via `aria-label="Throwing hand"` and mounted inside `PlayerSettingsCard.astro`'s "Handed" row (2026-08-16) | canonical |
| `app/src/services/rulesets/quick-score.validator.ts` | Shared RECREATIONAL + QUICK_SCORE batch rules (no dart rows, turn-total bound, ROUNDS attempt cap) (2026-07-26) | canonical |
| `app/src/services/rulesets/tuod/tuod.validator.ts` | `TUOD_V1` config + batch validation, ladder-derived turn-total bound (2026-07-26) | canonical |
| `app/src/services/rulesets/visual-board.validator.ts` | Re-derives every ANALYTICS + VISUAL_BOARD dart from its coordinate via `classify()`, refuses a batch that disagrees; `isVisualBoardCapture` predicate shared with the engines (2026-08-05) | canonical |
| `app/src/services/rulesets/three-dart.validator.ts` | `createThreeDartValidator` — the mode-pair, dartless-turn, negative-score and visual-board assertions five three-dart validators share; `label`, `configSchema` and `dartlessIssue` stay per-game (2026-08-19) | canonical |
| `app/src/services/exercise-rulesets/switching/switching.validator.ts` | `switchingValidator` for `SWITCHING_V1` — parses `targets`/`scoring` only; no mode pair, no dart-row bound (the §7 sixty-minute cap lives in `modules/training/routines/routine-duration.module.ts`) (2026-09-11) | canonical |
| `app/src/services/exercise-rulesets/target-scoring/target-scoring.validator.ts` | `targetScoringValidator` for `TARGET_SCORING_V1` — parses `targets` only (distinct, 1–20 or 25); scoring is locked in the engine (2026-09-23) | canonical |
| `app/src/services/exercise-rulesets/switching-target-scoring/switching-target-scoring.validator.ts` | `switchingTargetScoringValidator` for `SWITCHING_TARGET_SCORING_V1` — parses `targets` only (exactly three, distinct, 1–20 or 25) (2026-09-23) | canonical |
| `app/src/services/exercise-rulesets/score-threshold/score-threshold.validator.ts` | `scoreThresholdValidator` for `SCORE_THRESHOLD_V1` — parses `threshold` only (V1: `65`) (2026-09-23) | canonical |
| `app/src/services/exercise-rulesets/bullseye-checkout/bullseye-checkout.validator.ts` | `bullseyeCheckoutValidator` for `BULLSEYE_CHECKOUT_V1` — parses `startScore` only (V1: `81`) (2026-09-24) | canonical |
| `app/src/services/exercise-rulesets/bull-up/bull-up.validator.ts` | `bullUpValidator` for `BULL_UP_V1` — parses the empty configuration only (2026-09-24) | canonical |
| `app/src/services/exercise-rulesets/double-pattern/double-pattern.validator.ts` | `doublePatternValidator` for `DOUBLE_PATTERN_V1` — parses `patterns` only, same scope as `switching.validator.ts` (2026-09-11) | canonical |
| `scripts/check-game-engines.sh` | Guard: every engine exports a factory, registers it, and has a validator | canonical |
| `scripts/check-game-wiring.sh` | Guard: every `registry.ts` key has a validator, a capability declaration, and — unless engine-only — data files, pages and Alpine registrations; engine-only derived from `games-visibility.ts`, both slugs derived, blind spots documented in its header (2026-08-19) | canonical |
| `scripts/check-refinement-coverage.sh` | Guard: refinements in `types.ts` match the refinement contract; blind spots documented in its header | canonical |
| `scripts/check-type-barrels.sh` | Guard: no inline exported `type`/`interface` in implementation files, every `types.ts`/`interfaces.ts` raised by its parent, no aliased or relative barrel **type** import deeper than the importing file's own folder or the area root (value imports exempt, D156); blind spots documented in its header (2026-07-26) | canonical |

## Player settings and app mode (2026-08-08)

Registered for discoverability, not as reading material — the contract lives in `06-API/04-Endpoint-Contracts.md` §Player Settings and the rationale in D195/D196.

| File | Answers | Status |
| ---- | ------- | ------ |
| `app/src/services/session-seats.service.ts` | The single place seat/participant agreement is asserted (seat count, one PLAYER, guest names, one seat per side, ruleset support, DartBot admission via `supportsDartbot`) plus `composeSeatFacts` (which now emits a `DARTBOT` branch carrying `level`/`seed`/`levelSource`) (D221, 2026-08-21; DartBot 2026-09-01) | canonical |
| `app/src/services/settings.service.ts` | `readSettings` / `writeSettings` — quick-score fallback for a player with no row, and the `capableRulesets` guard that refuses an uncapable mode pair; returns `ServiceResult`, never throws (D195) | canonical |
| `app/src/repositories/settings.repository.ts` | `findSettings` reads `v_player_settings`; `upsertSettings` writes `player_settings`, resolving mode keys to ids and creating the row lazily (D195) | canonical |
| `app/src/pages/api/players/me/settings.ts` | `GET`/`PATCH` route handlers over that service, in the frozen `ok`/`fail` envelope (D195) | canonical |
| `app/src/lib/client/api/settings.ts` | Browser client for the settings pair, following the sibling API clients' auth handling | canonical |
| `app/src/stores/settings.store.ts` | Alpine store holding the player's app mode, registered in `register-stores.ts`; optimistic save with rollback | canonical |
| `app/src/components/forms/AppModeForm.astro` | Profile-page mode picker — roving-tabindex radiogroup with a non-colour selection cue; title/explanation moved into `components/ui/InfoSection.astro` (2026-08-11) | canonical |
| `app/src/components/forms/SettingRow.astro` | Generic label + text-value/pencil-`Button`/input row; local `x-data` toggle (`editing`/`openValue`/`commit()`), `x-model`/`x-model.number` chosen via a spread `modelDirective` (2026-08-16); `commit()` snapshots the pre-edit value on open and guards blur/Enter — unchanged is a no-op, a required field left empty reverts without saving — since `saveExpr` (`$store.profile.save()`) round-trips all three profile fields and client-side-validates the full payload, so an unguarded blur on one row could throw on another row's empty required value (2026-08-16) | canonical |
| `app/src/components/forms/Select.astro` | Shared custom glass dropdown (replaces the native `<select>`): full-width glass bar that grows into an option panel, value bound through the `model` prop, no placeholder; options fixed (`options`) or read at runtime from the caller's scope (`optionsExpr`); consumers: the Statistics page game and routine pickers (2026-09-24; `optionsExpr` 2026-09-25) | canonical |
| `app/src/components/forms/PlayerSettingsCard.astro` | Bordered card (reuses `InfoSection`'s own card classes, not the component) grouping display name, darts, weight (three `SettingRow`s) and the throwing-hand picker under a plain "Player settings" `h2` on `app/src/pages/profile/index.astro`, replacing the former always-open `DisplayNameForm`/`DartsConfigForm` (2026-08-16) | canonical |

## Statistics page (2026-09-08)

Registered for discoverability, not as reading material — the API contract lives in `06-API/04-Endpoint-Contracts.md` §Statistics Overview; the design rationale in `docs/superpowers/specs/2026-09-08-statcard-component-design.md`.

| File | Answers | Status |
| ---- | ------- | ------ |
| `app/src/lib/stats/format-statistics-overview.ts` | Pure `formatStatisticsOverview()` — turns `StatisticsOverviewResponseData` into the 19 display strings `StatCard` binds to; owns the `game_type_key` → title lookup table (2026-09-08) | canonical |
| `app/src/lib/client/api/statistics.ts` | `fetchStatisticsOverview()` browser client, mirrors `profile.ts`'s `apiRequest`/`*ApiError` shape (2026-09-08) | canonical |
| `app/src/stores/stats.store.ts` | `stats` Alpine store: loads + formats once via `init()`/`load()`, exposes plain formatted-string fields read by the Statistics page as `$store.stats.*`, registered in `register-stores.ts` (2026-09-08) | canonical |

## Custom routine builder (2026-09-19)

Registered for discoverability, not as reading material — the contract lives in `06-API/04-Endpoint-Contracts.md` §Custom Routine Write Contracts, the domain model in `09-Training/01-Routines.md` §7/§20, and the rationale in D305/D306/D321/D336/D337/D334.

| File | Answers | Status |
| ---- | ------- | ------ |
| `app/src/services/routine.service.ts` | `listRoutines`/`getRoutine`/`createRoutine`/`replaceRoutine`/`deleteRoutine`/`listExerciseTemplates` — owner-aware CRUD over `v_routine_execution`/`v_exercise_template_catalog`; `writeIssues` pre-checks catalog membership, a GAME step's timed bound and the D305 floor before the write, `isRoutineDurationViolation` catches the `0038` trigger's `23514` at commit and maps it to `VALIDATION_FAILED` (2026-09-19) | canonical |
| `app/src/repositories/routine.repository.ts` | `findRoutineExecutionRows` (system routines + the caller's own, one view read), `findExerciseTemplateCatalog`, `findDurationTypeId`, `insertRoutineTemplateRecord`/`updateRoutineTemplateRecord`/`deleteRoutineTemplateRecord` (all player-scoped), `insertRoutineStepRecords`/`deleteRoutineStepRecords` (transaction-scoped, no `playerId` — ownership is the caller's job) (2026-09-19) | canonical |
| `app/src/pages/api/routines/` (`index.ts`, `[routineId].ts`, `types.ts`) | `GET`/`POST /api/routines`, `GET`/`PUT`/`DELETE /api/routines/:routineId`; `types.ts` holds the shared caps (`MAX_ROUTINE_STEPS` 12, `MAX_ROUTINE_STEP_MINUTES` 60, `MAX_ROUTINE_NAME_LENGTH` 60) and Zod DTOs the client mirrors; `DELETE` returns a bare `204` with `X-Request-Id`, no envelope body (2026-09-19) | canonical |
| `app/src/pages/api/exercise-templates/` (`index.ts`, `types.ts`) | `GET /api/exercise-templates` — the builder's picker catalog, narrowed to `hasDefaultConfiguration` templates by the service, not the view (2026-09-19) | canonical |
| `app/src/lib/client/api/routines.ts` | Browser client: `listRoutines`/`getRoutine`/`createRoutine`/`updateRoutine`/`deleteRoutine`/`listExerciseTemplates`, following the sibling API clients' `unwrap`/`SessionApiError` shape (2026-09-19) | canonical |
| `app/src/lib/training/routines/training-index.data.ts` | `trainingIndex()` — `/training` list factory: fetches `listRoutines()`, exposes `detailHref`/`durationLabel` to `RoutineCard.astro`, plus `systemRoutines()`/`personalRoutines()` for the Routines and Personal routines cards (2026-09-21) | canonical |
| `app/src/lib/training/routines/routine-options.ts` | `routineSelectOptions()` — routines as `Select` options, default routines first then the player's own; used by `statisticsRoutines()` (2026-09-25) | canonical |
| `app/src/lib/training/routines/statistics-routines.data.ts` | `statisticsRoutines()` — `/statistics` Routines tab factory: fetches `listRoutines()`, exposes `routineOptions()`/`routineName()` and the picked `routine`, seeded to the first option (2026-09-25) | canonical |
| `app/src/lib/training/routines/routine-detail.data.ts` | `routineDetail()` — `/training/routines/detail` factory: loads by `?routine=` id, `start()`/`canEdit()`/`requestDelete()`/`confirmDelete()`/`cancelDelete()` (2026-09-19) | canonical |
| `app/src/lib/training/routines/routine-builder.data.ts` | `routineBuilder(mode)` — create/edit factory: catalog load, `addStep`/`removeStep`/`moveStep`/`setMinutes` (D351, supersedes D337), client-side pre-validation via `routine-duration.module.ts`, `save()` mapping server `VALIDATION_FAILED` details to `serverIssues`; `resetForm()` discards a create draft or reloads the saved routine in edit mode, for a modal host that closes without navigating (2026-09-19; `resetForm` 2026-09-21) | canonical |
| `app/src/lib/training/routines/routine-route.ts` | `routineIdFromLocation()` (reads the `?routine=` query param) and `routineDetailPath`/`routinePlayPath`/`routineEditPath` builders — the one place a routine id crosses into/out of the URL (2026-09-19) | canonical |
| `app/src/components/layout/training/routines/RoutineCard.astro`, `RoutineDetail.astro`, `RoutineBuilder.astro`, `RoutineStepRow.astro`, `ExercisePicker.astro` | The builder/list/detail component set — see `07-Frontend/08-Component-Inventory.md` for the per-component row (2026-09-19) | canonical |
| `app/src/icons/grip.svg` | Six-dot drag grip, the `x-sort:handle` affordance on a routine builder step row (D351, 2026-09-21) | canonical |
| `app/src/components/ui/ExpandingModal.astro` | Corner disclosure dialog primitive: 48px glass toggle expanding in place into a full-frame panel; contents held at the expanded size throughout the transition via `100cqw`/`100cqh` against the fixed layer, so nothing reflows while the panel grows; caller owns the open flag (2026-09-21) | canonical |
| `app/src/components/layout/training/routines/RoutineFormModal.astro` | `RoutineBuilder` in an `ExpandingModal` with its own `routineBuilder(mode)` scope — the modal form used by `/training` in place of a create/edit page (2026-09-21) | canonical |
| `app/src/pages/training/routines/detail/index.astro`, `app/src/pages/training/routines/edit/index.astro`, `app/src/pages/training/routines/play/index.astro` | The three data-driven routine routes, all `prerender = true`, routine id via `?routine=` query string; replace the retired static `/training/balanced-training` pair (2026-09-19). The `new/` route was retired 2026-09-21 — creating a routine is `RoutineFormModal` on `/training` (D351's modal host), not a page |  canonical |

## Routine step-kind generalisation (2026-09-20)

Registered for discoverability, not as reading material — the contract lives in `09-Training/01-Routines.md` §11/§17, the adapter registry shape in `07-Frontend/04-Modules-And-OOP.md`, and the rationale in D339 (`decisions/database.md`), D340 (`decisions/game-engine.md`), D341 (`decisions/frontend/architecture.md`).

| File | Answers | Status |
| ---- | ------- | ------ |
| `app/src/services/routines/game-step.ts` | `ROUTINE_GAME_STEPS` — the server eligibility table (`TUOD_V1`/`SCORE_TRAINING_V1`/`121_V2`), each entry's `applyStepDuration`/`minuteBounds`; `routineGameStepHook(key)` resolves one or `undefined` when not eligible; `ROUTINE_CAPTURE_MODE_KEY`/`ROUTINE_INPUT_MODE_KEY` (`ANALYTICS`/`VISUAL_BOARD`) (2026-09-20) | canonical |
| `app/src/services/routines/types.ts` | `RoutineGameStepHook` — raised into `services/types.ts` | canonical |
| `app/src/lib/training/routines/adapters/interfaces.ts` | `StepAdapter` — `open`/`facts`/`completesOwnSession`/`summarise`/`close`, `routine-play.data.ts`'s one per-step-kind extension point (2026-09-20) | canonical |
| `app/src/lib/training/routines/adapters/types.ts` | `StepAdapterKey` (`` `GAME:${string}` `` templated for a game step), `StepPanel` — the play page's `x-if` switch values (2026-09-20) | canonical |
| `app/src/lib/training/routines/adapters/step-adapter.registry.ts` | `STEP_ADAPTERS` — the four Phase-1 kinds, `TARGET_SCORING`, `SWITCHING_TARGET_SCORING`, `SCORE_THRESHOLD` (2026-09-23), `BULLSEYE_CHECKOUT`, `BULL_UP` (2026-09-24), plus `GAME:TUOD_V1`/`GAME:SCORE_TRAINING_V1`/`GAME:121_V2`; `stepAdapterKey(step)`/`resolveStepAdapter(key)`; hand-mirrored against `ROUTINE_GAME_STEPS` (client code never imports `services/**`), kept in step only by `step-adapter.registry.test.ts` (2026-09-20) | canonical |
| `app/src/lib/training/routines/adapters/warm-up.adapter.ts`, `switching.adapter.ts`, `double-pattern.adapter.ts`, `target-scoring.adapter.ts`, `switching-target-scoring.adapter.ts`, `score-threshold.adapter.ts`, `bullseye-checkout.adapter.ts`, `bull-up.adapter.ts` | The eight non-game adapters — plain object literals, each `open`/`close` moving what `routine-play.data.ts` used to do inline for that step kind (2026-09-20) | canonical |
| `app/src/lib/training/routines/adapters/game.adapter.ts` | `gameAdapter(input)` factory building one `StepAdapter` per routine-eligible game ruleset (generalises the old `startFinishingStep`); `summariseTuodStep`/`summariseScoreTrainingStep`/`summariseOneTwentyOneStep` read the finished game store's own results snapshot (2026-09-20) | canonical |
| `app/src/lib/training/routines/game-step.data.ts` | `gameStep(playFactory, onStepComplete, onAbandon)` — renamed from `finishing-step.data.ts`'s `finishingStep`, generalised over any routine-eligible game's own play store so its completion advances the routine (2026-09-20) | canonical |

## Weekly training schedules (2026-09-20)

Registered for discoverability, not as reading material — the domain model lives in `05-Database/06-Spec/02-Template-Layer.md` (`training_schedules`/`training_schedule_days`), the migration in `05-Database/03-Migrations.md` `## 0041`, the API contract in `06-API/04-Endpoint-Contracts.md` §Training Schedules, the frontend routes/components in `09-Training/01-Routines.md` §7 and `07-Frontend/08-Component-Inventory.md`, and the rationale in D342/D343 (`decisions/database.md`, `decisions/api.md`). Frontend (Task 4 of the implementation plan) is now built too.

| File | Answers | Status |
| ---- | ------- | ------ |
| `app/src/services/schedule.service.ts` | `listSchedules`/`getSchedule`/`getActiveSchedule`/`createSchedule`/`replaceSchedule`/`activateSchedule`/`deactivateSchedule`/`deleteSchedule` — owner-aware CRUD reading both views through `buildSchedule`; `validateDays` rejects a duplicate `dayOfWeek` and any `routineTemplateId` that does not resolve through `getRoutine` (`routine.service.ts`) before the write (2026-09-20) | canonical |
| `app/src/repositories/schedule.repository.ts` | `findScheduleRows`/`findScheduleDayRows` (both views, player-scoped, optionally narrowed to one schedule), `findScheduleIdsUsingRoutine` (the RESTRICT-mapping read), `insertScheduleRecord`/`updateScheduleRecord`/`replaceScheduleDayRecords` (delete-then-insert), `setActiveSchedule` (two statements in one transaction: clear every sibling, then set the target, ordered so the partial unique index never trips), `clearActiveSchedule`/`deleteScheduleRecord` (2026-09-20) | canonical |
| `app/src/services/db-errors.ts` | `matchesConstraintError(error, { code, constraint })` — walks a thrown error's `cause` chain (bounded, self-reference guarded) for a Postgres SQLSTATE + constraint/trigger name, matching the name in `message` too since a deferred trigger's violation surfaces there; extracted out of `routine.service.ts`'s inline cause-walk so `isRoutineDurationViolation` and the new `isScheduledRoutineViolation` (`fk_training_schedule_days_routine_template`) share one implementation (2026-09-20) | canonical |
| `app/src/pages/api/schedules/types.ts` | `MAX_SCHEDULE_NAME_LENGTH` (60); Zod DTOs `ScheduleDayInput`/`CreateScheduleRequest`/`UpdateScheduleRequest`/`ScheduleDay`/`ScheduleResponse`/`ScheduleSummary`/`ScheduleListResponse` the client mirrors (2026-09-20) | canonical |
| `app/src/pages/api/schedules/index.ts` | `GET`/`POST /api/schedules` — list, create (created inactive) (2026-09-20) | canonical |
| `app/src/pages/api/schedules/active.ts` | `GET /api/schedules/active` — the caller's active `Schedule`, or `null` when none is active (D343) (2026-09-20) | canonical |
| `app/src/pages/api/schedules/[scheduleId]/index.ts` | `GET`/`PUT`/`DELETE /api/schedules/:scheduleId`; `DELETE` returns a bare `204` with `X-Request-Id`, no envelope body (2026-09-20) | canonical |
| `app/src/pages/api/schedules/[scheduleId]/activate.ts` | `POST /api/schedules/:scheduleId/activate` (2026-09-20) | canonical |
| `app/src/pages/api/schedules/[scheduleId]/deactivate.ts` | `POST /api/schedules/:scheduleId/deactivate` (2026-09-20) | canonical |
| `app/src/lib/client/api/schedules.ts` | Browser client: `listSchedules`/`getSchedule`/`getActiveSchedule`/`createSchedule`/`updateSchedule`/`activateSchedule`/`deactivateSchedule`/`deleteSchedule`, following the sibling API clients' `unwrap`/`SessionApiError` shape (2026-09-20) | canonical |
| `app/src/lib/client/api/unwrap.ts` | `unwrapOrThrow(result)` — returns a success envelope's data or throws `SessionApiError`; shared by `routines.ts`/`schedules.ts`. Separate from `client.ts` so the transport never imports `sessions.ts` (D346, 2026-09-20) | canonical |
| `app/src/lib/training/schedules/types.ts` | `ScheduleDayEntry`, `SchedulesIndexContext`, `ScheduleEditorRow`/`ScheduleEditorContext`, `MyScheduleFormContext`, `HomeWeekContext` — the schedule data factories' Alpine context shapes (2026-09-23) | canonical |
| `app/src/lib/training/schedules/today.ts` | `startOfLocalDay(date)`/`isRoutineCompleted(entry, completions)` added (2026-09-22); `isoWeekday(date)` (JS `getDay()` 0..6 → ISO 1..7, Sunday = 7); `weekdayNames(locale?)` Monday..Sunday via `Intl.DateTimeFormat` pinned to a fixed UTC Monday; `todayEntry(schedule, date)` — the matching `ScheduleDayEntry` or `null` (2026-09-20) | canonical |
| `app/src/lib/training/schedules/schedule-route.ts` | `scheduleIdFromLocation()` (reads `?schedule=` from `window.location`), `scheduleEditPath(scheduleId)` (2026-09-20) | canonical |
| `app/src/lib/training/schedules/schedules-index.data.ts` | `schedulesIndex()` — loads `listSchedules`; `activate`/`deactivate` call the client then `refresh()`; `editHref` via `scheduleEditPath` (2026-09-20) | canonical |
| `app/src/lib/training/schedules/schedule-editor.data.ts` | `scheduleEditor(mode)` — seven `restRows()` Monday..Sunday (`routineTemplateId: string \| null`); loads `GET /api/routines` for the picker and, in edit mode, the existing schedule; `payload()` drops any falsy `routineTemplateId` (rest); `formatScheduleIssues` (moved to `schedule-issues.ts`, 2026-09-22) renders a `VALIDATION_FAILED` `{ reason, dayOfWeek? }` as one line (2026-09-20) | canonical |
| `app/src/components/layout/training/schedules/ScheduleEditor.astro`, `ScheduleDayRow.astro` | The schedule editor components — see `07-Frontend/08-Component-Inventory.md` for props (2026-09-23) | canonical |
| `app/src/pages/training/schedules/index.astro` | `/training/schedules` — own schedules list, active badge, Activate/Deactivate, `New schedule` `GameCard` (2026-09-20) | canonical |
| `app/src/pages/training/schedules/new/index.astro` | `/training/schedules/new` — mounts `scheduleEditor('create')` around `ScheduleEditor` (2026-09-20) | canonical |
| `app/src/pages/training/schedules/edit/index.astro` | `/training/schedules/edit?schedule=<id>` — mounts `scheduleEditor('edit')` around `ScheduleEditor` (2026-09-20) | canonical |

## My schedule and "done today" (2026-09-22)

Registered for discoverability, not as reading material — the view contract lives in `05-Database/06-Spec/05-Read-Model-Layer.md` §v_training_completions, the migration in `05-Database/03-Migrations.md` `## 0042`, the API contract in `06-API/04-Endpoint-Contracts.md` §Training Completions, the components in `07-Frontend/08-Component-Inventory.md`, the rationale in D353.

| File | Answers | Status |
| ---- | ------- | ------ |
| `database/migrations/0042_training_completions_view.sql` | `v_training_completions` — completed training activities with the routine snapshot they ran (D353, 2026-09-22) | canonical |
| `database/verification/0042_training_completions_view_checks.sql` | Live-DB proof: completed training listed with its snapshot routine; abandoned, active and snapshot-less activities excluded (2026-09-22) | canonical |
| `app/src/pages/api/training-sessions/completed.ts` | `GET /api/training-sessions/completed?since=` — `422` on a missing/non-ISO `since` (2026-09-22) | canonical |
| `app/src/lib/training/schedules/my-schedule.data.ts` | `myScheduleForm()` — day picker + single-select routine list; updates the active schedule or creates and activates `MY_SCHEDULE_NAME`; `save()` resolves `true` and dispatches `schedule-saved` (2026-09-22) | canonical |
| `app/src/lib/training/schedules/home-week.data.ts` | `homeWeek()` — active schedule + completions since local midnight; `isToday(index)`/`hasRoutine(index)`, `isRestDay()`, `showStart()` (active schedule, not done today — rest days included)/`showDone()`, `startHref()`, `start()` navigates (2026-09-23) | canonical |
| `app/src/lib/training/schedules/schedule-issues.ts` | `formatScheduleIssues(details)` — a schedule `VALIDATION_FAILED` envelope as one user-facing line; shared by `scheduleEditor` and `myScheduleForm` (2026-09-22) | canonical |
| `app/src/components/layout/training/schedules/ScheduleFormModal.astro`, `app/src/components/layout/home/TodayRoutineCard.astro` | The modal and homepage today card — see `07-Frontend/08-Component-Inventory.md` for props (2026-09-22) | canonical |
| `app/src/icons/flame.svg` | Flame icon for the "You're on fire" state (2026-09-22) | canonical |

## Cross-cutting mechanical guards (2026-07-28)

Guards not specific to the game-engine contract, registered here for discoverability.

| File | Answers | Status |
| ---- | ------- | ------ |
| `scripts/check-alias-sync.sh` | Guard: `tsconfig.json` compilerOptions.paths, `vitest.config.ts` resolve.alias and `07-Frontend/02-Folder-Structure.md`'s alias table never diverge (D113; doc as third source D302, 2026-09-17); `@styles` allowlisted as TS-only for the vitest comparison | canonical |
| `scripts/check-constraint-mirror.sh` | Guard: every live CHECK constraint on `exercise_stages`/`turns`/`darts` has a `// MIRRORS: chk_x` anchor in `app/src/pages/api/sessions/types.ts` (D149); bound agreement executed in `constraint-mirror.test.ts`, not by this script | canonical |
| `scripts/check-no-inline-comments.sh` | Guard: no `//` or non-JSDoc `/* */` comment inside a function/method body under `app/src/**/*.ts`; JSDoc `/** */` above a declaration stays exempt | canonical |
| `scripts/check-style-tokens.sh` | Guard: no `font-medium`, `{...rest}`, raw `bg-bg*`/`text-fg*`, Tailwind prefix-important (`!utility`), or leading-dash arbitrary (`-prop-[…]`) under `app/src/**/*.{astro,css}` | canonical |
| `scripts/check-file-locations.sh` | Guard: no `.ts` files directly under `components/`/`pages/` except `pages/api/**` | canonical |
| `scripts/check-agent-mirrors.sh` | Guard: every `CLAUDE.md` has an `AGENT.md` sibling holding the fixed pointer stub (D213) | canonical |
| `scripts/check-skill-pointers.sh` | Guard: every `.claude/skills/*/SKILL.md` and `.claude/rules/*.md` is registered in this file, and every `superpowers:<name>` in a live tracked Markdown file resolves under the installed plugin cache (skipped, not failed, when the cache is absent — CI installs no plugins). Closes D311's finding that no gate ever read the skill tree (2026-09-18, D314) | canonical |
| `scripts/check-worktree-location.sh` | Guard: every worktree inside the repo root lives under `.worktrees/`, and the retired `.claude/worktrees/` directory is absent; warns (non-fatally) when a `.worktrees/` worktree holds a branch already merged into `origin/main`. Checkouts beside the repo are out of scope. Closes the gap D312 named — nothing mechanically proved a landed branch's worktree was removed (2026-09-18, D319) | canonical |
| `scripts/check-game-rules.sh` | Guard: every `docs/game-rules/` rules file matches its shape's template contract — required/forbidden sections, `Version`/`Applies to` vocabulary, `Current version:` and `Entry points:` header fields, standalone `Ends when`/`Result`, Glossary → Features agreement. Shape inferred from the folder; `README.md` and `_drafts/` skipped. Blocking by default; `GAME_RULES_GATE=warn` downgrades it for mid-edit use (2026-09-19, D322; blocking D323) | canonical |
| `scripts/check-astro-class-composition.sh` | Guard: no `class:list` or manual class-join in `app/src/**/*.astro`; `cn()` only | canonical |
| `scripts/check-astro-conventions.sh` | Guard: every `x-show` has `x-cloak`; no HTML comments in `.astro` template regions | canonical |
| `scripts/check-context-map.sh` | Guard: every path referenced from a CLAUDE.md/README.md/context map exists; migration-range claims agree with `database/migrations/` (2026-07-23) | canonical |
| `scripts/check-doc-links.sh` | Guard: markdown links and path-like backtick refs across the canonical doc set resolve (D133) | canonical |
| `scripts/check-context-budget.sh` | Guard: this file's own `~Nk` token estimates don't drift from a chars/4 estimate (D133) | canonical |
| `scripts/check-decision-ids.sh` | Guard: every id across `decisions/**` is unique, none of the 163-id 2026-08-02 baseline has disappeared, every `Supersedes:` target exists, `DECISIONS.md` stays a router, every migrated row still hash-matches `scripts/decision-row-hashes.tsv` (D184+ out of scope by design), every `decisions/**.md` file is registered in the router's routing table; position-anchored to avoid darts `D18`/`D20` notation; blind spots documented in its header (2026-08-02; hash + registration checks 2026-08-03) | canonical |
| `scripts/next-decision-id.sh` | Helper: prints the next free decision id on one line of stdout, derived against fetched `origin/main` union the working tree — both the migrated table-row form and the `### D<n>` block form, plus the `Decision id:` reservations that plans under `docs/superpowers/plans/` declare before their block exists; advisory, not a gate (2026-09-18) | canonical |
| `scripts/renumber-decision.sh` | Helper: rewrites one decision id across every tracked file when CI catches a late collision; five refusals — a malformed or zero-padded id, a target already carrying a row or heading in `decisions/**`, a target in the 20-id never-issued set, an old id whose block in this tree is `origin/main`'s own or is declared twice (the unresolved post-merge state), and a dirty working tree; advisory, not a gate (2026-09-18) | canonical |
| `scripts/check-doc-sync.sh` | Guard: a change under `database/migrations/`, `app/src/services/` or a domain subdirectory of `app/src/modules/` (`game`, `training`, `dartbot`, `stats`) must also touch `docs/architecture/` or `decisions/`; `docs/superpowers/` does not satisfy it (D312). Paths calibrated over 80 first-parent merges to zero false positives; `app/src/modules/ui/` excluded as infrastructure shims. Absent from `.husky/pre-commit` by design — it diffs against the merge base (2026-09-18, D317) | canonical |
| `scripts/fallow-gate.sh` | Wrapper: runs `npx fallow` from `app/`, passes its output and exit code through unchanged, and on failure re-runs `npx fallow health --format json` to print the `findings` array's real violating file/function/line — fallow's own `start with <file>` names its top refactoring suggestion, not the threshold violator (2026-09-19, D332, #292). Wired into `validate:app` and `quality.yml`'s Stale-usage gate | canonical |
| `scripts/decision-row-hashes.tsv` | Data file: id → sha256 of the 163 migrated rows' exact text at the 2026-08-02 split, read by `scripts/check-decision-ids.sh`'s row-integrity check | canonical |

## Brand asset generators (2026-07-31)

Registered for discoverability — regenerate committed outputs via `npm run icons:generate` / `npm run logo:generate` in `app/`.

| File | Answers | Status |
| ---- | ------- | ------ |
| `app/scripts/generate-app-icons.ts` | Regenerate PWA/favicon PNGs + `favicon.svg`/`favicon.ico` from `bg-dartboard.svg` (`npm run icons:generate`) (2026-07-31) | canonical |
| `app/scripts/generate-logo-lockup.ts` | Regenerate outlined `logo-lockup.svg` lockup from Michroma outlines (`npm run logo:generate`) (2026-07-31) | canonical |
| `app/scripts/check-migration-drift.ts` | Guard (`npm run db:drift`): `schema_migrations` vs `database/migrations/` in both directions, and live `v_*` views vs the ones the chain's `migrate:up` regions leave behind. Catches a migration applied to shared Neon `dev` from a branch this checkout does not have — `dbmate status` enumerates files and reports `Pending: 0` regardless. Runs inside `validate:app` between `db:migrate` and `db:introspect`; view bodies out of scope (2026-09-19, D333, #503) | canonical |

## Decision ledger (repo root, `decisions/`) (2026-08-02)

`DECISIONS.md` routes to these; it holds no decision rows itself. Load-when lists mirror the router's own routing table — don't re-derive them here.

| File | Answers | Status | ~Tokens |
| ---- | ------- | ------ | ------- |
| `DECISIONS.md` | Router: authority note, Source key, routing table, Deferred list, facts-vs-decisions rule, how-to-add-a-decision (2026-08-02) | canonical | ~1.7k |
| `decisions/architecture.md` | 20 decisions — domain model, activity, session, stage, turn, dart, ruleset, platform, dart zones, client keys, mode capability | canonical | ~2.1k |
| `decisions/database.md` | 37 decisions — schema, migration, table, column, constraint, index, view, Neon, seed (D257 general statistics views, 2026-09-06; D263 exercise-type discriminator, D266 exercise_templates.exercise_type_id backfill shape, 2026-09-10; D284 training gameplay in the read model, 2026-09-16; D288 CI-applied production migrations behind a rehearsal, D290 schema.ts the only committed introspect artifact, D291 uq_sessions_single_active keys on COALESCE(game_type_id, exercise_type_id), D295 exercise templates pin their exercise ruleset version, D298 a read-model view carries the columns its consumer reads, D305 custom-routine duration floor is a deferred constraint trigger, 2026-09-17; D310 constraint names are prefix_table_column with the column named in full, 2026-09-18; D333 shared-`dev` drift caught by comparing applied migrations and live views to the chain, D334 the trigger raises one constraint name regardless of which trigger fired and the service matches on it literally, D335 the trigger function picks its row by TG_OP, never by COALESCE(NEW, OLD), D336 migration 0038 builds D305's trigger and D306/D321's owner-aware routine views, 2026-09-19; D342 weekly training schedules — weekday SMALLINT+CHECK as a Pattern 12 exception, rest as an absent row, RESTRICT + service-layer mapping, 2026-09-20; D344 a migration unmerged and provably pending everywhere is corrected in place, 0041's day_count is ::int; D348 a view-select repository narrows each non-null column with nonNull(), never a blanket row-array cast, 2026-09-21) | canonical | ~15.7k |
| `decisions/api.md` | 48 decisions — endpoint, contract, envelope, auth, middleware, idempotency, batch, Worker, player settings (D283 training activity read player-scoped, terminal transition guarded in the UPDATE; D285 active-session conflict detection walks the error `cause` chain, 2026-09-16; D292 starting a routine abandons the player's previous one, D296 startTraining validates every step's configuration, D299 routine steps read through v_routine_execution, D300 envelope builders emit X-Request-Id, D301 activity grouping restated as shipped, 2026-09-17; D350 session participants restated as shipped — guest/DartBot no longer deferred, closing #406, 2026-09-21) | canonical | ~12.8k |
| `decisions/game-engine.md` | 69 decisions — engine, GameEngine, ruleset, scoring, checkout, fact log, 501, Score Training (D230 win-condition categories/`SEAT_CAPS`/`activeSeat()` completion predicate; D231 corrects which engines pass that predicate, 2026-08-22; D238 exclusive score-band tallying, 2026-08-27; D242 `visitScoreBandCounts`'s `sixtyPlus` band and `checkout-bust.module.ts`'s `checkoutAttemptCount`; D252 the bot play loop's generic infrastructure and the QUICK_SCORE fold, 2026-09-01; D259 Shanghai V2 2-seat admission, 2026-09-07; D264/D267 the `ExerciseEngine`/`DartExerciseEngine` contracts parallel to `GameEngine`, 2026-09-11; D297 Switching targets stop at 20, the bull is not a Switching target, 2026-09-17; D313 adjacent DartBot levels never share a bias pair, 2026-09-18; D356 Target Scoring is its own exercise type with locked scoring, D357 Switching Target Scoring is its own type sharing that point table, D358 65 or More is a Score Threshold type locked to 65, 2026-09-23; D360 Around the Clock V2 training variants served by the V1 engine class, 2026-09-23, D361 Bullseye Checkouts is its own type locked to 81, 2026-09-24, D362 Bull Up Practice is its own type with one-dart turns, 2026-09-24) | canonical | ~35.1k |
| `decisions/testing.md` | 9 decisions — test, TDD, Vitest, mock, coverage, change-set test gate, verification-script parity gate, rendered-SQL repository tests, wrapped-store seam tests (2026-09-17) | canonical | ~3.2k |
| `decisions/frontend/architecture.md` | 25 decisions — layering, folder structure, suffix, barrel, type import, error mapping, API client, one-shape-per-game extraction; +D261 Trivia tools outside GameEngine (2026-09-09); +D302 alias table as a third gate source (2026-09-17); +D308 `training/` is the umbrella domain for trivia/exercises/routines (2026-09-17) | canonical | ~7.7k |
| `decisions/frontend/astro.md` | 24 decisions — .astro, component, prerender, routing, layout, cn(), props, frontmatter, PWA, manifest, icon, safe-area (D244 cross-cutting markup extraction inside D215's boundary, 2026-08-28; D265 `/training` replaces `/trivia` as a flat IA with a reusable routine-detail shell, 2026-09-11; D272 Balanced Training's play page reuses `GameLayout`, 2026-09-13; D275 dart exercise panels adopt the game play-screen shape, 2026-09-14) | canonical | ~6.6k |
| `decisions/frontend/alpine.md` | 26 decisions — Alpine, stores, state, persist, recovery, x-data, x-show (D286 in-routine blocking-game resolution, 2026-09-16; D307 the `forms/` layer retired unbuilt, `settings.store.ts` is D77's substitute in practice, 2026-09-17; D337 routine step reorder is move-up/move-down buttons, `@alpinejs/sort` declined for now, 2026-09-19) | canonical | ~10.0k |
| `decisions/frontend/style.md` | 15 decisions — style, CSS, token, Tailwind, primitive, typography, spacing, glass, surface, PWA, manifest, icon, safe-area, undo affordance, important-modifier ban, container queries, flex-item sizing, scoreboard band (2026-09-19) | canonical | ~4.3k |
| `decisions/context-system.md` | 49 decisions — docs, context map, CLAUDE.md, skill, gate, check script, knowledge graph, CI, deploy, Prettier, format, husky (D314 denies whole-file reads and gates skill/rule registration, D315 the knowledge-graph skill is a lookup skill and the graph is never read whole, D316 moves directory-scoped rules out of `app/CLAUDE.md`, D317 the calibrated doc-sync gate and its `Stop` drift hook, D318 decision ids derive against fetched origin/main and re-derive before the PR opens, D319 retires `.claude/worktrees/` and makes the worktree-location rule a gate, D320 the worktree Read deny is anchored at the session directory and lifted by entering the worktree, D324 worktrees are entered at launch via a `WorktreeCreate` hook rather than mid-session, 2026-09-19) | canonical | ~18.8k |

### Decision ledger migration tooling (2026-08-02, spent — 2026-08-03)

Registered for discoverability and provenance, not as reading material: `scripts/split-decisions.sh` ran once (2026-08-02) to produce the `decisions/**` tree above and now hard-fails if re-run (the ledger it reads from holds zero rows); `scripts/verify-decision-split.sh` proves that one run was lossless against an ephemeral `/tmp` snapshot and legitimately SKIPs once that snapshot is gone; `scripts/decision-map.txt` and `scripts/decision-front-matter.txt` were that run's inputs. None of the four is the ongoing guard — `scripts/check-decision-ids.sh` (registered under Cross-cutting mechanical guards below) is.

| File | Answers | Status |
| ---- | ------- | ------ |
| `scripts/split-decisions.sh` | Spent one-shot migration script: wrote `decisions/**` from `DECISIONS.md` + the map + the front-matter sidecar (2026-08-02) | historical |
| `scripts/verify-decision-split.sh` | Spent one-shot verifier: proves that migration was lossless against a `/tmp` snapshot (SKIPs once the snapshot is gone) | historical |
| `scripts/decision-map.txt` | Spent migration input: id → target-file assignment as it stood at the 2026-08-02 split | historical |
| `scripts/decision-front-matter.txt` | Spent migration input: per-target-file front-matter blocks as they stood at the 2026-08-02 split | historical |

---

## Context & history (repo root, `docs/`)

| File | Answers | Status |
| ---- | ------- | ------ |
| `README.md` | Repo orientation: project summary, folder layout, getting started (2026-07-14) | canonical |
| `.github/pull_request_template.md` | Default PR description scaffold + architecture checklist (2026-07-12) | canonical |
| `.github/ISSUE_TEMPLATE/discovered-work.yml` | Human-facing GitHub issue form mirroring `.claude/skills/capturing-discovered-work/SKILL.md`'s agent-facing procedure (2026-09-13, D273) | canonical |
| `docs/CLAUDE.md` | Docs-tree editing rules | canonical |
| `docs/superpowers/{specs,plans,handoffs}/` | Point-in-time task designs and plans | historical |
| `docs/braindump/` | Raw, pre-decision research notes and proposals — non-canonical, never authority; adopting anything in one is a separate task with its own `decisions/**` entry (`badges.md` 2026-07; `agent-context-hardening.md` 2026-09-18) | historical |
| `app/CLAUDE.md` (+ `app/src/**/CLAUDE.md`, `app/tests/CLAUDE.md`) | App implementation rules, validation procedure; mid-task fallow/`npm run check` gate; Prettier pre-PR gate after `superpowers:writing-plans` execution (2026-07-22; graphify section moved to the graph-lookup skill 2026-09-18; style, Alpine, store, engine and test-writing rules split out to the directory guides below 2026-09-18, D316) | canonical |
| `app/src/services/CLAUDE.md` | Service-layer scope guide: pack pointer, UUIDv7 minting, the two separated ruleset registries (D264), the Zod ↔ `chk_*` mirror, the engine/validator same-commit rule (2026-09-18, D316) | canonical |
| `app/src/components/CLAUDE.md` | Component scope guide: the style non-negotiables and Alpine rules relocated from `app/CLAUDE.md` (2026-09-18, D316) | canonical |
| `app/src/stores/CLAUDE.md` | Store scope guide: `.store.ts` suffix, `$persist` only in stores, `PersistFactory` once per field (D120) (2026-09-18, D316) | canonical |
| `app/src/modules/training/CLAUDE.md` | Training scope guide: the `ExerciseEngine` contract, the no-clock rule and the `ExerciseRulesetVersionKey` separation, relocated from `app/CLAUDE.md` (2026-09-18, D316, D264) | canonical |
| `app/src/modules/game/CLAUDE.md` | Game engine scope guide: the `GameEngine` contract, relocated from the retired .claude/rules/game-engines.md — directory-scoped rather than glob-scoped, since the training-exercise carve-out already lives in a sibling directory (2026-09-21, D349, supersedes D316) | canonical |
| `app/tests/CLAUDE.md` | Test scope guide: Vitest mocks, the `.astro` no-extraction carve-out (D101), the coverage gate; the red→green→refactor procedure stays in `app/CLAUDE.md` (2026-09-18, D316) | canonical |
| `app/DEPLOYMENT.md` | Cloudflare Worker deploy guide: Neon prod setup, Worker secrets, GitHub Actions deploy vars, rollback, troubleshooting; `PUBLIC_NEON_AUTH_BASE_URL` no longer read by app code post-D172 (2026-07-29) | canonical |
| `AGENT.md` (repo root, `app/`, `app/src/db/`, `app/src/pages/api/`, `app/src/services/`, `app/src/components/`, `app/src/stores/`, `app/src/modules/training/`, `app/src/modules/game/`, `app/tests/`, `database/`, `docs/`) | Fixed pointer stub redirecting to the sibling `CLAUDE.md` in the same directory — not a rule source, never carries content (D213, 2026-07-15) | canonical |
| `.claude/settings.json` | Committed Claude Code project settings: declares the `superpowers@claude-plugins-official` marketplace + plugin (sole source of the Superpowers process skills), the Bash permission allow/deny lists, the `Read(...)` denies for the four whole-file loads nothing should read entire (`graphify-out/graph.json`, both worktree roots — `./`-anchored, so lifted once the session has entered its worktree (2026-09-18, D320) — `app/src/db/schema.ts`), `claudeMdExcludes` for worktree copies, the concision `UserPromptSubmit` hook, the `WorktreeCreate` hook that places worktrees under `.worktrees/` (2026-09-19, D324), and the `Stop` hook that runs `.claude/hooks/context-drift.sh` (2026-09-18, D311; guardrails D314; `Stop` hook D317) | canonical |
| `.claude/hooks/worktree-create.sh` | `WorktreeCreate` hook: places every Claude-created worktree at `.worktrees/<name>` of the main checkout, branched from `origin/main`, so `claude --worktree <name>` satisfies the worktree-location invariant by default instead of raising an approval prompt per `EnterWorktree` call. Reopens an existing name; derives the branch from the name; no `WorktreeRemove` counterpart, so removal falls back to `git worktree remove` (2026-09-19, D324) | canonical |
| `.claude/hooks/context-drift.sh` | `Stop`-event reminder: shells out to `scripts/check-doc-sync.sh` and speaks only on drift, so a clean turn costs nothing; bails on `stop_hook_active` to avoid a re-entrancy loop (2026-09-18, D317) | canonical |
| `.claude/skills/graph-lookup/SKILL.md` | Locate a named entity in this repo via `graphify-out/graph.json` — one tested Python recipe, narrow trigger. Replaces the 678-line `graphify` skill, whose catch-all description fired on routine questions and whose build pipeline no path in this repo invoked (2026-09-18, D315) | canonical |
| `.claude/skills/graph-lookup/references/building.md` | How the graph is built and refreshed here: CI ownership, the `graphify update .` canonical command, optional local install; loaded only when the question is about building (2026-09-18, D315) | canonical |
| `.claude/skills/context-maintenance/SKILL.md` | Context Maintenance procedure, invoked before claiming any task done (2026-07-28) | canonical |
| `.claude/skills/validate-app/SKILL.md` | `validate:app` sequence + mid-task gate condition for `app/` changes (2026-07-28) | canonical |
| `.claude/skills/run-all-gates/SKILL.md` | Dispatches the right `check-*.sh` scripts by changed area, reports each result explicitly (2026-07-28) | canonical |
| `.claude/skills/finishing-a-dart-branch/SKILL.md` | Delta paired with `superpowers:finishing-a-development-branch`: skips its Step 4 menu (Option 2 — push + PR — every time) and names `run-all-gates` as the Step 1 check (2026-09-18, D312) | canonical |
| `.claude/skills/capturing-discovered-work/SKILL.md` | Scope-discipline procedure for anything noticed but not asked for: capture as a GitHub issue (`discovered-work` label), never fix in the same pass; replaces `FINDINGS.md` (2026-09-13, D273) | canonical |
| `.claude/skills/authoring-game-rules/SKILL.md` | Authoring procedure for game, exercise, routine and trivia rules in `docs/game-rules/`: the V1 cut test, the forced defer list, the version and applicability vocabulary, the two-axis execution-model/entry-point split. Runs before `superpowers:brainstorming` and produces its input (2026-09-19, D322) | canonical |
| `.claude/skills/authoring-game-rules/references/amending.md` | Amend mode: reading `Current version:` before classifying, the routing question (additive / training-only / new entry point / execution-model change / changes a shipped rule), the slot table, diff discipline (2026-09-19, D322) | canonical |
| `.claude/skills/authoring-game-rules/references/game.md` | Shape reference — game ruleset: what makes something a game, required headings, the four Capture questions, the common traps (2026-09-19, D322) | canonical |
| `.claude/skills/authoring-game-rules/references/exercise.md` | Shape reference — exercise type: the independence rule for a wrapped game, `Bound` vs `Ends when`, why an exercise is not a game (2026-09-19, D322) | canonical |
| `.claude/skills/authoring-game-rules/references/routine.md` | Shape reference — routine: composition only, the duration rules (§5-§7, D305), why a routine carries no `Capture` and no `Entry points:` (2026-09-19, D322) | canonical |
| `.claude/skills/authoring-game-rules/references/trivia.md` | Shape reference — trivia tool: the Quick Subtract precedent (D261), persistence as an explicit rule, no `Capture`/`Applies to`/`Entry points:` (2026-09-19, D322) | canonical |
| `.github/workflows/graph.yml` | CI-owned graph freshness: rebuilds `graphify-out/graph.json` (`GRAPH_REFRESH_STRICT=1`) on every push to `main`, then commits to `chore/graph-refresh` and opens/updates a PR with the delta — PR creation needs *Settings → Actions → General → "Allow GitHub Actions to create and approve pull requests"*; without it the branch is still pushed and the run summary carries a compare link (D186). On pull requests, posts a non-blocking delta comment via `scripts/graph-delta.py` without committing (2026-08-03; D186 2026-08-05) | canonical |
| `scripts/graph-delta.py` | Read-only helper: diffs two `graph.json` snapshots' node/link counts, normalising the volatile `built_at_commit` field away, for `graph.yml`'s PR body and delta comment | canonical |
| `graphify-out/graph.json` | Committed AST-only knowledge graph — CI-maintained (`.github/workflows/graph.yml` rebuilds on merge to `main`; local `scripts/refresh-graph.sh` remains available but is optional); generated, never hand-edited (2026-08-03) | generated |

---
